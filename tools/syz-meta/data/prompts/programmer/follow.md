Thank you for your implementation. After I called `MR(void)` in the main function and compiled the source code, the following error was reported:

[Errors reported by compiler]

There is no other content in my main function, only `MR(void)` is called, the details are as follows:

```c
int main(int argc, char** argv) { MR(); return 0; }
```

Please modify the code based on the reported error messages and show me the modified code again. Be careful that do not to alter the core logic of the code.