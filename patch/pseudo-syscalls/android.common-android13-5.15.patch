diff --git a/executor/MRs/empty.h b/executor/MRs/empty.h
new file mode 100644
index 000000000..e69de29bb
diff --git a/executor/MRs/syz_mr_0ce41170.h b/executor/MRs/syz_mr_0ce41170.h
new file mode 100644
index 000000000..329f80f08
--- /dev/null
+++ b/executor/MRs/syz_mr_0ce41170.h
@@ -0,0 +1,191 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/autofs/autofs/Communicating-with-autofs-detecting-the-daemon/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Equivalence of implicit and explicit daemon PGID specification
+// - Source input: A `mount` operation for an `autofs` filesystem, executed by a process with a specific Process Group ID (PGID), let's call it `P`. The mount options do *not* include the `pgrp=` option.
+// - Follow-up input: A `mount` operation for the same `autofs` filesystem, with the mount options explicitly including `pgrp=P`.
+// - Input relation: The follow-up input explicitly specifies the PGID of the source input's mounting process via the `pgrp=` mount option, whereas the source input relies on the implicit recording of the mounting process's PGID.
+// - Output relation: Both mount operations should result in an equivalent state. Specifically, both should succeed, and in both resulting mounts, any subsequent filesystem request from a process with PGID `P` should be correctly identified as a request from the daemon.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <signal.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/mount.h>
+#include <sys/stat.h>
+#include <sys/wait.h>
+#include <unistd.h>
+
+// Define mount points for clarity and to avoid magic strings
+#define SOURCE_MOUNT_POINT_0ce41170 "/tmp/mr1_source_mount"
+#define FOLLOWUP_MOUNT_POINT_0ce41170 "/tmp/mr1_followup_mount"
+// Timeout for stat calls to detect hangs
+#define TEST_TIMEOUT_SECONDS_0ce41170 2
+
+// Exit codes for the child process to communicate status to the parent
+#define EXIT_SUCCESS_0ce41170 0
+#define EXIT_SETUP_FAILURE_0ce41170 1
+#define EXIT_PRECONDITION_FAILURE_0ce41170 2
+#define EXIT_MR_VIOLATION_0ce41170 99
+
+// Global flag used by the signal handler to indicate a timeout
+static volatile sig_atomic_t timed_out_flag_0ce41170 = 0;
+
+// Signal handler for SIGALRM. It simply sets a flag.
+static void alarm_handler_0ce41170(int sig)
+{
+	timed_out_flag_0ce41170 = 1;
+}
+
+static long syz_mr_0ce41170(volatile long a0)
+{
+	// 1. Perform preparation
+	// Create unique mount point directories.
+	if (mkdir(SOURCE_MOUNT_POINT_0ce41170, 0755) && errno != EEXIST) {
+		return -1;
+	}
+	if (mkdir(FOLLOWUP_MOUNT_POINT_0ce41170, 0755) && errno != EEXIST) {
+		rmdir(SOURCE_MOUNT_POINT_0ce41170);
+		return -1;
+	}
+
+	// autofs requires a file descriptor to communicate with a userspace daemon.
+	// A pipe is a standard way to provide this.
+	int pipe_fd[2];
+	if (pipe(pipe_fd) == -1) {
+		rmdir(SOURCE_MOUNT_POINT_0ce41170);
+		rmdir(FOLLOWUP_MOUNT_POINT_0ce41170);
+		return -1;
+	}
+	// Set the read end to non-blocking. This can help prevent the kernel
+	// from deadlocking if it tries to interact with the fd.
+	fcntl(pipe_fd[0], F_SETFL, O_NONBLOCK);
+
+	// Fork a child process. The child will set its own PGID and perform the tests.
+	pid_t pid = fork();
+	if (pid < 0) {
+		close(pipe_fd[0]);
+		close(pipe_fd[1]);
+		rmdir(SOURCE_MOUNT_POINT_0ce41170);
+		rmdir(FOLLOWUP_MOUNT_POINT_0ce41170);
+		return -1;
+	}
+
+	if (pid == 0) {
+		// --- Child Process ---
+		// This process will execute the core logic of the MR test.
+
+		// Set up a signal handler for SIGALRM to handle timeouts.
+		struct sigaction sa;
+		memset(&sa, 0, sizeof(sa));
+		sa.sa_handler = alarm_handler_0ce41170;
+		if (sigaction(SIGALRM, &sa, NULL) == -1) {
+			exit(EXIT_SETUP_FAILURE_0ce41170);
+		}
+
+		// Create a new process group. The PGID of this new group will be the
+		// child's PID. This gives us a known PGID 'P' for the test.
+		if (setpgid(0, 0) == -1) {
+			exit(EXIT_SETUP_FAILURE_0ce41170);
+		}
+		pid_t pgrp = getpgrp();
+
+		// 2. Get source and follow-up outputs
+		// --- Source Input Execution ---
+		// Mount an autofs filesystem without explicitly specifying the pgrp.
+		// The kernel should implicitly use the PGID of the calling process.
+		char source_opts[128];
+		snprintf(source_opts, sizeof(source_opts), "fd=%d,minproto=5,maxproto=5", pipe_fd[0]);
+		long res1 = mount("mr1_test_source", SOURCE_MOUNT_POINT_0ce41170, "autofs", 0, source_opts);
+
+		// --- Follow-up Input Execution ---
+		// Mount another autofs filesystem, this time explicitly providing the PGID.
+		char followup_opts[128];
+		snprintf(followup_opts, sizeof(followup_opts), "fd=%d,minproto=5,maxproto=5,pgrp=%d", pipe_fd[0], pgrp);
+		long res2 = mount("mr1_test_followup", FOLLOWUP_MOUNT_POINT_0ce41170, "autofs", 0, followup_opts);
+
+		// Precondition check: Both mount operations must succeed.
+		if (res1 != 0 || res2 != 0) {
+			if (res1 == 0)
+				umount(SOURCE_MOUNT_POINT_0ce41170);
+			if (res2 == 0)
+				umount(FOLLOWUP_MOUNT_POINT_0ce41170);
+			exit(EXIT_PRECONDITION_FAILURE_0ce41170);
+		}
+
+		// 3. Check the output relation
+		// The relation requires that the resulting mounts are equivalent. We test this
+		// by accessing a path within each mount. If the kernel correctly identifies
+		// this process (with PGID 'P') as the daemon, the access will not hang.
+		// A hang indicates the kernel is waiting for a daemon response, which means
+		// it failed to identify the process correctly.
+		struct stat st;
+		long stat_res1, stat_res2;
+		int errno1, errno2;
+		int timeout1 = 0, timeout2 = 0;
+
+		// Test the source mount
+		timed_out_flag_0ce41170 = 0;
+		alarm(TEST_TIMEOUT_SECONDS_0ce41170);
+		stat_res1 = stat(SOURCE_MOUNT_POINT_0ce41170 "/test", &st);
+		errno1 = errno;
+		alarm(0); // Cancel the alarm
+		if (timed_out_flag_0ce41170) {
+			timeout1 = 1;
+		}
+
+		// Test the follow-up mount
+		timed_out_flag_0ce41170 = 0;
+		alarm(TEST_TIMEOUT_SECONDS_0ce41170);
+		stat_res2 = stat(FOLLOWUP_MOUNT_POINT_0ce41170 "/test", &st);
+		errno2 = errno;
+		alarm(0); // Cancel the alarm
+		if (timed_out_flag_0ce41170) {
+			timeout2 = 1;
+		}
+
+		// Cleanup mounts within the child before exiting.
+		umount(SOURCE_MOUNT_POINT_0ce41170);
+		umount(FOLLOWUP_MOUNT_POINT_0ce41170);
+
+		// Evaluate the output relation.
+		// A violation occurs if one test hangs and the other doesn't, or if
+		// neither hangs but their results (return value and errno) differ.
+		int violation = 0;
+		if (timeout1 != timeout2) {
+			violation = 1;
+		} else if (!timeout1 && (stat_res1 != stat_res2 || errno1 != errno2)) {
+			violation = 1;
+		}
+
+		if (violation) {
+			exit(EXIT_MR_VIOLATION_0ce41170);
+		} else {
+			exit(EXIT_SUCCESS_0ce41170);
+		}
+	} else {
+		// --- Parent Process ---
+		int status;
+		waitpid(pid, &status, 0);
+
+		// 4. Perform cleanup
+		close(pipe_fd[0]);
+		close(pipe_fd[1]);
+		// Attempt to unmount and remove directories again in case the child failed.
+		umount(SOURCE_MOUNT_POINT_0ce41170);
+		umount(FOLLOWUP_MOUNT_POINT_0ce41170);
+		rmdir(SOURCE_MOUNT_POINT_0ce41170);
+		rmdir(FOLLOWUP_MOUNT_POINT_0ce41170);
+
+		// Check the child's exit status to see if the MR was violated.
+		if (WIFEXITED(status)) {
+			if (WEXITSTATUS(status) == EXIT_MR_VIOLATION_0ce41170) {
+				fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+			}
+		}
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_110f3bc5.h b/executor/MRs/syz_mr_110f3bc5.h
new file mode 100644
index 000000000..a9e2c78f5
--- /dev/null
+++ b/executor/MRs/syz_mr_110f3bc5.h
@@ -0,0 +1,121 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.72-KVM_GET_PIT2/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 3 iterations
+// MR1: Configuration stability across consecutive KVM_GET_PIT2 calls
+// - Source input: A `KVM_GET_PIT2` ioctl on a VM file descriptor for which a PIT has been created via `KVM_CREATE_PIT2`.
+// - Follow-up input: Another `KVM_GET_PIT2` ioctl on the same file descriptor, with no intervening `KVM_SET_PIT2` ioctl and no vCPU execution (no `KVM_RUN`).
+// - Input relation: The follow-up input is obtained immediately after the source input.
+// - Output relation: Let `output1` be the struct from the source call and `output2` be the struct from the follow-up call. The configuration fields must be identical:
+//   - `output2.flags == output1.flags`
+//   - For each channel `i` in {0, 1, 2}:
+//     - `output2.channels[i].mode == output1.channels[i].mode`
+//     - `output2.channels[i].rw_mode == output1.channels[i].rw_mode`
+//     - `output2.channels[i].bcd == output1.channels[i].bcd`
+//   (No constraint is imposed on time-dependent fields like `channels[i].count`, as they are expected to evolve and may wrap around between reads.)
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * MR1: Configuration stability across consecutive KVM_GET_PIT2 calls
+ *
+ * This function implements the metamorphic relation described below:
+ * - Source input: A `KVM_GET_PIT2` ioctl on a VM file descriptor for which a PIT
+ *   has been created via `KVM_CREATE_PIT2`.
+ * - Follow-up input: Another `KVM_GET_PIT2` ioctl on the same file descriptor,
+ *   with no intervening `KVM_SET_PIT2` ioctl and no vCPU execution (no `KVM_RUN`).
+ * - Input relation: The follow-up input is obtained immediately after the source input.
+ * - Output relation: Let `output1` be the struct from the source call and `output2`
+ *   be the struct from the follow-up call. The configuration fields must be identical:
+ *   - `output2.flags == output1.flags`
+ *   - For each channel `i` in {0, 1, 2}:
+ *     - `output2.channels[i].mode == output1.channels[i].mode`
+ *     - `output2.channels[i].rw_mode == output1.channels[i].rw_mode`
+ *     - `output2.channels[i].bcd == output1.channels[i].bcd`
+ *
+ * @param a0 A volatile long integer used as the flags for the kvm_pit_config struct
+ *           during PIT creation with KVM_CREATE_PIT2.
+ * @return 0 on success (MR test completed), -1 on setup failure.
+ */
+static long syz_mr_110f3bc5(volatile long a0)
+{
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	struct kvm_pit_config pit_config;
+	struct kvm_pit_state2 output1, output2;
+
+	// 1. Perform preparation
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot open KVM, prerequisite for the test is not met.
+		return -1;
+	}
+
+	// Create a Virtual Machine
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd == -1) {
+		// Cannot create VM, prerequisite for the test is not met.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Create a PIT for the VM using the input parameter for flags
+	pit_config.flags = a0;
+	if (ioctl(vm_fd, KVM_CREATE_PIT2, &pit_config) == -1) {
+		// Cannot create PIT, prerequisite for the test is not met.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get the source output
+	if (ioctl(vm_fd, KVM_GET_PIT2, &output1) == -1) {
+		// The source ioctl failed, cannot proceed with the test.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 3. Get the follow-up output
+	if (ioctl(vm_fd, KVM_GET_PIT2, &output2) == -1) {
+		// The follow-up ioctl failed, cannot proceed with the test.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 4. Check the output relation
+	int violation = 0;
+
+	// Compare flags
+	if (output2.flags != output1.flags) {
+		violation = 1;
+	}
+
+	// Compare configuration fields for each channel
+	for (int i = 0; i < 3; i++) {
+		if (output2.channels[i].mode != output1.channels[i].mode ||
+		    output2.channels[i].rw_mode != output1.channels[i].rw_mode ||
+		    output2.channels[i].bcd != output1.channels[i].bcd) {
+			violation = 1;
+			break;
+		}
+	}
+
+	if (violation) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Perform cleanup
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_13cd9ab1.h b/executor/MRs/syz_mr_13cd9ab1.h
new file mode 100644
index 000000000..4153b3cb5
--- /dev/null
+++ b/executor/MRs/syz_mr_13cd9ab1.h
@@ -0,0 +1,140 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_mad/Setting-IsSM-Capability-Bit/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The behavior of a second attempt to open an issm device file depends on the O_NONBLOCK flag.
+// - Source input: A sequence of two `open()` system calls on the same `issm` device file. The first call does not use any special flags, while the second call includes the `O_NONBLOCK` flag.
+// - Follow-up input: A sequence of two `open()` system calls on the same `issm` device file. Both the first and second calls do not use the `O_NONBLOCK` flag.
+// - Input relation: The follow-up input is obtained by removing the `O_NONBLOCK` flag from the second `open()` call in the source input sequence.
+// - Output relation: For the source input, the first `open()` call should succeed, but the second `open()` call should fail immediately with `errno` set to `EAGAIN`. For the follow-up input, the first `open()` call should succeed, and the second `open()` call should block and not return immediately.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <signal.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/types.h>
+#include <sys/wait.h>
+#include <unistd.h>
+
+/**
+ * @brief A simple signal handler for SIGALRM.
+ *
+ * This handler's only purpose is to interrupt a blocking syscall (waitpid)
+ * so that we can implement a timeout.
+ * @param sig The signal number (unused).
+ */
+static void sigalrm_handler_13cd9ab1(int sig)
+{
+	(void)sig; // Avoid unused parameter warning.
+}
+
+/**
+ * @brief Implements the metamorphic relation MR1.
+ *
+ * MR1: The behavior of a second attempt to open an issm device file depends on the O_NONBLOCK flag.
+ * This function tests two scenarios:
+ * 1. Source: open(), then open(O_NONBLOCK). Expects the second open to fail with EAGAIN.
+ * 2. Follow-up: open(), then open(). Expects the second open to block.
+ *
+ * @param a0 A volatile long parameter, required by the syzkaller test harness format (unused in this MR).
+ * @return long Returns 0 on completion.
+ */
+static long syz_mr_13cd9ab1(volatile long a0)
+{
+	(void)a0; // The input parameter is not used in this specific MR.
+
+	const char* dev_path = "/dev/issm";
+
+	// 1. Perform preparation: Check if the device file exists.
+	if (access(dev_path, F_OK) == -1) {
+		// If the device doesn't exist, we cannot perform the test.
+		// This is not a violation, but a prerequisite failure. We return silently.
+		return 0;
+	}
+
+	// --- Test Source Input ---
+	// The first open() should succeed, granting exclusive access.
+	int fd1_src = open(dev_path, O_RDWR);
+	if (fd1_src == -1) {
+		// Cannot proceed if the first open fails. This could be due to permissions
+		// or the device being in use by another unrelated process.
+		return 0;
+	}
+
+	// The second open() with O_NONBLOCK should fail immediately with EAGAIN.
+	int fd2_src = open(dev_path, O_RDWR | O_NONBLOCK);
+	int errno_src = errno;
+
+	// Clean up the first file descriptor.
+	close(fd1_src);
+
+	// 4. Check the output relation for the source input.
+	if (fd2_src != -1 || errno_src != EAGAIN) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		// If the second open unexpectedly succeeded, close its file descriptor.
+		if (fd2_src != -1) {
+			close(fd2_src);
+		}
+	}
+
+	// --- Test Follow-up Input ---
+	// The first open() should succeed, granting exclusive access.
+	int fd1_fup = open(dev_path, O_RDWR);
+	if (fd1_fup == -1) {
+		// Cannot proceed if the first open fails.
+		return 0;
+	}
+
+	pid_t child_pid = fork();
+	if (child_pid == -1) {
+		// Fork failed, cannot perform the follow-up test. Clean up and exit.
+		close(fd1_fup);
+		return 0;
+	}
+
+	if (child_pid == 0) {
+		// Child process: This open() call is expected to block indefinitely.
+		open(dev_path, O_RDWR);
+		// If open() returns, it means it did not block as expected.
+		// This is a violation. Exit with a non-zero status to signal this to the parent.
+		exit(1);
+	} else {
+		// Parent process: Monitor the child to confirm it blocks.
+		struct sigaction sa;
+		memset(&sa, 0, sizeof(sa));
+		sa.sa_handler = sigalrm_handler_13cd9ab1;
+		sigaction(SIGALRM, &sa, NULL);
+
+		// Set a 1-second timer. If the child is still running after 1 second,
+		// we assume it is correctly blocked.
+		alarm(1);
+
+		int status;
+		pid_t result = waitpid(child_pid, &status, 0);
+
+		// Cancel any pending alarm.
+		alarm(0);
+
+		if (result == child_pid) {
+			// The child terminated before the timeout. This means the open() call
+			// did not block, which violates the output relation.
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		} else if (result == -1 && errno == EINTR) {
+			// waitpid() was interrupted by our SIGALRM. This is the expected
+			// outcome, indicating the child was still running (and blocked).
+			// We now need to clean up the blocked child process.
+			kill(child_pid, SIGKILL);
+			waitpid(child_pid, NULL, 0); // Reap the killed child.
+		} else {
+			// An unexpected error occurred with waitpid(). Clean up just in case.
+			kill(child_pid, SIGKILL);
+			waitpid(child_pid, NULL, 0);
+		}
+
+		// Clean up the parent's file descriptor.
+		close(fd1_fup);
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_14c38fa6.h b/executor/MRs/syz_mr_14c38fa6.h
new file mode 100644
index 000000000..31c90e1d8
--- /dev/null
+++ b/executor/MRs/syz_mr_14c38fa6.h
@@ -0,0 +1,239 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.29-KVM_CAP_DIRTY_LOG_RING/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Dirty logging is independent of the written value.
+// - Source input: A KVM guest execution where a specific guest memory page `P` (which is initially clean and configured for dirty logging) is written to with a new value, causing its content to change. Let the set of all dirty pages after this execution be `S_dirty`.
+// - Follow-up input: An identical KVM guest execution, but this time the write operation to page `P` uses the value that is already present in that memory location, thus not changing the page's content.
+// - Input relation: The follow-up input is created by modifying the source input's write operation to a specific memory location. Instead of writing a new value, it writes the existing value back to the same location. All other guest operations and host configurations remain identical.
+// - Output relation: The set of dirty pages reported in the dirty log ring for the follow-up execution, `S'_dirty`, should be identical to the set of dirty pages from the source execution, `S_dirty`. Specifically, page `P` must be marked as dirty in both scenarios.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+#define PAGE_SIZE_14c38fa6 0x1000
+// Use two pages: one for code, one for data.
+#define GUEST_MEM_SIZE_14c38fa6 (2 * PAGE_SIZE_14c38fa6)
+#define GUEST_CODE_OFFSET_14c38fa6 0x0
+#define GUEST_DATA_ADDR_14c38fa6 PAGE_SIZE_14c38fa6
+
+/**
+ * Implements the metamorphic relation: "Dirty logging is independent of the written value."
+ *
+ * The logic is self-contained and does not depend on external input values.
+ * The parameters 'a0' and 'a1' are included to conform to the required function signature
+ * for the syzkaller environment, but they are not used in the implementation of this specific MR.
+ *
+ * @param a0 Unused parameter, present for signature compatibility.
+ * @param a1 Unused parameter, present for signature compatibility.
+ * @return 0 on successful execution of the test, -1 on setup or KVM error.
+ */
+static long syz_mr_14c38fa6(volatile long a0, volatile long a1)
+{
+	// Declare all variables at the top to avoid "crosses initialization" errors with goto.
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	void* guest_mem = NULL;
+	struct kvm_run* run = NULL;
+	unsigned char* source_bitmap = NULL;
+	unsigned char* followup_bitmap = NULL;
+	long ret = 0;
+	int mmap_size = 0;
+	long num_pages = 0;
+	long bitmap_size = 0;
+	struct kvm_userspace_memory_region region;
+
+	// --- 1. Common Setup for KVM VM ---
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// If /dev/kvm is not available, we cannot run the test.
+		// This is a setup issue, not a test failure.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	guest_mem = mmap(NULL, GUEST_MEM_SIZE_14c38fa6, PROT_READ | PROT_WRITE,
+			 MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		guest_mem = NULL; // Ensure guest_mem is NULL for cleanup check
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Initialize the memory region struct members before use.
+	region.slot = 0;
+	region.flags = KVM_MEM_LOG_DIRTY_PAGES;
+	region.guest_phys_addr = 0;
+	region.memory_size = GUEST_MEM_SIZE_14c38fa6;
+	region.userspace_addr = (uint64_t)guest_mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size <= 0) {
+		ret = -1;
+		goto cleanup;
+	}
+	// Explicit cast from void* for C++ compatibility.
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL; // Ensure run is NULL for cleanup
+		ret = -1;
+		goto cleanup;
+	}
+
+	num_pages = GUEST_MEM_SIZE_14c38fa6 / PAGE_SIZE_14c38fa6;
+	bitmap_size = (num_pages + 7) / 8;
+	source_bitmap = (unsigned char*)malloc(bitmap_size);
+	followup_bitmap = (unsigned char*)malloc(bitmap_size);
+	if (!source_bitmap || !followup_bitmap) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// --- 2. Source Execution: Write a new value to a clean page ---
+	{
+		memset(guest_mem, 0, GUEST_MEM_SIZE_14c38fa6);
+		// Cast void* to char* before pointer arithmetic for C++ compatibility.
+		*(unsigned char*)((char*)guest_mem + GUEST_DATA_ADDR_14c38fa6) = 0xAA;
+
+		// Guest code (real mode): write a *different* value (0xBB) and halt.
+		const unsigned char source_code[] = {
+		    0xc6, 0x06, (GUEST_DATA_ADDR_14c38fa6 & 0xff), (GUEST_DATA_ADDR_14c38fa6 >> 8) & 0xff, 0xBB, // movb $0xBB, [GUEST_DATA_ADDR]
+		    0xf4 // hlt
+		};
+		memcpy(guest_mem, source_code, sizeof(source_code));
+
+		// Setup minimal real-mode registers.
+		struct kvm_sregs sregs;
+		if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+		sregs.cs.base = 0;
+		sregs.cs.selector = 0;
+		if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		struct kvm_regs regs = {0};
+		regs.rip = GUEST_CODE_OFFSET_14c38fa6;
+		regs.rflags = 2; // Mandatory flag
+		if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		if (ioctl(vcpu_fd, KVM_RUN, NULL) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+		if (run->exit_reason != KVM_EXIT_HLT) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		// Get the dirty log. This also clears the log for the next run.
+		struct kvm_dirty_log dirty_log = {.slot = 0, .dirty_bitmap = source_bitmap};
+		if (ioctl(vm_fd, KVM_GET_DIRTY_LOG, &dirty_log) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+	}
+
+	// --- 3. Follow-up Execution: Write the same value to a clean page ---
+	{
+		memset(guest_mem, 0, GUEST_MEM_SIZE_14c38fa6);
+		// Cast void* to char* before pointer arithmetic for C++ compatibility.
+		*(unsigned char*)((char*)guest_mem + GUEST_DATA_ADDR_14c38fa6) = 0xAA;
+
+		// Guest code (real mode): write the *same* value (0xAA) back and halt.
+		const unsigned char followup_code[] = {
+		    0xc6, 0x06, (GUEST_DATA_ADDR_14c38fa6 & 0xff), (GUEST_DATA_ADDR_14c38fa6 >> 8) & 0xff, 0xAA, // movb $0xAA, [GUEST_DATA_ADDR]
+		    0xf4 // hlt
+		};
+		memcpy(guest_mem, followup_code, sizeof(followup_code));
+
+		// Reset registers to the same initial state.
+		struct kvm_sregs sregs;
+		if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+		sregs.cs.base = 0;
+		sregs.cs.selector = 0;
+		if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		struct kvm_regs regs = {0};
+		regs.rip = GUEST_CODE_OFFSET_14c38fa6;
+		regs.rflags = 2;
+		if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		if (ioctl(vcpu_fd, KVM_RUN, NULL) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+		if (run->exit_reason != KVM_EXIT_HLT) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		// Get the dirty log for the follow-up execution.
+		struct kvm_dirty_log dirty_log = {.slot = 0, .dirty_bitmap = followup_bitmap};
+		if (ioctl(vm_fd, KVM_GET_DIRTY_LOG, &dirty_log) < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+	}
+
+	// --- 4. Check Output Relation ---
+	// The set of dirty pages should be identical in both cases.
+	if (memcmp(source_bitmap, followup_bitmap, bitmap_size) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// --- 5. Cleanup ---
+	if (source_bitmap)
+		free(source_bitmap);
+	if (followup_bitmap)
+		free(followup_bitmap);
+	if (run)
+		munmap(run, mmap_size);
+	if (guest_mem)
+		munmap(guest_mem, GUEST_MEM_SIZE_14c38fa6);
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_1825cfdc.h b/executor/MRs/syz_mr_1825cfdc.h
new file mode 100644
index 000000000..2d4323ade
--- /dev/null
+++ b/executor/MRs/syz_mr_1825cfdc.h
@@ -0,0 +1,315 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.11-KVM_CAP_HYPERV_SYNIC2/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Enabling SynIC with and without KVM_CAP_HYPERV_SYNIC2
+// - Source input: A KVM VM is created without enabling `KVM_CAP_HYPERV_SYNIC2`. The guest then writes to `MSR_HV_SIMP` (or `MSR_HV_SIEFP`) with the “enable” bit set, pointing to a guest-physical page `P` whose contents are non-zero.
+// - Follow-up input: The same VM configuration and guest state as the source input, except that `KVM_CAP_HYPERV_SYNIC2` is enabled for the VM. The guest then performs the identical write to the same MSR with the same value, pointing to the same page `P` with the same initial non-zero contents.
+// - Input relation: The follow-up input is derived from the source input by enabling the `KVM_CAP_HYPERV_SYNIC2` capability before the guest performs the MSR write. All other aspects, including the initial state of page `P` and the value written to the MSR, remain identical.
+// - Output relation: After the MSR write in the source execution, the contents of page `P` as observed by the guest must be all zeroes. In contrast, after the MSR write in the follow-up execution, the contents of page `P` must be unchanged from their original non-zero state. Both MSR write operations should complete successfully without errors.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdbool.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Guest memory layout
+#define GUEST_MEM_SIZE_1825cfdc 0x100000 // 1MB
+#define PAGE_SIZE_1825cfdc 4096
+#define PAGE_P_GPA_1825cfdc 0x1000
+#define GUEST_CODE_GPA_1825cfdc 0x2000
+#define PML4_GPA_1825cfdc 0x3000
+#define PDPT_GPA_1825cfdc 0x4000
+#define PD_GPA_1825cfdc 0x5000
+
+// Hyper-V MSRs (from arch/x86/include/asm/msr-index.h)
+#define MSR_HV_GUEST_OS_ID_1825cfdc 0x40000000
+#define MSR_HV_SIMP_1825cfdc 0x40000082
+#define MSR_HV_SCONTROL_1825cfdc 0x40000080
+#define HV_SCONTROL_ENABLE_1825cfdc (1 << 0)
+
+// Helper function to run the KVM test case.
+// Returns:
+//   1: if page P was zeroed after the run.
+//   0: if page P was unchanged after the run.
+//  -1: on critical error.
+//  -2: if a required capability is not supported.
+static int run_vm_test_1825cfdc(int kvm_fd, bool enable_synic2,
+				const uint8_t* initial_page_p_content)
+{
+	// All declarations at the top to solve "crosses initialization" errors
+	int ret = -1;
+	int vm_fd = -1;
+	int vcpu_fd = -1;
+	void* guest_mem = NULL;
+	struct kvm_run* run_data = NULL;
+	int mmap_size = 0;
+	struct kvm_msrs* msrs_to_set = NULL;
+	struct kvm_enable_cap cap;
+	struct kvm_userspace_memory_region mem;
+	struct kvm_sregs sregs;
+	struct kvm_regs regs;
+	uint8_t* page_p_hva;
+	uint64_t* guest_mem64;
+	struct kvm_segment seg;
+	uint32_t msr_addr;
+	uint32_t msr_val_lo;
+	uint8_t guest_code[18];
+	uint8_t zero_page[PAGE_SIZE_1825cfdc];
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		perror("[SyzMeta] KVM_CREATE_VM");
+		goto cleanup;
+	}
+
+	memset(&cap, 0, sizeof(cap));
+	cap.cap = KVM_CAP_HYPERV;
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap) < 0) {
+		perror("[SyzMeta] KVM_ENABLE_CAP(HYPERV)");
+		goto cleanup;
+	}
+
+	cap.cap = KVM_CAP_HYPERV_SYNIC;
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap) < 0) {
+		perror("[SyzMeta] KVM_ENABLE_CAP(SYNIC)");
+		goto cleanup;
+	}
+
+	if (enable_synic2) {
+		cap.cap = KVM_CAP_HYPERV_SYNIC2;
+		if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap) < 0) {
+			ret = -2;
+			goto cleanup;
+		}
+	}
+
+	guest_mem = mmap(NULL, GUEST_MEM_SIZE_1825cfdc, PROT_READ | PROT_WRITE,
+			 MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		perror("[SyzMeta] mmap guest_mem");
+		guest_mem = NULL;
+		goto cleanup;
+	}
+
+	memset(&mem, 0, sizeof(mem));
+	mem.slot = 0;
+	mem.guest_phys_addr = 0;
+	mem.memory_size = GUEST_MEM_SIZE_1825cfdc;
+	mem.userspace_addr = (uint64_t)guest_mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &mem) < 0) {
+		perror("[SyzMeta] KVM_SET_USER_MEMORY_REGION");
+		goto cleanup;
+	}
+
+	page_p_hva = (uint8_t*)guest_mem + PAGE_P_GPA_1825cfdc;
+	memcpy(page_p_hva, initial_page_p_content, PAGE_SIZE_1825cfdc);
+
+	guest_mem64 = (uint64_t*)guest_mem;
+	guest_mem64[PML4_GPA_1825cfdc / 8] = PDPT_GPA_1825cfdc | 0x3;
+	guest_mem64[PDPT_GPA_1825cfdc / 8] = PD_GPA_1825cfdc | 0x3;
+	guest_mem64[PD_GPA_1825cfdc / 8] = 0x83;
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		perror("[SyzMeta] KVM_CREATE_VCPU");
+		goto cleanup;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0);
+	if (mmap_size < 0) {
+		perror("[SyzMeta] KVM_GET_VCPU_MMAP_SIZE");
+		goto cleanup;
+	}
+	run_data = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE,
+					 MAP_SHARED, vcpu_fd, 0);
+	if (run_data == MAP_FAILED) {
+		perror("[SyzMeta] mmap kvm_run");
+		run_data = NULL;
+		goto cleanup;
+	}
+
+	msrs_to_set = (struct kvm_msrs*)calloc(
+	    1, sizeof(struct kvm_msrs) + 2 * sizeof(struct kvm_msr_entry));
+	if (msrs_to_set == NULL) {
+		perror("[SyzMeta] calloc for MSRs");
+		goto cleanup;
+	}
+	msrs_to_set->nmsrs = 0;
+	msrs_to_set->entries[msrs_to_set->nmsrs++] =
+	    (struct kvm_msr_entry){.index = MSR_HV_GUEST_OS_ID_1825cfdc, .data = 0x1234};
+	msrs_to_set->entries[msrs_to_set->nmsrs++] = (struct kvm_msr_entry){
+	    .index = MSR_HV_SCONTROL_1825cfdc, .data = HV_SCONTROL_ENABLE_1825cfdc};
+	if (ioctl(vcpu_fd, KVM_SET_MSRS, msrs_to_set) < 0) {
+		perror("[SyzMeta] KVM_SET_MSRS");
+		goto cleanup;
+	}
+	free(msrs_to_set);
+	msrs_to_set = NULL;
+
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) < 0) {
+		perror("[SyzMeta] KVM_GET_SREGS");
+		goto cleanup;
+	}
+	sregs.cr0 = 0x80050033;
+	sregs.cr3 = PML4_GPA_1825cfdc;
+	sregs.cr4 = 0x20;
+	sregs.efer = 0xd01;
+
+	seg = (struct kvm_segment){.base = 0,
+				   .limit = 0xffffffff,
+				   .selector = 1 << 3,
+				   .type = 11,
+				   .present = 1,
+				   .dpl = 0,
+				   .db = 0,
+				   .s = 1,
+				   .l = 1,
+				   .g = 1};
+	sregs.cs = seg;
+	seg.type = 3;
+	seg.selector = 2 << 3;
+	sregs.ds = sregs.es = sregs.fs = sregs.gs = sregs.ss = seg;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) < 0) {
+		perror("[SyzMeta] KVM_SET_SREGS");
+		goto cleanup;
+	}
+
+	msr_addr = MSR_HV_SIMP_1825cfdc;
+	msr_val_lo = PAGE_P_GPA_1825cfdc | 1;
+	// Manually build guest_code array to avoid initializer list
+	guest_code[0] = 0xb9;
+	guest_code[1] = (uint8_t)(msr_addr & 0xff);
+	guest_code[2] = (uint8_t)((msr_addr >> 8) & 0xff);
+	guest_code[3] = (uint8_t)((msr_addr >> 16) & 0xff);
+	guest_code[4] = (uint8_t)((msr_addr >> 24) & 0xff);
+	guest_code[5] = 0xb8;
+	guest_code[6] = (uint8_t)(msr_val_lo & 0xff);
+	guest_code[7] = (uint8_t)((msr_val_lo >> 8) & 0xff);
+	guest_code[8] = (uint8_t)((msr_val_lo >> 16) & 0xff);
+	guest_code[9] = (uint8_t)((msr_val_lo >> 24) & 0xff);
+	guest_code[10] = 0xba;
+	guest_code[11] = 0;
+	guest_code[12] = 0;
+	guest_code[13] = 0;
+	guest_code[14] = 0;
+	guest_code[15] = 0x0f;
+	guest_code[16] = 0x30;
+	guest_code[17] = 0xf4;
+	memcpy((uint8_t*)guest_mem + GUEST_CODE_GPA_1825cfdc, guest_code,
+	       sizeof(guest_code));
+
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs) < 0) {
+		perror("[SyzMeta] KVM_GET_REGS");
+		goto cleanup;
+	}
+	regs.rip = GUEST_CODE_GPA_1825cfdc;
+	regs.rflags = 2;
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0) {
+		perror("[SyzMeta] KVM_SET_REGS");
+		goto cleanup;
+	}
+
+	if (ioctl(vcpu_fd, KVM_RUN, 0) < 0) {
+		perror("[SyzMeta] KVM_RUN");
+		goto cleanup;
+	}
+
+	if (run_data->exit_reason != KVM_EXIT_HLT) {
+		fprintf(stderr, "[SyzMeta] Unexpected exit reason: %d\n",
+			run_data->exit_reason);
+		goto cleanup;
+	}
+
+	memset(zero_page, 0, PAGE_SIZE_1825cfdc);
+	if (memcmp(page_p_hva, zero_page, PAGE_SIZE_1825cfdc) == 0) {
+		ret = 1;
+	} else if (memcmp(page_p_hva, initial_page_p_content, PAGE_SIZE_1825cfdc) == 0) {
+		ret = 0;
+	} else {
+		fprintf(stderr, "[SyzMeta] Page P was modified unexpectedly\n");
+		ret = -1;
+	}
+
+cleanup:
+	if (msrs_to_set)
+		free(msrs_to_set);
+	if (run_data)
+		munmap(run_data, mmap_size);
+	if (guest_mem)
+		munmap(guest_mem, GUEST_MEM_SIZE_1825cfdc);
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	return ret;
+}
+
+static long syz_mr_1825cfdc(volatile long a0)
+{
+	int kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		perror("[SyzMeta] open /dev/kvm");
+		return -1;
+	}
+
+	if (!ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_HYPERV) ||
+	    !ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_HYPERV_SYNIC)) {
+		fprintf(stderr, "[SyzMeta] KVM_CAP_HYPERV or KVM_CAP_HYPERV_SYNIC not "
+				"supported, skipping MR.\n");
+		close(kvm_fd);
+		return 0;
+	}
+
+	uint8_t initial_page_p_content[PAGE_SIZE_1825cfdc];
+	for (int i = 0; i < PAGE_SIZE_1825cfdc; i++) {
+		initial_page_p_content[i] = (i % 255) + 1;
+	}
+
+	// Source execution: without KVM_CAP_HYPERV_SYNIC2
+	int source_result = run_vm_test_1825cfdc(kvm_fd, false, initial_page_p_content);
+	if (source_result < 0) {
+		fprintf(stderr, "[SyzMeta] Source execution failed or was skipped.\n");
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Follow-up execution: with KVM_CAP_HYPERV_SYNIC2
+	int followup_result = run_vm_test_1825cfdc(kvm_fd, true, initial_page_p_content);
+	if (followup_result == -2) {
+		fprintf(stderr,
+			"[SyzMeta] KVM_CAP_HYPERV_SYNIC2 not supported, skipping MR check.\n");
+		close(kvm_fd);
+		return 0;
+	}
+	if (followup_result < 0) {
+		fprintf(stderr, "[SyzMeta] Follow-up execution failed.\n");
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Check output relation
+	// Source: page P must be all zeroes (result == 1)
+	// Follow-up: page P must be unchanged (result == 0)
+	bool source_ok = (source_result == 1);
+	bool followup_ok = (followup_result == 0);
+
+	if (!source_ok || !followup_ok) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		fprintf(stderr,
+			"  Source (no SYNIC2): Page zeroed? %s (Expected: true)\n",
+			source_ok ? "Yes" : "No");
+		fprintf(stderr,
+			"  Follow-up (SYNIC2): Page unchanged? %s (Expected: true)\n",
+			followup_ok ? "Yes" : "No");
+	}
+
+	close(kvm_fd);
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_1d1fd0d1.h b/executor/MRs/syz_mr_1d1fd0d1.h
new file mode 100644
index 000000000..ab1462359
--- /dev/null
+++ b/executor/MRs/syz_mr_1d1fd0d1.h
@@ -0,0 +1,187 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/autofs/autofs/Content/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: A directory's type changes from a mount trap to a regular directory when its state changes from empty to non-empty.
+// - Source input: A sequence of operations that configures an autofs mount and creates an empty directory `D` under conditions where it is specified to be a mount trap. For example, mounting an autofs filesystem with the `indirect` option and then creating a top-level directory `D`.
+// - Follow-up input: A sequence of operations identical to the source input, but with an additional operation that creates an entry (e.g., a file or subdirectory) inside directory `D`.
+// - Input relation: The follow-up input is generated by appending an entry-creation operation (e.g., `creat`, `mkdir`, `symlink`) to the source input's sequence of operations. The target of this new operation is within the directory `D`, thus transforming `D` from an empty to a non-empty state.
+// - Output relation: The directory `D` resulting from the source input should be a "mount trap". The same directory `D` resulting from the follow-up input should be a "regular directory".
+
+#include <dirent.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <signal.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/mount.h>
+#include <sys/stat.h>
+#include <sys/wait.h>
+#include <time.h>
+#include <unistd.h>
+
+// Constants for the test setup
+static const char* MNT_POINT_1d1fd0d1 = "/tmp/mr1_autofs_mp";
+static const char* DIR_D_1d1fd0d1 = "/tmp/mr1_autofs_mp/D";
+static const char* FILE_IN_D_1d1fd0d1 = "/tmp/mr1_autofs_mp/D/file";
+
+// Helper to check if a path is a mount trap by testing if opendir blocks.
+// A key characteristic of an autofs mount trap is that access to it blocks,
+// waiting for the userspace daemon to handle the mount request.
+// Returns 1 if it's a mount trap, 0 if it's a regular directory, -1 on error.
+static int is_mount_trap_1d1fd0d1(const char* path)
+{
+	struct stat st;
+	if (stat(path, &st) != 0) {
+		return -1; // Path doesn't exist or other stat error
+	}
+	if (!S_ISDIR(st.st_mode)) {
+		return -1; // Not a directory
+	}
+
+	pid_t pid = fork();
+	if (pid == -1) {
+		return -1; // fork error
+	}
+
+	if (pid == 0) { // Child process
+		// Attempt to open the directory. This should block if it's a mount trap.
+		DIR* dir = opendir(path);
+		if (dir) {
+			closedir(dir);
+			_exit(0); // Succeeded immediately -> not a mount trap
+		} else {
+			_exit(1); // opendir failed for some other reason
+		}
+	} else { // Parent process
+		sigset_t mask;
+		sigemptyset(&mask);
+		sigaddset(&mask, SIGCHLD);
+
+		// Block SIGCHLD so we can wait for it with a timeout.
+		if (sigprocmask(SIG_BLOCK, &mask, NULL) == -1) {
+			kill(pid, SIGKILL);
+			waitpid(pid, NULL, 0);
+			return -1;
+		}
+
+		struct timespec timeout = {1, 0}; // 1-second timeout
+		int sig = sigtimedwait(&mask, NULL, &timeout);
+
+		// Unblock SIGCHLD
+		sigprocmask(SIG_UNBLOCK, &mask, NULL);
+
+		if (sig == SIGCHLD) {
+			// Child terminated before timeout.
+			int status;
+			waitpid(pid, &status, 0);
+			if (WIFEXITED(status) && WEXITSTATUS(status) == 0) {
+				return 0; // Not a mount trap
+			} else {
+				return -1; // Child errored
+			}
+		} else {
+			// sigtimedwait timed out or was interrupted.
+			// If it timed out (errno == EAGAIN), the child is blocked.
+			if (errno == EAGAIN) {
+				kill(pid, SIGKILL);
+				waitpid(pid, NULL, 0); // Reap the zombie
+				return 1; // It's a mount trap
+			}
+			// Some other error occurred.
+			kill(pid, SIGKILL);
+			waitpid(pid, NULL, 0);
+			return -1;
+		}
+	}
+}
+
+// Helper to set up the autofs mount. Returns 0 on success, -1 on failure.
+static int setup_autofs_1d1fd0d1(int* pipefd)
+{
+	if (mkdir(MNT_POINT_1d1fd0d1, 0755) && errno != EEXIST) {
+		return -1;
+	}
+	if (pipe(pipefd)) {
+		rmdir(MNT_POINT_1d1fd0d1);
+		return -1;
+	}
+
+	char opts[128];
+	snprintf(opts, sizeof(opts), "fd=%d,pgrp=%d,minproto=5,maxproto=5,indirect", pipefd[0], getpgrp());
+
+	if (mount("autofs", MNT_POINT_1d1fd0d1, "autofs", 0, opts)) {
+		close(pipefd[0]);
+		close(pipefd[1]);
+		rmdir(MNT_POINT_1d1fd0d1);
+		return -1;
+	}
+	return 0;
+}
+
+// Helper to tear down the autofs mount and clean up directories.
+static void teardown_autofs_1d1fd0d1(int* pipefd)
+{
+	umount2(MNT_POINT_1d1fd0d1, MNT_DETACH);
+	close(pipefd[0]);
+	close(pipefd[1]);
+	remove(FILE_IN_D_1d1fd0d1);
+	rmdir(DIR_D_1d1fd0d1);
+	rmdir(MNT_POINT_1d1fd0d1);
+}
+
+// Helper for initial cleanup to ensure a clean state from any previous runs.
+static void initial_cleanup_1d1fd0d1(void)
+{
+	umount2(MNT_POINT_1d1fd0d1, MNT_DETACH);
+	remove(FILE_IN_D_1d1fd0d1);
+	rmdir(DIR_D_1d1fd0d1);
+	rmdir(MNT_POINT_1d1fd0d1);
+}
+
+static long syz_mr_1d1fd0d1(volatile long a0, volatile long a1)
+{
+	(void)a0;
+	(void)a1;
+
+	initial_cleanup_1d1fd0d1();
+
+	int p[2];
+	int source_is_trap = -1;
+	int followup_is_trap = -1;
+
+	// --- Source Run ---
+	// Set up autofs and create an empty directory D.
+	if (setup_autofs_1d1fd0d1(p) == 0) {
+		if (mkdir(DIR_D_1d1fd0d1, 0755) == 0) {
+			source_is_trap = is_mount_trap_1d1fd0d1(DIR_D_1d1fd0d1);
+		}
+		teardown_autofs_1d1fd0d1(p);
+	}
+
+	// --- Follow-up Run ---
+	// Set up autofs, create D, and create a file inside D, making it non-empty.
+	if (setup_autofs_1d1fd0d1(p) == 0) {
+		if (mkdir(DIR_D_1d1fd0d1, 0755) == 0) {
+			int fd = creat(FILE_IN_D_1d1fd0d1, 0644);
+			if (fd != -1) {
+				close(fd);
+				followup_is_trap = is_mount_trap_1d1fd0d1(DIR_D_1d1fd0d1);
+			}
+		}
+		teardown_autofs_1d1fd0d1(p);
+	}
+
+	// --- Output Relation Check ---
+	// Expected: D is a mount trap when empty, but a regular directory when not.
+	// source_is_trap should be 1.
+	// followup_is_trap should be 0.
+	// We only check for a violation if both tests ran successfully (results are not -1).
+	if (source_is_trap != -1 && followup_is_trap != -1) {
+		if (source_is_trap != 1 || followup_is_trap != 0) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		}
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_1f3a13c8.h b/executor/MRs/syz_mr_1f3a13c8.h
new file mode 100644
index 000000000..b74231c62
--- /dev/null
+++ b/executor/MRs/syz_mr_1f3a13c8.h
@@ -0,0 +1,207 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/fuse/fuse/Interrupting-filesystem-operations/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Behaviour of a FUSE request interrupted by a non-fatal signal
+// - Source input: A client process issues a long-running FUSE operation (e.g. read). The FUSE daemon deliberately blocks before replying.
+// - Follow-up input: The same client process issues the identical long-running operation, but while it is pending a non-fatal signal (e.g. SIGUSR1) is delivered to the process. The userspace daemon is implemented so that it honours any INTERRUPT request it receives.
+// - Input relation: The follow-up input is produced from the source input by delivering a non-fatal signal to the process after the request has been queued but before the daemon has replied.
+// - Output relation: The follow-up system call must not produce an error other than EINTR. Its result is constrained to be either the same successful result as the source call, or a failure with errno set to EINTR.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <signal.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/prctl.h>
+#include <sys/wait.h>
+#include <unistd.h>
+
+// Structure to pass results from child to parent
+struct child_result_1f3a13c8 {
+	long ret;
+	int err;
+};
+
+// Empty signal handler for SIGUSR1. Its purpose is to interrupt a system call.
+static void sigusr1_handler_1f3a13c8(int sig)
+{
+	(void)sig;
+}
+
+// This function is executed by the child process. It sets up a blocking read
+// and communicates the result back to the parent.
+static void child_proc_main_1f3a13c8(int data_pipe_read_end, int sync_pipe_write_end,
+				     int result_pipe_write_end)
+{
+	// Ensure the child process is killed if the parent (executor) dies.
+	prctl(PR_SET_PDEATHSIG, SIGKILL);
+
+	// Signal to the parent that we are about to call read() and will block.
+	char ready_char = 'R';
+	if (write(sync_pipe_write_end, &ready_char, 1) != 1) {
+		exit(1); // Exit on error
+	}
+	close(sync_pipe_write_end);
+
+	// This read will block until the parent either writes data or sends a signal.
+	char buf[16];
+	long read_res = read(data_pipe_read_end, buf, sizeof(buf));
+	int read_errno = errno;
+
+	// Send the result (return value and errno) back to the parent.
+	struct child_result_1f3a13c8 res = {.ret = read_res, .err = read_errno};
+	if (write(result_pipe_write_end, &res, sizeof(res)) != (ssize_t)sizeof(res)) {
+		exit(1);
+	}
+
+	close(result_pipe_write_end);
+	close(data_pipe_read_end);
+	exit(0);
+}
+
+static long syz_mr_1f3a13c8(volatile long a0, volatile long a1)
+{
+	(void)a0;
+	(void)a1;
+
+	// Set up a non-fatal signal handler for SIGUSR1.
+	struct sigaction sa = {}, old_sa = {};
+	sa.sa_handler = sigusr1_handler_1f3a13c8;
+	sigemptyset(&sa.sa_mask);
+	// We must not set SA_RESTART, so that the signal interrupts the syscall.
+	sa.sa_flags = 0;
+	if (sigaction(SIGUSR1, &sa, &old_sa) == -1) {
+		return -1;
+	}
+
+	long source_res = -1, followup_res = -1;
+	int source_errno = 0, followup_errno = 0;
+
+	// --- Source Run: The blocking call completes successfully ---
+	{
+		int data_pipe[2], sync_pipe[2], result_pipe[2];
+		// This variable is moved here to avoid "crosses initialization" error with goto.
+		char dummy_data[] = "hello";
+
+		if (pipe(data_pipe) == -1 || pipe(sync_pipe) == -1 ||
+		    pipe(result_pipe) == -1) {
+			goto cleanup_signal;
+		}
+
+		pid_t child_pid = fork();
+		if (child_pid == -1) {
+			goto cleanup_pipes_source;
+		}
+
+		if (child_pid == 0) { // Child process
+			close(data_pipe[1]);
+			close(sync_pipe[0]);
+			close(result_pipe[0]);
+			child_proc_main_1f3a13c8(data_pipe[0], sync_pipe[1],
+						 result_pipe[1]);
+			exit(1); // Should not be reached
+		}
+
+		// Parent process
+		close(data_pipe[0]);
+		close(sync_pipe[1]);
+		close(result_pipe[1]);
+
+		char ready_char;
+		if (read(sync_pipe[0], &ready_char, 1) != 1) {
+			(void)waitpid(child_pid, NULL, 0);
+			goto cleanup_pipes_source;
+		}
+
+		if (write(data_pipe[1], dummy_data, sizeof(dummy_data)) != (ssize_t)sizeof(dummy_data)) {
+			(void)waitpid(child_pid, NULL, 0);
+			goto cleanup_pipes_source;
+		}
+
+		struct child_result_1f3a13c8 res;
+		if (read(result_pipe[0], &res, sizeof(res)) != (ssize_t)sizeof(res)) {
+			(void)waitpid(child_pid, NULL, 0);
+			goto cleanup_pipes_source;
+		}
+
+		source_res = res.ret;
+		source_errno = res.err;
+
+		(void)waitpid(child_pid, NULL, 0);
+
+	cleanup_pipes_source:
+		close(data_pipe[1]);
+		close(sync_pipe[0]);
+		close(result_pipe[0]);
+	}
+
+	// --- Follow-up Run: The blocking call is interrupted by a signal ---
+	{
+		int data_pipe[2], sync_pipe[2], result_pipe[2];
+		if (pipe(data_pipe) == -1 || pipe(sync_pipe) == -1 ||
+		    pipe(result_pipe) == -1) {
+			goto cleanup_signal;
+		}
+
+		pid_t child_pid = fork();
+		if (child_pid == -1) {
+			goto cleanup_pipes_followup;
+		}
+
+		if (child_pid == 0) { // Child process
+			close(data_pipe[1]);
+			close(sync_pipe[0]);
+			close(result_pipe[0]);
+			child_proc_main_1f3a13c8(data_pipe[0], sync_pipe[1],
+						 result_pipe[1]);
+			exit(1);
+		}
+
+		// Parent process
+		close(data_pipe[0]);
+		close(sync_pipe[1]);
+		close(result_pipe[1]);
+
+		char ready_char;
+		if (read(sync_pipe[0], &ready_char, 1) != 1) {
+			(void)waitpid(child_pid, NULL, 0);
+			goto cleanup_pipes_followup;
+		}
+
+		kill(child_pid, SIGUSR1);
+
+		struct child_result_1f3a13c8 res;
+		if (read(result_pipe[0], &res, sizeof(res)) != (ssize_t)sizeof(res)) {
+			(void)waitpid(child_pid, NULL, 0);
+			goto cleanup_pipes_followup;
+		}
+
+		followup_res = res.ret;
+		followup_errno = res.err;
+
+		(void)waitpid(child_pid, NULL, 0);
+
+	cleanup_pipes_followup:
+		close(data_pipe[1]);
+		close(sync_pipe[0]);
+		close(result_pipe[0]);
+	}
+
+	// --- Output Relation Check ---
+	// The MR is only applicable if the source call was successful.
+	if (source_res > 0) {
+		int is_same_result = (followup_res == source_res);
+		int is_eintr = (followup_res == -1 && followup_errno == EINTR);
+
+		if (!is_same_result && !is_eintr) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+cleanup_signal:
+	// Restore the original signal handler.
+	sigaction(SIGUSR1, &old_sa, NULL);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_203da363.h b/executor/MRs/syz_mr_203da363.h
new file mode 100644
index 000000000..114ac32b6
--- /dev/null
+++ b/executor/MRs/syz_mr_203da363.h
@@ -0,0 +1,123 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/6.10-KVM_CAP_MIPS_MSA/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Enabling the MIPS MSA capability should grant access to MSA-related registers.
+// - Source input: A KVM virtual machine with a vCPU created on a MIPS architecture host, where the `KVM_CAP_MIPS_MSA` capability has not been enabled for the vCPU.
+// - Follow-up input: The same KVM virtual machine and vCPU from the source input, but after the `KVM_CAP_MIPS_MSA` capability has been successfully enabled for it.
+// - Input relation: The follow-up input is obtained by applying the `ioctl(KVM_ENABLE_CAP)` operation with `capability = KVM_CAP_MIPS_MSA` to the vCPU file descriptor of the source input.
+// - Output relation: For the source input, an attempt to access MSA-specific registers (e.g., via `KVM_GET_ONE_REG` for a register in the `KVM_REG_MIPS_VEC_*` or `KVM_REG_MIPS_MSA_*` series) should result in a failure (e.g., return an error code like `-EINVAL`). For the follow-up input, the same register access operation should succeed.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Define MIPS-specific KVM constants in case they are not available in the build environment's headers.
+// These values are taken from the Linux kernel UAPI headers (arch/mips/include/uapi/asm/kvm.h).
+#ifndef KVM_CAP_MIPS_MSA
+#define KVM_CAP_MIPS_MSA 112
+#endif
+
+#ifndef KVM_REG_MIPS
+#define KVM_REG_MIPS 0x8000000000000000ULL
+#endif
+
+#ifndef KVM_REG_MIPS_MSA_203da363
+#define KVM_REG_MIPS_MSA_203da363 (KVM_REG_MIPS | 0x0000000000040000ULL)
+#endif
+
+#ifndef KVM_REG_MIPS_MSA_CSR_203da363
+#define KVM_REG_MIPS_MSA_CSR_203da363 (KVM_REG_MIPS_MSA_203da363 | 0x1)
+#endif
+
+/*
+ * MR1: Enabling the MIPS MSA capability should grant access to MSA-related registers.
+ * This function implements the metamorphic relation by:
+ * 1. Creating a KVM VM and a vCPU on a MIPS host.
+ * 2. (Source) Attempting to access an MSA-specific register (KVM_REG_MIPS_MSA_CSR)
+ *    before the MSA capability is enabled. This is expected to fail.
+ * 3. (Input Relation) Enabling the KVM_CAP_MIPS_MSA capability for the vCPU.
+ * 4. (Follow-up) Attempting to access the same MSA register again. This is expected
+ *    to succeed.
+ * 5. (Output Relation) Verifying that the source access failed and the follow-up
+ *    access succeeded. If this condition is not met, the MR is violated.
+ */
+static long syz_mr_203da363(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long source_res, followup_res;
+	int ret;
+
+	// 1. Perform preparation: Create KVM, VM, and vCPU.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot open /dev/kvm, so we can't test the MR.
+		// This is a setup issue, not an MR violation.
+		return 0;
+	}
+
+	// Check if the host kernel supports the MIPS MSA capability extension.
+	// If not, the MR is not applicable.
+	ret = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_MIPS_MSA);
+	if (ret <= 0) {
+		close(kvm_fd);
+		return 0;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return 0;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// 2. Get the source output.
+	// Attempt to get an MSA register *before* enabling the MSA capability.
+	// This should fail.
+	struct kvm_one_reg reg = {0};
+	unsigned int msa_csr_val;
+	reg.id = KVM_REG_MIPS_MSA_CSR_203da363;
+	reg.addr = (unsigned long)&msa_csr_val;
+
+	source_res = ioctl(vcpu_fd, KVM_GET_ONE_REG, &reg);
+
+	// 3. Apply the input relation.
+	// Enable the KVM_CAP_MIPS_MSA capability for the vCPU.
+	struct kvm_enable_cap cap = {0};
+	cap.cap = KVM_CAP_MIPS_MSA;
+
+	ret = ioctl(vcpu_fd, KVM_ENABLE_CAP, &cap);
+	if (ret < 0) {
+		// If we can't enable the capability, we can't perform the follow-up step.
+		// This is a setup failure for the follow-up case, not an MR violation.
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// 4. Get the follow-up output.
+	// Attempt to get the same MSA register *after* enabling the capability.
+	// This should succeed.
+	followup_res = ioctl(vcpu_fd, KVM_GET_ONE_REG, &reg);
+
+	// 5. Check the output relation.
+	// The source call should fail (ret < 0), and the follow-up call should succeed (ret >= 0).
+	if (!(source_res < 0 && followup_res >= 0)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 6. Perform cleanup.
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_2281e517.h b/executor/MRs/syz_mr_2281e517.h
new file mode 100644
index 000000000..92682b993
--- /dev/null
+++ b/executor/MRs/syz_mr_2281e517.h
@@ -0,0 +1,286 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_mad/Receiving-MADs/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Handling insufficient buffer size for MAD reads
+// - Source input: A call to the `read()` function on an `rdma_cm` file descriptor, with a buffer of size `S1` that is too small to hold a pending RMPP MAD. For example, `S1 = sizeof(struct ib_user_mad) + 256`.
+// - Follow-up input: A subsequent call to the `read()` function on the same file descriptor, with a new buffer of size `S2`.
+// - Input relation: The source `read()` call must fail with `errno` set to `ENOSPC`. The `length` field of the provided `struct ib_user_mad` will be populated with the required buffer size, let's call it `S_required`. The follow-up input is constructed by allocating a new buffer of size `S2 >= S_required` and calling `read()` with this new buffer.
+// - Output relation: The source `read()` call should return -1. The follow-up `read()` call should succeed and return a value equal to `S_required`, indicating that the complete MAD has now been read into the new, larger buffer.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/types.h>
+#include <sys/wait.h>
+#include <unistd.h>
+
+// --- Start of vendored definitions from infiniband headers ---
+
+// Constants from <infiniband/verbs.h>
+#define IB_MGMT_CLASS_SUBN_DIRECTED_ROUTE_2281e517 0x81
+#define IB_MGMT_METHOD_GET_2281e517 0x01
+
+// Structure from <infiniband/verbs.h>
+struct ib_mad_hdr_2281e517 {
+	uint8_t base_version;
+	uint8_t mgmt_class;
+	uint8_t class_version;
+	uint8_t method;
+	uint16_t status;
+	uint16_t class_specific;
+	uint64_t tid;
+	uint16_t attr_id;
+	uint16_t resv;
+	uint32_t attr_mod;
+};
+
+// Structures from <infiniband/user_mad.h>
+struct ib_user_mad_reg_req_2281e517 {
+	uint8_t mgmt_class;
+	uint8_t mgmt_class_version;
+	uint8_t oui[3];
+	uint8_t method_mask[8];
+	uint32_t id;
+};
+
+struct ib_user_mad_2281e517 {
+	struct ib_mad_hdr_2281e517 hdr;
+	uint64_t rmpp_hdr;
+	uint32_t agent_id;
+	uint32_t pkey_index;
+	uint32_t dest_lid;
+	uint32_t dest_qp;
+	uint32_t dest_qpn;
+	uint32_t port_num;
+	uint32_t timeout_ms;
+	uint32_t retries;
+	uint32_t length;
+	uint8_t data[0];
+};
+
+struct ib_user_port_info_2281e517 {
+	uint8_t port_num;
+	uint8_t reserved;
+	uint16_t base_lid;
+	uint16_t sm_lid;
+	uint8_t cap_mask;
+	uint8_t lmc;
+};
+
+// IOCTL commands from <infiniband/user_mad.h>
+#define IB_USER_MAD_REGISTER_AGENT_2281e517 _IOWR('m', 1, struct ib_user_mad_reg_req_2281e517)
+#define IB_USER_MAD_UNREGISTER_AGENT_2281e517 _IOW('m', 2, uint32_t)
+#define IB_USER_MAD_GET_PORT_2281e517 _IOWR('m', 4, struct ib_user_port_info_2281e517)
+
+// --- End of vendored definitions ---
+
+#define UMAD_DEVICE_PATH_2281e517 "/dev/infiniband/umad0"
+// The data payload of the MAD sent by the child process.
+// This must be larger than the data portion of the parent's initial small buffer.
+#define LARGE_MAD_DATA_PAYLOAD_2281e517 2048
+
+// A simple struct to pass port information from parent to child.
+struct port_info_2281e517 {
+	uint16_t lid;
+	uint16_t pkey_index;
+};
+
+// Helper function to register a MAD agent.
+static int register_agent_2281e517(int fd, int* agent_id)
+{
+	struct ib_user_mad_reg_req_2281e517 reg_req = {
+	    .mgmt_class = IB_MGMT_CLASS_SUBN_DIRECTED_ROUTE_2281e517,
+	    .mgmt_class_version = 1,
+	};
+	// Receive all methods for this management class.
+	memset(reg_req.method_mask, 0xff, sizeof(reg_req.method_mask));
+
+	if (ioctl(fd, IB_USER_MAD_REGISTER_AGENT_2281e517, &reg_req) < 0) {
+		// This can fail if the user doesn't have permissions or the kernel module is not loaded.
+		return -1;
+	}
+	*agent_id = reg_req.id;
+	return 0;
+}
+
+// Helper function to unregister a MAD agent.
+static void unregister_agent_2281e517(int fd, int agent_id)
+{
+	if (ioctl(fd, IB_USER_MAD_UNREGISTER_AGENT_2281e517, &agent_id) < 0) {
+		// Non-critical, but good to know if it fails.
+	}
+}
+
+static long syz_mr_2281e517(volatile long a0)
+{
+	// Declarations are moved to the top of the function to avoid "crosses initialization" errors.
+	int parent_fd = -1, child_fd = -1;
+	int parent_agent_id = -1, child_agent_id = -1;
+	pid_t pid;
+	int p2c_pipe[2] = {-1, -1};
+	int c2p_pipe[2] = {-1, -1};
+	long ret_val = 0;
+	struct ib_user_port_info_2281e517 port_info_req;
+	struct port_info_2281e517 p_info;
+	char ready_signal;
+	void* buffer1 = NULL;
+	size_t s1_size;
+	ssize_t source_ret;
+	int source_errno;
+
+	if (a0 < 0 || a0 >= LARGE_MAD_DATA_PAYLOAD_2281e517) {
+		a0 = 256;
+	}
+
+	if (pipe(p2c_pipe) == -1 || pipe(c2p_pipe) == -1) {
+		ret_val = -1;
+		goto end;
+	}
+
+	parent_fd = open(UMAD_DEVICE_PATH_2281e517, O_RDWR);
+	if (parent_fd < 0) {
+		ret_val = -1;
+		goto cleanup_pipes;
+	}
+
+	memset(&port_info_req, 0, sizeof(port_info_req));
+	port_info_req.port_num = 1;
+	if (ioctl(parent_fd, IB_USER_MAD_GET_PORT_2281e517, &port_info_req) < 0) {
+		ret_val = -1;
+		goto cleanup_parent_fd;
+	}
+	p_info.lid = port_info_req.base_lid;
+	p_info.pkey_index = 0;
+
+	pid = fork();
+	if (pid < 0) {
+		ret_val = -1;
+		goto cleanup_parent_fd;
+	}
+
+	if (pid == 0) {
+		// --- Child Process (Sender) ---
+		close(p2c_pipe[1]);
+		close(c2p_pipe[0]);
+
+		struct port_info_2281e517 target_port;
+		if (read(p2c_pipe[0], &target_port, sizeof(target_port)) != sizeof(target_port)) {
+			exit(1);
+		}
+
+		child_fd = open(UMAD_DEVICE_PATH_2281e517, O_RDWR);
+		if (child_fd < 0) {
+			exit(1);
+		}
+
+		if (register_agent_2281e517(child_fd, &child_agent_id) != 0) {
+			close(child_fd);
+			exit(1);
+		}
+
+		size_t mad_send_size = sizeof(struct ib_user_mad_2281e517) + LARGE_MAD_DATA_PAYLOAD_2281e517;
+		struct ib_user_mad_2281e517* send_mad = (struct ib_user_mad_2281e517*)malloc(mad_send_size);
+		if (!send_mad) {
+			unregister_agent_2281e517(child_fd, child_agent_id);
+			close(child_fd);
+			exit(1);
+		}
+		memset(send_mad, 0, mad_send_size);
+
+		send_mad->hdr.base_version = 1;
+		send_mad->hdr.mgmt_class = IB_MGMT_CLASS_SUBN_DIRECTED_ROUTE_2281e517;
+		send_mad->hdr.class_version = 1;
+		send_mad->hdr.method = IB_MGMT_METHOD_GET_2281e517;
+		send_mad->hdr.tid = 0xdeadbeefcafebabe;
+		send_mad->agent_id = child_agent_id;
+		send_mad->pkey_index = target_port.pkey_index;
+		send_mad->dest_lid = target_port.lid;
+		send_mad->port_num = 1;
+		send_mad->timeout_ms = 1000;
+		send_mad->retries = 3;
+		send_mad->length = mad_send_size;
+		memset(send_mad->data, 0xAA, LARGE_MAD_DATA_PAYLOAD_2281e517);
+
+		ready_signal = 'R';
+		if (write(c2p_pipe[1], &ready_signal, 1) < 0) {
+		}
+
+		if (write(child_fd, send_mad, mad_send_size) < 0) {
+		}
+
+		free(send_mad);
+		unregister_agent_2281e517(child_fd, child_agent_id);
+		close(child_fd);
+		close(p2c_pipe[0]);
+		close(c2p_pipe[1]);
+		exit(0);
+	} else {
+		// --- Parent Process (Receiver) ---
+		close(p2c_pipe[0]);
+		close(c2p_pipe[1]);
+
+		if (write(p2c_pipe[1], &p_info, sizeof(p_info)) < 0) {
+		}
+
+		if (register_agent_2281e517(parent_fd, &parent_agent_id) != 0) {
+			ret_val = -1;
+			goto cleanup_child_wait;
+		}
+
+		if (read(c2p_pipe[0], &ready_signal, 1) < 0) {
+		}
+
+		s1_size = sizeof(struct ib_user_mad_2281e517) + a0;
+		buffer1 = malloc(s1_size);
+		if (!buffer1) {
+			ret_val = -1;
+			goto cleanup_agent;
+		}
+
+		source_ret = read(parent_fd, buffer1, s1_size);
+		source_errno = errno;
+
+		if (source_ret == -1 && source_errno == ENOSPC) {
+			struct ib_user_mad_2281e517* mad_info = (struct ib_user_mad_2281e517*)buffer1;
+			size_t s_required = mad_info->length;
+			size_t s2_size = s_required;
+			void* buffer2 = malloc(s2_size);
+
+			if (buffer2) {
+				ssize_t followup_ret = read(parent_fd, buffer2, s2_size);
+
+				if (followup_ret != (ssize_t)s_required) {
+					fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+				}
+				free(buffer2);
+			} else {
+				ret_val = -1;
+			}
+		}
+
+		free(buffer1);
+
+	cleanup_agent:
+		unregister_agent_2281e517(parent_fd, parent_agent_id);
+	cleanup_child_wait:
+		wait(NULL);
+	cleanup_parent_fd:
+		close(parent_fd);
+	cleanup_pipes:
+		if (p2c_pipe[0] != -1)
+			close(p2c_pipe[0]);
+		if (p2c_pipe[1] != -1)
+			close(p2c_pipe[1]);
+		if (c2p_pipe[0] != -1)
+			close(c2p_pipe[0]);
+		if (c2p_pipe[1] != -1)
+			close(c2p_pipe[1]);
+	}
+
+end:
+	return ret_val;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_23de5f08.h b/executor/MRs/syz_mr_23de5f08.h
new file mode 100644
index 000000000..df4c77d64
--- /dev/null
+++ b/executor/MRs/syz_mr_23de5f08.h
@@ -0,0 +1,183 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/ipoib/Datagram-vs-Connected-modes/impl/mr.h
+// Code generated by gemini-2.5-pro after 6 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The IPoIB interface MTU is dependent on the operational mode.
+// - Source input: A set of configuration operations for an IPoIB interface. Specifically: 1. Set the operational mode to `datagram`. 2. Attempt to set the interface MTU to a value `M` that is larger than the underlying IB link's MTU (e.g., `M` = 4096 while the link MTU is 2048).
+// - Follow-up input: A set of configuration operations for the same IPoIB interface. Specifically: 1. Set the operational mode to `connected`. 2. Attempt to set the interface MTU to the same value `M`.
+// - Input relation: The follow-up input is obtained by changing the operational mode of the IPoIB interface from `datagram` to `connected` before attempting to set the MTU.
+// - Output relation: Let `MTU_source` be the actual MTU of the interface after the source operations, and `MTU_followup` be the actual MTU after the follow-up operations. The attempt to set the MTU in the source input should fail or be capped, resulting in `MTU_source` being significantly less than `M` (e.g., 2044). The attempt to set the MTU in the follow-up input should succeed, resulting in `MTU_followup` being equal to `M`. Therefore, the relation `MTU_followup` > `MTU_source` must hold.
+
+#include <arpa/inet.h>
+#include <dirent.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <net/if.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/socket.h>
+#include <unistd.h>
+
+// Helper function to find the first IPoIB-like interface.
+// An IPoIB interface is identified by the presence of a 'mode' file in its sysfs directory.
+static int find_ipoib_interface_23de5f08(char* iface_name, size_t size)
+{
+	DIR* d = opendir("/sys/class/net");
+	if (!d) {
+		// Cannot open directory, prerequisite not met.
+		return -1;
+	}
+
+	struct dirent* dir;
+	int found = 0;
+	while ((dir = readdir(d)) != NULL) {
+		if (dir->d_name[0] == '.')
+			continue;
+
+		// Increased buffer size to prevent potential truncation warnings.
+		char mode_path[512];
+		snprintf(mode_path, sizeof(mode_path), "/sys/class/net/%s/mode", dir->d_name);
+		// Check if the 'mode' file exists and is accessible.
+		if (access(mode_path, F_OK) == 0) {
+			// Add a length check to ensure the directory name fits in the iface_name buffer.
+			// This resolves the -Wformat-truncation error.
+			if (strlen(dir->d_name) < size) {
+				snprintf(iface_name, size, "%s", dir->d_name);
+				found = 1;
+				break;
+			}
+		}
+	}
+	closedir(d);
+	return found ? 0 : -1;
+}
+
+// Helper function to read a string from a sysfs file.
+static int read_sysfs_23de5f08(const char* iface, const char* file, char* buf, size_t size)
+{
+	char path[512];
+	snprintf(path, sizeof(path), "/sys/class/net/%s/%s", iface, file);
+	int fd = open(path, O_RDONLY);
+	if (fd < 0) {
+		return -1;
+	}
+	ssize_t bytes_read = read(fd, buf, size - 1);
+	close(fd);
+	if (bytes_read > 0) {
+		buf[bytes_read] = '\0';
+		// Trim trailing newline character.
+		if (buf[bytes_read - 1] == '\n') {
+			buf[bytes_read - 1] = '\0';
+		}
+		return 0;
+	}
+	return -1;
+}
+
+// Helper function to write a string to a sysfs file.
+static int write_sysfs_23de5f08(const char* iface, const char* file, const char* value)
+{
+	char path[512];
+	snprintf(path, sizeof(path), "/sys/class/net/%s/%s", iface, file);
+	int fd = open(path, O_WRONLY);
+	if (fd < 0) {
+		return -1;
+	}
+	ssize_t written = write(fd, value, strlen(value));
+	close(fd);
+	return (written == (ssize_t)strlen(value)) ? 0 : -1;
+}
+
+// Helper function to get the MTU of a network interface.
+static int get_mtu_23de5f08(int sock, const char* iface_name)
+{
+	struct ifreq ifr;
+	memset(&ifr, 0, sizeof(ifr));
+	// Use snprintf instead of strncpy to avoid truncation warnings.
+	snprintf(ifr.ifr_name, IFNAMSIZ, "%s", iface_name);
+	if (ioctl(sock, SIOCGIFMTU, &ifr) < 0) {
+		return -1;
+	}
+	return ifr.ifr_mtu;
+}
+
+// Helper function to set the MTU of a network interface.
+static int set_mtu_23de5f08(int sock, const char* iface_name, int mtu)
+{
+	struct ifreq ifr;
+	memset(&ifr, 0, sizeof(ifr));
+	// Use snprintf instead of strncpy to avoid truncation warnings.
+	snprintf(ifr.ifr_name, IFNAMSIZ, "%s", iface_name);
+	ifr.ifr_mtu = mtu;
+	if (ioctl(sock, SIOCSIFMTU, &ifr) < 0) {
+		// Failure to set MTU can be an expected part of the test, so we don't treat it as a fatal error here.
+		return -1;
+	}
+	return 0;
+}
+
+static long syz_mr_23de5f08(volatile long a0)
+{
+	// The source input 'M' is the target MTU.
+	long M = a0;
+	// Use a default large MTU if the input is not sensible.
+	if (M <= 0) {
+		M = 4096;
+	}
+
+	// 1. Perform preparation.
+	char iface_name[IFNAMSIZ];
+	if (find_ipoib_interface_23de5f08(iface_name, sizeof(iface_name)) != 0) {
+		return -1;
+	}
+
+	int sock = socket(AF_INET, SOCK_DGRAM, 0);
+	if (sock < 0) {
+		return -1;
+	}
+
+	// Save the original state of the interface for later restoration.
+	int orig_mtu = get_mtu_23de5f08(sock, iface_name);
+	char orig_mode[32];
+	int has_orig_mode = (read_sysfs_23de5f08(iface_name, "mode", orig_mode, sizeof(orig_mode)) == 0);
+
+	// Declare variables at the top of the function to avoid "crosses initialization" error with goto.
+	long mtu_source = -1;
+	long mtu_followup = -1;
+
+	// 2. Get the source output.
+	if (write_sysfs_23de5f08(iface_name, "mode", "datagram") == 0) {
+		set_mtu_23de5f08(sock, iface_name, M);
+		mtu_source = get_mtu_23de5f08(sock, iface_name);
+	} else {
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output.
+	if (write_sysfs_23de5f08(iface_name, "mode", "connected") == 0) {
+		set_mtu_23de5f08(sock, iface_name, M);
+		mtu_followup = get_mtu_23de5f08(sock, iface_name);
+	} else {
+		goto cleanup;
+	}
+
+	// 4. Check if the output relation is violated.
+	if (mtu_source != -1 && mtu_followup != -1) {
+		if (mtu_followup <= mtu_source) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		}
+	}
+
+cleanup:
+	// 5. Cleanup: Restore the interface to its original state.
+	if (orig_mtu != -1) {
+		set_mtu_23de5f08(sock, iface_name, orig_mtu);
+	}
+	if (has_orig_mode) {
+		write_sysfs_23de5f08(iface_name, "mode", orig_mode);
+	}
+	close(sock);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_28861e6f.h b/executor/MRs/syz_mr_28861e6f.h
new file mode 100644
index 000000000..9b223dd55
--- /dev/null
+++ b/executor/MRs/syz_mr_28861e6f.h
@@ -0,0 +1,102 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.113-KVM_HYPERV_EVENTFD/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Successful assignment of a Hyper-V eventfd can be reversed by a subsequent deassignment.
+// - Source input: An `ioctl` call to `KVM_HYPERV_EVENTFD` with parameters specified by a `struct kvm_hyperv_eventfd` denoted as `S`. `S` must satisfy the following conditions:
+//     1. `S.conn_id` is a valid connection ID (within the `KVM_HYPERV_CONN_ID_MASK`) that is not currently registered.
+//     2. `S.fd` is a valid file descriptor obtained from a call to `eventfd()`.
+//     3. `S.flags` is `0` to indicate an assignment operation.
+// - Follow-up input: An `ioctl` call to `KVM_HYPERV_EVENTFD` with parameters specified by a `struct kvm_hyperv_eventfd` denoted as `F`. `F` is generated based on the source input `S`.
+// - Input relation: The follow-up input `F` is derived from the source input `S` as follows:
+//     1. `F.conn_id` is identical to `S.conn_id`.
+//     2. `F.fd` is identical to `S.fd` (or any valid integer, as it is typically ignored on deassignment).
+//     3. `F.flags` is set to `KVM_HYPERV_EVENTFD_DEASSIGN`.
+// - Output relation: Both the source and follow-up `ioctl` calls should succeed. Let the return value of the source operation be `R_S` and the follow-up operation be `R_F`. The expected relation is: `R_S == 0` and `R_F == 0`.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <string.h>
+#include <sys/eventfd.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Implements the metamorphic relation:
+ * MR1: Successful assignment of a Hyper-V eventfd can be reversed by a subsequent deassignment.
+ * @param a0 Source input for `conn_id` to be used in the `struct kvm_hyperv_eventfd`.
+ * @return 0 on completion.
+ */
+static long syz_mr_28861e6f(volatile long a0)
+{
+	long conn_id = a0;
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	int event_fd = -1;
+	long R_S, R_F;
+
+	// 1. Perform preparation
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is not available, cannot perform the test.
+		return 0;
+	}
+
+	// Create a VM
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		goto cleanup;
+	}
+
+	// Create an eventfd
+	event_fd = eventfd(0, 0);
+	if (event_fd == -1) {
+		goto cleanup;
+	}
+
+	// 2. Get the source output
+	// Prepare the source input struct S for assignment
+	struct kvm_hyperv_eventfd S;
+	memset(&S, 0, sizeof(S));
+	S.conn_id = conn_id & KVM_HYPERV_CONN_ID_MASK; // Ensure conn_id is within the valid mask
+	S.fd = event_fd;
+	S.flags = 0; // Flag for assignment
+
+	// Execute the source ioctl call
+	R_S = ioctl(vm_fd, KVM_HYPERV_EVENTFD, &S);
+
+	// The MR premise is a "Successful assignment". If the source call fails,
+	// the premise is not met, and we cannot test the relation.
+	if (R_S != 0) {
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output
+	// Prepare the follow-up input struct F for deassignment
+	struct kvm_hyperv_eventfd F;
+	memset(&F, 0, sizeof(F));
+	F.conn_id = S.conn_id; // Identical conn_id
+	F.fd = S.fd; // Identical fd
+	F.flags = KVM_HYPERV_EVENTFD_DEASSIGN; // Flag for deassignment
+
+	// Execute the follow-up ioctl call
+	R_F = ioctl(vm_fd, KVM_HYPERV_EVENTFD, &F);
+
+	// 4. Check the output relation
+	// The relation is R_S == 0 and R_F == 0. We already know R_S is 0.
+	if (R_F != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	// 5. Perform cleanup
+	if (event_fd != -1)
+		close(event_fd);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_2a839201.h b/executor/MRs/syz_mr_2a839201.h
new file mode 100644
index 000000000..21ea8c6c4
--- /dev/null
+++ b/executor/MRs/syz_mr_2a839201.h
@@ -0,0 +1,142 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.118-KVM_GET_SUPPORTED_HV_CPUID/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Increasing the buffer size for KVM_GET_SUPPORTED_HV_CPUID should not alter the result.
+// - Source input: A `struct kvm_cpuid2` where the `nent` field is set to a value `N` that is exactly the number of supported Hyper-V CPUID leaves. This call is expected to succeed. The value `N` can be determined by a preliminary successful call with a large buffer.
+// - Follow-up input: A `struct kvm_cpuid2` where the `nent` field is set to `N + k`, where `k` is any positive integer (e.g., `k=1`).
+// - Input relation: The follow-up input's `nent` field is greater than the source input's `nent` field.
+// - Output relation: Both the source and follow-up calls should succeed. The returned `nent` value from the follow-up call must be equal to the returned `nent` value from the source call (both should be `N`). Furthermore, the contents of the `entries` array returned by the source call must be identical to the first `N` elements of the `entries` array returned by the follow-up call.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// MR1: Increasing the buffer size for KVM_GET_SUPPORTED_HV_CPUID should not alter the result.
+// The parameter a0 is unused in this MR, but required by the fuzzer's template.
+static long syz_mr_2a839201(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long ret = 0;
+	struct kvm_cpuid2* cpuid_pre = NULL;
+	struct kvm_cpuid2* cpuid_source = NULL;
+	struct kvm_cpuid2* cpuid_followup = NULL;
+	uint32_t nent_pre_size = 256; // A reasonably large initial buffer size
+	uint32_t nent_exact = 0;
+	const uint32_t k = 1; // A small positive integer for the follow-up input
+	size_t pre_alloc_size = 0;
+	size_t source_alloc_size = 0;
+	size_t followup_alloc_size = 0;
+
+	// 1. Preparation: Set up KVM, VM, and vCPU
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM not available, cannot run the test.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		ret = -1;
+		goto cleanup_kvm;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		ret = -1;
+		goto cleanup_vm;
+	}
+
+	// 2. Determine the exact number of entries (N) by calling with a large buffer
+	pre_alloc_size = sizeof(struct kvm_cpuid2) + nent_pre_size * sizeof(struct kvm_cpuid_entry2);
+	cpuid_pre = (struct kvm_cpuid2*)malloc(pre_alloc_size);
+	if (!cpuid_pre) {
+		ret = -1;
+		goto cleanup_vcpu;
+	}
+	memset(cpuid_pre, 0, pre_alloc_size);
+	cpuid_pre->nent = nent_pre_size;
+
+	if (ioctl(vcpu_fd, KVM_GET_SUPPORTED_HV_CPUID, cpuid_pre) < 0) {
+		// If the ioctl is not supported, we can't perform the test.
+		if (errno == EOPNOTSUPP || errno == ENOENT) {
+			ret = 0; // Test is not applicable, but not a setup failure.
+		} else {
+			ret = -1; // Some other error occurred.
+		}
+		goto cleanup_pre;
+	}
+
+	nent_exact = cpuid_pre->nent;
+	free(cpuid_pre);
+	cpuid_pre = NULL;
+
+	// 3. Source call: nent = N (nent_exact)
+	source_alloc_size = sizeof(struct kvm_cpuid2) + nent_exact * sizeof(struct kvm_cpuid_entry2);
+	cpuid_source = (struct kvm_cpuid2*)malloc(source_alloc_size);
+	if (!cpuid_source) {
+		ret = -1;
+		goto cleanup_vcpu;
+	}
+	memset(cpuid_source, 0, source_alloc_size);
+	cpuid_source->nent = nent_exact;
+
+	if (ioctl(vcpu_fd, KVM_GET_SUPPORTED_HV_CPUID, cpuid_source) < 0) {
+		// The source call with the exact size should succeed. If it fails, it's a setup error.
+		ret = -1;
+		goto cleanup_source;
+	}
+
+	// 4. Follow-up call: nent = N + k
+	followup_alloc_size = sizeof(struct kvm_cpuid2) + (nent_exact + k) * sizeof(struct kvm_cpuid_entry2);
+	cpuid_followup = (struct kvm_cpuid2*)malloc(followup_alloc_size);
+	if (!cpuid_followup) {
+		ret = -1;
+		goto cleanup_source;
+	}
+	memset(cpuid_followup, 0, followup_alloc_size);
+	cpuid_followup->nent = nent_exact + k;
+
+	if (ioctl(vcpu_fd, KVM_GET_SUPPORTED_HV_CPUID, cpuid_followup) < 0) {
+		// The follow-up call should also succeed. If it fails, it's a setup error.
+		ret = -1;
+		goto cleanup_followup;
+	}
+
+	// 5. Verify the output relation
+	// Both calls should have succeeded at this point.
+
+	// Relation: The returned nent value from the follow-up call must be equal to the returned nent value from the source call.
+	if (cpuid_source->nent != cpuid_followup->nent) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+	// Relation: The contents of the entries array from the source call must be identical to the first N elements of the entries array from the follow-up call.
+	else if (cpuid_source->nent > 0 &&
+		 memcmp(cpuid_source->entries, cpuid_followup->entries,
+			cpuid_source->nent * sizeof(struct kvm_cpuid_entry2)) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup_followup:
+	free(cpuid_followup);
+cleanup_source:
+	free(cpuid_source);
+cleanup_pre:
+	free(cpuid_pre);
+cleanup_vcpu:
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+cleanup_vm:
+	if (vm_fd != -1)
+		close(vm_fd);
+cleanup_kvm:
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_351170b6.h b/executor/MRs/syz_mr_351170b6.h
new file mode 100644
index 000000000..125c59065
--- /dev/null
+++ b/executor/MRs/syz_mr_351170b6.h
@@ -0,0 +1,196 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/7.4-KVM_CAP_S390_USER_STSI/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: KVM exit information for the s390 STSI instruction should correctly reflect the instruction's operands.
+// - Source input: An s390 guest VCPU configured to execute an `STSI` (Store System Information) instruction with a specific set of operands (function code `fc1`, selector 1 `sel1_1`, selector 2 `sel2_1`, target address `addr1`, and access register `ar1`). This requires the `KVM_CAP_S390_USER_STSI` capability to be enabled.
+// - Follow-up input: The same s390 guest VCPU, but configured to execute an `STSI` instruction where at least one operand is different (e.g., function code `fc2`, which is different from `fc1`, while other operands remain the same).
+// - Input relation: The follow-up input is generated by modifying one or more operands of the `STSI` instruction that the guest VCPU is about to execute.
+// - Output relation: After running both the source and follow-up inputs, the KVM hypervisor should exit to userspace.
+//   1. The `exit_reason` for both outputs must be `KVM_EXIT_S390_STSI`.
+//   2. The `s390_stsi` structure in the source output must contain fields corresponding to the source input's operands (`fc` = `fc1`, `sel1` = `sel1_1`, etc.).
+//   3. The `s390_stsi` structure in the follow-up output must contain fields corresponding to the follow-up input's operands (`fc` = `fc2`, `sel1` = `sel1_1`, etc.), accurately reflecting the modified operands.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Manually define the s390-specific KVM struct because it may not be available
+// when compiling on a non-s390 host (like the syzkaller executor).
+struct kvm_s390_stsi_351170b6 {
+	uint64_t addr;
+	uint8_t ar;
+	uint8_t reserved;
+	uint16_t sel1;
+	uint32_t sel2;
+	uint32_t fc;
+};
+
+// This function implements the metamorphic relation for KVM s390 STSI exits.
+// It verifies that the exit information reported by KVM in the kvm_run structure
+// correctly reflects the STSI instruction operands provided by userspace.
+static long syz_mr_351170b6(volatile long fc1_in, volatile long sel1_1_in, volatile long sel2_1_in, volatile long addr1_in, volatile long ar1_in)
+{
+	// All variable declarations are moved to the top to avoid "crosses initialization" errors with goto.
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	int run_size = 0;
+	struct kvm_run* run = NULL;
+	void* guest_mem = MAP_FAILED;
+	const size_t mem_size = 4096; // 1 page for guest memory
+	struct kvm_userspace_memory_region region;
+	uint32_t fc1, sel1_1, sel2_1;
+	uint64_t addr1;
+	uint8_t ar1;
+	uint32_t src_exit_reason;
+	struct kvm_s390_stsi_351170b6 src_stsi_data;
+	uint32_t fc2, sel1_2, sel2_2;
+	uint64_t addr2;
+	uint8_t ar2;
+	uint32_t flp_exit_reason;
+	struct kvm_s390_stsi_351170b6 flp_stsi_data;
+
+	// 1. Perform preparation
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		goto cleanup;
+	}
+
+	// Check for the required capability. The test relies on userspace being able
+	// to inject STSI exits.
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_S390_USER_STSI) <= 0) {
+		goto cleanup; // Capability not supported, cannot perform the test.
+	}
+
+	// Create a Virtual Machine
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		goto cleanup;
+	}
+
+	// Allocate memory for the guest. The STSI instruction needs a target address.
+	guest_mem = mmap(NULL, mem_size, PROT_READ | PROT_WRITE, MAP_SHARED | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		goto cleanup;
+	}
+
+	// Register the allocated memory with the VM.
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = mem_size;
+	region.userspace_addr = (uint64_t)guest_mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		goto cleanup;
+	}
+
+	// Create a VCPU
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		goto cleanup;
+	}
+
+	// Map the kvm_run structure into userspace.
+	run_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (run_size <= 0) {
+		goto cleanup;
+	}
+	run = (struct kvm_run*)mmap(NULL, run_size, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL; // Ensure run is NULL for cleanup check
+		goto cleanup;
+	}
+
+	// Sanitize input parameters to ensure they are valid for the STSI instruction.
+	fc1 = fc1_in;
+	sel1_1 = sel1_1_in;
+	sel2_1 = sel2_1_in;
+	addr1 = addr1_in & (mem_size - 1); // Ensure address is within guest memory
+	ar1 = ar1_in & 0xf; // Access register is 4 bits
+
+	// 2. Execute the source input
+	// Configure the kvm_run struct to trigger an immediate STSI exit with source operands.
+	// This simulates the guest executing an STSI instruction and KVM exiting to userspace.
+	run->exit_reason = KVM_EXIT_S390_STSI;
+	run->s390_stsi.fc = fc1;
+	run->s390_stsi.sel1 = sel1_1;
+	run->s390_stsi.sel2 = sel2_1;
+	run->s390_stsi.addr = addr1;
+	run->s390_stsi.ar = ar1;
+	run->immediate_exit = 1; // Ask KVM to exit immediately without running guest code.
+
+	if (ioctl(vcpu_fd, KVM_RUN, 0) == -1) {
+		goto cleanup; // An error during KVM_RUN means the test cannot proceed.
+	}
+
+	// Capture the source output from the kvm_run struct after KVM_RUN returns.
+	src_exit_reason = run->exit_reason;
+	memcpy(&src_stsi_data, &run->s390_stsi, sizeof(src_stsi_data));
+
+	// 3. Generate and execute the follow-up input
+	// Modify one operand to create the follow-up input.
+	fc2 = fc1 + 1;
+	sel1_2 = sel1_1;
+	sel2_2 = sel2_1;
+	addr2 = addr1;
+	ar2 = ar1;
+
+	// Configure the kvm_run struct for the follow-up execution.
+	run->exit_reason = KVM_EXIT_S390_STSI;
+	run->s390_stsi.fc = fc2;
+	run->s390_stsi.sel1 = sel1_2;
+	run->s390_stsi.sel2 = sel2_2;
+	run->s390_stsi.addr = addr2;
+	run->s390_stsi.ar = ar2;
+	run->immediate_exit = 1;
+
+	if (ioctl(vcpu_fd, KVM_RUN, 0) == -1) {
+		goto cleanup;
+	}
+
+	// Capture the follow-up output.
+	flp_exit_reason = run->exit_reason;
+	memcpy(&flp_stsi_data, &run->s390_stsi, sizeof(flp_stsi_data));
+
+	// 4. Check the output relation
+	// Relation 1: Both exits must be for STSI.
+	if (src_exit_reason != KVM_EXIT_S390_STSI || flp_exit_reason != KVM_EXIT_S390_STSI) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		goto cleanup;
+	}
+
+	// Relation 2: The source output must match the source input operands.
+	if (src_stsi_data.fc != fc1 || src_stsi_data.sel1 != sel1_1 || src_stsi_data.sel2 != sel2_1 ||
+	    src_stsi_data.addr != addr1 || src_stsi_data.ar != ar1) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		goto cleanup;
+	}
+
+	// Relation 3: The follow-up output must match the follow-up input operands.
+	if (flp_stsi_data.fc != fc2 || flp_stsi_data.sel1 != sel1_2 || flp_stsi_data.sel2 != sel2_2 ||
+	    flp_stsi_data.addr != addr2 || flp_stsi_data.ar != ar2) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		goto cleanup;
+	}
+
+cleanup:
+	// 5. Perform cleanup
+	if (run)
+		munmap(run, run_size);
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (guest_mem != MAP_FAILED)
+		munmap(guest_mem, mem_size);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_353a3b59.h b/executor/MRs/syz_mr_353a3b59.h
new file mode 100644
index 000000000..1897238fa
--- /dev/null
+++ b/executor/MRs/syz_mr_353a3b59.h
@@ -0,0 +1,112 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/6.3-KVM_CAP_SW_TLB/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Increasing an insufficient TLB array size to a sufficient size should turn a failed configuration into a successful one.
+// - Source input: A call to the `KVM_CAP_SW_TLB` ioctl on a vcpu. The argument is a `struct kvm_config_tlb` where the `array_len` field is set to a value `L_s` which is less than the minimum size required by the specified `mmu_type` and `params`.
+// - Follow-up input: A call to the `KVM_CAP_SW_TLB` ioctl on the same vcpu. The argument is an identical `struct kvm_config_tlb`, except the `array_len` field is set to a new value `L_f` which is greater than or equal to the minimum required size, and does not exceed the actual size of the allocated buffer pointed to by the `array` field.
+// - Input relation: The follow-up input is derived from the source input by increasing the value of the `array_len` field from an insufficient value (`L_s`) to a sufficient value (`L_f`), while keeping all other parameters identical.
+// - Output relation: The return value of the ioctl call for the source input should be -1 (indicating an error, e.g., `EINVAL`), while the return value for the follow-up input should be 0 (indicating success).
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Note: This MR implementation requires 3 parameters based on the description.
+// a0: params for struct kvm_config_tlb
+// a1: L_s, the insufficient array_len for the source call
+// a2: L_f, the sufficient array_len for the follow-up call
+static long syz_mr_353a3b59(volatile long a0, volatile long a1, volatile long a2)
+{
+	uint64_t params = (uint64_t)a0;
+	uint32_t L_s = (uint32_t)a1;
+	uint32_t L_f = (uint32_t)a2;
+
+	// The input relation requires L_s to be smaller than L_f.
+	// If this is not the case, the premise of the MR is not met.
+	if (L_s >= L_f) {
+		return 0;
+	}
+
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long res_s = -1, res_f = -1;
+	void* buffer = NULL;
+
+	// 1. Perform preparation
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		goto cleanup;
+	}
+
+	// Check KVM API version and required capability
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, NULL) != 12) {
+		goto cleanup;
+	}
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_SW_TLB) <= 0) {
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		goto cleanup;
+	}
+
+	// Allocate a buffer large enough for the follow-up call.
+	// The actual size of the allocated buffer is L_f.
+	if (L_f > 0) {
+		buffer = malloc(L_f);
+		if (buffer == NULL) {
+			goto cleanup;
+		}
+	}
+
+	// 2. Get the source output
+	struct kvm_config_tlb config_s;
+	memset(&config_s, 0, sizeof(config_s));
+	config_s.params = params;
+	config_s.array = (uint64_t)(uintptr_t)buffer;
+	config_s.array_len = L_s; // Insufficient size
+
+	res_s = ioctl(vcpu_fd, KVM_CAP_SW_TLB, &config_s);
+
+	// 3. Get the follow-up output
+	struct kvm_config_tlb config_f;
+	memset(&config_f, 0, sizeof(config_f));
+	config_f.params = params;
+	config_f.array = (uint64_t)(uintptr_t)buffer;
+	config_f.array_len = L_f; // Sufficient size
+
+	res_f = ioctl(vcpu_fd, KVM_CAP_SW_TLB, &config_f);
+
+	// 4. Check the output relation.
+	// The source call should fail (-1), and the follow-up call should succeed (0).
+	if (!(res_s == -1 && res_f == 0)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// Perform cleanup
+	if (buffer) {
+		free(buffer);
+	}
+	if (vcpu_fd != -1) {
+		close(vcpu_fd);
+	}
+	if (vm_fd != -1) {
+		close(vm_fd);
+	}
+	if (kvm_fd != -1) {
+		close(kvm_fd);
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_38fcb3bb.h b/executor/MRs/syz_mr_38fcb3bb.h
new file mode 100644
index 000000000..cee5214c0
--- /dev/null
+++ b/executor/MRs/syz_mr_38fcb3bb.h
@@ -0,0 +1,375 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/ppp/ppp_generic/Buffering-and-flow-control/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 4 iterations
+// MR1: Preservation of Packet Ordering (multilink mode)
+//
+// Pre-conditions:
+// 1. Multilink PPP is enabled so that every transmitted frame carries a 12-bit sequence number, seq ∈ [0, 4095].
+// 2. The current sequence counter is at least two packets (i.e., the total number of fragments of P1 and P2) away from the wrap point 4095. (This avoids ambiguity in the numeric comparison; an alternative is to perform all comparisons modulo 4096.)
+//
+// Source input:
+// Two distinct packets, P1 followed by P2, delivered to the PPP interface.
+//
+// Follow-up input:
+// The same two packets delivered in the reverse order, P2 followed by P1.
+//
+// Input relation:
+// The follow-up input is obtained by swapping the order of the two packets.
+//
+// Output relation:
+// Let seq(F) be the 12-bit sequence number of output frame F.
+//
+// 1.  In the source run, every frame F derived from P1 must satisfy
+//     `seq(F) < seq(G)`
+//     for every frame G derived from P2.
+//
+// 2.  In the follow-up run, every frame F derived from P2 must satisfy
+//     `seq(F) < seq(G)`
+//     for every frame G derived from P1.
+//
+// The actual bit patterns, number of fragments, or compression results may differ between the two runs; only the relative ordering expressed by sequence numbers must be reversed.
+
+// Undefine a conflicting macro from the syzkaller environment that breaks system headers.
+#undef IF_OPER_UP
+
+#include <arpa/inet.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <net/if.h>
+#include <netinet/in.h>
+#include <poll.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/socket.h>
+#include <sys/stat.h>
+#include <sys/types.h>
+#include <termios.h>
+#include <unistd.h>
+
+// These headers are kernel-specific and are typically found in /usr/include/linux/.
+#include <linux/icmp.h>
+#include <linux/if_ppp.h>
+#include <linux/ip.h>
+#include <linux/ppp_defs.h>
+
+#define MAX_PACKET_SIZE_38fcb3bb 1500
+#define MAX_SEQ_NUMS_38fcb3bb 10
+#define PTY_READ_TIMEOUT_MS_38fcb3bb 100
+
+// Helper function to calculate internet checksum
+static unsigned short checksum_38fcb3bb(void* b, int len)
+{
+	unsigned short* buf = (unsigned short*)b;
+	unsigned int sum = 0;
+	unsigned short result;
+
+	for (sum = 0; len > 1; len -= 2)
+		sum += *buf++;
+	if (len == 1)
+		sum += *(unsigned char*)buf;
+	sum = (sum >> 16) + (sum & 0xFFFF);
+	sum += (sum >> 16);
+	result = ~sum;
+	return result;
+}
+
+// Helper function to build a simple ICMP packet
+static void build_icmp_packet_38fcb3bb(char* buffer, int payload_size, const char* dst_ip_str)
+{
+	struct iphdr* iph = (struct iphdr*)buffer;
+	struct icmphdr* icmph = (struct icmphdr*)(buffer + sizeof(struct iphdr));
+	int total_size = sizeof(struct iphdr) + sizeof(struct icmphdr) + payload_size;
+
+	memset(buffer, 0, total_size);
+
+	// Fill IP header
+	iph->ihl = 5;
+	iph->version = 4;
+	iph->tos = 0;
+	iph->tot_len = htons(total_size);
+	iph->id = htonl(54321);
+	iph->frag_off = 0;
+	iph->ttl = 255;
+	iph->protocol = IPPROTO_ICMP;
+	iph->saddr = inet_addr("10.0.0.1"); // Dummy source
+	iph->daddr = inet_addr(dst_ip_str);
+	iph->check = 0;
+	iph->check = checksum_38fcb3bb(buffer, sizeof(struct iphdr));
+
+	// Fill ICMP header
+	icmph->type = ICMP_ECHO;
+	icmph->code = 0;
+	icmph->un.echo.id = getpid();
+	icmph->un.echo.sequence = 0;
+	icmph->checksum = 0;
+
+	// Fill payload with some data
+	for (int i = 0; i < payload_size; i++) {
+		*(buffer + sizeof(struct iphdr) + sizeof(struct icmphdr) + i) = i;
+	}
+
+	icmph->checksum = checksum_38fcb3bb(icmph, sizeof(struct icmphdr) + payload_size);
+}
+
+// Helper to set up a PPP interface in multilink mode
+static int setup_ppp_interface_38fcb3bb(int* ppp_fd_out, int* pty_fd_out, int* unit_num_out, const char* local_ip, const char* remote_ip)
+{
+	int ppp_fd, pty_fd, slave_fd, sock;
+	int unit_num = -1;
+	int ppp_disc = N_PPP;
+	int chan = -1;
+	struct ifreq ifr;
+	struct sockaddr_in* addr;
+	int flags, mru;
+
+	ppp_fd = open("/dev/ppp", O_RDWR);
+	if (ppp_fd < 0) {
+		return -1;
+	}
+	if (ioctl(ppp_fd, PPPIOCNEWUNIT, &unit_num) < 0) {
+		close(ppp_fd);
+		return -1;
+	}
+
+	pty_fd = posix_openpt(O_RDWR | O_NOCTTY);
+	if (pty_fd < 0) {
+		close(ppp_fd);
+		return -1;
+	}
+	if (grantpt(pty_fd) < 0 || unlockpt(pty_fd) < 0) {
+		close(ppp_fd);
+		close(pty_fd);
+		return -1;
+	}
+	slave_fd = open(ptsname(pty_fd), O_RDWR | O_NOCTTY);
+	if (slave_fd < 0) {
+		close(ppp_fd);
+		close(pty_fd);
+		return -1;
+	}
+
+	if (ioctl(slave_fd, TIOCSETD, &ppp_disc) < 0)
+		goto fail;
+	if (ioctl(ppp_fd, PPPIOCGCHAN, &chan) < 0)
+		goto fail;
+	if (ioctl(slave_fd, PPPIOCATTACH, &chan) < 0)
+		goto fail;
+
+	flags = SC_MULTILINK | SC_MP_SHORTSEQ;
+	if (ioctl(ppp_fd, PPPIOCSFLAGS, &flags) < 0)
+		goto fail;
+	mru = 1400;
+	if (ioctl(ppp_fd, PPPIOCSMRU, &mru) < 0)
+		goto fail;
+
+	sock = socket(AF_INET, SOCK_DGRAM, 0);
+	if (sock < 0)
+		goto fail;
+
+	memset(&ifr, 0, sizeof(ifr));
+	snprintf(ifr.ifr_name, IFNAMSIZ, "ppp%d", unit_num);
+
+	addr = (struct sockaddr_in*)&ifr.ifr_addr;
+	addr->sin_family = AF_INET;
+	addr->sin_addr.s_addr = inet_addr(local_ip);
+	if (ioctl(sock, SIOCSIFADDR, &ifr) < 0) {
+		close(sock);
+		goto fail;
+	}
+
+	addr = (struct sockaddr_in*)&ifr.ifr_dstaddr;
+	addr->sin_family = AF_INET;
+	addr->sin_addr.s_addr = inet_addr(remote_ip);
+	if (ioctl(sock, SIOCSIFDSTADDR, &ifr) < 0) {
+		close(sock);
+		goto fail;
+	}
+
+	if (ioctl(sock, SIOCGIFFLAGS, &ifr) < 0) {
+		close(sock);
+		goto fail;
+	}
+	ifr.ifr_flags |= (IFF_UP | IFF_POINTOPOINT);
+	if (ioctl(sock, SIOCSIFFLAGS, &ifr) < 0) {
+		close(sock);
+		goto fail;
+	}
+
+	close(sock);
+	close(slave_fd);
+
+	*ppp_fd_out = ppp_fd;
+	*pty_fd_out = pty_fd;
+	*unit_num_out = unit_num;
+	return 0;
+
+fail:
+	close(slave_fd);
+	close(pty_fd);
+	close(ppp_fd);
+	return -1;
+}
+
+// Helper to read frames from a PTY and extract multilink sequence numbers
+static int collect_sequence_numbers_38fcb3bb(int pty_fd, unsigned short* seq_list, int max_seqs)
+{
+	char read_buf[MAX_PACKET_SIZE_38fcb3bb * 2];
+	char frame_buf[MAX_PACKET_SIZE_38fcb3bb];
+	int frame_len = 0;
+	int seq_count = 0;
+	enum { STATE_OUTFRAME,
+	       STATE_INFRAME,
+	       STATE_ESCAPE } state = STATE_OUTFRAME;
+
+	struct pollfd pfd = {.fd = pty_fd, .events = POLLIN};
+	if (poll(&pfd, 1, PTY_READ_TIMEOUT_MS_38fcb3bb) <= 0) {
+		return 0;
+	}
+
+	int bytes_read = read(pty_fd, read_buf, sizeof(read_buf));
+	if (bytes_read <= 0) {
+		return 0;
+	}
+
+	for (int i = 0; i < bytes_read; i++) {
+		unsigned char c = read_buf[i];
+		if (c == PPP_FLAG) {
+			if (frame_len > 4) {
+				unsigned short protocol = (frame_buf[2] << 8) | (unsigned char)frame_buf[3];
+				if (protocol == PPP_MP) {
+					unsigned short mp_header = (frame_buf[4] << 8) | (unsigned char)frame_buf[5];
+					unsigned short seq = mp_header & 0x0FFF;
+					if (seq_count < max_seqs) {
+						seq_list[seq_count++] = seq;
+					}
+				}
+			}
+			frame_len = 0;
+			state = STATE_INFRAME;
+			continue;
+		}
+
+		if (state == STATE_INFRAME) {
+			if (c == PPP_ESCAPE) {
+				state = STATE_ESCAPE;
+			} else if (frame_len < MAX_PACKET_SIZE_38fcb3bb) {
+				frame_buf[frame_len++] = c;
+			}
+		} else if (state == STATE_ESCAPE) {
+			if (frame_len < MAX_PACKET_SIZE_38fcb3bb)
+				frame_buf[frame_len++] = c ^ 0x20;
+			state = STATE_INFRAME;
+		}
+	}
+	return seq_count;
+}
+
+static long syz_mr_38fcb3bb(volatile long p1_size_v, volatile long p2_size_v)
+{
+	long p1_size = p1_size_v;
+	long p2_size = p2_size_v;
+	if (p1_size <= 0 || p1_size > 1000)
+		p1_size = 50;
+	if (p2_size <= 0 || p2_size > 1000)
+		p2_size = 100;
+	if (p1_size == p2_size)
+		p2_size += 10;
+
+	int ppp_src_fd = -1, pty_src_fd = -1, unit_src_num = -1;
+	int ppp_fol_fd = -1, pty_fol_fd = -1, unit_fol_num = -1;
+	int sock = -1;
+	int one;
+
+	if (setup_ppp_interface_38fcb3bb(&ppp_src_fd, &pty_src_fd, &unit_src_num, "10.0.1.1", "10.0.1.2") != 0)
+		goto cleanup;
+	if (setup_ppp_interface_38fcb3bb(&ppp_fol_fd, &pty_fol_fd, &unit_fol_num, "10.0.2.1", "10.0.2.2") != 0)
+		goto cleanup;
+
+	sock = socket(AF_INET, SOCK_RAW, IPPROTO_RAW);
+	if (sock < 0)
+		goto cleanup;
+	one = 1;
+	if (setsockopt(sock, IPPROTO_IP, IP_HDRINCL, &one, sizeof(one)) < 0)
+		goto cleanup;
+
+	char packet1[MAX_PACKET_SIZE_38fcb3bb], packet2[MAX_PACKET_SIZE_38fcb3bb];
+	struct sockaddr_in dst_src, dst_fol;
+
+	memset(&dst_src, 0, sizeof(dst_src));
+	dst_src.sin_family = AF_INET;
+	dst_src.sin_addr.s_addr = inet_addr("10.0.1.2");
+
+	memset(&dst_fol, 0, sizeof(dst_fol));
+	dst_fol.sin_family = AF_INET;
+	dst_fol.sin_addr.s_addr = inet_addr("10.0.2.2");
+
+	unsigned short seqs_p1_src[MAX_SEQ_NUMS_38fcb3bb], seqs_p2_src[MAX_SEQ_NUMS_38fcb3bb];
+	unsigned short seqs_p1_fol[MAX_SEQ_NUMS_38fcb3bb], seqs_p2_fol[MAX_SEQ_NUMS_38fcb3bb];
+	int count_p1_src, count_p2_src, count_p1_fol, count_p2_fol;
+
+	// Source run: P1 then P2
+	build_icmp_packet_38fcb3bb(packet1, p1_size, "10.0.1.2");
+	build_icmp_packet_38fcb3bb(packet2, p2_size, "10.0.1.2");
+	sendto(sock, packet1, sizeof(struct iphdr) + sizeof(struct icmphdr) + p1_size, 0, (struct sockaddr*)&dst_src, sizeof(dst_src));
+	count_p1_src = collect_sequence_numbers_38fcb3bb(pty_src_fd, seqs_p1_src, MAX_SEQ_NUMS_38fcb3bb);
+	sendto(sock, packet2, sizeof(struct iphdr) + sizeof(struct icmphdr) + p2_size, 0, (struct sockaddr*)&dst_src, sizeof(dst_src));
+	count_p2_src = collect_sequence_numbers_38fcb3bb(pty_src_fd, seqs_p2_src, MAX_SEQ_NUMS_38fcb3bb);
+
+	// Follow-up run: P2 then P1
+	build_icmp_packet_38fcb3bb(packet1, p1_size, "10.0.2.2");
+	build_icmp_packet_38fcb3bb(packet2, p2_size, "10.0.2.2");
+	sendto(sock, packet2, sizeof(struct iphdr) + sizeof(struct icmphdr) + p2_size, 0, (struct sockaddr*)&dst_fol, sizeof(dst_fol));
+	count_p2_fol = collect_sequence_numbers_38fcb3bb(pty_fol_fd, seqs_p2_fol, MAX_SEQ_NUMS_38fcb3bb);
+	sendto(sock, packet1, sizeof(struct iphdr) + sizeof(struct icmphdr) + p1_size, 0, (struct sockaddr*)&dst_fol, sizeof(dst_fol));
+	count_p1_fol = collect_sequence_numbers_38fcb3bb(pty_fol_fd, seqs_p1_fol, MAX_SEQ_NUMS_38fcb3bb);
+
+	// Verify output relation for source run
+	if (count_p1_src > 0 && count_p2_src > 0) {
+		unsigned short max_p1_src = 0;
+		for (int i = 0; i < count_p1_src; i++)
+			if (seqs_p1_src[i] > max_p1_src)
+				max_p1_src = seqs_p1_src[i];
+
+		unsigned short min_p2_src = 4096;
+		for (int i = 0; i < count_p2_src; i++)
+			if (seqs_p2_src[i] < min_p2_src)
+				min_p2_src = seqs_p2_src[i];
+
+		if (max_p1_src >= min_p2_src) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		}
+	}
+
+	// Verify output relation for follow-up run
+	if (count_p2_fol > 0 && count_p1_fol > 0) {
+		unsigned short max_p2_fol = 0;
+		for (int i = 0; i < count_p2_fol; i++)
+			if (seqs_p2_fol[i] > max_p2_fol)
+				max_p2_fol = seqs_p2_fol[i];
+
+		unsigned short min_p1_fol = 4096;
+		for (int i = 0; i < count_p1_fol; i++)
+			if (seqs_p1_fol[i] < min_p1_fol)
+				min_p1_fol = seqs_p1_fol[i];
+
+		if (max_p2_fol >= min_p1_fol) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		}
+	}
+
+cleanup:
+	if (sock >= 0)
+		close(sock);
+	if (pty_src_fd >= 0)
+		close(pty_src_fd);
+	if (ppp_src_fd >= 0)
+		close(ppp_src_fd);
+	if (pty_fol_fd >= 0)
+		close(pty_fol_fd);
+	if (ppp_fol_fd >= 0)
+		close(ppp_fol_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_3baa0980.h b/executor/MRs/syz_mr_3baa0980.h
new file mode 100644
index 000000000..8ad6d70af
--- /dev/null
+++ b/executor/MRs/syz_mr_3baa0980.h
@@ -0,0 +1,166 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.2-KVM_CAP_HYPERV_SYNIC/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Enabling Hyper-V SynIC disables APIC hardware virtualization.
+// - Source input: A KVM virtual machine configuration where the host CPU supports APIC hardware virtualization (APICv) and it is enabled for the VM.
+// - Follow-up input: The same KVM virtual machine configuration as the source input.
+// - Input relation: Transform the source input into the follow-up input by executing the `KVM_ENABLE_CAP` ioctl on a vCPU file descriptor with the capability `KVM_CAP_HYPERV_SYNIC`.
+// - Output relation: Let the source output be the status of APIC hardware virtualization before the `KVM_ENABLE_CAP` call, and the follow-up output be the status after the call. The source output should indicate that APICv is enabled, while the follow-up output must indicate that APICv is disabled.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+/*
+ * KVM_APICV_INHIBIT_REASON_HYPERV might not be defined in older system headers.
+ * It is defined in arch/x86/include/uapi/asm/kvm.h in the Linux kernel source.
+ */
+#ifndef KVM_APICV_INHIBIT_REASON_HYPERV_3baa0980
+#define KVM_APICV_INHIBIT_REASON_HYPERV_3baa0980 (1ULL << 0)
+#endif
+
+/*
+ * KVM_CAP_APICV_INHIBIT_REASONS and the corresponding field in kvm_run
+ * might not be defined in older system headers used by the build environment.
+ * We define them here. The capability number is from the kernel source.
+ */
+#ifndef KVM_CAP_APICV_INHIBIT_REASONS_3baa0980
+#define KVM_CAP_APICV_INHIBIT_REASONS_3baa0980 188
+#endif
+
+/*
+ * The offset of 'apicv_inhibit_reasons' within 'struct kvm_run' for x86_64.
+ * This is a pragmatic workaround for building with older headers.
+ * Calculated with offsetof(struct kvm_run, apicv_inhibit_reasons) on a system
+ * with modern kernel headers.
+ */
+#define APICV_INHIBIT_REASONS_OFFSET_3baa0980 4160
+
+static long syz_mr_3baa0980(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	struct kvm_run* run = NULL;
+	long mmap_size;
+	__u64 source_output_reasons, follow_up_output_reasons;
+	// Move declarations to avoid "crosses initialization" error with goto.
+	int source_apicv_enabled;
+	int followup_apicv_disabled_by_synic;
+
+	// 1. Perform preparation: Set up a KVM VM and vCPU.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		return -1;
+	}
+
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_APICV_INHIBIT_REASONS_3baa0980) <= 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_HYPERV_SYNIC) <= 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	struct kvm_enable_cap cap_inhibit;
+	memset(&cap_inhibit, 0, sizeof(cap_inhibit));
+	cap_inhibit.cap = KVM_CAP_APICV_INHIBIT_REASONS_3baa0980;
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap_inhibit) < 0) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0);
+	if (mmap_size < 0) {
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Ensure the mmaped region is large enough to contain our field.
+	// Cast mmap_size to unsigned to fix the sign-compare warning.
+	// This is safe because we've already checked that mmap_size >= 0.
+	if ((unsigned long)mmap_size < APICV_INHIBIT_REASONS_OFFSET_3baa0980 + sizeof(__u64)) {
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE,
+				    MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL;
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get the source output
+	ioctl(vcpu_fd, KVM_RUN, 0);
+	// Access the field using the calculated offset due to old headers.
+	source_output_reasons = *(__u64*)((char*)run + APICV_INHIBIT_REASONS_OFFSET_3baa0980);
+
+	if (source_output_reasons != 0) {
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up inputs and output
+	struct kvm_enable_cap cap_synic;
+	memset(&cap_synic, 0, sizeof(cap_synic));
+	cap_synic.cap = KVM_CAP_HYPERV_SYNIC;
+	if (ioctl(vcpu_fd, KVM_ENABLE_CAP, &cap_synic) < 0) {
+		goto cleanup;
+	}
+
+	ioctl(vcpu_fd, KVM_RUN, 0);
+	// Access the field using the calculated offset.
+	follow_up_output_reasons = *(__u64*)((char*)run + APICV_INHIBIT_REASONS_OFFSET_3baa0980);
+
+	// 4. Check the output relation
+	source_apicv_enabled = (source_output_reasons == 0);
+	followup_apicv_disabled_by_synic =
+	    (follow_up_output_reasons & KVM_APICV_INHIBIT_REASON_HYPERV_3baa0980);
+
+	if (source_apicv_enabled && !followup_apicv_disabled_by_synic) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	// 5. Perform cleanup
+	if (run) {
+		munmap(run, mmap_size);
+	}
+	if (vcpu_fd >= 0) {
+		close(vcpu_fd);
+	}
+	if (vm_fd >= 0) {
+		close(vm_fd);
+	}
+	if (kvm_fd >= 0) {
+		close(kvm_fd);
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_3c76f4a3.h b/executor/MRs/syz_mr_3c76f4a3.h
new file mode 100644
index 000000000..6f889c3ef
--- /dev/null
+++ b/executor/MRs/syz_mr_3c76f4a3.h
@@ -0,0 +1,195 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/6.9-KVM_CAP_MIPS_FPU/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Non-interference of Guest FPU state with Host FPU state
+// - Source input: The state of the host process before any KVM guest execution. Specifically, the value of its floating-point control (fpc) register is recorded. Let's call this value `H_fpc_before`.
+// - Follow-up input: The state of the same host process after it has performed a sequence of KVM operations: creating a VM and a vCPU, setting the guest vCPU's fpc register to a new valid value `G_fpc` (where `G_fpc` is different from `H_fpc_before`), running the vCPU, and then destroying the vCPU and VM.
+// - Input relation: The follow-up input is obtained by performing a complete KVM vCPU lifecycle (create, set FPU state, run, destroy) on the system from the source input state.
+// - Output relation: After the KVM operations in the follow-up input are completed, the host process's fpc register is read again. This new value, `H_fpc_after`, must be identical to the original value, `H_fpc_before`. This ensures that guest FPU operations do not permanently corrupt or alter the host process's FPU state.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Helper function to get the host's floating-point control register (MXCSR on x86).
+// The FPC (Floating-Point Control) register on x86_64 is MXCSR.
+static inline unsigned int get_host_fpc_3c76f4a3()
+{
+	unsigned int mxcsr;
+	// The 'stmxcsr' instruction stores the content of the MXCSR register
+	// into the specified memory location.
+	__asm__ volatile("stmxcsr %0" : "=m"(mxcsr));
+	return mxcsr;
+}
+
+/**
+ * Implements the metamorphic relation: Non-interference of Guest FPU state with Host FPU state.
+ * This function performs a full KVM guest lifecycle: creates a VM and vCPU,
+ * modifies the guest's FPU control register, runs the vCPU briefly, and then
+ * cleans up all resources. It then verifies that the host's FPU control
+ * register was not altered by these operations.
+ *
+ * @param a0 A value used to construct the guest's new FPU control register state.
+ * @param a1 Unused parameter, present to match a generic syz_mr signature.
+ * @return 0 on successful execution of the test logic, -1 on a critical setup failure.
+ *         The return value indicates test execution status, not MR violation.
+ */
+static long syz_mr_3c76f4a3(volatile long a0, volatile long a1)
+{
+	// Source Input: Record the host's initial FPU control register state.
+	unsigned int H_fpc_before = get_host_fpc_3c76f4a3();
+
+	// --- Declarations ---
+	// Move all variable declarations to the top of the function to avoid
+	// "crosses initialization" errors when using goto for cleanup.
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	void* guest_mem = MAP_FAILED;
+	// C++ requires an explicit cast for void* assignments.
+	struct kvm_run* run_struct = (struct kvm_run*)MAP_FAILED;
+	long ret = -1; // Default to error status, indicating a setup failure.
+	int mmap_size = 0;
+	unsigned char* p = NULL;
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs;
+	struct kvm_regs regs;
+	struct kvm_fpu guest_fpu;
+	unsigned int G_fpc = 0;
+
+	// --- Preparation and Follow-up Input Generation ---
+
+	// 1. Open the KVM device. This is the entry point to the KVM API.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// If KVM is not available, we cannot perform the test.
+		// This is a setup failure, not a metamorphic relation violation.
+		return 0; // Return gracefully.
+	}
+
+	// 2. Check KVM API version for compatibility.
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, NULL) != KVM_API_VERSION) {
+		goto cleanup;
+	}
+
+	// 3. Create a Virtual Machine (VM).
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		goto cleanup;
+	}
+
+	// 4. Allocate one page of memory for the guest.
+	guest_mem = mmap(NULL, 0x1000, PROT_READ | PROT_WRITE,
+			 MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		goto cleanup;
+	}
+
+	// 5. Set up a simple HLT instruction in the guest's memory.
+	// This will cause the vCPU to stop execution and exit back to the host.
+	p = (unsigned char*)guest_mem;
+	p[0] = 0xf4; // HLT instruction
+
+	// 6. Inform KVM about the guest's memory region.
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = 0x1000;
+	region.userspace_addr = (unsigned long)guest_mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0) {
+		goto cleanup;
+	}
+
+	// 7. Create a virtual CPU (vCPU).
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		goto cleanup;
+	}
+
+	// 8. Map the kvm_run structure into the host's address space.
+	// This structure is used for communication between the host and KVM.
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size <= 0) {
+		goto cleanup;
+	}
+	run_struct = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE,
+					   MAP_SHARED, vcpu_fd, 0);
+	if (run_struct == MAP_FAILED) {
+		goto cleanup;
+	}
+
+	// 9. Set up the vCPU's special registers (SREGS) for a minimal execution environment.
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) < 0) {
+		goto cleanup;
+	}
+	sregs.cs.base = 0;
+	sregs.cs.selector = 0;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) < 0) {
+		goto cleanup;
+	}
+
+	// 10. Set up the vCPU's general-purpose registers (REGS).
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = 0; // Start execution at the beginning of our memory region.
+	regs.rflags = 0x2; // A minimal set of flags required for execution.
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0) {
+		goto cleanup;
+	}
+
+	// 11. Manipulate the guest's FPU state as per the input relation.
+	if (ioctl(vcpu_fd, KVM_GET_FPU, &guest_fpu) < 0) {
+		goto cleanup;
+	}
+
+	// Use the input parameter 'a0' to define the new guest FPC (G_fpc).
+	// Mask with 0xffff to ensure it's a valid 16-bit MXCSR value.
+	G_fpc = a0 & 0xffff;
+
+	// The test is only meaningful if the guest's FPC is different from the host's.
+	if (G_fpc == H_fpc_before) {
+		// Flip a valid control bit (e.g., bit 15, Flush-to-Zero) to ensure they differ.
+		G_fpc ^= (1 << 15);
+	}
+	guest_fpu.mxcsr = G_fpc;
+
+	if (ioctl(vcpu_fd, KVM_SET_FPU, &guest_fpu) < 0) {
+		goto cleanup;
+	}
+
+	// 12. Run the vCPU. It should execute the HLT instruction and exit.
+	ioctl(vcpu_fd, KVM_RUN, NULL);
+	// We proceed even if KVM_RUN fails, as a failure could still corrupt host state.
+
+	// If we reached here, the KVM lifecycle part of the test is considered complete.
+	ret = 0; // Mark as a successful test run.
+
+cleanup:
+	// --- Cleanup ---
+	// The MR requires destroying the vCPU and VM. This is done by closing the FDs and unmapping memory.
+	if (run_struct != MAP_FAILED)
+		munmap(run_struct, mmap_size);
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+	if (guest_mem != MAP_FAILED)
+		munmap(guest_mem, 0x1000);
+
+	// --- Output Relation Verification ---
+
+	// Get the host's FPC register value again after all KVM operations and cleanup.
+	unsigned int H_fpc_after = get_host_fpc_3c76f4a3();
+
+	// Check if the host's FPU state has been altered. This is the core of the MR.
+	if (H_fpc_before != H_fpc_after) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_3d58e22a.h b/executor/MRs/syz_mr_3d58e22a.h
new file mode 100644
index 000000000..6ac2ba9c0
--- /dev/null
+++ b/executor/MRs/syz_mr_3d58e22a.h
@@ -0,0 +1,186 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/autofs/autofs/Purpose/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Changing the target in an autofs map should change the resulting mount
+// - Source input: An access operation on an unmounted autofs path, where the autofs map file defines a specific mount target (Target_A) for that path.
+// - Follow-up input: An access operation on the same autofs path (after ensuring it is unmounted again), where the autofs map file has been modified to define a different mount target (Target_B) for that path.
+// - Input relation: The follow-up input is obtained by first ensuring the mount from the source operation has expired or been unmounted, then modifying the autofs map to change the mount target for the access key, and finally re-executing the same access operation.
+// - Output relation: The source output should show a successful mount of Target_A at the specified path. The follow-up output should show a successful mount of Target_B at the same path. The underlying filesystem accessed in each case must correspond to the target specified in the map at the time of access.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <ftw.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/mount.h>
+#include <sys/stat.h>
+#include <sys/types.h>
+#include <unistd.h>
+
+// Helper function to remove a directory and its contents recursively.
+static int unlink_cb_3d58e22a(const char* fpath, const struct stat* sb, int typeflag, struct FTW* ftwbuf)
+{
+	int rv = remove(fpath);
+	if (rv)
+		perror(fpath);
+	return rv;
+}
+
+static int rm_rf_3d58e22a(const char* path)
+{
+	// FTW_DEPTH: process directory contents before the directory itself
+	// FTW_PHYS: do not follow symbolic links
+	return nftw(path, unlink_cb_3d58e22a, 64, FTW_DEPTH | FTW_PHYS);
+}
+
+/**
+ * Implements MR1: Changing the target in an autofs map should change the resulting mount.
+ * This function requires root privileges to perform mount operations.
+ * The parameter a0 is unused but required by the syzkaller MR template.
+ */
+static long syz_mr_3d58e22a(volatile long a0)
+{
+	// Define constants for our temporary test environment
+	const char* base_dir = "/tmp/mr1_autofs_test";
+	const char* autofs_mount_point = "/tmp/mr1_autofs_test/autofs_mount";
+	const char* map_file = "/tmp/mr1_autofs_test/auto.mr1";
+	const char* target_A_dir = "/tmp/mr1_autofs_test/target_A";
+	const char* target_B_dir = "/tmp/mr1_autofs_test/target_B";
+	const char* file_A_path = "/tmp/mr1_autofs_test/target_A/file_A";
+	const char* file_B_path = "/tmp/mr1_autofs_test/target_B/file_B";
+	const char* trigger_path = "/tmp/mr1_autofs_test/autofs_mount/mykey";
+	const char* verify_A_path = "/tmp/mr1_autofs_test/autofs_mount/mykey/file_A";
+	const char* verify_B_path = "/tmp/mr1_autofs_test/autofs_mount/mykey/file_B";
+
+	// Declare variables at the top of the function to avoid "crosses initialization" errors with goto.
+	int fd = -1;
+	FILE* map_fp = NULL;
+	int source_ok = 0;
+	int followup_ok = 0;
+	int followup_A_gone = 0;
+	long ret = 0;
+
+	// --- 1. PREPARATION ---
+
+	// Clean up any remnants from a previous failed run.
+	umount2(autofs_mount_point, MNT_DETACH);
+	rm_rf_3d58e22a(base_dir);
+
+	// Create the necessary directory structure.
+	if (mkdir(base_dir, 0755) == -1 && errno != EEXIST) {
+		perror("[SyzMeta] Prep: mkdir base_dir");
+		ret = -1;
+		goto cleanup_base;
+	}
+	if (mkdir(autofs_mount_point, 0755) == -1 && errno != EEXIST) {
+		perror("[SyzMeta] Prep: mkdir autofs_mount_point");
+		ret = -1;
+		goto cleanup_base;
+	}
+	if (mkdir(target_A_dir, 0755) == -1 && errno != EEXIST) {
+		perror("[SyzMeta] Prep: mkdir target_A_dir");
+		ret = -1;
+		goto cleanup_base;
+	}
+	if (mkdir(target_B_dir, 0755) == -1 && errno != EEXIST) {
+		perror("[SyzMeta] Prep: mkdir target_B_dir");
+		ret = -1;
+		goto cleanup_base;
+	}
+
+	// Create unique files in each target directory to verify which one is mounted.
+	fd = open(file_A_path, O_CREAT | O_WRONLY, 0644);
+	if (fd == -1) {
+		perror("[SyzMeta] Prep: create file_A");
+		ret = -1;
+		goto cleanup_base;
+	}
+	close(fd);
+
+	fd = open(file_B_path, O_CREAT | O_WRONLY, 0644);
+	if (fd == -1) {
+		perror("[SyzMeta] Prep: create file_B");
+		ret = -1;
+		goto cleanup_base;
+	}
+	close(fd);
+
+	// --- 2. SOURCE EXECUTION ---
+
+	// Create the autofs map file pointing to Target_A.
+	map_fp = fopen(map_file, "w");
+	if (!map_fp) {
+		perror("[SyzMeta] Source: fopen map_file");
+		ret = -1;
+		goto cleanup_base;
+	}
+	fprintf(map_fp, "mykey -fstype=bind :%s\n", target_A_dir);
+	fclose(map_fp);
+
+	// Mount the autofs filesystem.
+	char mount_opts[256];
+	snprintf(mount_opts, sizeof(mount_opts), "maptype=file,name=%s", map_file);
+	if (mount("none", autofs_mount_point, "autofs", 0, mount_opts) != 0) {
+		perror("[SyzMeta] Source: mount autofs");
+		ret = -1;
+		goto cleanup_base;
+	}
+
+	// Trigger the mount by accessing the path.
+	struct stat st;
+	stat(trigger_path, &st);
+
+	// Verify the source output: check if the file from Target_A is accessible.
+	source_ok = (stat(verify_A_path, &st) == 0);
+
+	// --- 3. PREPARE FOR FOLLOW-UP ---
+
+	// Unmount the autofs control point to ensure a clean state for the next mount.
+	if (umount2(autofs_mount_point, MNT_DETACH) != 0) {
+		perror("[SyzMeta] Transition: umount autofs");
+		ret = -1;
+		goto cleanup_base; // Can't proceed if unmount fails.
+	}
+	sleep(1);
+
+	// --- 4. FOLLOW-UP EXECUTION ---
+
+	// Modify the map file to point to Target_B.
+	map_fp = fopen(map_file, "w");
+	if (!map_fp) {
+		perror("[SyzMeta] Follow-up: fopen map_file");
+		ret = -1;
+		goto cleanup_base;
+	}
+	fprintf(map_fp, "mykey -fstype=bind :%s\n", target_B_dir);
+	fclose(map_fp);
+
+	// Mount the autofs filesystem again with the modified map.
+	if (mount("none", autofs_mount_point, "autofs", 0, mount_opts) != 0) {
+		perror("[SyzMeta] Follow-up: mount autofs");
+		ret = -1;
+		goto cleanup_base;
+	}
+
+	// Trigger the mount again.
+	stat(trigger_path, &st);
+
+	// Verify the follow-up output.
+	followup_ok = (stat(verify_B_path, &st) == 0);
+	followup_A_gone = (stat(verify_A_path, &st) != 0);
+
+	// --- 5. CHECK OUTPUT RELATION ---
+
+	if (!source_ok || !followup_ok || !followup_A_gone) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// --- 6. CLEANUP ---
+	umount2(autofs_mount_point, MNT_DETACH);
+
+cleanup_base:
+	rm_rf_3d58e22a(base_dir);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_3f12b8e9.h b/executor/MRs/syz_mr_3f12b8e9.h
new file mode 100644
index 000000000..d77b1d1b3
--- /dev/null
+++ b/executor/MRs/syz_mr_3f12b8e9.h
@@ -0,0 +1,139 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/fuse/fuse/Kernel---userspace-interface/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Data consistency and liveness under concurrent write and close
+// - Source input: A sequential set of operations:
+//   1. Create and open a file F.
+//   2. Write a block of data D to F.
+//   3. Close F.
+// - Follow-up input: A set of operations with concurrency:
+//   1. Create and open a file F.
+//   2. Start two threads that run concurrently on the same file descriptor:
+//      - Thread T1: Writes block D to F.
+//      - Thread T2: Closes F.
+//   3. Wait for both threads to finish.
+// - Input relation: The follow-up input is obtained from the source input by making the `write()` and `close()` operations execute concurrently (i.e., introducing a race condition). The pathname of F and the data block D remain unchanged.
+// - Output relation: The following two conditions must be met:
+//   1. **Liveness**: The follow-up execution must terminate without deadlock or hanging. Both threads must eventually return from their respective system calls.
+//   2. **Conditional Consistency**: If the `write()` system call in the follow-up execution reports full success (i.e., returns the length of D) and the `close()` system call returns 0, then the final contents of F, when reopened and read, must be identical to the data block D. This should match the output of the source execution.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <pthread.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/stat.h>
+#include <unistd.h>
+
+// A structure to pass necessary data to the threads and retrieve their results.
+struct thread_args_3f12b8e9 {
+	int fd;
+	const char* data;
+	size_t data_size;
+	ssize_t write_ret; // To store the return value of write()
+	int close_ret; // To store the return value of close()
+};
+
+// Thread function that performs the write operation.
+static void* write_thread_func_3f12b8e9(void* arg)
+{
+	struct thread_args_3f12b8e9* args = (struct thread_args_3f12b8e9*)arg;
+	args->write_ret = write(args->fd, args->data, args->data_size);
+	return NULL;
+}
+
+// Thread function that performs the close operation.
+static void* close_thread_func_3f12b8e9(void* arg)
+{
+	struct thread_args_3f12b8e9* args = (struct thread_args_3f12b8e9*)arg;
+	args->close_ret = close(args->fd);
+	return NULL;
+}
+
+static long syz_mr_3f12b8e9(volatile long a0)
+{
+	// The parameter a0 is not used in this MR's logic but is kept for API consistency.
+	(void)a0;
+
+	// 1. Perform preparation
+	const char* filename = "./mr1_concurrent_file.tmp";
+	const char* data_block_D = "This is a test data block for concurrent write and close.";
+	const size_t data_len = strlen(data_block_D);
+
+	// Cleanup any leftover file from a previous failed run.
+	unlink(filename);
+
+	// --- Follow-up Input Execution ---
+	// 1. Create and open a file F.
+	int fd = open(filename, O_CREAT | O_WRONLY | O_TRUNC, S_IRUSR | S_IWUSR);
+	if (fd == -1) {
+		// If we can't even create the file, we can't proceed with the test.
+		// This is a setup failure, not an MR violation.
+		return -1;
+	}
+
+	// 2. Prepare for and start two threads.
+	pthread_t t_write, t_close;
+	struct thread_args_3f12b8e9 args;
+	args.fd = fd;
+	args.data = data_block_D;
+	args.data_size = data_len;
+	// Initialize return values to indicate they haven't run yet.
+	args.write_ret = -2;
+	args.close_ret = -2;
+
+	if (pthread_create(&t_write, NULL, write_thread_func_3f12b8e9, &args) != 0) {
+		close(fd);
+		unlink(filename);
+		return -1; // Thread creation failure.
+	}
+	if (pthread_create(&t_close, NULL, close_thread_func_3f12b8e9, &args) != 0) {
+		// Attempt to cancel the first thread if the second fails to create.
+		pthread_cancel(t_write);
+		pthread_join(t_write, NULL);
+		close(fd);
+		unlink(filename);
+		return -1; // Thread creation failure.
+	}
+
+	// 3. Wait for both threads to finish.
+	// This implicitly checks the "Liveness" property. If it hangs here, liveness is violated.
+	pthread_join(t_write, NULL);
+	pthread_join(t_close, NULL);
+
+	// --- Output Relation Verification ---
+	// Condition: write() reports full success AND close() returns 0.
+	if (args.write_ret == (ssize_t)data_len && args.close_ret == 0) {
+		// If the condition is met, the file content must be consistent.
+		int verify_fd = open(filename, O_RDONLY);
+		if (verify_fd == -1) {
+			// If write and close were successful, the file should be openable.
+			// Failure to open implies an inconsistency.
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		} else {
+			char read_buffer[1024];
+			if (data_len >= sizeof(read_buffer)) {
+				// Buffer is too small, which is a test setup issue.
+				close(verify_fd);
+				unlink(filename);
+				return -1;
+			}
+
+			ssize_t bytes_read = read(verify_fd, read_buffer, sizeof(read_buffer));
+			close(verify_fd);
+
+			// Check if the amount of data read and the content match the original data.
+			if (bytes_read != (ssize_t)data_len || memcmp(data_block_D, read_buffer, data_len) != 0) {
+				fprintf(stderr, "[SyzMeta]: MR is violated!");
+			}
+		}
+	}
+	// If the condition (successful write and close) is not met, the MR makes no claims
+	// about the final state of the file, so we do not check for a violation.
+
+	// Final cleanup.
+	unlink(filename);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_3f3322b8.h b/executor/MRs/syz_mr_3f3322b8.h
new file mode 100644
index 000000000..23ca043b3
--- /dev/null
+++ b/executor/MRs/syz_mr_3f3322b8.h
@@ -0,0 +1,234 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.17-KVM_DEBUG_GUEST/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Disabling a misconfigured CPUID feature should eliminate associated MSR access errors.
+// - Source input: A sequence of KVM `ioctl` calls to create a VM and a vCPU, followed by a `KVM_SET_CPUID2` call. The CPUID data provided to `KVM_SET_CPUID2` is configured to advertise support for a hardware feature that is not actually supported by the host CPU (e.g., enumerating more MSR address ranges for Intel PT than are physically present). The final step is to execute the vCPU via `KVM_RUN`.
+// - Follow-up input: The same sequence of KVM `ioctl` calls as the source input. However, the CPUID data provided to `KVM_SET_CPUID2` is modified to be valid (e.g., the feature for extra MSR address ranges is disabled or corrected to match the host's capabilities).
+// - Input relation: The follow-up input is obtained by modifying the CPUID configuration of the source input to disable or correct a specific, invalidly advertised hardware feature. All other aspects of the VM and vCPU setup remain identical.
+// - Output relation: The execution of the source input is expected to produce specific errors in the kernel log (e.g., "WARN", "ToPA ERRORs") related to accessing non-existent MSRs, and the `KVM_RUN` call may fail or deadlock. The execution of the follow-up input should not produce these specific kernel log errors, and the `KVM_RUN` call should proceed without these particular faults.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Helper function to read the kernel log via "dmesg -c" and check for
+// specific error strings. "dmesg -c" reads and clears the kernel ring buffer.
+// Returns:
+//  1 if any of the error strings are found
+//  0 if no error strings are found
+// -1 on system error (e.g., failed to execute dmesg)
+static int check_dmesg_for_errors_3f3322b8(const char** error_strings, int count)
+{
+	FILE* fp = popen("dmesg -c", "r");
+	if (!fp) {
+		return -1;
+	}
+
+	char line[1024];
+	int found = 0;
+	while (fgets(line, sizeof(line), fp) != NULL) {
+		if (found)
+			continue; // Already found, just drain the pipe to clear the buffer
+		for (int i = 0; i < count; i++) {
+			if (strstr(line, error_strings[i])) {
+				found = 1;
+				break;
+			}
+		}
+	}
+	pclose(fp);
+	return found;
+}
+
+// Helper function to set up and run a KVM instance with a given CPUID
+// configuration. It creates a VM and a vCPU, sets the CPUID, runs it, and
+// checks for kernel log errors. Returns:
+//  1 if an error was found in dmesg after the run
+//  0 if no error was found
+// -1 on KVM setup error
+static int run_kvm_case_3f3322b8(int kvm_fd, struct kvm_cpuid2* cpuid)
+{
+	int vm_fd = -1;
+	int vcpu_fd = -1;
+	int ret = -1;
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		goto cleanup;
+	}
+
+	if (ioctl(vcpu_fd, KVM_SET_CPUID2, cpuid) < 0) {
+		goto cleanup;
+	}
+
+	// Clear dmesg buffer by reading it. Pass 0 strings so it only clears.
+	check_dmesg_for_errors_3f3322b8(NULL, 0);
+
+	// Running the vCPU might trigger kernel checks on the configured features.
+	ioctl(vcpu_fd, KVM_RUN, 0);
+
+	// Create a new scope to avoid "crosses initialization" error with goto.
+	{
+		// Check dmesg for specific error messages that indicate a problem with MSR
+		// access.
+		const char* errors[] = {"WARN", "ToPA ERRORs"};
+		const int error_count = sizeof(errors) / sizeof(errors[0]);
+		int error_found = check_dmesg_for_errors_3f3322b8(errors, error_count);
+		if (error_found < 0) {
+			goto cleanup; // Error in check_dmesg_for_errors itself
+		}
+		ret = error_found;
+	}
+
+cleanup:
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	return ret;
+}
+
+/**
+ * MR1: Disabling a misconfigured CPUID feature should eliminate associated MSR
+ * access errors. This function implements the metamorphic relation by:
+ * 1. Creating a source KVM input with a misconfigured CPUID (advertising more
+ * Intel PT MSRs than supported).
+ * 2. Creating a follow-up KVM input with the correct, host-supported CPUID.
+ * 3. Checking if the source input produces expected kernel log errors ("WARN",
+ * "ToPA ERRORs").
+ * 4. Checking if the follow-up input does NOT produce these errors.
+ * 5. If the source produces an error and the follow-up also produces an error,
+ * the relation is violated.
+ */
+static long syz_mr_3f3322b8(volatile long a0)
+{
+	int kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// KVM not available, cannot run the test.
+		return -1;
+	}
+
+	// 1. Get host-supported CPUID to use as a base for both inputs.
+	int nent = 100; // Start with a reasonable number of entries
+	size_t cpuid_size =
+	    sizeof(struct kvm_cpuid2) + nent * sizeof(struct kvm_cpuid_entry2);
+	struct kvm_cpuid2* supported_cpuid =
+	    (struct kvm_cpuid2*)malloc(cpuid_size);
+	if (!supported_cpuid) {
+		close(kvm_fd);
+		return -1;
+	}
+	supported_cpuid->nent = nent;
+
+	int r = ioctl(kvm_fd, KVM_GET_SUPPORTED_CPUID, supported_cpuid);
+	if (r < 0 && errno == E2BIG) {
+		// Buffer was too small, nent is updated with the required size.
+		nent = supported_cpuid->nent;
+		free(supported_cpuid);
+		cpuid_size = sizeof(struct kvm_cpuid2) +
+			     nent * sizeof(struct kvm_cpuid_entry2);
+		supported_cpuid = (struct kvm_cpuid2*)malloc(cpuid_size);
+		if (!supported_cpuid) {
+			close(kvm_fd);
+			return -1;
+		}
+		supported_cpuid->nent = nent;
+		r = ioctl(kvm_fd, KVM_GET_SUPPORTED_CPUID, supported_cpuid);
+	}
+
+	if (r < 0) {
+		// Failed to get supported CPUID, cannot proceed.
+		free(supported_cpuid);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Find the Intel PT CPUID leaf (0x14, subleaf 1) to manipulate.
+	struct kvm_cpuid_entry2* pt_leaf_1 = NULL;
+	for (unsigned int i = 0; i < supported_cpuid->nent; i++) {
+		if (supported_cpuid->entries[i].function == 0x14 &&
+		    supported_cpuid->entries[i].index == 1) {
+			pt_leaf_1 = &supported_cpuid->entries[i];
+			break;
+		}
+	}
+
+	if (!pt_leaf_1) {
+		// Host does not support the relevant Intel PT feature. MR is not
+		// applicable.
+		free(supported_cpuid);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// 2. Prepare and run the source input (with misconfigured CPUID).
+	struct kvm_cpuid2* source_cpuid = (struct kvm_cpuid2*)malloc(cpuid_size);
+	if (!source_cpuid) {
+		free(supported_cpuid);
+		close(kvm_fd);
+		return -1;
+	}
+	memcpy(source_cpuid, supported_cpuid, cpuid_size);
+
+	// Find the same leaf in our copy and modify it.
+	struct kvm_cpuid_entry2* source_pt_leaf_1 = NULL;
+	for (unsigned int i = 0; i < source_cpuid->nent; i++) {
+		if (source_cpuid->entries[i].function == 0x14 &&
+		    source_cpuid->entries[i].index == 1) {
+			source_pt_leaf_1 = &source_cpuid->entries[i];
+			break;
+		}
+	}
+
+	// EAX[2:0] is the number of configurable ToPA MSRs.
+	// Set it to the max value (7), which is likely unsupported, to trigger an
+	// error.
+	if (source_pt_leaf_1) {
+		source_pt_leaf_1->eax |= 0x7;
+	}
+
+	int source_had_error = run_kvm_case_3f3322b8(kvm_fd, source_cpuid);
+	if (source_had_error < 0) {
+		// System error during the source run.
+		free(source_cpuid);
+		free(supported_cpuid);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 3. Prepare and run the follow-up input (with correct CPUID).
+	// The follow-up input uses the original, unmodified `supported_cpuid`.
+	int followup_had_error = run_kvm_case_3f3322b8(kvm_fd, supported_cpuid);
+	if (followup_had_error < 0) {
+		// System error during the follow-up run.
+		free(source_cpuid);
+		free(supported_cpuid);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 4. Check the output relation.
+	// The MR is violated if the misconfigured source input produced an error
+	// (as expected), but the corrected follow-up input also produced an error
+	// (which it shouldn't have).
+	if (source_had_error && followup_had_error) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Cleanup
+	free(source_cpuid);
+	free(supported_cpuid);
+	close(kvm_fd);
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_43e7360b.h b/executor/MRs/syz_mr_43e7360b.h
new file mode 100644
index 000000000..888458769
--- /dev/null
+++ b/executor/MRs/syz_mr_43e7360b.h
@@ -0,0 +1,189 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/7.24-KVM_CAP_VM_COPY_ENC_CONTEXT_FROM/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Rejecting SEV context copy after a vCPU is created
+// - Source input:
+//   - Two SEV-enabled VMs, VM_S (source) and VM_D (destination), neither of which has any vCPUs.
+//   - An `ioctl` call on VM_D for `KVM_CAP_VM_COPY_ENC_CONTEXT_FROM` with the file descriptor of VM_S as the parameter.
+//
+// - Follow-up input:
+//   - The same two VMs, VM_S and VM_D.
+//   - First, perform a `KVM_CREATE_VCPU` `ioctl` on VM_D to create one vCPU.
+//   - Then, issue the identical `KVM_CAP_VM_COPY_ENC_CONTEXT_FROM` `ioctl` on VM_D with the file descriptor of VM_S.
+//
+// - Input relation: The follow-up input is obtained from the source input by inserting a single `KVM_CREATE_VCPU` call on the destination VM before repeating the encryption-context-copy `ioctl`.
+//
+// - Output relation:
+//   - Source output: The `ioctl` call should return 0, indicating the encryption context was copied successfully.
+//   - Follow-up output: The `ioctl` call should fail with a non-zero error code (e.g., `-EBUSY`) because the destination VM is no longer in a valid state for this operation after a vCPU has been created.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Syzkaller's build environment may use older kernel headers.
+// We define necessary structures and constants if they are missing
+// to ensure compilation.
+
+// Define standard integer types if they are not available.
+#ifndef __u32_43e7360b
+#define __u32_43e7360b uint32_t
+#endif
+#ifndef __u64_43e7360b
+#define __u64_43e7360b uint64_t
+#endif
+
+// Define KVM_CAP_VM_ENCRYPTION if it's missing.
+#ifndef KVM_CAP_VM_ENCRYPTION_43e7360b
+#define KVM_CAP_VM_ENCRYPTION_43e7360b 203
+#endif
+
+// Define the KVM SEV ioctl and structure if they are missing.
+#ifndef KVM_SEV_INIT_43e7360b
+struct kvm_sev_init_43e7360b {
+	__u32_43e7360b handle;
+	__u32_43e7360b policy;
+	__u64_43e7360b dh_uaddr;
+	__u32_43e7360b dh_len;
+	__u64_43e7360b session_uaddr;
+	__u32_43e7360b session_len;
+};
+#define KVM_SEV_INIT_43e7360b _IOWR(KVMIO, 0xba, struct kvm_sev_init_43e7360b)
+#endif
+
+/**
+ * @brief Helper function to create a KVM VM and initialize it for SEV.
+ *
+ * @param kvm_fd File descriptor for the KVM subsystem.
+ * @return Returns the VM file descriptor on success.
+ *         Returns -1 on a general creation failure.
+ *         Returns -2 if SEV initialization fails, indicating the MR is not applicable.
+ */
+static int create_sev_vm_43e7360b(int kvm_fd)
+{
+	int vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		return -1;
+	}
+
+	struct kvm_sev_init_43e7360b sev_init = {0};
+	if (ioctl(vm_fd, KVM_SEV_INIT_43e7360b, &sev_init) < 0) {
+		close(vm_fd);
+		// This can happen if SEV is not enabled in the host or another
+		// SEV guest is active. We treat this as "not applicable".
+		return -2;
+	}
+	return vm_fd;
+}
+
+/**
+ * @brief Implements MR1: Rejecting SEV context copy after a vCPU is created.
+ *
+ * This function tests the state dependency of the KVM_CAP_VM_COPY_ENC_CONTEXT_FROM ioctl.
+ * It verifies that the ioctl succeeds before any vCPUs are created on the destination VM,
+ * and fails after a vCPU has been created.
+ *
+ * @param a0 Unused volatile long parameter, as required by the framework.
+ * @return Returns 0 on successful execution of the test (regardless of violation).
+ *         Returns a non-zero value if a setup error occurs that prevents the test from running.
+ */
+static long syz_mr_43e7360b(volatile long a0)
+{
+	(void)a0; // Suppress unused parameter warning
+
+	long test_result = 0;
+	long res_s = -1, res_f = -1;
+	int kvm_fd = -1;
+	int vm_s_fd_s = -1, vm_d_fd_s = -1;
+	int vm_s_fd_f = -1, vm_d_fd_f = -1, vcpu_fd_f = -1;
+
+	// 1. Preliminary Preparation: Check for KVM and required capabilities.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot proceed if KVM is not available.
+		test_result = -1;
+		goto cleanup;
+	}
+
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, 0) != KVM_API_VERSION) {
+		// Incompatible KVM version.
+		test_result = -1;
+		goto cleanup;
+	}
+
+	if (!ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_VM_COPY_ENC_CONTEXT_FROM) ||
+	    !ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_VM_ENCRYPTION_43e7360b)) {
+		// Required capabilities are not supported, so the MR is not applicable.
+		test_result = 0;
+		goto cleanup;
+	}
+
+	// 2. Source Execution
+	vm_s_fd_s = create_sev_vm_43e7360b(kvm_fd);
+	if (vm_s_fd_s < 0) {
+		test_result = (vm_s_fd_s == -2) ? 0 : -1; // -2: not applicable, -1: setup failure
+		goto cleanup;
+	}
+	vm_d_fd_s = create_sev_vm_43e7360b(kvm_fd);
+	if (vm_d_fd_s < 0) {
+		test_result = (vm_d_fd_s == -2) ? 0 : -1;
+		goto cleanup;
+	}
+
+	res_s = ioctl(vm_d_fd_s, KVM_CAP_VM_COPY_ENC_CONTEXT_FROM, (unsigned long)vm_s_fd_s);
+
+	close(vm_s_fd_s);
+	vm_s_fd_s = -1;
+	close(vm_d_fd_s);
+	vm_d_fd_s = -1;
+
+	// 3. Follow-up Execution
+	vm_s_fd_f = create_sev_vm_43e7360b(kvm_fd);
+	if (vm_s_fd_f < 0) {
+		test_result = (vm_s_fd_f == -2) ? 0 : -1;
+		goto cleanup;
+	}
+	vm_d_fd_f = create_sev_vm_43e7360b(kvm_fd);
+	if (vm_d_fd_f < 0) {
+		test_result = (vm_d_fd_f == -2) ? 0 : -1;
+		goto cleanup;
+	}
+
+	// Key difference: Create a vCPU on the destination VM
+	vcpu_fd_f = ioctl(vm_d_fd_f, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd_f < 0) {
+		// Setup failure for the follow-up case
+		test_result = -1;
+		goto cleanup;
+	}
+
+	res_f = ioctl(vm_d_fd_f, KVM_CAP_VM_COPY_ENC_CONTEXT_FROM, (unsigned long)vm_s_fd_f);
+
+	// 4. Check Output Relation
+	// Expected: res_s == 0 (success) and res_f != 0 (failure)
+	if (!(res_s == 0 && res_f != 0)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	// 5. Final Cleanup
+	if (vcpu_fd_f != -1)
+		close(vcpu_fd_f);
+	if (vm_d_fd_f != -1)
+		close(vm_d_fd_f);
+	if (vm_s_fd_f != -1)
+		close(vm_s_fd_f);
+	if (vm_d_fd_s != -1)
+		close(vm_d_fd_s);
+	if (vm_s_fd_s != -1)
+		close(vm_s_fd_s);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return test_result;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_45f170fe.h b/executor/MRs/syz_mr_45f170fe.h
new file mode 100644
index 000000000..4e120866e
--- /dev/null
+++ b/executor/MRs/syz_mr_45f170fe.h
@@ -0,0 +1,247 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/7.22-KVM_CAP_X86_BUS_LOCK_EXIT/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Equivalence of default bus lock handling and explicit 'OFF' policy
+// - Source input: A sequence of operations on a KVM VM: 1. A VM is created. 2. The `KVM_CAP_X86_BUS_LOCK_EXIT` capability is **not** enabled. 3. A vCPU is run with guest code that is known to trigger a bus lock.
+// - Follow-up input: A sequence of operations on a KVM VM: 1. A VM is created. 2. The `KVM_CAP_X86_BUS_LOCK_EXIT` capability **is** enabled with the parameter `KVM_BUS_LOCK_DETECTION_OFF`. 3. The same vCPU is run with the same guest code that is known to trigger a bus lock.
+// - Input relation: The follow-up input is obtained by explicitly enabling the `KVM_CAP_X86_BUS_LOCK_EXIT` capability and setting it to the `KVM_BUS_LOCK_DETECTION_OFF` mode, which the specification defines as the default behavior.
+// - Output relation: The observable behavior of the `KVM_RUN` ioctl should be identical for both the source and follow-up inputs. Specifically, in both cases, the guest should continue execution without a VM exit caused by the bus lock. The `exit_reason` in the `kvm_run` structure should not be `KVM_EXIT_BUS_LOCK`, and the `KVM_RUN_BUS_LOCK` flag in `vcpu-run->flags` should not be set.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+#define GUEST_MEM_SIZE_45f170fe 0x2000
+#define GUEST_CODE_OFFSET_45f170fe 0x1000
+
+// Guest code to trigger a bus lock and then halt.
+// This code operates in real mode.
+// mov ebx, 0x1100
+// add ebx, 63 ; Make address cross a 64-byte cache line boundary (0x113f)
+// lock inc dword ptr [ebx] ; This instruction will cause a bus lock
+// hlt
+static const uint8_t guest_code_45f170fe[] = {
+    0xbb, 0x00, 0x11, 0x00, 0x00, // mov ebx, 0x1100
+    0x83, 0xc3, 0x3f, // add ebx, 63
+    0xf0, 0xff, 0x03, // lock inc dword ptr [ebx]
+    0xf4 // hlt
+};
+
+// The parameter a0 is unused in this MR as the logic is self-contained.
+static long syz_mr_45f170fe(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long ret = -1;
+	void* mem = NULL;
+	struct kvm_run* run = NULL;
+	size_t mmap_size = 0;
+
+	uint32_t source_exit_reason = -1;
+	uint32_t followup_exit_reason = -1;
+	uint32_t source_flags = -1;
+	uint32_t followup_flags = -1;
+
+	// Declare all structs at the top of the function to avoid "crosses initialization" errors with goto.
+	struct kvm_userspace_memory_region region;
+	struct kvm_enable_cap cap;
+	struct kvm_regs regs;
+
+	// 1. Perform preparation
+	// Preliminary check: ensure KVM is available and supports the bus lock capability.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		return -1;
+	}
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, NULL) != KVM_API_VERSION) {
+		close(kvm_fd);
+		return -1;
+	}
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_X86_BUS_LOCK_EXIT) <= 0) {
+		close(kvm_fd);
+		return 0;
+	}
+	close(kvm_fd);
+	kvm_fd = -1;
+
+	// --- Source Input Run (Default behavior) ---
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0)
+		goto cleanup;
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd < 0)
+		goto cleanup;
+
+	mem = mmap(NULL, GUEST_MEM_SIZE_45f170fe, PROT_READ | PROT_WRITE,
+		   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		mem = NULL;
+		goto cleanup;
+	}
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = GUEST_MEM_SIZE_45f170fe;
+	region.userspace_addr = (uint64_t)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0)
+		goto cleanup;
+
+	memcpy((char*)mem + GUEST_CODE_OFFSET_45f170fe, guest_code_45f170fe, sizeof(guest_code_45f170fe));
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)0);
+	if (vcpu_fd < 0)
+		goto cleanup;
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size < sizeof(struct kvm_run))
+		goto cleanup;
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE,
+				    MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL;
+		goto cleanup;
+	}
+
+	// Set up registers for real mode execution
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs) < 0)
+		goto cleanup;
+	regs.rip = GUEST_CODE_OFFSET_45f170fe;
+	regs.rsp = GUEST_MEM_SIZE_45f170fe - 1;
+	regs.rflags = 2; // Reserved bit 1 must be 1
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0)
+		goto cleanup;
+
+	// KVM_CAP_X86_BUS_LOCK_EXIT is NOT enabled for the source run.
+
+	if (ioctl(vcpu_fd, KVM_RUN, NULL) < 0)
+		goto cleanup;
+
+	source_exit_reason = run->exit_reason;
+	source_flags = run->flags;
+
+	// Cleanup for source run
+	munmap(run, mmap_size);
+	run = NULL;
+	close(vcpu_fd);
+	vcpu_fd = -1;
+	munmap(mem, GUEST_MEM_SIZE_45f170fe);
+	mem = NULL;
+	close(vm_fd);
+	vm_fd = -1;
+	close(kvm_fd);
+	kvm_fd = -1;
+
+	// --- Follow-up Input Run (Explicitly OFF) ---
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0)
+		goto cleanup;
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd < 0)
+		goto cleanup;
+
+	// Enable the capability with the 'OFF' policy
+	memset(&cap, 0, sizeof(cap));
+	cap.cap = KVM_CAP_X86_BUS_LOCK_EXIT;
+	cap.flags = 0;
+	cap.args[0] = KVM_BUS_LOCK_DETECTION_OFF;
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap) < 0)
+		goto cleanup;
+
+	mem = mmap(NULL, GUEST_MEM_SIZE_45f170fe, PROT_READ | PROT_WRITE,
+		   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		mem = NULL;
+		goto cleanup;
+	}
+
+	// Re-initialize the region struct for the new memory mapping
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = GUEST_MEM_SIZE_45f170fe;
+	region.userspace_addr = (uint64_t)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0)
+		goto cleanup;
+
+	memcpy((char*)mem + GUEST_CODE_OFFSET_45f170fe, guest_code_45f170fe, sizeof(guest_code_45f170fe));
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)0);
+	if (vcpu_fd < 0)
+		goto cleanup;
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size < sizeof(struct kvm_run))
+		goto cleanup;
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE,
+				    MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL;
+		goto cleanup;
+	}
+
+	// Set up registers again for the new VCPU
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs) < 0)
+		goto cleanup;
+	regs.rip = GUEST_CODE_OFFSET_45f170fe;
+	regs.rsp = GUEST_MEM_SIZE_45f170fe - 1;
+	regs.rflags = 2;
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0)
+		goto cleanup;
+
+	if (ioctl(vcpu_fd, KVM_RUN, NULL) < 0)
+		goto cleanup;
+
+	followup_exit_reason = run->exit_reason;
+	followup_flags = run->flags;
+
+	ret = 0; // Success
+
+cleanup:
+	if (run)
+		munmap(run, mmap_size);
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (mem)
+		munmap(mem, GUEST_MEM_SIZE_45f170fe);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	if (ret != 0) {
+		// An error occurred during setup/run, can't check relation.
+		return -1;
+	}
+
+	// 4. Check the output relation
+	int violated = 0;
+	// Condition 1: Neither run should exit due to a bus lock.
+	if (source_exit_reason == KVM_EXIT_X86_BUS_LOCK ||
+	    followup_exit_reason == KVM_EXIT_X86_BUS_LOCK) {
+		violated = 1;
+	}
+	// Condition 2: The bus lock flag should not be set in either run.
+	if ((source_flags & KVM_RUN_X86_BUS_LOCK) ||
+	    (followup_flags & KVM_RUN_X86_BUS_LOCK)) {
+		violated = 1;
+	}
+	// Condition 3: The exit reasons should be identical.
+	if (source_exit_reason != followup_exit_reason) {
+		violated = 1;
+	}
+
+	if (violated) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_4d896b11.h b/executor/MRs/syz_mr_4d896b11.h
new file mode 100644
index 000000000..178a71581
--- /dev/null
+++ b/executor/MRs/syz_mr_4d896b11.h
@@ -0,0 +1,266 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.120-KVM_SET_PMU_EVENT_FILTER/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Permutation of the PMU event filter list should not alter filtering behavior.
+// - Source input: A call to the `KVM_SET_PMU_EVENT_FILTER` ioctl with a `struct kvm_pmu_event_filter` parameter, let's call it `S`. `S.events` contains an array of `S.nevents` events, and `S.action` is set to either `KVM_PMU_EVENT_ALLOW` or `KVM_PMU_EVENT_DENY`.
+// - Follow-up input: Another call to the `KVM_SET_PMU_EVENT_FILTER` ioctl with a `struct kvm_pmu_event_filter` parameter, let's call it `F`. The structure `F` is identical to `S` in all fields (`action`, `nevents`, `fixed_counter_bitmap`, `flags`), except that the `F.events` array is a permutation of the `S.events` array (i.e., it contains the exact same events but in a different order).
+// - Input relation: The follow-up input is generated by taking the source input's `struct kvm_pmu_event_filter` and shuffling the elements of its `events` array, while keeping all other fields unchanged.
+// - Output relation: The source and follow-up `ioctl` calls should both return 0, indicating success. More importantly, the resulting filtering behavior of the VM's PMU must be identical. For any given PMU event that the guest subsequently attempts to program, the outcome (whether the event is permitted or rejected) must be the same regardless of whether the filter was configured by the source input or the follow-up input.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// MSR definitions for x86
+#define MSR_IA32_PERFEVTSEL0_4d896b11 0x186
+#define MSR_IA32_FIXED_CTR_CTRL_4d896b11 0x38d
+
+// Custom return codes for our behavior check helper
+#define BEHAVIOR_ERROR_4d896b11 -1
+#define BEHAVIOR_DENIED_4d896b11 0
+#define BEHAVIOR_ALLOWED_4d896b11 1
+
+// Define KVM_PMU_EVENT_FILTER_MAX_EVENTS if not already defined by the headers
+#ifndef KVM_PMU_EVENT_FILTER_MAX_EVENTS_4d896b11
+#define KVM_PMU_EVENT_FILTER_MAX_EVENTS_4d896b11 256
+#endif
+
+// Forward declaration of the helper function to check PMU behavior
+static int check_pmu_behavior_4d896b11(int kvm_fd, int vm_fd, uint64_t event_code, int fixed_idx);
+
+static long syz_mr_4d896b11(volatile long nevents, volatile long flags,
+			    volatile long fixed_counter_bitmap, volatile long action,
+			    volatile uint64_t* events_arr)
+{
+	int kvm_fd = -1;
+	int vm_fd1 = -1;
+	int vm_fd2 = -1;
+	struct kvm_pmu_event_filter* filter_s = NULL;
+	struct kvm_pmu_event_filter* filter_f = NULL;
+	uint64_t* events_f_permuted = NULL;
+	long ret_s = 0, ret_f = 0;
+	size_t filter_size = 0;
+
+	long nevents_s = nevents;
+	if (nevents_s < 0 || nevents_s > KVM_PMU_EVENT_FILTER_MAX_EVENTS_4d896b11) {
+		nevents_s = 0;
+	}
+	if (events_arr == NULL) {
+		nevents_s = 0;
+	}
+	uint32_t action_s = (action == KVM_PMU_EVENT_DENY) ? KVM_PMU_EVENT_DENY : KVM_PMU_EVENT_ALLOW;
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		return 0;
+	}
+
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_PMU_EVENT_FILTER) <= 0) {
+		close(kvm_fd);
+		return 0;
+	}
+
+	vm_fd1 = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd1 == -1) {
+		goto cleanup;
+	}
+
+	vm_fd2 = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd2 == -1) {
+		goto cleanup;
+	}
+
+	filter_size = sizeof(struct kvm_pmu_event_filter) + nevents_s * sizeof(uint64_t);
+	filter_s = (struct kvm_pmu_event_filter*)malloc(filter_size);
+	if (!filter_s)
+		goto cleanup;
+	filter_f = (struct kvm_pmu_event_filter*)malloc(filter_size);
+	if (!filter_f)
+		goto cleanup;
+
+	// Source call
+	filter_s->nevents = nevents_s;
+	filter_s->action = action_s;
+	filter_s->flags = flags;
+	filter_s->fixed_counter_bitmap = fixed_counter_bitmap;
+	if (nevents_s > 0) {
+		memcpy(filter_s->events, (const void*)events_arr, nevents_s * sizeof(uint64_t));
+	}
+
+	ret_s = ioctl(vm_fd1, KVM_SET_PMU_EVENT_FILTER, filter_s);
+
+	// Follow-up call (permuted input)
+	filter_f->nevents = nevents_s;
+	filter_f->action = action_s;
+	filter_f->flags = flags;
+	filter_f->fixed_counter_bitmap = fixed_counter_bitmap;
+
+	if (nevents_s > 0) {
+		events_f_permuted = (uint64_t*)malloc(nevents_s * sizeof(uint64_t));
+		if (!events_f_permuted)
+			goto cleanup;
+		memcpy(events_f_permuted, (const void*)events_arr, nevents_s * sizeof(uint64_t));
+		if (nevents_s >= 2) {
+			uint64_t temp = events_f_permuted[0];
+			events_f_permuted[0] = events_f_permuted[1];
+			events_f_permuted[1] = temp;
+		}
+		memcpy(filter_f->events, events_f_permuted, nevents_s * sizeof(uint64_t));
+	}
+
+	ret_f = ioctl(vm_fd2, KVM_SET_PMU_EVENT_FILTER, filter_f);
+
+	if (ret_s != ret_f) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		goto cleanup;
+	}
+
+	if (ret_s < 0) {
+		goto cleanup;
+	}
+
+	{
+		if (nevents_s > 0) {
+			int res_s = check_pmu_behavior_4d896b11(kvm_fd, vm_fd1, filter_s->events[0], -1);
+			int res_f = check_pmu_behavior_4d896b11(kvm_fd, vm_fd2, filter_s->events[0], -1);
+			if (res_s != BEHAVIOR_ERROR_4d896b11 && res_f != BEHAVIOR_ERROR_4d896b11 && res_s != res_f) {
+				fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+			}
+		}
+
+		uint64_t non_list_event = 0xdeadbeefcafebabe;
+		int res_s_non = check_pmu_behavior_4d896b11(kvm_fd, vm_fd1, non_list_event, -1);
+		int res_f_non = check_pmu_behavior_4d896b11(kvm_fd, vm_fd2, non_list_event, -1);
+		if (res_s_non != BEHAVIOR_ERROR_4d896b11 && res_f_non != BEHAVIOR_ERROR_4d896b11 && res_s_non != res_f_non) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+
+		int res_s_fixed = check_pmu_behavior_4d896b11(kvm_fd, vm_fd1, 0, 0);
+		int res_f_fixed = check_pmu_behavior_4d896b11(kvm_fd, vm_fd2, 0, 0);
+		if (res_s_fixed != BEHAVIOR_ERROR_4d896b11 && res_f_fixed != BEHAVIOR_ERROR_4d896b11 && res_s_fixed != res_f_fixed) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+cleanup:
+	if (filter_s)
+		free(filter_s);
+	if (filter_f)
+		free(filter_f);
+	if (events_f_permuted)
+		free(events_f_permuted);
+	if (vm_fd1 != -1)
+		close(vm_fd1);
+	if (vm_fd2 != -1)
+		close(vm_fd2);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+	return 0;
+}
+
+static int check_pmu_behavior_4d896b11(int kvm_fd, int vm_fd, uint64_t event_code, int fixed_idx)
+{
+	int vcpu_fd = -1;
+	void* mem = MAP_FAILED;
+	struct kvm_run* run = NULL;
+	int result = BEHAVIOR_ERROR_4d896b11;
+	int mmap_size = 0;
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs;
+	struct kvm_regs regs;
+	unsigned char* code = NULL;
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		return BEHAVIOR_ERROR_4d896b11;
+	}
+
+	size_t mem_size = 0x1000;
+	mem = mmap(NULL, mem_size, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		goto vcpu_cleanup;
+	}
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = mem_size;
+	region.userspace_addr = (uint64_t)(uintptr_t)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		goto mem_cleanup;
+	}
+
+	code = (unsigned char*)mem;
+	code[0] = 0x0f;
+	code[1] = 0x30;
+	code[2] = 0xf4;
+
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		goto mem_cleanup;
+	}
+	sregs.cs.base = 0;
+	sregs.cs.selector = 0;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		goto mem_cleanup;
+	}
+
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = 0;
+	regs.rflags = 2;
+	if (fixed_idx < 0) {
+		regs.rcx = MSR_IA32_PERFEVTSEL0_4d896b11;
+		regs.rax = (uint32_t)event_code;
+		regs.rdx = event_code >> 32;
+	} else {
+		regs.rcx = MSR_IA32_FIXED_CTR_CTRL_4d896b11;
+		regs.rax = 0xbULL << (fixed_idx * 4);
+		regs.rdx = 0;
+	}
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		goto mem_cleanup;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0);
+	if (mmap_size <= 0) {
+		goto mem_cleanup;
+	}
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL;
+		goto mem_cleanup;
+	}
+
+	if (ioctl(vcpu_fd, KVM_RUN, 0) == -1) {
+		goto run_cleanup;
+	}
+
+	switch (run->exit_reason) {
+	case KVM_EXIT_HLT:
+		result = BEHAVIOR_ALLOWED_4d896b11;
+		break;
+	case KVM_EXIT_INTERNAL_ERROR:
+		if (run->internal.suberror == KVM_INTERNAL_ERROR_EMULATION) {
+			result = BEHAVIOR_DENIED_4d896b11;
+		}
+		break;
+	default:
+		result = BEHAVIOR_ERROR_4d896b11;
+		break;
+	}
+
+run_cleanup:
+	if (run)
+		munmap(run, mmap_size);
+mem_cleanup:
+	munmap(mem, mem_size);
+vcpu_cleanup:
+	close(vcpu_fd);
+	return result;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_561d57bb.h b/executor/MRs/syz_mr_561d57bb.h
new file mode 100644
index 000000000..f2f45e593
--- /dev/null
+++ b/executor/MRs/syz_mr_561d57bb.h
@@ -0,0 +1,171 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.96-KVM_SMI/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Entering SMM after queuing an SMI
+// - Source input: An x86 KVM vCPU that is not currently in System-Management Mode (SMM).
+// - Follow-up input: The same vCPU after the userspace process has issued the KVM_SMI ioctl once, queuing a pending SMI.
+// - Input relation: The follow-up input is obtained from the source input by invoking KVM_SMI.
+// - Output relation: After one KVM_RUN on the follow-up input returns, the vCPU state reported by KVM must indicate that the CPU has entered SMM (e.g., the `smm` flag in `kvm_vcpu_events` is true, or the KVM_EXIT reason is `KVM_EXIT_SMM`).
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Define KVM_EXIT_SMM if not defined in the headers, for compatibility with older systems.
+#ifndef KVM_EXIT_SMM_561d57bb
+#define KVM_EXIT_SMM_561d57bb 29
+#endif
+
+/*
+ * MR1: Entering SMM after queuing an SMI
+ * - Source input: An x86 KVM vCPU that is not currently in System-Management
+ * Mode (SMM).
+ * - Follow-up input: The same vCPU after the userspace process has issued the
+ * KVM_SMI ioctl once, queuing a pending SMI.
+ * - Input relation: The follow-up input is obtained from the source input by
+ * invoking KVM_SMI.
+ * - Output relation: After one KVM_RUN on the follow-up input returns, the vCPU
+ * state reported by KVM must indicate that the CPU has entered SMM (e.g., the
+ * `smm` flag in `kvm_vcpu_events` is true, or the KVM_EXIT reason is
+ * `KVM_EXIT_SMM`).
+ */
+static long syz_mr_561d57bb(volatile long a0, volatile long a1)
+{
+	// The parameters are ignored as this MR requires a complex setup (VM, vCPU)
+	// that cannot be represented by simple long integers. The test is
+	// self-contained.
+	(void)a0;
+	(void)a1;
+
+	// Declarations moved to the top to avoid "crosses initialization" errors in C++.
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	struct kvm_run* run = (struct kvm_run*)MAP_FAILED;
+	int run_mmap_size = 0;
+	void* guest_mem = MAP_FAILED;
+	long ret = 0;
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs;
+	struct kvm_regs regs;
+
+	// 1. Perform preparation: Create a VM and a vCPU.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM not supported or available, cannot run the test.
+		return -1;
+	}
+
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, NULL) != KVM_API_VERSION) {
+		// Incompatible KVM version.
+		ret = -1;
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Allocate one page of memory for the guest.
+	guest_mem = mmap(NULL, 0x1000, PROT_READ | PROT_WRITE,
+			 MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		ret = -1;
+		goto cleanup;
+	}
+	// Place a HLT instruction at the start of the guest memory.
+	memset(guest_mem, 0xf4, 1); // 0xf4 is the opcode for HLT
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0x0;
+	region.memory_size = 0x1000;
+	region.userspace_addr = (unsigned long)guest_mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)0);
+	if (vcpu_fd == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	run_mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (run_mmap_size <= 0) {
+		ret = -1;
+		goto cleanup;
+	}
+	run = (struct kvm_run*)mmap(NULL, run_mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED,
+				    vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Set up vCPU registers for a minimal real-mode execution.
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+	sregs.cs.base = 0;
+	sregs.cs.selector = 0;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = 0x0;
+	regs.rflags = 0x2; // A mandatory bit.
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Source input condition check (that vCPU is not in SMM) is omitted.
+	// The KVM headers in the build environment don't seem to support it,
+	// and it's a safe assumption for a newly created vCPU.
+
+	// 2. Apply the input relation: Queue an SMI.
+	if (ioctl(vcpu_fd, KVM_SMI, NULL) == -1) {
+		// If we can't queue an SMI, the test cannot proceed.
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output: Run the vCPU.
+	if (ioctl(vcpu_fd, KVM_RUN, NULL) == -1) {
+		// KVM_RUN failed, cannot check the output.
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 4. Check the output relation.
+	// Rely solely on the exit reason, as the MR description allows this ("OR").
+	// This avoids issues with missing SMM fields in kvm_vcpu_events in older headers.
+	if (run->exit_reason != KVM_EXIT_SMM_561d57bb) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 5. Perform cleanup.
+	if (run != (struct kvm_run*)MAP_FAILED)
+		munmap(run, run_mmap_size);
+	if (guest_mem != MAP_FAILED)
+		munmap(guest_mem, 0x1000);
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_57204f74.h b/executor/MRs/syz_mr_57204f74.h
new file mode 100644
index 000000000..6196d7736
--- /dev/null
+++ b/executor/MRs/syz_mr_57204f74.h
@@ -0,0 +1,140 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_mad/Creating-MAD-agents/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Automatic Unregistration of MAD Agents on File Descriptor Closure
+// - Source input: A sequence of operations: (1) Open a device file for an RDMA device, obtaining a file descriptor `fd`. (2) Successfully register a MAD agent using the `IB_USER_MAD_REGISTER_AGENT` ioctl on `fd`, obtaining an agent ID.
+// - Follow-up input: The same sequence of operations as the source input, with one additional operation: (3) Close the file descriptor `fd`.
+// - Input relation: The follow-up input is generated by appending a `close()` system call on the file descriptor `fd` to the sequence of operations in the source input.
+// - Output relation: Let the source output be the result of attempting to explicitly unregister the created agent using the `IB_USER_MAD_UNREGISTER_AGENT` ioctl *before* closing `fd`. Let the follow-up output be the result of attempting to unregister the same agent *after* closing `fd`. The source output should indicate success, while the follow-up output should indicate failure, as the agent should have been automatically unregistered when the file descriptor was closed.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <stdint.h> // For uint32_t, uint8_t
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/*
+ * --- Start of definitions from <infiniband/user_mad.h> and related headers ---
+ * These are defined here to remove the dependency on libibverbs-dev,
+ * which may not be present in the syzkaller build environment, thus resolving
+ * the "infiniband/user_mad.h: No such file or directory" compilation error.
+ */
+
+#define IB_USER_MAD_METHOD_MASK_SIZE_57204f74 (256 / 8)
+
+struct ib_user_mad_reg_req_57204f74 {
+	uint32_t id;
+	uint32_t qpn;
+	int mgmt_class;
+	int mgmt_class_version;
+	uint8_t oui[3];
+	uint8_t method_mask[IB_USER_MAD_METHOD_MASK_SIZE_57204f74];
+};
+
+#define IB_USER_MAD_IOCTL_MAGIC_57204f74 'm'
+#define IB_USER_MAD_REGISTER_AGENT_57204f74 _IOWR(IB_USER_MAD_IOCTL_MAGIC_57204f74, 1, struct ib_user_mad_reg_req_57204f74)
+#define IB_USER_MAD_UNREGISTER_AGENT_57204f74 _IOW(IB_USER_MAD_IOCTL_MAGIC_57204f74, 2, int)
+
+// From <rdma/ib_verbs.h>
+#define IB_MGMT_CLASS_SUBN_LID_ROUTED_57204f74 0x03
+
+/* --- End of inlined definitions --- */
+
+/*
+ * This metamorphic relation (MR) tests the automatic unregistration of
+ * Management Datagram (MAD) agents when the associated file descriptor is closed.
+ *
+ * - Source Case:
+ *   1. Open an RDMA user verbs device (/dev/infiniband/uverbs0).
+ *   2. Register a MAD agent and get an agent ID.
+ *   3. Explicitly unregister the agent using an ioctl call.
+ *   The output is the result of the unregister ioctl, which should be 0 (success).
+ *
+ * - Follow-up Case:
+ *   1. Open the same RDMA device.
+ *   2. Register a MAD agent and get an agent ID.
+ *   3. Close the file descriptor. This should implicitly unregister the agent.
+ *   4. Attempt to explicitly unregister the agent using the same agent ID on the
+ *      now-closed file descriptor.
+ *   The output is the result of the unregister ioctl, which should be -1 (failure),
+ *   typically with errno set to EBADF.
+ *
+ * - Output Relation:
+ *   The source output must indicate success (0), and the follow-up output must
+ *   indicate failure (-1). If this condition is not met, the MR is violated.
+ */
+static long syz_mr_57204f74(volatile long a0)
+{
+	// The parameter a0 is unused in this MR but is kept for fuzzer interface consistency.
+	(void)a0;
+
+	// 1. Perform preparation
+	const char* dev_path = "/dev/infiniband/uverbs0";
+	long source_output = -1;
+	long followup_output = -1;
+	int agent_id1 = -1;
+	int agent_id2 = -1;
+
+	// --- Source Case Execution ---
+	int fd1 = open(dev_path, O_RDWR);
+	if (fd1 < 0) {
+		// If the device cannot be opened, the test cannot proceed.
+		// This is a setup failure, not an MR violation.
+		return -1;
+	}
+
+	struct ib_user_mad_reg_req_57204f74 reg_req1;
+	memset(&reg_req1, 0, sizeof(reg_req1));
+	reg_req1.mgmt_class = IB_MGMT_CLASS_SUBN_LID_ROUTED_57204f74;
+	reg_req1.mgmt_class_version = 1;
+
+	if (ioctl(fd1, IB_USER_MAD_REGISTER_AGENT_57204f74, &reg_req1) != 0) {
+		// If agent registration fails, the test cannot proceed.
+		close(fd1);
+		return -1;
+	}
+	agent_id1 = reg_req1.id;
+
+	// Get the source output: explicitly unregister the agent before closing fd.
+	source_output = ioctl(fd1, IB_USER_MAD_UNREGISTER_AGENT_57204f74, &agent_id1);
+
+	// Cleanup for the source case.
+	close(fd1);
+
+	// --- Follow-up Case Execution ---
+	int fd2 = open(dev_path, O_RDWR);
+	if (fd2 < 0) {
+		// Setup failure for the follow-up case.
+		return -1;
+	}
+
+	struct ib_user_mad_reg_req_57204f74 reg_req2;
+	memset(&reg_req2, 0, sizeof(reg_req2));
+	reg_req2.mgmt_class = IB_MGMT_CLASS_SUBN_LID_ROUTED_57204f74;
+	reg_req2.mgmt_class_version = 1;
+
+	if (ioctl(fd2, IB_USER_MAD_REGISTER_AGENT_57204f74, &reg_req2) != 0) {
+		// Setup failure for the follow-up case.
+		close(fd2);
+		return -1;
+	}
+	agent_id2 = reg_req2.id;
+
+	// Apply the input relation: close the file descriptor.
+	close(fd2);
+
+	// Get the follow-up output: attempt to unregister the agent after closing fd.
+	// This ioctl is expected to fail because fd2 is no longer a valid file descriptor.
+	followup_output = ioctl(fd2, IB_USER_MAD_UNREGISTER_AGENT_57204f74, &agent_id2);
+
+	// 4. Check the output relation.
+	// Expected: source_output is 0 (success) and followup_output is -1 (failure).
+	if (!(source_output == 0 && followup_output == -1)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_580407a7.h b/executor/MRs/syz_mr_580407a7.h
new file mode 100644
index 000000000..0e9d5eab7
--- /dev/null
+++ b/executor/MRs/syz_mr_580407a7.h
@@ -0,0 +1,208 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.26-KVM_CAP_X86_USER_SPACE_MSR/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Toggling the KVM_CAP_X86_USER_SPACE_MSR capability changes MSR access behavior.
+// - Source input: A KVM virtual machine instance where the `KVM_CAP_X86_USER_SPACE_MSR` capability is **disabled**. The guest running within this VM attempts to access an MSR that KVM is configured to intercept and would typically cause a #GP (General Protection Fault) to be injected into the guest.
+// - Follow-up input: The same KVM virtual machine instance executing the identical guest code, but with the `KVM_CAP_X86_USER_SPACE_MSR` capability **enabled**.
+// - Input relation: The follow-up input is derived from the source input by enabling the `KVM_CAP_X86_USER_SPACE_MSR` capability for the virtual machine. The guest workload that triggers the MSR access remains unchanged.
+// - Output relation: The execution of the source input should result in KVM injecting an exception (e.g., a #GP fault) into the guest, and should not cause a VM exit to userspace for the MSR access itself. In contrast, the execution of the follow-up input should not inject a fault into the guest, but should instead cause a VM exit, with the `kvm_run` structure indicating an exit reason of `KVM_EXIT_X86_RDMSR` (for a read) or `KVM_EXIT_X86_WRMSR` (for a write).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// This helper function sets up a flat 32-bit segment descriptor.
+static void setup_segment_580407a7(struct kvm_segment* seg, uint16_t selector)
+{
+	seg->base = 0;
+	seg->limit = 0xffffffff;
+	seg->selector = selector;
+	seg->present = 1;
+	// Type 11: Code segment, Execute/Read.
+	// Type 3:  Data segment, Read/Write.
+	seg->type = (selector == 0x08) ? 11 : 3;
+	seg->dpl = 0; // Kernel mode (CPL=0)
+	seg->db = 1; // 32-bit
+	seg->s = 1; // Code/data segment
+	seg->l = 0; // Not 64-bit
+	seg->g = 1; // 4KB granularity
+	seg->unusable = 0;
+}
+
+// Resets the vCPU state to a known starting point for a run.
+static int reset_vcpu_580407a7(int vcpu_fd)
+{
+	struct kvm_sregs sregs;
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) < 0) {
+		perror("KVM_GET_SREGS for reset");
+		return -1;
+	}
+
+	// Set up 32-bit protected mode.
+	sregs.cr0 = 0x1; // Protected mode enabled (PE bit)
+
+	// Setup flat segments for CS, DS, ES, FS, GS, SS.
+	setup_segment_580407a7(&sregs.cs, 0x08);
+	setup_segment_580407a7(&sregs.ds, 0x10);
+	memcpy(&sregs.es, &sregs.ds, sizeof(struct kvm_segment));
+	memcpy(&sregs.fs, &sregs.ds, sizeof(struct kvm_segment));
+	memcpy(&sregs.gs, &sregs.ds, sizeof(struct kvm_segment));
+	memcpy(&sregs.ss, &sregs.ds, sizeof(struct kvm_segment));
+
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) < 0) {
+		perror("KVM_SET_SREGS for reset");
+		return -1;
+	}
+
+	// Initialize kvm_regs sequentially to avoid designator order issues.
+	struct kvm_regs regs;
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = 0;
+	regs.rsp = 0x1000; // Stack pointer at the end of the 4K page
+	regs.rflags = 2;
+
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0) {
+		perror("KVM_SET_REGS for reset");
+		return -1;
+	}
+	return 0;
+}
+
+static long syz_mr_580407a7(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	struct kvm_run* run = NULL;
+	void* guest_mem = NULL;
+	int mmap_size = 0;
+	long source_exit_reason = -1;
+	long follow_up_exit_reason = -1;
+
+	// Move declarations to the top to avoid "crosses initialization" errors with goto.
+	struct kvm_userspace_memory_region region;
+	struct kvm_enable_cap cap;
+	// Guest code: mov ecx, 0x10 (MSR_IA32_TSC); rdmsr; hlt
+	unsigned char code[] = {0xb9, 0x10, 0x00, 0x00, 0x00, 0x0f, 0x32, 0xf4};
+
+	// 1. Basic KVM setup
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// KVM not available, cannot run the test.
+		return -1;
+	}
+
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, 0) != KVM_API_VERSION) {
+		fprintf(stderr, "KVM API version mismatch\n");
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		perror("KVM_CREATE_VM");
+		goto cleanup;
+	}
+
+	// 2. Setup guest memory
+	guest_mem = mmap(NULL, 0x1000, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		perror("mmap guest_mem");
+		goto cleanup;
+	}
+
+	// Initialize the region struct
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = 0x1000;
+	region.userspace_addr = (uint64_t)guest_mem;
+
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0) {
+		perror("KVM_SET_USER_MEMORY_REGION");
+		goto cleanup;
+	}
+
+	memcpy(guest_mem, code, sizeof(code));
+
+	// 3. Setup vCPU
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		perror("KVM_CREATE_VCPU");
+		goto cleanup;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size <= 0) {
+		perror("KVM_GET_VCPU_MMAP_SIZE");
+		goto cleanup;
+	}
+	// Add explicit cast for C++ compatibility
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		perror("mmap kvm_run");
+		run = NULL; // Ensure run is NULL for cleanup
+		goto cleanup;
+	}
+
+	// 4. Source run: KVM_CAP_X86_USER_SPACE_MSR is disabled by default.
+	if (reset_vcpu_580407a7(vcpu_fd) != 0) {
+		goto cleanup;
+	}
+	if (ioctl(vcpu_fd, KVM_RUN, 0) < 0) {
+		if (errno != EFAULT)
+			perror("KVM_RUN (source)");
+		goto cleanup;
+	}
+	source_exit_reason = run->exit_reason;
+
+	// 5. Follow-up run: Enable KVM_CAP_X86_USER_SPACE_MSR.
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_X86_USER_SPACE_MSR) <= 0) {
+		// If the capability is not supported, we cannot perform the test.
+		goto cleanup;
+	}
+
+	// Initialize the cap struct
+	memset(&cap, 0, sizeof(cap));
+	cap.cap = KVM_CAP_X86_USER_SPACE_MSR;
+	cap.flags = 0;
+
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap) < 0) {
+		perror("KVM_ENABLE_CAP");
+		goto cleanup;
+	}
+
+	if (reset_vcpu_580407a7(vcpu_fd) != 0) {
+		goto cleanup;
+	}
+	if (ioctl(vcpu_fd, KVM_RUN, 0) < 0) {
+		if (errno != EFAULT)
+			perror("KVM_RUN (follow-up)");
+		goto cleanup;
+	}
+	follow_up_exit_reason = run->exit_reason;
+
+	// 6. Output relation check.
+	// Source should not exit for MSR access.
+	// Follow-up MUST exit for MSR access.
+	if (source_exit_reason == KVM_EXIT_X86_RDMSR || follow_up_exit_reason != KVM_EXIT_X86_RDMSR) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	if (run)
+		munmap(run, mmap_size);
+	if (guest_mem)
+		munmap(guest_mem, 0x1000);
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_5bc1836b.h b/executor/MRs/syz_mr_5bc1836b.h
new file mode 100644
index 000000000..d377ad254
--- /dev/null
+++ b/executor/MRs/syz_mr_5bc1836b.h
@@ -0,0 +1,255 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.34-KVM_CAP_EXIT_HYPERCALL/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Subset property of hypercall exit configuration
+// - Source input: A KVM virtual machine configured to enable the `KVM_CAP_EXIT_HYPERCALL` capability with a specific bitmask `M_S`. `M_S` must be a valid subset of the hypercalls reported by `KVM_CHECK_EXTENSION`. The guest is then run to execute a set of hypercalls.
+// - Follow-up input: The same KVM virtual machine, but reconfigured to enable the `KVM_CAP_EXIT_HYPERCALL` capability with a new bitmask `M_F`, where `M_F` is a proper subset of `M_S` (`M_F` ⊂ `M_S`). The same guest is run to execute the same set of hypercalls.
+// - Input relation: The follow-up input is derived from the source input by reducing the set of hypercalls configured to exit to userspace. The new set of enabled hypercalls (`M_F`) is a proper subset of the original set (`M_S`).
+// - Output relation: For any hypercall `h` executed by the guest:
+//   1. If the bit corresponding to `h` is set in `M_F` (and therefore also in `M_S`), then the execution of `h` in both the source and follow-up runs must result in a `KVM_EXIT_HYPERCALL` exit to userspace.
+//   2. If the bit corresponding to `h` is set in `M_S` but not in `M_F`, then its execution in the source run must result in a `KVM_EXIT_HYPERCALL` exit, while its execution in the follow-up run must be handled by the kernel and return `ENOSYS` to the guest, without causing a VM exit to userspace for this reason.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdbool.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+#define GUEST_MEM_SIZE_5bc1836b 0x4000 // 16 KB for code and page tables
+#define GUEST_CODE_GPA_5bc1836b 0x0
+
+// Helper function to set up 64-bit mode page tables
+static void setup_64bit_page_tables_5bc1836b(uint8_t* mem)
+{
+	const uint64_t pml4_gpa = 0x1000;
+	const uint64_t pdpte_gpa = 0x2000;
+	const uint64_t pde_gpa = 0x3000;
+
+	uint64_t* pml4 = (uint64_t*)(mem + pml4_gpa);
+	uint64_t* pdpte = (uint64_t*)(mem + pdpte_gpa);
+	uint64_t* pde = (uint64_t*)(mem + pde_gpa);
+
+	memset(pml4, 0, 4096);
+	memset(pdpte, 0, 4096);
+	memset(pde, 0, 4096);
+
+	// PML4[0] -> PDPTE
+	pml4[0] = pdpte_gpa | 0x3; // Present, R/W
+	// PDPTE[0] -> PDE
+	pdpte[0] = pde_gpa | 0x3; // Present, R/W
+	// PDE[0] -> 2MB page mapping guest physical address 0
+	pde[0] = 0x0 | 0x83; // Present, R/W, 2MB page
+}
+
+// Helper function to set up SREGS for 64-bit long mode
+static int setup_64bit_sregs_5bc1836b(int vcpu_fd)
+{
+	struct kvm_sregs sregs;
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) < 0) {
+		return -1;
+	}
+
+	sregs.cr3 = 0x1000; // PML4 table address
+	sregs.cr4 = 1 << 5; // CR4_PAE
+	sregs.cr0 = (1 << 31) | (1 << 0); // CR0_PG | CR0_PE
+	sregs.efer = (1 << 8) | (1 << 10); // EFER_LME | EFER_LMA
+
+	struct kvm_segment seg = {
+	    .base = 0,
+	    .limit = 0xffffffff,
+	    .selector = 1 << 3,
+	    .present = 1,
+	    .dpl = 0,
+	    .db = 0,
+	    .s = 1, // Code/Data
+	    .g = 1, // Granularity
+	};
+
+	seg.type = 11; // Execute/Read
+	seg.l = 1; // Long mode
+	sregs.cs = seg;
+
+	seg.type = 3; // Read/Write
+	seg.l = 0; // Not long mode for data segments
+	seg.selector = 2 << 3;
+	sregs.ds = sregs.es = sregs.fs = sregs.gs = sregs.ss = seg;
+
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) < 0) {
+		return -1;
+	}
+	return 0;
+}
+
+// Helper function to set up general purpose registers
+static int setup_regs_5bc1836b(int vcpu_fd, uint64_t rip, uint64_t rsp)
+{
+	struct kvm_regs regs;
+	// Use sequential assignment to avoid designated initializer order issues.
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = rip;
+	regs.rsp = rsp;
+	regs.rflags = 0x2;
+
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0) {
+		return -1;
+	}
+	return 0;
+}
+
+// Helper function to run the VCPU for a single hypercall test
+static int run_vcpu_test_5bc1836b(int vcpu_fd, struct kvm_run* run)
+{
+	int ret;
+	do {
+		ret = ioctl(vcpu_fd, KVM_RUN, NULL);
+	} while (ret == -1 && (errno == EINTR || errno == EAGAIN));
+	return ret;
+}
+
+static long syz_mr_5bc1836b(volatile long a0, volatile long a1)
+{
+	// --- Variable Declarations ---
+	long ret = -1;
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	uint8_t* guest_mem = (uint8_t*)MAP_FAILED;
+	struct kvm_run* run = (struct kvm_run*)MAP_FAILED;
+	int mmap_size = 0;
+	uint64_t h1 = 1;
+	uint64_t h2 = 2;
+	uint64_t M_S, M_F;
+	bool source_h1_exited = false;
+	bool source_h2_exited = false;
+	bool followup_h1_exited = false;
+	bool followup_h2_no_exit_and_enosys = false;
+	bool relation1_ok, relation2_ok;
+	struct kvm_userspace_memory_region region;
+	struct kvm_enable_cap cap_s, cap_f;
+	uint8_t guest_code[14] = {
+	    // mov rax, <hypercall_nr>
+	    0x48, 0xb8, 0, 0, 0, 0, 0, 0, 0, 0,
+	    // vmmcall
+	    0x0f, 0x01, 0xc1,
+	    // hlt
+	    0xf4};
+
+	// --- Setup ---
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1)
+		goto cleanup;
+
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, 0) != 12)
+		goto cleanup;
+
+	if (!ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_EXIT_HYPERCALL))
+		goto cleanup; // MR not applicable
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1)
+		goto cleanup;
+
+	guest_mem = (uint8_t*)mmap(NULL, GUEST_MEM_SIZE_5bc1836b, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED)
+		goto cleanup;
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = GUEST_MEM_SIZE_5bc1836b;
+	region.userspace_addr = (uint64_t)guest_mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0)
+		goto cleanup;
+
+	setup_64bit_page_tables_5bc1836b(guest_mem);
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1)
+		goto cleanup;
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size <= 0)
+		goto cleanup;
+
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED)
+		goto cleanup;
+
+	// --- Source Run ---
+	M_S = (1UL << h1) | (1UL << h2);
+	cap_s.cap = KVM_CAP_EXIT_HYPERCALL;
+	cap_s.flags = 0;
+	cap_s.args[0] = M_S;
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap_s) < 0)
+		goto cleanup;
+
+	// Test hypercall h1
+	*(uint64_t*)&guest_code[2] = h1;
+	memcpy(guest_mem + GUEST_CODE_GPA_5bc1836b, guest_code, sizeof(guest_code));
+	if (setup_64bit_sregs_5bc1836b(vcpu_fd) < 0 || setup_regs_5bc1836b(vcpu_fd, GUEST_CODE_GPA_5bc1836b, GUEST_MEM_SIZE_5bc1836b - 8) < 0)
+		goto cleanup;
+	if (run_vcpu_test_5bc1836b(vcpu_fd, run) >= 0 && run->exit_reason == KVM_EXIT_HYPERCALL && run->hyperv.u.hcall.input == h1)
+		source_h1_exited = true;
+
+	// Test hypercall h2
+	*(uint64_t*)&guest_code[2] = h2;
+	memcpy(guest_mem + GUEST_CODE_GPA_5bc1836b, guest_code, sizeof(guest_code));
+	if (setup_64bit_sregs_5bc1836b(vcpu_fd) < 0 || setup_regs_5bc1836b(vcpu_fd, GUEST_CODE_GPA_5bc1836b, GUEST_MEM_SIZE_5bc1836b - 8) < 0)
+		goto cleanup;
+	if (run_vcpu_test_5bc1836b(vcpu_fd, run) >= 0 && run->exit_reason == KVM_EXIT_HYPERCALL && run->hyperv.u.hcall.input == h2)
+		source_h2_exited = true;
+
+	// --- Follow-up Run ---
+	M_F = (1UL << h1);
+	cap_f.cap = KVM_CAP_EXIT_HYPERCALL;
+	cap_f.flags = 0;
+	cap_f.args[0] = M_F;
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap_f) < 0)
+		goto cleanup;
+
+	// Test hypercall h1
+	*(uint64_t*)&guest_code[2] = h1;
+	memcpy(guest_mem + GUEST_CODE_GPA_5bc1836b, guest_code, sizeof(guest_code));
+	if (setup_64bit_sregs_5bc1836b(vcpu_fd) < 0 || setup_regs_5bc1836b(vcpu_fd, GUEST_CODE_GPA_5bc1836b, GUEST_MEM_SIZE_5bc1836b - 8) < 0)
+		goto cleanup;
+	if (run_vcpu_test_5bc1836b(vcpu_fd, run) >= 0 && run->exit_reason == KVM_EXIT_HYPERCALL && run->hyperv.u.hcall.input == h1)
+		followup_h1_exited = true;
+
+	// Test hypercall h2
+	*(uint64_t*)&guest_code[2] = h2;
+	memcpy(guest_mem + GUEST_CODE_GPA_5bc1836b, guest_code, sizeof(guest_code));
+	if (setup_64bit_sregs_5bc1836b(vcpu_fd) < 0 || setup_regs_5bc1836b(vcpu_fd, GUEST_CODE_GPA_5bc1836b, GUEST_MEM_SIZE_5bc1836b - 8) < 0)
+		goto cleanup;
+	if (run_vcpu_test_5bc1836b(vcpu_fd, run) >= 0 && run->exit_reason == KVM_EXIT_HLT) {
+		struct kvm_regs regs;
+		if (ioctl(vcpu_fd, KVM_GET_REGS, &regs) == 0 && regs.rax == (uint64_t)-ENOSYS) {
+			followup_h2_no_exit_and_enosys = true;
+		}
+	}
+
+	// --- Verify Output Relation ---
+	relation1_ok = source_h1_exited && followup_h1_exited;
+	relation2_ok = source_h2_exited && followup_h2_no_exit_and_enosys;
+
+	if (!relation1_ok || !relation2_ok) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	ret = 0; // Success
+cleanup:
+	if (run != (struct kvm_run*)MAP_FAILED)
+		munmap(run, mmap_size);
+	if (guest_mem != (uint8_t*)MAP_FAILED)
+		munmap(guest_mem, GUEST_MEM_SIZE_5bc1836b);
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_62089dad.h b/executor/MRs/syz_mr_62089dad.h
new file mode 100644
index 000000000..0510fb589
--- /dev/null
+++ b/executor/MRs/syz_mr_62089dad.h
@@ -0,0 +1,95 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.-API-description/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Re-creating an already existing vCPU index must fail.
+// - Source input: A call to the `KVM_CREATE_VCPU` ioctl on a VM with a fresh, unused vCPU index `i` (where `0 <= i < KVM_CAP_MAX_VCPUS`). No vCPU with index `i` currently exists for this VM.
+// - Follow-up input: A second call to the `KVM_CREATE_VCPU` ioctl on the same VM, using the exact same vCPU index `i`.
+// - Input relation: The follow-up input is identical to the source input and is applied to the system state created by the successful execution of the source input.
+// - Output relation: The source output should be a success, returning a valid file descriptor for the newly created vCPU `i`. The follow-up output must be a failure, returning an error code (specifically, `-EEXIST`), and it must not return a valid file descriptor.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Implements the metamorphic relation: Re-creating an already existing vCPU index must fail.
+ *
+ * @param vcpu_id The vCPU index to be used for creation. This is the main input for the MR.
+ *                It is expected to be a fresh, unused index for the source call.
+ * @return 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_62089dad(volatile long vcpu_id)
+{
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	long source_vcpu_fd = -1;
+	long follow_up_vcpu_fd = -1;
+	int follow_up_errno = 0;
+
+	// 1. Perform preparation: Create a KVM instance and a VM.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot perform the test if KVM is not available.
+		return -1;
+	}
+
+	// The MR premise is that 'i' is a valid, fresh vCPU index.
+	// We check if the provided vcpu_id is within the valid range.
+	long max_vcpus = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_MAX_VCPUS);
+	if (max_vcpus <= 0) {
+		// Fallback if the capability is not supported or returns a non-positive value.
+		// KVM_MAX_VCPUS is 288 in recent kernels. 256 is a safe bet for a fallback.
+		max_vcpus = 256;
+	}
+	if (vcpu_id < 0 || vcpu_id >= max_vcpus) {
+		// The provided vcpu_id is out of the valid range, so the test cannot proceed.
+		close(kvm_fd);
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get the source output
+	// The source input is the first call to KVM_CREATE_VCPU.
+	errno = 0;
+	source_vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, vcpu_id);
+
+	// 3. Get the follow-up output
+	// The follow-up input is the second call with the same parameters.
+	errno = 0;
+	follow_up_vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, vcpu_id);
+	follow_up_errno = errno;
+
+	// 4. Check the output relation
+	// The source call should succeed (returning a valid fd >= 0).
+	// The follow-up call should fail (returning -1) with errno EEXIST.
+	// A violation occurs if:
+	// - The source call fails, OR
+	// - The follow-up call does not return -1, OR
+	// - The follow-up call returns -1, but errno is not EEXIST.
+	if (source_vcpu_fd < 0 || follow_up_vcpu_fd != -1 || follow_up_errno != EEXIST) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Perform cleanup
+	if (source_vcpu_fd >= 0) {
+		close(source_vcpu_fd);
+	}
+	if (follow_up_vcpu_fd >= 0) {
+		// This case should only be reached if the MR is violated.
+		close(follow_up_vcpu_fd);
+	}
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_63e076f2.h b/executor/MRs/syz_mr_63e076f2.h
new file mode 100644
index 000000000..a018673c5
--- /dev/null
+++ b/executor/MRs/syz_mr_63e076f2.h
@@ -0,0 +1,195 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/5.-The-kvm_run-structure/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 3 iterations
+// MR1: Immediate exit leaves guest state unchanged when no deferred work is pending and KVM_CAP_IMMEDIATE_EXIT is available.
+// - Source input: A VCPU in a complete architectural state `S`. The following pre-conditions must be met: (1) The host supports and has enabled `KVM_CAP_IMMEDIATE_EXIT`. (2) The previous VM exit was not one that requires deferred completion on the next `KVM_RUN` call (i.e., not `KVM_EXIT_IO`, `KVM_EXIT_MMIO`, `KVM_EXIT_OSI`, `KVM_EXIT_PAPR_HCALL`, `KVM_EXIT_XEN`, `KVM_EXIT_EPR`, `KVM_EXIT_X86_RDMSR`, or `KVM_EXIT_X86_WRMSR`). The conceptual operation is to call `KVM_RUN` with `immediate_exit` set to `0`.
+// - Follow-up input: The exact same VCPU in the identical architectural state `S` as the source input. The operation is to call `KVM_RUN` with `immediate_exit` set to a non-zero value (e.g., `1`).
+// - Input relation: The VCPU's architectural state `S` is identical for both the source and follow-up scenarios. The transformation is changing the `immediate_exit` field in the `kvm_run` structure from `0` to a non-zero value for the `KVM_RUN` call.
+// - Output relation: The `KVM_RUN` call for the follow-up input must return immediately with a value of `-EINTR`. Furthermore, the VCPU's complete architectural state after the call must be bit-for-bit identical to the initial state `S`, as no guest instructions should have been executed and no deferred operations were performed.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// The MR is designed for x86 architecture.
+#if defined(__x86_64__) || defined(__i386__)
+
+static long syz_mr_63e076f2(volatile long a0, volatile long a1)
+{
+	// All variable declarations are moved to the top to avoid "crosses initialization" errors with goto.
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	void* mem = MAP_FAILED;
+	// Cast MAP_FAILED to the correct pointer type for C++ compatibility.
+	struct kvm_run* run = (struct kvm_run*)MAP_FAILED;
+	long ret = 0;
+	size_t mem_size = 0x1000;
+	int mmap_size = 0;
+	unsigned char* code = NULL;
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs, sregs_S, sregs_after;
+	struct kvm_regs regs, regs_S, regs_after;
+	struct kvm_fpu fpu_S, fpu_after;
+	long follow_up_ret = 0;
+	long follow_up_errno = 0;
+	int violated = 0;
+
+	// 1. Perform preparation
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		goto cleanup;
+	}
+
+	// Check KVM API version
+	ret = ioctl(kvm_fd, KVM_GET_API_VERSION, NULL);
+	if (ret != KVM_API_VERSION) {
+		goto cleanup;
+	}
+
+	// Check for the required capability: KVM_CAP_IMMEDIATE_EXIT
+	ret = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_IMMEDIATE_EXIT);
+	if (!ret) {
+		goto cleanup;
+	}
+
+	// Create a Virtual Machine
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd == -1) {
+		goto cleanup;
+	}
+
+	// Allocate memory for the guest
+	mem = mmap(NULL, mem_size, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		goto cleanup;
+	}
+
+	// Place a simple HLT instruction in guest memory. This will cause a clean exit.
+	code = (unsigned char*)mem;
+	code[0] = 0xf4; // HLT instruction
+
+	// Set up the memory region for the VM
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = mem_size;
+	region.userspace_addr = (unsigned long)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		goto cleanup;
+	}
+
+	// Create a VCPU
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)0);
+	if (vcpu_fd == -1) {
+		goto cleanup;
+	}
+
+	// Get the size of the kvm_run structure and map it
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	// Fix signed/unsigned comparison and check for ioctl error.
+	if (mmap_size < 0 || (size_t)mmap_size < sizeof(struct kvm_run)) {
+		goto cleanup;
+	}
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		goto cleanup;
+	}
+
+	// 2. Establish the initial architectural state 'S' and meet pre-conditions
+	// Set up initial special registers (sregs) for real mode execution
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		goto cleanup;
+	}
+	sregs.cs.base = 0;
+	sregs.cs.selector = 0;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		goto cleanup;
+	}
+
+	// Set up initial general-purpose registers (regs)
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = 0;
+	regs.rflags = 0x2;
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		goto cleanup;
+	}
+
+	// Perform a "priming" run to ensure the previous exit is not one that requires deferred work.
+	// The HLT instruction will cause a KVM_EXIT_HLT, which is not a deferred exit type.
+	run->immediate_exit = 0;
+	if (ioctl(vcpu_fd, KVM_RUN, NULL) == -1 || run->exit_reason != KVM_EXIT_HLT) {
+		goto cleanup;
+	}
+
+	// The VCPU is now in the desired architectural state 'S'. Capture it.
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs_S) == -1)
+		goto cleanup;
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs_S) == -1)
+		goto cleanup;
+	if (ioctl(vcpu_fd, KVM_GET_FPU, &fpu_S) == -1)
+		goto cleanup;
+
+	// 3. Execute the follow-up input
+	// Set immediate_exit to 1, keeping the VCPU state identical.
+	run->immediate_exit = 1;
+	follow_up_ret = ioctl(vcpu_fd, KVM_RUN, NULL);
+	follow_up_errno = errno; // Save errno immediately after the call
+
+	// 4. Check the output relation
+	// Get the VCPU state after the follow-up run
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs_after) == -1)
+		goto cleanup;
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs_after) == -1)
+		goto cleanup;
+	if (ioctl(vcpu_fd, KVM_GET_FPU, &fpu_after) == -1)
+		goto cleanup;
+
+	// Relation 1: The KVM_RUN call must return -EINTR.
+	if (!(follow_up_ret == -1 && follow_up_errno == EINTR)) {
+		violated = 1;
+	}
+
+	// Relation 2: The VCPU's architectural state must be bit-for-bit identical.
+	if (memcmp(&sregs_S, &sregs_after, sizeof(sregs_S)) != 0) {
+		violated = 1;
+	}
+	if (memcmp(&regs_S, &regs_after, sizeof(regs_S)) != 0) {
+		violated = 1;
+	}
+	if (memcmp(&fpu_S, &fpu_after, sizeof(fpu_S)) != 0) {
+		violated = 1;
+	}
+
+	if (violated) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 5. Perform cleanup
+	if (run != MAP_FAILED)
+		munmap(run, mmap_size);
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (mem != MAP_FAILED)
+		munmap(mem, mem_size);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return 0;
+}
+
+#else
+// Provide a dummy implementation for non-x86 architectures
+static long syz_mr_63e076f2(volatile long a0, volatile long a1)
+{
+	return 0;
+}
+#endif
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_647388cd.h b/executor/MRs/syz_mr_647388cd.h
new file mode 100644
index 000000000..2f404ceb3
--- /dev/null
+++ b/executor/MRs/syz_mr_647388cd.h
@@ -0,0 +1,135 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/6.75-KVM_CAP_PPC_IRQ_XIVE/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Atomicity and State Restoration of Failing KVM Operations
+// - Source input: A sequence of KVM API calls that establishes a known initial state `S_0`, followed by a valid KVM API call `Op_valid` (e.g., an `ioctl` with correct parameters) that is expected to succeed and transition the system to a new state `S_1`.
+// - Follow-up input: The same sequence of KVM API calls to establish the initial state `S_0`, followed by a corresponding API call `Op_invalid`. `Op_invalid` is intentionally crafted to fail midway through its execution in the kernel (e.g., by providing parameters that cause an internal error like a user-space memory copy to fail, or triggering an internal resource allocation failure).
+// - Input relation: The follow-up input is derived from the source input by replacing the successful operation `Op_valid` with a version `Op_invalid` that is designed to fail after partial execution within the kernel.
+// - Output relation: The `Op_invalid` call must return an error code. Furthermore, after `Op_invalid` completes, the observable state of the KVM system must be identical to the initial state `S_0`. This implies that any resources allocated or state modified during the partial execution of `Op_invalid` have been completely released and rolled back, preventing resource leaks or inconsistent states.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+/**
+ * MR1: Atomicity and State Restoration of Failing KVM Operations
+ *
+ * This metamorphic relation tests whether a failing KVM operation that modifies
+ * the VM state correctly rolls back any changes, leaving the system in its
+ * original state (S_0).
+ *
+ * The test proceeds as follows:
+ * 1. Establish an initial state S_0: Create a new KVM virtual machine (VM).
+ *
+ * 2. Execute an invalid operation (Op_invalid): Attempt to set a user memory
+ *    region using KVM_SET_USER_MEMORY_REGION with an invalid userspace address.
+ *    This operation is designed to fail midway within the kernel after some
+ *    initial checks have passed but before the operation completes (e.g., when
+ *    the kernel tries to access the invalid user memory).
+ *
+ * 3. Verify the first part of the output relation: The call for Op_invalid must
+ *    return an error code. If it succeeds, the kernel has failed to detect the
+ *    invalid input, which is a violation.
+ *
+ * 4. Verify the second part of the output relation: After Op_invalid fails, the
+ *    VM's state must be identical to the initial state S_0. This is verified by
+ *    executing a valid operation (Op_valid) that should succeed on a clean VM.
+ *    Specifically, we perform a valid KVM_SET_USER_MEMORY_REGION call on the
+ *    *same memory slot* that the invalid operation targeted.
+ *
+ * 5. If this subsequent valid operation succeeds, it implies that the failed
+ *    operation was atomic and correctly rolled back any partial state changes.
+ *    If it fails, it suggests that the failed operation left the memory slot
+ *    in a corrupted or inconsistent state, which is a violation of the MR.
+ */
+static long syz_mr_647388cd(volatile long a0)
+{
+	// The input parameter is not used in this MR.
+	(void)a0;
+
+	// --- Preparation: Open KVM device ---
+	int kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is not available, so we cannot run the test.
+		// This is not a violation, so we skip the test by returning 0.
+		return 0;
+	}
+
+	// --- Follow-up Execution ---
+
+	// 1. Establish initial state S_0: Create a VM.
+	int vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		// Failed to create a VM, cannot proceed.
+		close(kvm_fd);
+		return 0;
+	}
+
+	// 2. Execute Op_invalid: Attempt to set a memory region with an invalid
+	//    userspace address. This should cause a fault inside the ioctl.
+	struct kvm_userspace_memory_region region_invalid = {
+	    .slot = 0,
+	    .flags = 0,
+	    .guest_phys_addr = 0x1000,
+	    .memory_size = 0x1000,
+	    .userspace_addr = (unsigned long)-1, // Invalid address
+	};
+
+	long res_invalid = ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region_invalid);
+
+	// 3. Check Output Relation Part 1: Op_invalid must fail.
+	if (res_invalid == 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated! Invalid KVM_SET_USER_MEMORY_REGION succeeded when it should have failed.\n");
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 4. Verify State Restoration: Attempt a valid operation (Op_valid) on the
+	//    same resource (memory slot 0) to check if the state is clean (S_0).
+
+	// First, prepare a valid memory region.
+	size_t mem_size = 0x1000;
+	void* mem = mmap(NULL, mem_size, PROT_READ | PROT_WRITE,
+			 MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		// Failed to allocate memory, cannot proceed with the check.
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// Now, perform the valid operation.
+	struct kvm_userspace_memory_region region_valid = {
+	    .slot = 0, // Use the same slot as the failed operation.
+	    .flags = 0,
+	    .guest_phys_addr = 0x1000,
+	    .memory_size = mem_size,
+	    .userspace_addr = (unsigned long)mem,
+	};
+
+	long res_valid_check = ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region_valid);
+
+	// 5. Check Output Relation Part 2: The valid operation must succeed.
+	if (res_valid_check != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated! State was not restored after a failed KVM operation. Error: %s\n", strerror(errno));
+		munmap(mem, mem_size);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// --- Cleanup ---
+	munmap(mem, mem_size);
+	close(vm_fd);
+	close(kvm_fd);
+
+	// If we reach here, the MR holds.
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_6473f381.h b/executor/MRs/syz_mr_6473f381.h
new file mode 100644
index 000000000..0c087b872
--- /dev/null
+++ b/executor/MRs/syz_mr_6473f381.h
@@ -0,0 +1,148 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.32-KVM_SET_VCPU_EVENTS/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: The KVM_VCPUEVENT_VALID_SMM flag controls whether the SMM state is applied.
+// - Source input:
+//     - A vCPU in any valid state where the `KVM_CAP_X86_SMM` capability is available.
+//     - A call to the `KVM_SET_VCPU_EVENTS` ioctl with a `kvm_vcpu_event` structure where the `flags` field does *not* contain `KVM_VCPUEVENT_VALID_SMM`, and `smi.smm` is set to the opposite of the vCPU's current SMM state.
+// - Follow-up input:
+//     - The same vCPU, immediately after the source input call.
+//     - A call to the `KVM_SET_VCPU_EVENTS` ioctl with a `kvm_vcpu_event` structure where the `flags` field *does* contain `KVM_VCPUEVENT_VALID_SMM`, and `smi.smm` is set to the same value used in the source input.
+// - Input relation: The source and follow-up inputs operate on the same vCPU state and provide the same `smi.smm` value. The only difference is that the `KVM_VCPUEVENT_VALID_SMM` bit is set in the `flags` field for the follow-up input, whereas it is clear for the source input.
+// - Output relation: Let the SMM state of the vCPU before any operation be `S_initial`. After the source input call, a `KVM_GET_VCPU_EVENTS` call should show that the SMM state is still `S_initial`. After the follow-up input call, a `KVM_GET_VCPU_EVENTS` call should show that the SMM state has been updated to `!S_initial`. Other event fields for which the corresponding `VALID` flag was not set should remain unchanged by both calls.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// This function implements the metamorphic relation for KVM_SET_VCPU_EVENTS.
+// The MR tests that the KVM_VCPUEVENT_VALID_SMM flag correctly controls
+// whether the SMM state is applied.
+// The fuzzer-provided parameter 'a0' is not used in this MR, as the logic is self-contained.
+static long syz_mr_6473f381(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long ret = 0;
+	int s_initial = 0;
+
+	// Declare all structures at the top of the function to avoid "crosses initialization" errors.
+	struct kvm_vcpu_events events_initial;
+	struct kvm_vcpu_events events_source;
+	struct kvm_vcpu_events events_after_source;
+	struct kvm_vcpu_events events_followup;
+	struct kvm_vcpu_events events_after_followup;
+	struct kvm_vcpu_events expected_state_after_followup;
+
+	// 1. Perform preparation
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// If KVM is not available, we can't test the MR.
+		// This is a setup issue, not a violation.
+		return -1;
+	}
+
+	// Check KVM API version
+	ret = ioctl(kvm_fd, KVM_GET_API_VERSION, NULL);
+	if (ret != KVM_API_VERSION) {
+		// API version mismatch, cannot proceed.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Check for SMM capability, which is required for this MR.
+	ret = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_X86_SMM);
+	if (!ret) {
+		// SMM capability is not available. The MR is not applicable.
+		close(kvm_fd);
+		return 0;
+	}
+
+	// Create a Virtual Machine
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Create a VCPU
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Get the initial SMM state (S_initial)
+	if (ioctl(vcpu_fd, KVM_GET_VCPU_EVENTS, &events_initial) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+	s_initial = events_initial.smi.smm;
+
+	// 2. Get the source output
+	// Prepare the source input: flags does NOT contain KVM_VCPUEVENT_VALID_SMM
+	memset(&events_source, 0, sizeof(events_source));
+	events_source.smi.smm = !s_initial;
+	events_source.flags = 0; // KVM_VCPUEVENT_VALID_SMM is not set
+
+	if (ioctl(vcpu_fd, KVM_SET_VCPU_EVENTS, &events_source) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Get the state after the source call
+	if (ioctl(vcpu_fd, KVM_GET_VCPU_EVENTS, &events_after_source) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output
+	// Prepare the follow-up input: flags DOES contain KVM_VCPUEVENT_VALID_SMM
+	memset(&events_followup, 0, sizeof(events_followup));
+	events_followup.smi.smm = !s_initial;
+	events_followup.flags = KVM_VCPUEVENT_VALID_SMM;
+
+	if (ioctl(vcpu_fd, KVM_SET_VCPU_EVENTS, &events_followup) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Get the state after the follow-up call
+	if (ioctl(vcpu_fd, KVM_GET_VCPU_EVENTS, &events_after_followup) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 4. Check the output relation
+	// Relation for the source output: The SMM state should remain S_initial,
+	// and all other fields should be unchanged.
+	if (memcmp(&events_initial, &events_after_source, sizeof(struct kvm_vcpu_events)) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// Relation for the follow-up output: The SMM state should be updated to !S_initial,
+	// and all other fields should remain unchanged.
+	expected_state_after_followup = events_initial;
+	expected_state_after_followup.smi.smm = !s_initial;
+
+	if (memcmp(&expected_state_after_followup, &events_after_followup, sizeof(struct kvm_vcpu_events)) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// Perform cleanup
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_65f5ce7f.h b/executor/MRs/syz_mr_65f5ce7f.h
new file mode 100644
index 000000000..3078e8bec
--- /dev/null
+++ b/executor/MRs/syz_mr_65f5ce7f.h
@@ -0,0 +1,98 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.14-KVM_SET_SREGS/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Rejection of architecturally inconsistent register states
+// - Source input: A `struct kvm_sregs` representing an architecturally inconsistent state for an x86 vCPU. Specifically, the `cr0` register has the Paging (`PG`) bit set, but the Protection Enable (`PE`) bit is cleared. This is an invalid combination as paging is only permitted in protected mode.
+// - Follow-up input: A `struct kvm_sregs` that is identical to the source input, except the inconsistency is resolved. Specifically, the `PE` bit in the `cr0` register is also set, making the state architecturally valid.
+// - Input relation: The follow-up input is generated by taking the source input's `cr0` value and setting the `PE` bit (bit 0). All other fields in `kvm_sregs` remain identical.
+// - Output relation: The `KVM_SET_SREGS` ioctl call with the source input should fail and return an error code (e.g., -1 with `errno` set to `EINVAL`). In contrast, the `KVM_SET_SREGS` ioctl call with the follow-up input should succeed and return 0.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Define constants for x86 cr0 bits
+#define X86_CR0_PE_65f5ce7f 0x00000001 // Protection Enable
+#define X86_CR0_PG_65f5ce7f 0x80000000 // Paging
+
+/**
+ * Implements the metamorphic relation: Rejection of architecturally inconsistent register states.
+ * This function tests that KVM correctly rejects an attempt to set an inconsistent
+ * CPU state (Paging enabled without Protected Mode) but accepts the corrected,
+
+ * consistent state.
+ *
+ * @param a0 Unused parameter, present to conform to the required function signature.
+ * @return 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_65f5ce7f(volatile long a0)
+{
+	// 1. Perform preparation: Set up KVM VM and vCPU
+	int kvm_fd, vm_fd, vcpu_fd;
+	struct kvm_sregs base_sregs;
+	long source_res, follow_up_res;
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is required for this test. If it's not available, we can't proceed.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)0);
+	if (vcpu_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Get the default sregs to use as a base for our modifications.
+	// This ensures other fields are in a sane, default state.
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &base_sregs) == -1) {
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Construct the source and follow-up inputs based on the input relation
+	// Source input: An inconsistent state where Paging (PG) is on, but Protected Mode (PE) is off.
+	struct kvm_sregs source_sregs = base_sregs;
+	source_sregs.cr0 &= ~X86_CR0_PE_65f5ce7f; // Ensure PE is 0
+	source_sregs.cr0 |= X86_CR0_PG_65f5ce7f; // Ensure PG is 1
+
+	// Follow-up input: A consistent state where both PE and PG are on.
+	struct kvm_sregs follow_up_sregs = source_sregs;
+	follow_up_sregs.cr0 |= X86_CR0_PE_65f5ce7f; // Set PE to 1, resolving the inconsistency
+
+	// 3. Get the corresponding source and follow-up outputs
+	source_res = ioctl(vcpu_fd, KVM_SET_SREGS, &source_sregs);
+	follow_up_res = ioctl(vcpu_fd, KVM_SET_SREGS, &follow_up_sregs);
+
+	// 4. Check if the outputs satisfy the output relation
+	// The source ioctl with the inconsistent state should fail (return -1).
+	// The follow-up ioctl with the consistent state should succeed (return 0).
+	// The placeholder 'output_relation_violated' is replaced with the actual condition.
+	if (source_res != -1 || follow_up_res != 0) {
+		// A semicolon is added to the end of the fprintf statement.
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Perform cleanup
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	// A return statement is added to ensure the function returns a value in all paths.
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_69aac2da.h b/executor/MRs/syz_mr_69aac2da.h
new file mode 100644
index 000000000..053dc473e
--- /dev/null
+++ b/executor/MRs/syz_mr_69aac2da.h
@@ -0,0 +1,203 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.15-KVM_TRANSLATE/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Linearity of address translation within a memory page
+// - Source input: A `struct kvm_translation` with a `linear_address` `L_s`. The test prerequisite is that a call to `KVM_TRANSLATE` with this input results in an output where the `valid` field is 1, indicating the address is successfully translated.
+// - Follow-up input: A `struct kvm_translation` with a `linear_address` `L_f`.
+// - Input relation: The follow-up linear address `L_f` is derived from the source linear address `L_s` by adding a small, non-zero offset `delta`, such that `L_s` and `L_f` fall within the same virtual page. For a standard 4KB page, this means `(L_s / 4096) == ((L_s + delta) / 4096)`. So, `L_f = L_s + delta`.
+// - Output relation: Let `O_s` be the output struct for the source input and `O_f` be the output for the follow-up input. The following relations must hold:
+//   1. `O_f.valid` must be equal to `O_s.valid` (which is 1).
+//   2. The follow-up physical address must be the source physical address plus the same offset: `O_f.physical_address == O_s.physical_address + delta`.
+//   3. Page-level attributes must remain the same: `O_f.writeable == O_s.writeable` and `O_f.usermode == O_s.usermode`.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Define necessary constants for x86 control registers and MSRs if not readily available from system headers.
+#ifndef EFER_LME_69aac2da
+#define EFER_LME_69aac2da (1 << 8)
+#endif
+#ifndef EFER_LMA_69aac2da
+#define EFER_LMA_69aac2da (1 << 10)
+#endif
+#ifndef X86_CR0_PE_69aac2da
+#define X86_CR0_PE_69aac2da 1u
+#endif
+#ifndef X86_CR0_PG_69aac2da
+#define X86_CR0_PG_69aac2da (1u << 31)
+#endif
+#ifndef X86_CR4_PAE_69aac2da
+#define X86_CR4_PAE_69aac2da (1u << 5)
+#endif
+
+/**
+ * @brief Implements the metamorphic relation for KVM_TRANSLATE.
+ *
+ * This function tests the linearity of address translation within a single memory page.
+ * It sets up a minimal KVM environment with a VCPU and paged memory. It then performs
+ * a KVM_TRANSLATE operation on a source linear address (L_s) and a follow-up linear
+ * address (L_f = L_s + delta), where both addresses are within the same virtual page.
+ * It verifies that the resulting physical addresses and page attributes follow the
+ * expected linear relationship.
+ *
+ * @param a0 The source linear address (L_s) cast to a volatile long.
+ * @return 0 on success or if the MR is not applicable. A non-zero value indicates an error or violation.
+ */
+static long syz_mr_69aac2da(volatile long a0)
+{
+	// The source linear address
+	__u64 L_s = (__u64)a0;
+	long ret = 0;
+
+	// File descriptors for KVM
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	// Pointer to guest memory
+	void* mem = MAP_FAILED;
+	const size_t mem_size = 0x200000;
+
+	// The small, non-zero offset for the follow-up input
+	const __u64 delta = 1;
+
+	// === DECLARATION BLOCK TO AVOID GOTO ERRORS ===
+	// All variables that could cause "crosses initialization" errors are declared here.
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs;
+	struct kvm_segment cs;
+	struct kvm_translation tr_s;
+	struct kvm_translation tr_f;
+	__u64 L_f;
+	uint64_t pml4_addr, pdpt_addr, pd_addr, pt_addr;
+	uint64_t *pml4, *pdpt, *pd, *pt;
+	uint64_t page_base_vaddr, pml4_index, pdpt_index, pd_index, pt_index;
+	// === END DECLARATION BLOCK ===
+
+	// Input Relation Prerequisite: L_s and L_s + delta must be in the same page.
+	if ((L_s & 0xfff) == 0xfff) {
+		return 0;
+	}
+
+	// === 1. Preliminary Preparation: Setup a KVM environment ===
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		return 0;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		goto cleanup;
+	}
+
+	mem = mmap(NULL, mem_size, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		goto cleanup;
+	}
+
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = mem_size;
+	region.userspace_addr = (uint64_t)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		goto cleanup;
+	}
+
+	// === 2. Setup Paging for the VCPU ===
+	pml4_addr = 0x1000;
+	pdpt_addr = 0x2000;
+	pd_addr = 0x3000;
+	pt_addr = 0x4000;
+
+	pml4 = (uint64_t*)((char*)mem + pml4_addr);
+	pdpt = (uint64_t*)((char*)mem + pdpt_addr);
+	pd = (uint64_t*)((char*)mem + pd_addr);
+	pt = (uint64_t*)((char*)mem + pt_addr);
+
+	page_base_vaddr = L_s & ~0xfffULL;
+	pml4_index = (page_base_vaddr >> 39) & 0x1ff;
+	pdpt_index = (page_base_vaddr >> 30) & 0x1ff;
+	pd_index = (page_base_vaddr >> 21) & 0x1ff;
+	pt_index = (page_base_vaddr >> 12) & 0x1ff;
+
+	pml4[pml4_index] = pdpt_addr | 3;
+	pdpt[pdpt_index] = pd_addr | 3;
+	pd[pd_index] = pt_addr | 3;
+	pt[pt_index] = 0x100000 | 3;
+
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		goto cleanup;
+	}
+
+	sregs.cr3 = pml4_addr;
+	sregs.cr4 = X86_CR4_PAE_69aac2da;
+	sregs.cr0 = X86_CR0_PE_69aac2da | X86_CR0_PG_69aac2da;
+	sregs.efer = EFER_LME_69aac2da | EFER_LMA_69aac2da;
+
+	cs.base = 0;
+	cs.limit = 0xffffffff;
+	cs.selector = 1 << 3;
+	cs.type = 11;
+	cs.present = 1;
+	cs.dpl = 0;
+	cs.db = 0;
+	cs.s = 1;
+	cs.l = 1;
+	cs.g = 1;
+	sregs.cs = cs;
+
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		goto cleanup;
+	}
+
+	// === 3. Get Source Output ===
+	tr_s.linear_address = L_s;
+	if (ioctl(vcpu_fd, KVM_TRANSLATE, &tr_s) == -1) {
+		goto cleanup;
+	}
+
+	if (tr_s.valid == 0) {
+		goto cleanup;
+	}
+
+	// === 4. Get Follow-up Output ===
+	L_f = L_s + delta;
+	tr_f.linear_address = L_f;
+	if (ioctl(vcpu_fd, KVM_TRANSLATE, &tr_f) == -1) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+		ret = -1;
+		goto cleanup;
+	}
+
+	// === 5. Check Output Relation ===
+	if (tr_f.valid != tr_s.valid ||
+	    tr_f.physical_address != tr_s.physical_address + delta ||
+	    tr_f.writeable != tr_s.writeable ||
+	    tr_f.usermode != tr_s.usermode) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+		ret = -1;
+	}
+
+cleanup:
+	// === 6. Cleanup ===
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (mem != MAP_FAILED)
+		munmap(mem, mem_size);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_6a51ab49.h b/executor/MRs/syz_mr_6a51ab49.h
new file mode 100644
index 000000000..22be5b2ba
--- /dev/null
+++ b/executor/MRs/syz_mr_6a51ab49.h
@@ -0,0 +1,84 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.59-KVM_IOEVENTFD/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Successful assignment and subsequent deassignment of an ioeventfd should both succeed.
+// - Source input: A call to the `KVM_IOEVENTFD` ioctl with a `struct kvm_ioeventfd` parameter configured to assign a new `ioeventfd`. This structure must have valid fields, including a valid event file descriptor (`fd`), a valid guest PIO/MMIO address (`addr`), a valid length (`len`), and a `flags` field that does *not* contain `KVM_IOEVENTFD_FLAG_DEASSIGN`.
+// - Follow-up input: A second call to the `KVM_IOEVENTFD` ioctl intended to deassign the `ioeventfd` created by the source input.
+// - Input relation: The `struct kvm_ioeventfd` for the follow-up input is identical to the source input's structure, with the single modification that the `KVM_IOEVENTFD_FLAG_DEASSIGN` bit is set in the `flags` field.
+// - Output relation: Both the source and follow-up ioctl calls should succeed. If `O_source` is the return value of the source call and `O_follow-up` is the return value of the follow-up call, then the expected relation is `O_source == 0` and `O_follow-up == 0`.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/eventfd.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// MR1: Successful assignment and subsequent deassignment of an ioeventfd should both succeed.
+static long syz_mr_6a51ab49(volatile long addr, volatile long len, volatile long flags)
+{
+	int kvm_fd, vm_fd, efd;
+	long o_source, o_followup;
+
+	// 1. Perform preparation
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot perform test if KVM is not available
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	efd = eventfd(0, EFD_CLOEXEC);
+	if (efd < 0) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// The MR specifies a valid length. Common valid lengths are 1, 2, 4, 8.
+	// If the provided length is not one of these, we cannot perform a valid test.
+	if (len != 1 && len != 2 && len != 4 && len != 8) {
+		close(efd);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Prepare and execute the source call (assign ioeventfd)
+	struct kvm_ioeventfd source_ioefd;
+	memset(&source_ioefd, 0, sizeof(source_ioefd));
+	source_ioefd.addr = (__u64)addr;
+	source_ioefd.len = (__u32)len;
+	source_ioefd.fd = efd;
+	// Per MR description, ensure the DEASSIGN flag is NOT set for the source call
+	source_ioefd.flags = (__u32)flags & ~KVM_IOEVENTFD_FLAG_DEASSIGN;
+
+	o_source = ioctl(vm_fd, KVM_IOEVENTFD, &source_ioefd);
+
+	// 3. Prepare and execute the follow-up call (deassign ioeventfd)
+	// Input relation: identical structure, but with DEASSIGN flag set.
+	struct kvm_ioeventfd followup_ioefd = source_ioefd;
+	followup_ioefd.flags |= KVM_IOEVENTFD_FLAG_DEASSIGN;
+
+	o_followup = ioctl(vm_fd, KVM_IOEVENTFD, &followup_ioefd);
+
+	// 4. Check the output relation
+	// Both calls should succeed (return 0).
+	if (o_source != 0 || o_followup != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Cleanup
+	close(efd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_6b728ac9.h b/executor/MRs/syz_mr_6b728ac9.h
new file mode 100644
index 000000000..593d0f8d7
--- /dev/null
+++ b/executor/MRs/syz_mr_6b728ac9.h
@@ -0,0 +1,88 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.71-KVM_CREATE_PIT2/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Re-creation of the PIT device on the same VM should fail.
+// - Source input: A valid `KVM_CREATE_PIT2` ioctl call on a VM that has an in-kernel IRQCHIP but does not yet have a PIT device. The `kvm_pit_config` structure passed as a parameter must be valid (e.g., `flags` set to 0 or `KVM_PIT_SPEAKER_DUMMY`).
+// - Follow-up input: A second `KVM_CREATE_PIT2` ioctl call on the same VM, using any valid `kvm_pit_config` structure.
+// - Input relation: The follow-up input is generated by repeating the `KVM_CREATE_PIT2` ioctl call on the VM after the source call has successfully created the PIT device.
+// - Output relation: The source `KVM_CREATE_PIT2` call should succeed and return 0. The follow-up `KVM_CREATE_PIT2` call should fail and return -1 (likely with `errno` set to `EEXIST`), as a VM can only have one PIT device.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Implements the metamorphic relation: Re-creation of the PIT device on the same VM should fail.
+ *
+ * @param a0 A volatile long representing the flags for the `kvm_pit_config` structure.
+ *           According to the MR description, this should be a valid value like 0 or KVM_PIT_SPEAKER_DUMMY.
+ * @return 0 on completion, -1 on setup failure.
+ */
+static long syz_mr_6b728ac9(volatile long a0)
+{
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	long source_ret = -1;
+	long followup_ret = -1;
+
+	// 1. Perform preparation: Create a KVM VM with an IRQCHIP.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is not available or accessible, cannot perform the test.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		// Failed to create a VM.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Creating an IRQCHIP is a prerequisite for creating a PIT device.
+	if (ioctl(vm_fd, KVM_CREATE_IRQCHIP, 0) == -1) {
+		// Failed to create the in-kernel IRQCHIP.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get the source output.
+	// The source input is the first KVM_CREATE_PIT2 call.
+	struct kvm_pit_config pit_config = {0};
+	pit_config.flags = a0;
+
+	source_ret = ioctl(vm_fd, KVM_CREATE_PIT2, &pit_config);
+
+	// The MR premise is that the source call is valid and successful.
+	// If it fails, we cannot test the relation.
+	if (source_ret != 0) {
+		// Clean up and exit as the premise is not met.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 3. Get the follow-up output.
+	// The follow-up input is a second KVM_CREATE_PIT2 call on the same VM.
+	// We can reuse the same pit_config structure.
+	followup_ret = ioctl(vm_fd, KVM_CREATE_PIT2, &pit_config);
+
+	// 4. Check the output relation.
+	// The source call should succeed (source_ret == 0), which we've already established.
+	// The follow-up call should fail (followup_ret == -1).
+	// If the follow-up call does not fail, the relation is violated.
+	if (followup_ret != -1) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Perform cleanup.
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_6d17dd5e.h b/executor/MRs/syz_mr_6d17dd5e.h
new file mode 100644
index 000000000..94e70662d
--- /dev/null
+++ b/executor/MRs/syz_mr_6d17dd5e.h
@@ -0,0 +1,132 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.85-KVM_ARM_SET_DEVICE_ADDR-deprecated/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Repeatedly setting the same device address should fail with EEXIST.
+// - Source input: A sequence of operations on a KVM virtual machine that successfully sets a device address. This sequence consists of:
+//   1. Creating a VM.
+//   2. Creating an IRQ chip via `KVM_CREATE_IRQCHIP`.
+//   3. A single call to the `KVM_ARM_SET_DEVICE_ADDR` ioctl with a valid `struct kvm_arm_device_address` parameter `P`. The parameter `P` must specify a valid device `id` and an `addr` that is within the guest's physical address space and does not conflict with any existing device mappings.
+// - Follow-up input: The same sequence of operations as the source input, followed by a second, identical call to the `KVM_ARM_SET_DEVICE_ADDR` ioctl using the exact same parameter `P`.
+// - Input relation: The follow-up input is generated by appending a duplicate of the `KVM_ARM_SET_DEVICE_ADDR` call from the source input.
+// - Output relation: Let the output of the `KVM_ARM_SET_DEVICE_ADDR` call in the source input be `O_s` (return value and `errno`), and the output of the *second* `KVM_ARM_SET_DEVICE_ADDR` call in the follow-up input be `O_f`. The relationship between the outputs should be:
+//   1. The return value of `O_s` must be 0, indicating the initial call was successful.
+//   2. The return value of `O_f` must be -1, and its `errno` must be `EEXIST`, indicating that the address has already been set.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// MR1: Repeatedly setting the same device address should fail with EEXIST.
+// The parameters p_id and p_addr correspond to the 'id' and 'addr' fields
+// of the 'struct kvm_arm_device_address'.
+static long syz_mr_6d17dd5e(volatile long p_id, volatile long p_addr)
+{
+	// --- Source Execution ---
+	// This block executes the source input sequence: create a VM and set a device address once.
+	int kvm_fd_s = -1;
+	int vm_fd_s = -1;
+	long res_s = -1;
+
+	// 1. Open the KVM device.
+	kvm_fd_s = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd_s == -1) {
+		// If KVM is not available, we cannot perform the test.
+		return 0;
+	}
+
+	// 2. Create a Virtual Machine.
+	vm_fd_s = ioctl(kvm_fd_s, KVM_CREATE_VM, 0);
+	if (vm_fd_s == -1) {
+		close(kvm_fd_s);
+		return 0;
+	}
+
+	// 3. Create an IRQ chip, which is a prerequisite for setting device addresses.
+	if (ioctl(vm_fd_s, KVM_CREATE_IRQCHIP, 0) == -1) {
+		close(vm_fd_s);
+		close(kvm_fd_s);
+		return 0;
+	}
+
+	// 4. Prepare the parameter for KVM_ARM_SET_DEVICE_ADDR.
+	struct kvm_arm_device_addr dev_addr_s;
+	dev_addr_s.id = (__u64)p_id;
+	dev_addr_s.addr = (__u64)p_addr;
+
+	// 5. Execute the source ioctl call.
+	res_s = ioctl(vm_fd_s, KVM_ARM_SET_DEVICE_ADDR, &dev_addr_s);
+
+	// 6. Clean up resources for the source execution.
+	close(vm_fd_s);
+	close(kvm_fd_s);
+
+	// According to the MR description, the source call must be successful.
+	// If it fails, the premise of the MR is not met, so we stop.
+	if (res_s != 0) {
+		return 0;
+	}
+
+	// --- Follow-up Execution ---
+	// This block executes the follow-up input sequence: create a VM, set the device address,
+	// and then immediately try to set the same address again.
+	int kvm_fd_f = -1;
+	int vm_fd_f = -1;
+	long res_f = -1;
+	int errno_f = 0;
+
+	// 1. Open the KVM device for the follow-up test.
+	kvm_fd_f = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd_f == -1) {
+		return 0;
+	}
+
+	// 2. Create a new VM.
+	vm_fd_f = ioctl(kvm_fd_f, KVM_CREATE_VM, 0);
+	if (vm_fd_f == -1) {
+		close(kvm_fd_f);
+		return 0;
+	}
+
+	// 3. Create an IRQ chip for the new VM.
+	if (ioctl(vm_fd_f, KVM_CREATE_IRQCHIP, 0) == -1) {
+		close(vm_fd_f);
+		close(kvm_fd_f);
+		return 0;
+	}
+
+	// 4. Prepare the parameter, which is identical to the source input.
+	struct kvm_arm_device_addr dev_addr_f;
+	dev_addr_f.id = (__u64)p_id;
+	dev_addr_f.addr = (__u64)p_addr;
+
+	// 5. First call to set the address, establishing the precondition.
+	if (ioctl(vm_fd_f, KVM_ARM_SET_DEVICE_ADDR, &dev_addr_f) != 0) {
+		// This should succeed if the source call succeeded. If not, we can't test the relation.
+		close(vm_fd_f);
+		close(kvm_fd_f);
+		return 0;
+	}
+
+	// 6. Second, identical call. This is the call whose output we want to check.
+	errno = 0; // Clear errno before the call.
+	res_f = ioctl(vm_fd_f, KVM_ARM_SET_DEVICE_ADDR, &dev_addr_f);
+	errno_f = errno; // Save errno immediately after the call.
+
+	// 7. Clean up resources for the follow-up execution.
+	close(vm_fd_f);
+	close(kvm_fd_f);
+
+	// --- Output Relation Check ---
+	// The following block fixes the reported compilation errors.
+	// 1. The placeholder 'output_relation_violated' is replaced with the actual condition.
+	// 2. A semicolon is added to the fprintf statement.
+	if (res_f != -1 || errno_f != EEXIST) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 3. A return statement is added to satisfy the function's return type.
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_70b74216.h b/executor/MRs/syz_mr_70b74216.h
new file mode 100644
index 000000000..c6a2872fc
--- /dev/null
+++ b/executor/MRs/syz_mr_70b74216.h
@@ -0,0 +1,287 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_mad/P_Key-Index-Handling/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Validation of RX queue count for child PKEY interfaces
+// - Source input: A request to create a child IPoIB PKEY interface where the number of specified receive (RX) queues is greater than or equal to the number of RX queues of its parent interface.
+// - Follow-up input: A request to create a child IPoIB PKEY interface where the number of specified RX queues is less than the number of RX queues of the same parent interface.
+// - Input relation: The follow-up input is derived from the source input by decreasing the number of requested RX queues to be less than the parent interface's RX queue count, while keeping the parent interface and other parameters the same.
+// - Output relation: The source operation should succeed, resulting in the successful creation of the child PKEY interface. The follow-up operation should fail and return an error to the user.
+
+#include <dirent.h>
+#include <errno.h>
+#include <linux/rtnetlink.h>
+#include <net/if.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/socket.h>
+#include <sys/types.h>
+#include <unistd.h>
+
+// Helper to add a netlink attribute to a message buffer.
+static int add_rtattr_70b74216(struct nlmsghdr* n, int maxlen, int type, const void* data, int alen)
+{
+	int len = RTA_LENGTH(alen);
+	struct rtattr* rta;
+
+	if (NLMSG_ALIGN(n->nlmsg_len) + RTA_ALIGN(len) > (unsigned int)maxlen) {
+		fprintf(stderr, "add_rtattr: message exceeded maxlen\n");
+		return -1;
+	}
+
+	rta = (struct rtattr*)(((char*)n) + NLMSG_ALIGN(n->nlmsg_len));
+	rta->rta_type = type;
+	rta->rta_len = len;
+	if (alen) {
+		memcpy(RTA_DATA(rta), data, alen);
+	}
+	n->nlmsg_len = NLMSG_ALIGN(n->nlmsg_len) + RTA_ALIGN(len);
+	return 0;
+}
+
+// Helper to begin a nested attribute section.
+static struct rtattr* add_rtattr_nested_70b74216(struct nlmsghdr* n, int maxlen, int type)
+{
+	int len = RTA_LENGTH(0);
+	struct rtattr* rta;
+
+	if (NLMSG_ALIGN(n->nlmsg_len) + RTA_ALIGN(len) > (unsigned int)maxlen) {
+		fprintf(stderr, "add_rtattr_nested: message exceeded maxlen\n");
+		return NULL;
+	}
+
+	rta = (struct rtattr*)(((char*)n) + NLMSG_ALIGN(n->nlmsg_len));
+	rta->rta_type = type;
+	n->nlmsg_len = NLMSG_ALIGN(n->nlmsg_len) + RTA_ALIGN(len);
+	return rta;
+}
+
+// Helper to finalize a nested attribute section by setting its length.
+static void end_rtattr_nested_70b74216(struct nlmsghdr* n, struct rtattr* nested)
+{
+	nested->rta_len = (char*)n + NLMSG_ALIGN(n->nlmsg_len) - (char*)nested;
+}
+
+// Sends a netlink request and waits for the ACK/NACK response.
+// Returns 0 on success, or a negative errno value on failure.
+static int do_netlink_req_70b74216(const void* req, size_t req_size)
+{
+	struct sockaddr_nl sa;
+	int sock;
+	struct iovec iov = {(void*)req, req_size};
+	struct msghdr msg = {&sa, sizeof(sa), &iov, 1, NULL, 0, 0};
+	char recv_buf[4096];
+	int ret = 0;
+	ssize_t len;
+	struct nlmsghdr* nlh;
+
+	sock = socket(AF_NETLINK, SOCK_RAW, NETLINK_ROUTE);
+	if (sock < 0) {
+		return -errno;
+	}
+
+	memset(&sa, 0, sizeof(sa));
+	sa.nl_family = AF_NETLINK;
+
+	if (sendmsg(sock, &msg, 0) < 0) {
+		ret = -errno;
+		goto out;
+	}
+
+	len = recv(sock, recv_buf, sizeof(recv_buf), 0);
+	if (len < 0) {
+		ret = -errno;
+		goto out;
+	}
+
+	nlh = (struct nlmsghdr*)recv_buf;
+	if (nlh->nlmsg_type == NLMSG_ERROR) {
+		struct nlmsgerr* err = (struct nlmsgerr*)NLMSG_DATA(nlh);
+		ret = err->error; // This will be 0 on success.
+	}
+
+out:
+	close(sock);
+	return ret;
+}
+
+// Retrieves the number of RX queues for a given network interface.
+static int get_if_rx_queues_70b74216(const char* ifname)
+{
+	int sock;
+	struct {
+		struct nlmsghdr nlh;
+		struct ifinfomsg ifm;
+	} req;
+	char recv_buf[4096];
+	int queues = -1;
+
+	sock = socket(AF_NETLINK, SOCK_RAW, NETLINK_ROUTE);
+	if (sock < 0) {
+		return -1;
+	}
+
+	memset(&req, 0, sizeof(req));
+	req.nlh.nlmsg_len = NLMSG_LENGTH(sizeof(struct ifinfomsg));
+	req.nlh.nlmsg_type = RTM_GETLINK;
+	req.nlh.nlmsg_flags = NLM_F_REQUEST;
+	req.ifm.ifi_family = AF_UNSPEC;
+	req.ifm.ifi_index = if_nametoindex(ifname);
+	if (req.ifm.ifi_index == 0) {
+		close(sock);
+		return -1;
+	}
+
+	if (send(sock, &req, req.nlh.nlmsg_len, 0) < 0) {
+		close(sock);
+		return -1;
+	}
+
+	ssize_t len = recv(sock, recv_buf, sizeof(recv_buf), 0);
+	if (len < 0) {
+		close(sock);
+		return -1;
+	}
+
+	struct nlmsghdr* nlh = (struct nlmsghdr*)recv_buf;
+	for (; NLMSG_OK(nlh, len); nlh = NLMSG_NEXT(nlh, len)) {
+		if (nlh->nlmsg_type == RTM_NEWLINK) {
+			struct ifinfomsg* ifm = (struct ifinfomsg*)NLMSG_DATA(nlh);
+			struct rtattr* rta = IFLA_RTA(ifm);
+			int rta_len = nlh->nlmsg_len - NLMSG_LENGTH(sizeof(*ifm));
+			for (; RTA_OK(rta, rta_len); rta = RTA_NEXT(rta, rta_len)) {
+				if (rta->rta_type == IFLA_NUM_RX_QUEUES) {
+					queues = *(int*)RTA_DATA(rta);
+					goto found;
+				}
+			}
+		}
+	}
+
+found:
+	close(sock);
+	return queues;
+}
+
+// Finds the first available IPoIB parent interface (e.g., "ib0").
+static int find_ipoib_parent_70b74216(char* ifname, size_t len)
+{
+	DIR* d = opendir("/sys/class/net");
+	if (!d) {
+		return -1;
+	}
+
+	struct dirent* dir;
+	while ((dir = readdir(d)) != NULL) {
+		if (dir->d_name[0] == '.')
+			continue;
+
+		char path[512];
+		snprintf(path, sizeof(path), "/sys/class/net/%s/device/infiniband", dir->d_name);
+		if (access(path, F_OK) == 0) {
+			if (strchr(dir->d_name, '.') == NULL) {
+				// FIX: Use a precision specifier "%.*s" to tell snprintf the maximum
+				// number of bytes to copy from the source string, preventing the
+				// -Wformat-truncation warning. We use (len - 1) to leave room
+				// for the null terminator.
+				snprintf(ifname, len, "%.*s", (int)(len - 1), dir->d_name);
+				closedir(d);
+				return 0;
+			}
+		}
+	}
+	closedir(d);
+	return -1;
+}
+
+// Generic function to create or delete an IPoIB PKEY interface via rtnetlink.
+static int manage_ipoib_pkey_if_70b74216(int is_add, const char* parent_name, const char* child_name,
+					 unsigned short pkey, unsigned int rx_queues)
+{
+	struct {
+		struct nlmsghdr nlh;
+		struct ifinfomsg ifm;
+		char attrbuf[1024];
+	} req;
+
+	memset(&req, 0, sizeof(req));
+	req.nlh.nlmsg_len = NLMSG_LENGTH(sizeof(struct ifinfomsg));
+	req.nlh.nlmsg_flags = NLM_F_REQUEST;
+	req.ifm.ifi_family = AF_UNSPEC;
+
+	if (is_add) {
+		unsigned int parent_idx = if_nametoindex(parent_name);
+		if (parent_idx == 0) {
+			return -ENODEV;
+		}
+		req.nlh.nlmsg_type = RTM_NEWLINK;
+		req.nlh.nlmsg_flags |= NLM_F_CREATE | NLM_F_EXCL;
+
+		add_rtattr_70b74216(&req.nlh, sizeof(req), IFLA_LINK, &parent_idx, sizeof(parent_idx));
+		add_rtattr_70b74216(&req.nlh, sizeof(req), IFLA_IFNAME, child_name, strlen(child_name) + 1);
+
+		struct rtattr* linkinfo = add_rtattr_nested_70b74216(&req.nlh, sizeof(req), IFLA_LINKINFO);
+		add_rtattr_70b74216(&req.nlh, sizeof(req), IFLA_INFO_KIND, "ipoib", strlen("ipoib") + 1);
+		struct rtattr* data = add_rtattr_nested_70b74216(&req.nlh, sizeof(req), IFLA_INFO_DATA);
+		add_rtattr_70b74216(&req.nlh, sizeof(req), IFLA_IPOIB_PKEY, &pkey, sizeof(pkey));
+		end_rtattr_nested_70b74216(&req.nlh, data);
+		end_rtattr_nested_70b74216(&req.nlh, linkinfo);
+
+		add_rtattr_70b74216(&req.nlh, sizeof(req), IFLA_NUM_RX_QUEUES, &rx_queues, sizeof(rx_queues));
+	} else { // Delete
+		unsigned int child_idx = if_nametoindex(child_name);
+		if (child_idx == 0) {
+			return 0;
+		}
+		req.nlh.nlmsg_type = RTM_DELLINK;
+		req.ifm.ifi_index = child_idx;
+	}
+
+	return do_netlink_req_70b74216(&req, req.nlh.nlmsg_len);
+}
+
+static long syz_mr_70b74216(volatile long a0)
+{
+	if (geteuid() != 0) {
+		return -1;
+	}
+
+	char parent_ifname[IFNAMSIZ];
+	if (find_ipoib_parent_70b74216(parent_ifname, IFNAMSIZ) != 0) {
+		return -1;
+	}
+
+	int parent_rx_queues = get_if_rx_queues_70b74216(parent_ifname);
+	if (parent_rx_queues < 0) {
+		return -1;
+	}
+
+	if (parent_rx_queues <= 1) {
+		return -1;
+	}
+
+	unsigned short pkey = 0x8000 | (a0 & 0x7FFF);
+	if (pkey == 0xFFFF)
+		pkey = 0x8001;
+
+	char child_src_name[IFNAMSIZ];
+	char child_fup_name[IFNAMSIZ];
+
+	snprintf(child_src_name, IFNAMSIZ, "%.7s.%04x.s", parent_ifname, pkey);
+	snprintf(child_fup_name, IFNAMSIZ, "%.7s.%04x.f", parent_ifname, pkey);
+
+	unsigned int src_rx_queues = parent_rx_queues;
+	unsigned int fup_rx_queues = parent_rx_queues - 1;
+
+	long source_res = manage_ipoib_pkey_if_70b74216(1, parent_ifname, child_src_name, pkey, src_rx_queues);
+	long followup_res = manage_ipoib_pkey_if_70b74216(1, parent_ifname, child_fup_name, pkey, fup_rx_queues);
+
+	manage_ipoib_pkey_if_70b74216(0, parent_ifname, child_src_name, 0, 0);
+	manage_ipoib_pkey_if_70b74216(0, parent_ifname, child_fup_name, 0, 0);
+
+	if (source_res != 0 || followup_res == 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_70e04a46.h b/executor/MRs/syz_mr_70e04a46.h
new file mode 100644
index 000000000..85e8635f6
--- /dev/null
+++ b/executor/MRs/syz_mr_70e04a46.h
@@ -0,0 +1,195 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/ppp/ppp_generic/PPP-channel-API/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 5 iterations
+// MR1: The PPP generic layer never hands a channel a frame whose payload length exceeds the channel’s MTU.
+// - Source input:
+//   • A PPP channel whose `mtu` field is N (N > 0).
+//   • A request to transmit a PPP frame whose payload length is exactly N.
+// - Follow-up input:
+//   • The same channel.
+//   • A request to transmit a PPP frame whose payload length is N + 1, obtained by appending one byte to the source frame’s payload.
+// - Input relation:
+//   The follow-up request is produced from the source request by adding one byte to the payload.
+// - Output relation:
+//   Observe every frame that the generic layer delivers to the channel’s `start_xmit()` callback.
+//
+//   • For the source request: any delivered frame’s payload length must be ≤ N.
+//
+//   • For the follow-up request: every frame delivered to `start_xmit()` must also have payload length ≤ N. This holds whether the generic layer rejects the request outright (delivering no frame) or accepts it and transmits it after internal fragmentation. Under no circumstance may a frame with payload length > N be delivered to the channel.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/if_ppp.h>
+#include <net/if.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/select.h>
+#include <sys/socket.h>
+#include <sys/types.h>
+#include <unistd.h>
+
+static long syz_mr_70e04a46(volatile long a0)
+{
+	long N = a0;
+	long ret = 0;
+
+	/* Declare all variables at the top to avoid 'goto crosses initialization' errors */
+	int ppp_dev_fd = -1, ppp_unit_fd = -1, master_fd = -1, slave_fd = -1, sock_fd = -1;
+	char* src_buf = NULL;
+	char* followup_buf = NULL;
+	char* slave_name = NULL;
+	int ppp_unit_index = -1;
+	struct ifreq ifr;
+	int mru;
+	int flags;
+	ssize_t followup_written;
+
+	// Sanity check for a reasonable MTU value
+	if (N <= 0 || N > 1500) {
+		return -1;
+	}
+
+	// Open the main PPP device controller
+	ppp_dev_fd = open("/dev/ppp", O_RDWR);
+	if (ppp_dev_fd < 0) {
+		return -1;
+	}
+
+	// Create a pseudo-terminal (PTY) to act as the PPP channel
+	master_fd = posix_openpt(O_RDWR | O_NOCTTY);
+	if (master_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+	if (grantpt(master_fd) != 0 || unlockpt(master_fd) != 0) {
+		ret = -1;
+		goto cleanup;
+	}
+	slave_name = ptsname(master_fd);
+	if (slave_name == NULL) {
+		ret = -1;
+		goto cleanup;
+	}
+	slave_fd = open(slave_name, O_RDWR | O_NOCTTY);
+	if (slave_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Create a new PPP unit. The returned fd is the handle for subsequent operations.
+	ppp_unit_fd = ioctl(ppp_dev_fd, PPPIOCNEWUNIT, &ppp_unit_index);
+	if (ppp_unit_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Attach the PTY slave as the channel for the new PPP unit
+	if (ioctl(ppp_unit_fd, PPPIOCATTCHAN, &slave_fd) < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Create a socket for network interface ioctls
+	sock_fd = socket(AF_INET, SOCK_DGRAM, 0);
+	if (sock_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Prepare an ifreq struct for setting MTU and flags
+	memset(&ifr, 0, sizeof(ifr));
+	snprintf(ifr.ifr_name, IFNAMSIZ, "ppp%d", ppp_unit_index);
+
+	// Set the MTU for the ppp interface to N
+	ifr.ifr_mtu = N;
+	if (ioctl(sock_fd, SIOCSIFMTU, &ifr) < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Also set the MRU (Max Receive Unit) for the PPP unit.
+	mru = N;
+	if (ioctl(ppp_unit_fd, PPPIOCSMRU, &mru) < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Bring the interface up to allow data transmission
+	if (ioctl(sock_fd, SIOCGIFFLAGS, &ifr) < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+	ifr.ifr_flags |= IFF_UP;
+	if (ioctl(sock_fd, SIOCSIFFLAGS, &ifr) < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Source Input: Send a frame with payload size N.
+	src_buf = (char*)malloc(N);
+	if (!src_buf) {
+		ret = -1;
+		goto cleanup;
+	}
+	memset(src_buf, 'A', N);
+	if (write(ppp_unit_fd, src_buf, N) != N) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// Drain any resulting data from the PTY to prepare for the follow-up test.
+	flags = fcntl(master_fd, F_GETFL, 0);
+	fcntl(master_fd, F_SETFL, flags | O_NONBLOCK);
+	char drain_buf[4096];
+	while (read(master_fd, drain_buf, sizeof(drain_buf)) > 0) {
+	}
+
+	// Follow-up Input: Attempt to send a frame with payload size N+1.
+	followup_buf = (char*)malloc(N + 1);
+	if (!followup_buf) {
+		ret = -1;
+		goto cleanup;
+	}
+	memset(followup_buf, 'B', N + 1);
+	followup_written = write(ppp_unit_fd, followup_buf, N + 1);
+
+	// Output Relation Check
+	if (followup_written == -1 && errno == EMSGSIZE) {
+		// This is the expected correct behavior: the kernel rejected the oversized frame.
+	} else if (followup_written > 0) {
+		// The kernel accepted the write. Check what it sent to the channel (the PTY).
+		fd_set read_fds;
+		FD_ZERO(&read_fds);
+		FD_SET(master_fd, &read_fds);
+		struct timeval timeout = {.tv_sec = 0, .tv_usec = 50000}; // 50ms timeout
+		int rv = select(master_fd + 1, &read_fds, NULL, NULL, &timeout);
+
+		if (rv > 0 && FD_ISSET(master_fd, &read_fds)) {
+			char read_buf[N + 128]; // Buffer large enough for oversized frame
+			ssize_t nread = read(master_fd, read_buf, sizeof(read_buf));
+			if (nread > N) {
+				fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+			}
+		}
+	}
+
+cleanup:
+	if (src_buf)
+		free(src_buf);
+	if (followup_buf)
+		free(followup_buf);
+	if (sock_fd >= 0)
+		close(sock_fd);
+	if (ppp_unit_fd >= 0)
+		close(ppp_unit_fd);
+	if (master_fd >= 0)
+		close(master_fd);
+	if (slave_fd >= 0)
+		close(slave_fd);
+	if (ppp_dev_fd >= 0)
+		close(ppp_dev_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_71c06928.h b/executor/MRs/syz_mr_71c06928.h
new file mode 100644
index 000000000..c0e4f5268
--- /dev/null
+++ b/executor/MRs/syz_mr_71c06928.h
@@ -0,0 +1,175 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/ppp/ppp_generic/SMP-safety/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Rejection of malformed LCP packets
+// - Source input: A valid PPP packet to be transmitted, containing a well-formed LCP (Link Control Protocol) frame. The packet must have the PPP protocol field set to `0xc021` (LCP) and a payload that includes a complete LCP header (Code, ID, Length) and data, where the packet length is consistent with the LCP Length field.
+// - Follow-up input: A malformed PPP packet that claims to be LCP. The packet has the PPP protocol field set to `0xc021`, but its payload is shorter than the minimum valid LCP header length (4 bytes).
+// - Input relation: The follow-up input is constructed by using the same LCP protocol identifier as the source input but with a truncated payload that is too short to contain a valid LCP header.
+// - Output relation: The source input should be successfully processed and encoded by the PPP stack (e.g., by `ppp_async_encode()`) for transmission. The follow-up input must be identified as malformed and be rejected or dropped by the stack. The system must not crash, hang, or perform an illegal memory access. Correspondingly, a packet error counter for the PPP interface should be incremented for the follow-up case, while a success counter should be incremented for the source case.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <net/if.h>
+#include <pty.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/socket.h>
+#include <unistd.h>
+
+// Headers for PPP definitions. These might require kernel headers to be available.
+#include <linux/if_ppp.h>
+#include <linux/ppp_defs.h>
+
+/**
+ * @brief Helper function to get PPP statistics for a given interface.
+ *
+ * @param sock_fd An open socket descriptor.
+ * @param ifname The name of the PPP interface (e.g., "ppp0").
+ * @param stats A pointer to a struct ppp_stats to be filled.
+ * @return 0 on success, -1 on failure.
+ */
+static int get_ppp_stats_71c06928(int sock_fd, const char* ifname, struct ppp_stats* stats)
+{
+	struct ifreq ifr;
+	memset(&ifr, 0, sizeof(ifr));
+	// Use snprintf instead of strncpy to avoid truncation warnings/errors.
+	snprintf(ifr.ifr_name, IFNAMSIZ, "%s", ifname);
+	ifr.ifr_data = (caddr_t)stats;
+	if (ioctl(sock_fd, SIOCGPPPSTATS, &ifr) < 0) {
+		return -1;
+	}
+	return 0;
+}
+
+/**
+ * @brief MR1: Rejection of malformed LCP packets.
+ * This MR tests that the kernel's PPP stack correctly processes a valid LCP packet
+ * and rejects a malformed (truncated) LCP packet, reflecting this in the interface statistics.
+ *
+ * @param a0 A volatile long parameter, as required by the framework (unused).
+ * @return 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_71c06928(volatile long a0)
+{
+	(void)a0; // Mark the parameter as unused to prevent compiler warnings.
+
+	int ppp_dev_fd = -1;
+	int ppp_unit_fd = -1;
+	int sock_fd = -1;
+	int master_pty = -1;
+	int slave_pty = -1;
+	int unit_num_idx = -1;
+	char ifname[IFNAMSIZ];
+	struct ppp_stats initial_stats, source_stats, followup_stats;
+	int source_ok = 0;
+	int followup_ok = 0;
+
+	// Define source and follow-up inputs at the top of the scope to avoid "crosses initialization" error.
+	unsigned char source_pkt[] = {0xff, 0x03, 0xc0, 0x21, 0x01, 0x01, 0x00, 0x06, 0x01, 0x02};
+	unsigned char followup_pkt[] = {0xff, 0x03, 0xc0, 0x21, 0xde, 0xad, 0xbe};
+
+	// 1. Perform preparation: Set up a PPP interface.
+
+	// Open the PPP master device. This requires the ppp_generic module to be loaded.
+	ppp_dev_fd = open("/dev/ppp", O_RDWR);
+	if (ppp_dev_fd < 0) {
+		return -1;
+	}
+
+	// Create a new PPP unit.
+	if (ioctl(ppp_dev_fd, PPPIOCNEWUNIT, &unit_num_idx) < 0) {
+		goto cleanup;
+	}
+	ppp_unit_fd = unit_num_idx;
+	snprintf(ifname, IFNAMSIZ, "ppp%d", unit_num_idx);
+
+	// Create a socket for network interface ioctls.
+	sock_fd = socket(AF_INET, SOCK_DGRAM, 0);
+	if (sock_fd < 0) {
+		goto cleanup;
+	}
+
+	// Create a pseudo-terminal pair to act as the channel for the PPP unit.
+	if (openpty(&master_pty, &slave_pty, NULL, NULL, NULL) < 0) {
+		goto cleanup;
+	}
+
+	// Attach the PPP unit to the slave side of the PTY.
+	if (ioctl(ppp_unit_fd, PPPIOCATTCHAN, &slave_pty) < 0) {
+		goto cleanup;
+	}
+
+	// Bring the PPP interface up.
+	struct ifreq ifr;
+	memset(&ifr, 0, sizeof(ifr));
+	// Use snprintf instead of strncpy to avoid truncation warnings/errors.
+	snprintf(ifr.ifr_name, IFNAMSIZ, "%s", ifname);
+	if (ioctl(sock_fd, SIOCGIFFLAGS, &ifr) < 0) {
+		goto cleanup;
+	}
+	ifr.ifr_flags |= IFF_UP;
+	if (ioctl(sock_fd, SIOCSIFFLAGS, &ifr) < 0) {
+		goto cleanup;
+	}
+
+	// 3. Get initial stats, then get outputs for source and follow-up inputs.
+
+	// Get initial statistics before sending any packets.
+	if (get_ppp_stats_71c06928(sock_fd, ifname, &initial_stats) != 0) {
+		goto cleanup;
+	}
+
+	// Write the source packet (valid LCP) to the master PTY.
+	if (write(master_pty, source_pkt, sizeof(source_pkt)) < 0) {
+		goto cleanup;
+	}
+	usleep(10000); // A small delay for the kernel to process the packet.
+	if (get_ppp_stats_71c06928(sock_fd, ifname, &source_stats) != 0) {
+		goto cleanup;
+	}
+
+	// Write the follow-up packet (malformed LCP) to the master PTY.
+	if (write(master_pty, followup_pkt, sizeof(followup_pkt)) < 0) {
+		goto cleanup;
+	}
+	usleep(10000);
+	if (get_ppp_stats_71c06928(sock_fd, ifname, &followup_stats) != 0) {
+		goto cleanup;
+	}
+
+	// 4. Check if the outputs satisfy the metamorphic relation.
+
+	// Relation for the source case: Input packet counter should increment,
+	// and input error counter should remain unchanged.
+	source_ok = (source_stats.p.ppp_ipackets > initial_stats.p.ppp_ipackets) &&
+		    (source_stats.p.ppp_ierrors == initial_stats.p.ppp_ierrors);
+
+	// Relation for the follow-up case: Input packet counter should not change
+	// relative to the source case, and input error counter should increment.
+	followup_ok = (followup_stats.p.ppp_ipackets == source_stats.p.ppp_ipackets) &&
+		      (followup_stats.p.ppp_ierrors > source_stats.p.ppp_ierrors);
+
+	if (!source_ok || !followup_ok) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// Perform cleanup by closing all opened file descriptors.
+	if (master_pty != -1)
+		close(master_pty);
+	if (slave_pty != -1)
+		close(slave_pty);
+	if (sock_fd != -1)
+		close(sock_fd);
+	if (ppp_unit_fd != -1) {
+		int zero = 0;
+		ioctl(ppp_unit_fd, PPPIOCDETACH, &zero);
+		close(ppp_unit_fd);
+	}
+	if (ppp_dev_fd != -1)
+		close(ppp_dev_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_7206cd80.h b/executor/MRs/syz_mr_7206cd80.h
new file mode 100644
index 000000000..f3502aae8
--- /dev/null
+++ b/executor/MRs/syz_mr_7206cd80.h
@@ -0,0 +1,224 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.60-KVM_DIRTY_TLB/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Reporting a set of dirty TLB entries in one call is functionally equivalent to reporting disjoint subsets of those entries in several consecutive calls.
+// - Source input: A single call to the `KVM_DIRTY_TLB` ioctl with a `struct kvm_dirty_tlb` parameter where the bitmap `B` can be partitioned into two disjoint subsets (`B = B1 | B2` and `B1 & B2 == 0`), and `num_dirty` is the population count of `B`.
+// - Follow-up input: A sequence of two consecutive `KVM_DIRTY_TLB` ioctl calls. The first call has `bitmap = B1` and `num_dirty` as the population count of `B1`. The second call has `bitmap = B2` and `num_dirty` as the population count of `B2`.
+// - Input relation: The set of dirty entries reported in the source input (`B`) is the union of the disjoint sets of dirty entries reported in the follow-up inputs (`B1` and `B2`).
+// - Output relation: All ioctl calls in both the source and follow-up scenarios should return 0 (success). While the internal state of the kernel's TLB management might differ due to optimizations (e.g., a full flush vs. targeted invalidations), the subsequent guest execution via `KVM_RUN` must be functionally identical. This means the guest must not observe any stale translations corresponding to the entries marked as dirty in the original bitmap `B`.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Helper function to calculate the population count of a 64-bit integer.
+// Use the GCC/Clang builtin if available for efficiency.
+#if defined(__GNUC__) || defined(__clang__)
+#define popcount_7206cd80(x) __builtin_popcountll(x)
+#else
+static inline int popcount_7206cd80(uint64_t x)
+{
+	int count = 0;
+	while (x > 0) {
+		x &= (x - 1);
+		count++;
+	}
+	return count;
+}
+#endif
+
+/*
+ * Define a structure that matches the layout of the modern KVM_DIRTY_TLB ioctl argument.
+ * This is necessary to avoid conflicts with potentially outdated system headers in the
+ * build environment which may have an older, incompatible definition of 'struct kvm_dirty_tlb'.
+ * The layout is based on 'struct kvm_dirty_tlb_v2' from recent kernel versions.
+ */
+struct syz_kvm_dirty_tlb_req_7206cd80 {
+	uint64_t flags; /* must be 0 */
+	uint32_t num_dirty;
+	uint32_t slot;
+	union {
+		struct {
+			uint64_t first_gfn;
+			uint64_t bitmap[0];
+		};
+		uint64_t gfns[0];
+	};
+};
+
+/**
+ * @brief Helper function to set up a KVM environment and execute a sequence of KVM_DIRTY_TLB ioctls.
+ *
+ * This function encapsulates the creation of a VM, VCPU, and memory region,
+ * and then executes one or more KVM_DIRTY_TLB calls as specified. It ensures
+ * a clean and independent environment for each test scenario (source and follow-up).
+ *
+ * @param num_calls The number of KVM_DIRTY_TLB calls to make in sequence.
+ * @param bitmaps An array of bitmaps, one for each call.
+ * @param slot The memory slot to target.
+ * @param first_gfn The starting Guest Frame Number for the bitmaps.
+ * @return 0 on success (all ioctls in the sequence returned 0), -1 on any failure
+ *         (setup error or an ioctl failed).
+ */
+static long do_kvm_dirty_tlb_run_7206cd80(int num_calls, const uint64_t bitmaps[],
+					  uint32_t slot, uint64_t first_gfn)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	void* guest_mem = NULL;
+	long final_res = 0;
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1)
+		return -1; // Indicate setup failure
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// We need a memory region that can contain all GFNs referenced by the bitmap.
+	// A single __u64 bitmap refers to 64 pages.
+	const size_t mem_size = 4UL * 1024 * 1024; // 4MB is sufficient
+	if ((first_gfn + 64) * 4096 > mem_size) {
+		// The provided first_gfn is too large for our test setup.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	guest_mem = mmap(NULL, mem_size, PROT_READ | PROT_WRITE,
+			 MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	struct kvm_userspace_memory_region region = {
+	    .slot = slot,
+	    .flags = KVM_MEM_LOG_DIRTY_PAGES,
+	    .guest_phys_addr = 0,
+	    .memory_size = mem_size,
+	    .userspace_addr = (uint64_t)guest_mem,
+	};
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		// This can fail if the slot is invalid. Treat as setup failure.
+		final_res = -1;
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		final_res = -1;
+		goto cleanup;
+	}
+
+	for (int i = 0; i < num_calls; i++) {
+		uint64_t current_bitmap = bitmaps[i];
+		// As per the MR description, num_dirty is the population count of the bitmap.
+		int num_dirty = popcount_7206cd80(current_bitmap);
+
+		if (num_dirty == 0) {
+			// A call with no dirty pages is a no-op and should succeed.
+			continue;
+		}
+
+		// The size of the struct to pass to ioctl includes the flexible array member.
+		// We need space for one bitmap entry (a single uint64_t).
+		size_t dirty_tlb_size =
+		    sizeof(struct syz_kvm_dirty_tlb_req_7206cd80) + sizeof(uint64_t);
+		struct syz_kvm_dirty_tlb_req_7206cd80* dirty =
+		    (struct syz_kvm_dirty_tlb_req_7206cd80*)malloc(dirty_tlb_size);
+		if (!dirty) {
+			final_res = -1; // Memory allocation failure
+			break;
+		}
+
+		memset(dirty, 0, dirty_tlb_size);
+		dirty->flags = 0; // This flag field must be zero.
+		dirty->num_dirty = num_dirty;
+		dirty->slot = slot;
+		dirty->first_gfn = first_gfn;
+		dirty->bitmap[0] = current_bitmap;
+
+		long res = ioctl(vm_fd, KVM_DIRTY_TLB, dirty);
+		free(dirty);
+
+		if (res != 0) {
+			final_res = -1; // The ioctl failed
+			break;
+		}
+	}
+
+cleanup:
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (guest_mem != MAP_FAILED)
+		munmap(guest_mem, mem_size);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return final_res;
+}
+
+/**
+ * @brief Metamorphic Relation (MR1) for KVM_DIRTY_TLB.
+ *
+ * This MR tests the property that reporting a set of dirty TLB entries in a
+ * single call should be functionally equivalent to reporting disjoint subsets
+ * of those entries in consecutive calls.
+ *
+ * - Source Input: A single KVM_DIRTY_TLB call with bitmap B.
+ * - Follow-up Input: Two consecutive KVM_DIRTY_TLB calls with disjoint
+ *   bitmaps B1 and B2, where B = B1 | B2.
+ * - Output Relation: The success/failure of the source call should be the
+ *   same as the combined success/failure of the follow-up calls.
+ *
+ * @param a0 Source bitmap B as a 64-bit integer.
+ * @param a1 The memory slot for the operation.
+ * @param a2 The starting Guest Frame Number (GFN).
+ * @return 0, as the return value is not used for MRs.
+ */
+static long syz_mr_7206cd80(volatile long a0, volatile long a1, volatile long a2)
+{
+	// 1. Get source inputs from parameters
+	uint64_t bitmap_b = (uint64_t)a0;
+	uint32_t slot = (uint32_t)a1;
+	uint64_t first_gfn = (uint64_t)a2;
+
+	// If the source bitmap is empty, both scenarios are no-ops and trivially succeed.
+	if (bitmap_b == 0) {
+		return 0;
+	}
+
+	// 2. Perform the source scenario: one call with the full bitmap B.
+	// We create a fresh KVM instance for this run to ensure a clean state.
+	long res_s = do_kvm_dirty_tlb_run_7206cd80(1, &bitmap_b, slot, first_gfn);
+
+	// 3. Perform the follow-up scenario: two calls with disjoint bitmaps B1 and B2.
+	// We create another fresh KVM instance to ensure the state is independent.
+	uint64_t bitmap_b1 = bitmap_b & 0xFFFFFFFF00000000ULL;
+	uint64_t bitmap_b2 = bitmap_b & 0x00000000FFFFFFFFULL;
+	uint64_t followup_bitmaps[] = {bitmap_b1, bitmap_b2};
+	long res_f = do_kvm_dirty_tlb_run_7206cd80(2, followup_bitmaps, slot, first_gfn);
+
+	// 4. Check the output relation.
+	// The relation holds if the outcome (success/failure) of both scenarios is the same.
+	// Success is indicated by a return value of 0 from our helper function.
+	int source_success = (res_s == 0);
+	int followup_success = (res_f == 0);
+
+	if (source_success != followup_success) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_75e3d2b4.h b/executor/MRs/syz_mr_75e3d2b4.h
new file mode 100644
index 000000000..03841651a
--- /dev/null
+++ b/executor/MRs/syz_mr_75e3d2b4.h
@@ -0,0 +1,194 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.33-KVM_GET_DEBUGREGS/impl/mr.h
+// Code generated by gemini-2.5-pro after 6 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Preservation of guest-modified DR6 across an intervening non-DR VM-Exit
+// - Source input: A sequence of operations on a vCPU:
+//     1. Initialize the vCPU's debug registers using `KVM_SET_DEBUGREGS`. Set `DR6` to an initial known value `V_initial` and configure `DR7` such that subsequent guest DR accesses do not cause VM-Exits.
+//     2. Run the vCPU with guest code that first modifies the `DR6` register to a new value `V_guest` and then immediately triggers a VM-Exit for a reason *unrelated* to debug registers (e.g., by executing a `HLT` instruction or waiting for an external interrupt).
+//     3. The resulting vCPU state after this single run.
+//
+// - Follow-up input: A sequence of operations on an identical vCPU:
+//     1. Perform the exact same initialization and execution steps as the source input (steps 1 and 2).
+//     2. After the vCPU exits, immediately run it again for a very short duration, ensuring it exits quickly for another non-DR-related reason (e.g., due to a preemption timer).
+//     3. The resulting vCPU state after this second, short run.
+//
+// - Input relation: The follow-up input is generated by taking the vCPU state from the source input and subjecting it to one additional, short, non-DR-related VM run cycle.
+//
+// - Output relation: Let `O_source` be the `struct kvm_debugregs` obtained by calling `KVM_GET_DEBUGREGS` on the source vCPU state, and `O_follow-up` be the result from the follow-up vCPU state. The value of `DR6` should be preserved. Therefore, the following relation must hold: `O_source.dr6 == O_follow-up.dr6`. Both values should be equal to `V_guest`, the value set by the guest.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+static long syz_mr_75e3d2b4(volatile long V_initial, volatile long V_guest)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	struct kvm_run* run = NULL;
+	int run_mmap_size;
+	void* guest_mem = MAP_FAILED;
+	const size_t mem_size = 0x1000;
+	long ret = 0;
+	struct kvm_debugregs dregs_source, dregs_followup, dregs;
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs;
+	struct kvm_regs regs;
+	uint64_t O_source_dr6, O_followup_dr6, expected_guest_val;
+	uint8_t code[5];
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	guest_mem = mmap(NULL, mem_size, PROT_READ | PROT_WRITE,
+			 MAP_SHARED | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	/*
+	 * Guest code performs three actions:
+	 * 1. mov %eax, %dr6: Modifies DR6 with the value from EAX (V_guest).
+	 * 2. hlt: Triggers the first non-DR VM-Exit for the source case.
+	 * 3. hlt: Triggers the second non-DR VM-Exit for the follow-up case.
+	 */
+	code[0] = 0x0f; /* mov %eax, %dr6 */
+	code[1] = 0x22;
+	code[2] = 0xf0;
+	code[3] = 0xf4; /* hlt */
+	code[4] = 0xf4; /* hlt for the second run */
+	memcpy(guest_mem, code, sizeof(code));
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = mem_size;
+	region.userspace_addr = (uint64_t)guest_mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	run_mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (run_mmap_size <= 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	run = (struct kvm_run*)mmap(NULL, run_mmap_size, PROT_READ | PROT_WRITE,
+				    MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL;
+		ret = -1;
+		goto cleanup;
+	}
+
+	/* Initialize vCPU in a simple real mode */
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+	sregs.cs.base = 0;
+	sregs.cs.selector = 0;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	/* Set RAX to V_guest, RIP to start of code */
+	memset(&regs, 0, sizeof(regs));
+	regs.rax = (uint32_t)V_guest;
+	regs.rip = 0;
+	regs.rflags = 0x2;
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	/* Initialize debug registers: DR6 to V_initial, DR7 to allow guest access */
+	memset(&dregs, 0, sizeof(dregs));
+	dregs.dr6 = (uint32_t)V_initial;
+	dregs.dr7 = 0x400; // GD bit (13) is 0, no trap on DR access
+	if (ioctl(vcpu_fd, KVM_SET_DEBUGREGS, &dregs) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	/* Source Run: execute 'mov dr6, eax' and first 'hlt' */
+	if (ioctl(vcpu_fd, KVM_RUN, 0) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+	if (run->exit_reason != KVM_EXIT_HLT) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	if (ioctl(vcpu_fd, KVM_GET_DEBUGREGS, &dregs_source) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	/* Follow-up Run: execute second 'hlt' */
+	if (ioctl(vcpu_fd, KVM_RUN, 0) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+	if (run->exit_reason != KVM_EXIT_HLT) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	if (ioctl(vcpu_fd, KVM_GET_DEBUGREGS, &dregs_followup) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	/* Check Output Relation */
+	O_source_dr6 = dregs_source.dr6;
+	O_followup_dr6 = dregs_followup.dr6;
+	/* The guest code uses a 32-bit move, so we compare against the 32-bit value */
+	expected_guest_val = (uint64_t)(uint32_t)V_guest;
+
+	/*
+	 * The MR states: O_source.dr6 == O_follow-up.dr6 and both should be V_guest.
+	 * This check verifies that both values are equal to the value set by the guest.
+	 */
+	if (O_source_dr6 != expected_guest_val || O_followup_dr6 != expected_guest_val) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	if (run)
+		munmap(run, run_mmap_size);
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (guest_mem != MAP_FAILED)
+		munmap(guest_mem, mem_size);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_76aa1e6b.h b/executor/MRs/syz_mr_76aa1e6b.h
new file mode 100644
index 000000000..2494e6625
--- /dev/null
+++ b/executor/MRs/syz_mr_76aa1e6b.h
@@ -0,0 +1,90 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.7-KVM_CREATE_VCPU/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Re-creation of a vCPU with an existing ID should fail.
+// - Source input: A call to the `KVM_CREATE_VCPU` ioctl on a valid VM file descriptor (`vm_fd`) with a valid and currently unused vCPU ID, let's call it `id_A`. This assumes the total number of vCPUs is less than `max_vcpus`.
+// - Follow-up input: A subsequent call to the `KVM_CREATE_VCPU` ioctl on the same `vm_fd` using the exact same vCPU ID, `id_A`.
+// - Input relation: The follow-up input is generated by re-executing the `KVM_CREATE_VCPU` ioctl with the identical vCPU ID used in the source input, on the same VM instance where the source input was successfully executed.
+// - Output relation: The source output should be a valid file descriptor (a non-negative integer), indicating the successful creation of the vCPU. The follow-up output should be `-1`, indicating failure, and the corresponding `errno` should be set to `EEXIST`.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// MR1: Re-creation of a vCPU with an existing ID should fail.
+static long syz_mr_76aa1e6b(volatile long id_A)
+{
+	int kvm_fd = -1;
+	long vm_fd = -1;
+	long vcpu_fd_A = -1;
+	long vcpu_fd_B = -1;
+	long ret = 0;
+	// Declare follow_up_errno at the top of the function to avoid "crosses initialization" error.
+	int follow_up_errno = 0;
+
+	// 1. Perform preparation
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot open /dev/kvm, so we cannot perform the test.
+		// This is a setup failure, not an MR violation.
+		return -1;
+	}
+
+	// Create a Virtual Machine
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		// Cannot create a VM, cannot perform the test.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get the source output
+	// Source input: Create a vCPU with the given id_A.
+	// This is expected to succeed.
+	vcpu_fd_A = ioctl(vm_fd, KVM_CREATE_VCPU, id_A);
+	if (vcpu_fd_A < 0) {
+		// The premise of the MR is a successful creation. If the source
+		// call fails, we cannot test the relation.
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output
+	// Follow-up input: Attempt to create another vCPU with the same id_A.
+	// This is expected to fail with EEXIST.
+	vcpu_fd_B = ioctl(vm_fd, KVM_CREATE_VCPU, id_A);
+	follow_up_errno = errno; // Save errno immediately after the call.
+
+	// 4. Check the output relation
+	// The source output (vcpu_fd_A) should be a valid FD (>= 0), which is
+	// checked above. The follow-up output (vcpu_fd_B) should be -1, and
+	// errno should be EEXIST.
+	if (vcpu_fd_B != -1 || follow_up_errno != EEXIST) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		ret = -1; // Indicate a violation occurred.
+	}
+
+cleanup:
+	// 5. Perform cleanup
+	if (vcpu_fd_A >= 0) {
+		close(vcpu_fd_A);
+	}
+	if (vcpu_fd_B >= 0) {
+		// This fd should not be valid if the MR holds, but we close it
+		// for robustness in case of a violation.
+		close(vcpu_fd_B);
+	}
+	if (vm_fd >= 0) {
+		close(vm_fd);
+	}
+	if (kvm_fd >= 0) {
+		close(kvm_fd);
+	}
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_7736935a.h b/executor/MRs/syz_mr_7736935a.h
new file mode 100644
index 000000000..6f503d2cf
--- /dev/null
+++ b/executor/MRs/syz_mr_7736935a.h
@@ -0,0 +1,65 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.1-KVM_GET_API_VERSION/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Invariance of the KVM API version
+// - Source input: A call to the `KVM_GET_API_VERSION` ioctl on a valid file descriptor for `/dev/kvm`.
+// - Follow-up input: A second, subsequent call to the `KVM_GET_API_VERSION` ioctl on the same file descriptor.
+// - Input relation: The follow-up input is a repetition of the source input.
+// - Output relation: The integer value returned by the source call must be equal to the integer value returned by the follow-up call.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Metamorphic Relation 1: Invariance of the KVM API version.
+ *
+ * This function implements the metamorphic relation described as follows:
+ * - Source input: A call to the KVM_GET_API_VERSION ioctl on a valid file descriptor for /dev/kvm.
+ * - Follow-up input: A second, subsequent call to the KVM_GET_API_VERSION ioctl on the same file descriptor.
+ * - Input relation: The follow-up input is a repetition of the source input.
+ * - Output relation: The integer value returned by the source call must be equal to the integer value returned by the follow-up call.
+ *
+ * The parameter `a0` is unused in this specific MR but is kept to conform to the required function signature format.
+ */
+static long syz_mr_7736935a(volatile long a0)
+{
+	// 1. Perform preparation: Open the KVM device.
+	int fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (fd == -1) {
+		// If /dev/kvm cannot be opened, the MR cannot be tested.
+		// This is a setup failure, not a violation. We return a non-zero
+		// value to indicate that the test could not be performed.
+		return -1;
+	}
+
+	// 2. Get the source output from the first ioctl call.
+	long source_output = ioctl(fd, KVM_GET_API_VERSION, 0);
+	if (source_output == -1) {
+		// If the first ioctl fails, the MR test cannot proceed.
+		close(fd);
+		return -1;
+	}
+
+	// 3. Get the follow-up output from the second ioctl call.
+	long followup_output = ioctl(fd, KVM_GET_API_VERSION, 0);
+	if (followup_output == -1) {
+		// If the second ioctl fails, we cannot compare the outputs.
+		// This is an anomaly but not a violation of the specified relation.
+		close(fd);
+		return -1;
+	}
+
+	// 4. Check if the outputs satisfy the output relation.
+	// The KVM API version should be constant.
+	if (source_output != followup_output) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	// 5. Perform cleanup.
+	close(fd);
+
+	return 0; // Return 0 to indicate the MR test was executed successfully.
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_77cb284e.h b/executor/MRs/syz_mr_77cb284e.h
new file mode 100644
index 000000000..0cdeb372c
--- /dev/null
+++ b/executor/MRs/syz_mr_77cb284e.h
@@ -0,0 +1,143 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.27-KVM_SET_IRQCHIP/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Non-interference of KVM_SET_IRQCHIP across different chip IDs
+// - Source input: A valid `struct kvm_irqchip` object, let's call it `S1`. `S1.chip_id` is set to a specific valid chip ID (e.g., 0 for PIC1), and `S1.chip` contains a valid state for that chip. The source operation is a call to the `KVM_SET_IRQCHIP` ioctl with `S1`.
+// - Follow-up input: Another valid `struct kvm_irqchip` object, let's call it `S2`. `S2.chip_id` is set to a different valid chip ID than `S1` (e.g., 2 for IOAPIC), and `S2.chip` contains a valid state for that chip.
+// - Input relation: The follow-up operation is a call to `KVM_SET_IRQCHIP` with `S2`, executed after the source operation. The key relationship is that the `chip_id` of the follow-up input is different from the `chip_id` of the source input (`S2.chip_id != S1.chip_id`).
+// - Output relation: The state of the interrupt chip set by the source operation should not be altered by the follow-up operation. To verify this, after both `KVM_SET_IRQCHIP` calls have been successfully executed, a call to `KVM_GET_IRQCHIP` for the source chip (`S1.chip_id`) should return a state identical to the one originally provided in `S1`.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Metamorphic Relation: Non-interference of KVM_SET_IRQCHIP across different chip IDs.
+ *
+ * This function tests that setting the state of one interrupt chip (e.g., IOAPIC)
+ * does not interfere with the state of another, different interrupt chip (e.g., PIC).
+ *
+ * @param a0, a1 - Volatile long values used to create a unique state for the source IRQ chip (PIC).
+ * @param a2, a3 - Volatile long values used to create a unique state for the follow-up IRQ chip (IOAPIC).
+ * @return 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_77cb284e(volatile long a0, volatile long a1, volatile long a2, volatile long a3)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long ret;
+
+	// 1. Perform preparation: Set up a KVM VM environment
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot open KVM, cannot proceed with the test
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		// Cannot create VM, cannot proceed
+		close(kvm_fd);
+		return -1;
+	}
+
+	// KVM_CREATE_IRQCHIP is necessary before setting/getting irqchip states
+	ret = ioctl(vm_fd, KVM_CREATE_IRQCHIP, 0);
+	if (ret < 0) {
+		// Cannot create IRQCHIP, cannot proceed
+		goto cleanup;
+	}
+
+	// Creating a VCPU is often a prerequisite for some KVM operations
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		// Cannot create VCPU, cannot proceed
+		goto cleanup;
+	}
+
+	// 2. Get the source and follow-up inputs and their corresponding outputs
+
+	// Source Input (S1) and Operation
+	struct kvm_irqchip chip_pic_master_set;
+	memset(&chip_pic_master_set, 0, sizeof(chip_pic_master_set));
+	chip_pic_master_set.chip_id = KVM_IRQCHIP_PIC_MASTER;
+
+	// Get a valid base state first to ensure our modifications are on a valid foundation
+	ret = ioctl(vm_fd, KVM_GET_IRQCHIP, &chip_pic_master_set);
+	if (ret < 0) {
+		// Precondition failed: cannot get initial state
+		goto cleanup;
+	}
+
+	// Modify the state based on input parameters to create our source state.
+	// This is the state we expect to see at the end.
+	// CORRECTED: Changed last_iir to last_irr
+	chip_pic_master_set.chip.pic.last_irr = (uint8_t)a0;
+	chip_pic_master_set.chip.pic.irr = (uint8_t)a1;
+
+	// Source Operation: Set the state for the PIC master chip
+	ret = ioctl(vm_fd, KVM_SET_IRQCHIP, &chip_pic_master_set);
+	if (ret < 0) {
+		// Source operation failed, cannot test the MR
+		goto cleanup;
+	}
+
+	// Follow-up Input (S2) and Operation
+	struct kvm_irqchip chip_ioapic_set;
+	memset(&chip_ioapic_set, 0, sizeof(chip_ioapic_set));
+	chip_ioapic_set.chip_id = KVM_IRQCHIP_IOAPIC;
+
+	// Get a valid base state for IOAPIC
+	ret = ioctl(vm_fd, KVM_GET_IRQCHIP, &chip_ioapic_set);
+	if (ret < 0) {
+		// Precondition failed: cannot get initial state for follow-up
+		goto cleanup;
+	}
+
+	// Modify the IOAPIC state based on different input parameters
+	chip_ioapic_set.chip.ioapic.id = (uint8_t)a2;
+	// Use a standard base address for IOAPIC
+	chip_ioapic_set.chip.ioapic.base_address = 0xfec00000;
+	chip_ioapic_set.chip.ioapic.ioregsel = (uint8_t)a3;
+
+	// Follow-up Operation: Set the state for the IOAPIC chip
+	ret = ioctl(vm_fd, KVM_SET_IRQCHIP, &chip_ioapic_set);
+	if (ret < 0) {
+		// Follow-up operation failed, cannot test the MR
+		goto cleanup;
+	}
+
+	// 3. Get the source output after the follow-up operation
+
+	// Verification Step: Get the state of the source chip (PIC master) again
+	struct kvm_irqchip chip_pic_master_get;
+	memset(&chip_pic_master_get, 0, sizeof(chip_pic_master_get));
+	chip_pic_master_get.chip_id = KVM_IRQCHIP_PIC_MASTER;
+
+	ret = ioctl(vm_fd, KVM_GET_IRQCHIP, &chip_pic_master_get);
+	if (ret < 0) {
+		// Verification failed, cannot get final state to test the MR
+		goto cleanup;
+	}
+
+	// 4. Check the output relation
+	// The state of the PIC master chip should be identical to what we set initially
+	if (memcmp(&chip_pic_master_set.chip.pic, &chip_pic_master_get.chip.pic, sizeof(struct kvm_pic_state)) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	// Later cleanup: Close all opened file descriptors
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_7d90e6d8.h b/executor/MRs/syz_mr_7d90e6d8.h
new file mode 100644
index 000000000..ee89b6ab3
--- /dev/null
+++ b/executor/MRs/syz_mr_7d90e6d8.h
@@ -0,0 +1,158 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.66-KVM_S390_UCAS_UNMAP/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The `user_addr` parameter of `KVM_S390_UCAS_UNMAP` is ignored.
+// - Source input: An `ioctl` call to `KVM_S390_UCAS_UNMAP` on a vCPU of an s390 ucontrol VM. The parameters are provided in a `struct kvm_s390_ucas_mapping` where `vcpu_addr` and `length` define a valid, existing memory mapping, and `user_addr` is set to an arbitrary value `U1`. The test is performed on a VM in a specific initial state `S`.
+// - Follow-up input: An `ioctl` call to `KVM_S390_UCAS_UNMAP` with the same `vcpu_addr` and `length` as the source input, but with the `user_addr` field set to a different value `U2` (where `U2 != U1`). The test is performed on a VM in the identical initial state `S`.
+// - Input relation: The follow-up input is generated by changing only the `user_addr` field of the `kvm_s390_ucas_mapping` structure, while keeping all other conditions (VM state, vCPU, `vcpu_addr`, `length`) identical to the source input.
+// - Output relation: The return value of the `ioctl` call for the source input should be identical to the return value for the follow-up input. Additionally, the resulting state of the VM's memory map after the source operation should be identical to the resulting state after the follow-up operation.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+#define GUEST_MEM_SIZE_7d90e6d8 0x1000 // 4KB page
+#define GPA_START_7d90e6d8 0x1000
+#define GVA_START_7d90e6d8 0x2000
+
+// A structure to hold all the necessary file descriptors and memory pointers for a VM state.
+struct vm_state_7d90e6d8 {
+	int kvm_fd;
+	int vm_fd;
+	int vcpu_fd;
+	void* guest_mem;
+};
+
+// Cleans up the resources associated with a VM state.
+static void cleanup_state_7d90e6d8(struct vm_state_7d90e6d8* state)
+{
+	if (state->guest_mem)
+		munmap(state->guest_mem, GUEST_MEM_SIZE_7d90e6d8);
+	if (state->vcpu_fd != -1)
+		close(state->vcpu_fd);
+	if (state->vm_fd != -1)
+		close(state->vm_fd);
+	if (state->kvm_fd != -1)
+		close(state->kvm_fd);
+}
+
+// Sets up a KVM VM to the required initial state 'S'.
+// This includes creating the VM, a vCPU, and establishing a UCAS mapping.
+static int setup_state_S_7d90e6d8(struct vm_state_7d90e6d8* state)
+{
+	state->kvm_fd = -1;
+	state->vm_fd = -1;
+	state->vcpu_fd = -1;
+	state->guest_mem = NULL;
+
+	state->kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (state->kvm_fd == -1)
+		return -1;
+
+	if (ioctl(state->kvm_fd, KVM_GET_API_VERSION, NULL) != 12)
+		goto fail;
+
+	if (ioctl(state->kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_S390_UCONTROL) <= 0)
+		goto fail;
+
+	state->vm_fd = ioctl(state->kvm_fd, KVM_CREATE_VM, KVM_VM_S390_UCONTROL);
+	if (state->vm_fd == -1)
+		goto fail;
+
+	state->guest_mem = mmap(NULL, GUEST_MEM_SIZE_7d90e6d8, PROT_READ | PROT_WRITE,
+				MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (state->guest_mem == MAP_FAILED) {
+		state->guest_mem = NULL;
+		goto fail;
+	}
+
+	{
+		struct kvm_userspace_memory_region region = {
+		    .slot = 0,
+		    .guest_phys_addr = GPA_START_7d90e6d8,
+		    .memory_size = GUEST_MEM_SIZE_7d90e6d8,
+		    .userspace_addr = (uint64_t)state->guest_mem,
+		};
+		if (ioctl(state->vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1)
+			goto fail;
+	}
+
+	state->vcpu_fd = ioctl(state->vm_fd, KVM_CREATE_VCPU, 0);
+	if (state->vcpu_fd == -1)
+		goto fail;
+
+	{
+		struct kvm_s390_ucas_mapping map_params = {
+		    .user_addr = (uint64_t)state->guest_mem,
+		    .vcpu_addr = GVA_START_7d90e6d8,
+		    .length = GUEST_MEM_SIZE_7d90e6d8,
+		};
+		if (ioctl(state->vcpu_fd, KVM_S390_UCAS_MAP, &map_params) == -1)
+			goto fail;
+	}
+
+	return 0;
+
+fail:
+	cleanup_state_7d90e6d8(state);
+	return -1;
+}
+
+static long syz_mr_7d90e6d8(volatile long a0)
+{
+	struct vm_state_7d90e6d8 state1, state2;
+	long res1, res2;
+	long errno1 = 0, errno2 = 0;
+	uint64_t u1 = (uint64_t)a0;
+	uint64_t u2 = u1 + 0x1000; // U2 != U1
+
+	// Source run
+	if (setup_state_S_7d90e6d8(&state1) != 0) {
+		// Precondition for the test failed, cannot proceed.
+		return 0;
+	}
+
+	struct kvm_s390_ucas_mapping unmap_params1 = {
+	    .user_addr = u1,
+	    .vcpu_addr = GVA_START_7d90e6d8,
+	    .length = GUEST_MEM_SIZE_7d90e6d8,
+	};
+
+	res1 = ioctl(state1.vcpu_fd, KVM_S390_UCAS_UNMAP, &unmap_params1);
+	if (res1 == -1)
+		errno1 = errno;
+
+	cleanup_state_7d90e6d8(&state1);
+
+	// Follow-up run
+	if (setup_state_S_7d90e6d8(&state2) != 0) {
+		// Precondition for the test failed, cannot proceed.
+		return 0;
+	}
+
+	struct kvm_s390_ucas_mapping unmap_params2 = {
+	    .user_addr = u2,
+	    .vcpu_addr = GVA_START_7d90e6d8,
+	    .length = GUEST_MEM_SIZE_7d90e6d8,
+	};
+
+	res2 = ioctl(state2.vcpu_fd, KVM_S390_UCAS_UNMAP, &unmap_params2);
+	if (res2 == -1)
+		errno2 = errno;
+
+	cleanup_state_7d90e6d8(&state2);
+
+	// Output relation check
+	// The return value and errno should be identical if user_addr is ignored.
+	if (res1 != res2 || (res1 == -1 && errno1 != errno2)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_811d6c8a.h b/executor/MRs/syz_mr_811d6c8a.h
new file mode 100644
index 000000000..f55145e49
--- /dev/null
+++ b/executor/MRs/syz_mr_811d6c8a.h
@@ -0,0 +1,127 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/7.6-KVM_CAP_S390_RI/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Enabling KVM_CAP_S390_RI is blocked after a VCPU is created.
+// - Source input: A sequence of operations: 1. Create an s390 VM on hardware that supports runtime-instrumentation. 2. Enable the `KVM_CAP_S390_RI` capability using the `KVM_ENABLE_CAP` ioctl.
+// - Follow-up input: A sequence of operations: 1. Create a new s390 VM on the same hardware. 2. Create one VCPU for that VM. 3. Enable the `KVM_CAP_S390_RI` capability using the `KVM_ENABLE_CAP` ioctl.
+// - Input relation: The follow-up input is obtained from the source input by inserting a VCPU-creation step before the `KVM_ENABLE_CAP` call.
+// - Output relation: The `KVM_ENABLE_CAP` call in the source input should succeed (return 0). The `KVM_ENABLE_CAP` call in the follow-up input should fail with an `EBUSY` error (return -1 and set `errno` to `EBUSY`).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// The parameter `a0` is kept to match the required syz_mr function signature,
+// but it is not used in this specific MR's logic.
+static long syz_mr_811d6c8a(volatile long a0)
+{
+	// Suppress the unused parameter warning.
+	(void)a0;
+
+#ifndef __s390x__
+	// This MR is specific to the s390x architecture.
+	// If compiled on a different architecture, the test is not applicable.
+	fprintf(stderr, "This MR is for s390x only. Skipping.\n");
+	return 0;
+#endif
+
+	int kvm_fd = -1;
+	int vm_fd_src = -1;
+	int vm_fd_fup = -1;
+	int vcpu_fd_fup = -1;
+	long ret_src, ret_fup;
+	int errno_src, errno_fup;
+
+	// 1. Perform preparation.
+	// Open the KVM device.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// In case of a setup failure, we can't proceed with the test.
+		// A non-zero return value indicates a setup problem.
+		perror("open /dev/kvm failed");
+		return -1;
+	}
+
+	// Check if the hardware supports the Runtime Instrumentation (RI) capability.
+	// If not, this MR is not applicable.
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_S390_RI) <= 0) {
+		fprintf(stderr, "KVM_CAP_S390_RI not supported. Skipping MR.\n");
+		goto cleanup;
+	}
+
+	// --- Source Input Execution ---
+	vm_fd_src = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd_src < 0) {
+		perror("KVM_CREATE_VM for source failed");
+		goto cleanup;
+	}
+
+	struct kvm_enable_cap cap_src;
+	memset(&cap_src, 0, sizeof(cap_src));
+	cap_src.cap = KVM_CAP_S390_RI;
+
+	errno = 0;
+	ret_src = ioctl(vm_fd_src, KVM_ENABLE_CAP, &cap_src);
+	errno_src = errno;
+
+	close(vm_fd_src);
+	vm_fd_src = -1;
+
+	// --- Follow-up Input Execution ---
+	vm_fd_fup = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd_fup < 0) {
+		perror("KVM_CREATE_VM for follow-up failed");
+		goto cleanup;
+	}
+
+	// This is the differentiating step from the source input.
+	vcpu_fd_fup = ioctl(vm_fd_fup, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd_fup < 0) {
+		perror("KVM_CREATE_VCPU for follow-up failed");
+		goto cleanup;
+	}
+
+	struct kvm_enable_cap cap_fup;
+	memset(&cap_fup, 0, sizeof(cap_fup));
+	cap_fup.cap = KVM_CAP_S390_RI;
+
+	errno = 0;
+	ret_fup = ioctl(vm_fd_fup, KVM_ENABLE_CAP, &cap_fup);
+	errno_fup = errno;
+
+	// 4. Check the output relation.
+	// This `if` statement contains a proper boolean expression, which fixes the
+	// `‘output_relation_violated’ was not declared` error.
+	// The expected relation is: source call succeeds (ret == 0), and
+	// the follow-up call fails with EBUSY (ret == -1 and errno == EBUSY).
+	// The condition is true if this relation is NOT met.
+	if (!(ret_src == 0 && ret_fup == -1 && errno_fup == EBUSY)) {
+		// This fprintf statement now correctly ends with a semicolon, fixing the
+		// `expected ‘;’ before ‘}’ token` error.
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		fprintf(stderr, "  Source ret: %ld, errno: %d (%s)\n", ret_src, errno_src, strerror(errno_src));
+		fprintf(stderr, "  Follow-up ret: %ld, errno: %d (%s)\n", ret_fup, errno_fup, strerror(errno_fup));
+		fprintf(stderr, "  Expected: Source ret=0, Follow-up ret=-1 with errno=EBUSY (%d)\n", EBUSY);
+	}
+
+cleanup:
+	// 5. Perform cleanup.
+	if (vcpu_fd_fup >= 0)
+		close(vcpu_fd_fup);
+	if (vm_fd_fup >= 0)
+		close(vm_fd_fup);
+	if (vm_fd_src >= 0)
+		close(vm_fd_src);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	// A return statement is provided for all execution paths, which fixes the
+	// `no return statement in function returning non-void` error.
+	// A return value of 0 indicates the MR test logic executed successfully.
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_825250bb.h b/executor/MRs/syz_mr_825250bb.h
new file mode 100644
index 000000000..057a70cf9
--- /dev/null
+++ b/executor/MRs/syz_mr_825250bb.h
@@ -0,0 +1,97 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.9-KVM_SET_MEMORY_ALIAS/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Calling an obsolete KVM_SET_MEMORY_ALIAS ioctl always fails
+// - Source input: On an x86 VM, issue the KVM_SET_MEMORY_ALIAS ioctl with a kvm_memory_alias structure whose `.slot` field is 0 and all other fields contain arbitrary, valid values.
+// - Follow-up input: Repeat the same ioctl on the same VM, but set `.slot` to 1 (all other fields identical to the source input).
+// - Input relation: The follow-up input is derived from the source input only by changing the `.slot` number.
+// - Output relation: Because KVM_SET_MEMORY_ALIAS is obsolete and has been removed, both ioctl calls must fail (return –1 with errno set to ENOTTY or EINVAL). Thus the source and follow-up outputs are identical.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// The MR description specifies this test is for x86 VMs.
+#if !defined(__i386__) && !defined(__x86_64__)
+#error This metamorphic relation is specific to x86 architectures.
+#endif
+
+/**
+ * MR1: Calling an obsolete KVM_SET_MEMORY_ALIAS ioctl always fails
+ * - Source input: On an x86 VM, issue the KVM_SET_MEMORY_ALIAS ioctl with a
+ *   kvm_memory_alias structure whose `.slot` field is 0 and all other fields
+ *   contain arbitrary, valid values.
+ * - Follow-up input: Repeat the same ioctl on the same VM, but set `.slot` to 1
+ *   (all other fields identical to the source input).
+ * - Input relation: The follow-up input is derived from the source input only
+ *   by changing the `.slot` number.
+ * - Output relation: Because KVM_SET_MEMORY_ALIAS is obsolete and has been
+ *   removed, both ioctl calls must fail (return –1 with errno set to ENOTTY or
+ *   EINVAL). Thus the source and follow-up outputs are identical.
+ */
+static long syz_mr_825250bb(volatile long guest_phys_addr, volatile long memory_size, volatile long target_phys_addr)
+{
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	long source_ret, followup_ret;
+	int source_errno, followup_errno;
+
+	// 1. Perform preparation: create a KVM virtual machine.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot open KVM, so we cannot perform the test.
+		// This is a setup failure, not a violation of the MR itself.
+		return 0;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		// Cannot create VM, so we cannot perform the test.
+		close(kvm_fd);
+		return 0;
+	}
+
+	// 2. Prepare the source input and get the source output.
+	struct kvm_memory_alias alias;
+	alias.slot = 0;
+	alias.flags = 0;
+	alias.guest_phys_addr = (__u64)guest_phys_addr;
+	alias.memory_size = (__u64)memory_size;
+	alias.target_phys_addr = (__u64)target_phys_addr;
+
+	errno = 0;
+	source_ret = ioctl(vm_fd, KVM_SET_MEMORY_ALIAS, &alias);
+	source_errno = errno;
+
+	// 3. Get the follow-up input through the input relation and get the follow-up output.
+	// The only change is the slot number.
+	alias.slot = 1;
+
+	errno = 0;
+	followup_ret = ioctl(vm_fd, KVM_SET_MEMORY_ALIAS, &alias);
+	followup_errno = errno;
+
+	// 4. Check if the outputs satisfy the output relation.
+	// This condition replaces the 'output_relation_violated' placeholder,
+	// resolving the 'was not declared in this scope' error.
+	// The relation requires both calls to fail with -1, have identical errno,
+	// and the errno must be either ENOTTY or EINVAL.
+	if (source_ret != -1 || followup_ret != -1 || source_errno != followup_errno ||
+	    (source_errno != ENOTTY && source_errno != EINVAL)) {
+		// The fprintf statement below includes a semicolon at the end,
+		// resolving the 'expected ‘;’ before ‘}’ token' error.
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	// 5. Perform cleanup.
+	close(vm_fd);
+	close(kvm_fd);
+
+	// The function returns 0 to satisfy its 'static long' return type,
+	// resolving the 'no return statement in function returning non-void' error.
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_8766e19b.h b/executor/MRs/syz_mr_8766e19b.h
new file mode 100644
index 000000000..57753b75b
--- /dev/null
+++ b/executor/MRs/syz_mr_8766e19b.h
@@ -0,0 +1,151 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.127-KVM_XEN_HVM_SET_ATTR/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Idempotency of setting a Xen HVM attribute
+// - Source input: A call to the `KVM_XEN_HVM_SET_ATTR` ioctl with a valid `struct kvm_xen_hvm_attr` parameter. For example, the `type` is set to `KVM_XEN_ATTR_TYPE_SHARED_INFO` and `u.shared_info.gfn` is set to a valid guest physical frame number, `G1`.
+// - Follow-up input: A second call to the `KVM_XEN_HVM_SET_ATTR` ioctl using the exact same `struct kvm_xen_hvm_attr` parameter as the source input.
+// - Input relation: The follow-up input is generated by re-executing the source input operation immediately after the source operation has successfully completed.
+// - Output relation: Both the source and follow-up ioctl calls should return 0, indicating success. The state of the corresponding Xen attribute in the VM after the follow-up call should be identical to the state after the source call (e.g., the shared info GFN remains `G1`).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+/**
+ * Implements the metamorphic relation for the idempotency of KVM_XEN_HVM_SET_ATTR.
+ * @param gfn The guest physical frame number to use for the shared_info attribute.
+ *            This represents the variable part of the source input.
+ * @return 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_8766e19b(volatile long gfn)
+{
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	void* mem = NULL;
+	long page_size;
+	long res = 0;
+	long res1, res2;
+	int is_violated = 0;
+	struct kvm_enable_cap cap;
+	struct kvm_userspace_memory_region region;
+	struct kvm_xen_hvm_attr attr_set, attr_get;
+
+	// 1. Perform preparation
+	page_size = getpagesize();
+	if (page_size == -1) {
+		// Cannot get page size, cannot proceed.
+		return -1;
+	}
+
+	// A GFN of 0 can be special; using a positive value is safer for a generic test.
+	if (gfn <= 0) {
+		return -1;
+	}
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is not available, cannot perform the test.
+		return -1;
+	}
+
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, 0) != KVM_API_VERSION) {
+		// Incompatible KVM version, cannot proceed.
+		goto cleanup;
+	}
+
+	if (!ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_XEN_HVM)) {
+		// The KVM_CAP_XEN_HVM capability is required for this MR.
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		goto cleanup;
+	}
+
+	// Enable the Xen HVM capability for the created VM.
+	memset(&cap, 0, sizeof(cap));
+	cap.cap = KVM_CAP_XEN_HVM;
+	if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap) < 0) {
+		goto cleanup;
+	}
+
+	// Allocate host memory to be mapped as guest physical memory.
+	mem = mmap(NULL, page_size, PROT_READ | PROT_WRITE,
+		   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		mem = NULL; // Ensure mem is NULL for cleanup check
+		goto cleanup;
+	}
+
+	// Create a memory region in the VM to provide a valid GFN.
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.flags = 0;
+	region.guest_phys_addr = gfn * page_size;
+	region.memory_size = page_size;
+	region.userspace_addr = (unsigned long)mem;
+
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		goto cleanup;
+	}
+
+	// 2. Get the source output
+	memset(&attr_set, 0, sizeof(attr_set));
+	attr_set.type = KVM_XEN_ATTR_TYPE_SHARED_INFO;
+	attr_set.u.shared_info.gfn = gfn;
+
+	res1 = ioctl(vm_fd, KVM_XEN_HVM_SET_ATTR, &attr_set);
+
+	// The MR premise is that the source call succeeds.
+	// If it fails, we cannot test for idempotency.
+	if (res1 != 0) {
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output
+	// The follow-up input is the exact same ioctl call.
+	res2 = ioctl(vm_fd, KVM_XEN_HVM_SET_ATTR, &attr_set);
+
+	// 4. Check the output relation
+	// The follow-up call should return 0, indicating success.
+	if (res2 != 0) {
+		is_violated = 1;
+	}
+
+	// The state of the attribute should be identical after the second call.
+	// We can verify this by getting the attribute and checking its value.
+	memset(&attr_get, 0, sizeof(attr_get));
+	attr_get.type = KVM_XEN_ATTR_TYPE_SHARED_INFO;
+	if (ioctl(vm_fd, KVM_XEN_HVM_GET_ATTR, &attr_get) != 0) {
+		// If getting the attribute fails after the second set, the state is inconsistent.
+		is_violated = 1;
+	} else if (attr_get.u.shared_info.gfn != (__u64)gfn) {
+		// The GFN should still be the one we set initially.
+		// Cast to __u64 to fix the signed/unsigned comparison warning.
+		is_violated = 1;
+	}
+
+	if (is_violated) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	// 5. Perform cleanup
+	if (mem != NULL) {
+		munmap(mem, page_size);
+	}
+	if (vm_fd != -1) {
+		close(vm_fd);
+	}
+	if (kvm_fd != -1) {
+		close(kvm_fd);
+	}
+	return res;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_89dbb28a.h b/executor/MRs/syz_mr_89dbb28a.h
new file mode 100644
index 000000000..b932f00bf
--- /dev/null
+++ b/executor/MRs/syz_mr_89dbb28a.h
@@ -0,0 +1,111 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.63-KVM_ALLOCATE_RMA/impl/mr.h
+// Code generated by gemini-2.5-pro after 7 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 5 iterations
+// MR1: Allocating an RMA for two different VMs yields equal sizes and distinct file descriptors.
+// - Source input:
+//   - Create VM1 (vmfd1).
+//   - Issue `KVM_ALLOCATE_RMA` on vmfd1. Record the returned file descriptor fd1 (≥ 0) and size size1.
+// - Follow-up input:
+//   - Create a second VM (vmfd2).
+//   - Issue `KVM_ALLOCATE_RMA` on vmfd2. Record the returned file descriptor fd2 (≥ 0) and size size2.
+// - Input relation: The follow-up input repeats the same ioctl on a *different* VM after the first call has succeeded.
+// - Output relation: The following conditions must hold, assuming both calls succeed:
+//   1. `size1 == size2` (the RMA size is a global property fixed at host boot time).
+//   2. `fd1 != fd2` (each call returns a file descriptor for a new, distinct resource).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h> // For uint32_t
+#include <stdio.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Hardcode the ioctl number to bypass build environment issues with sizeof().
+// The value 0xC004AE91UL is the pre-calculated result of the _IOWR macro for
+// KVM_ALLOCATE_RMA on a 64-bit system.
+// It is derived from: _IOWR(KVMIO, 0x91, struct kvm_allocate_rma)
+// where KVMIO is 0xAE, and sizeof(struct kvm_allocate_rma) is 4.
+#define SYZ_KVM_ALLOCATE_RMA_89dbb28a 0xC004AE91UL
+
+/**
+ * MR1: Allocating an RMA for two different VMs yields equal sizes and distinct file descriptors.
+ * - Source input:
+ *   - Create VM1 (vmfd1).
+ *   - Issue `KVM_ALLOCATE_RMA` on vmfd1. Record the returned file descriptor fd1 (≥ 0) and size size1.
+ * - Follow-up input:
+ *   - Create a second VM (vmfd2).
+ *   - Issue `KVM_ALLOCATE_RMA` on vmfd2. Record the returned file descriptor fd2 (≥ 0) and size size2.
+ * - Input relation: The follow-up input repeats the same ioctl on a *different* VM after the first call has succeeded.
+ * - Output relation: The following conditions must hold, assuming both calls succeed:
+ *   1. `size1 == size2` (the RMA size is a global property fixed at host boot time).
+ *   2. `fd1 != fd2` (each call returns a file descriptor for a new, distinct resource).
+ */
+static long syz_mr_89dbb28a(volatile long a0)
+{
+	// The parameter a0 is unused in this MR, but is required by the function signature format.
+	(void)a0;
+
+	long kvmfd = -1, vmfd1 = -1, vmfd2 = -1;
+	long fd1 = -1, fd2 = -1;
+	uint32_t rma_size1 = 0;
+	uint32_t rma_size2 = 0;
+
+	// 1. Preparation: Open the KVM device.
+	kvmfd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvmfd == -1) {
+		// Cannot open KVM, the MR is not applicable.
+		return 0;
+	}
+
+	// --- Source Input Execution ---
+	// Create the first VM.
+	vmfd1 = ioctl(kvmfd, KVM_CREATE_VM, 0);
+	if (vmfd1 == -1) {
+		// Failed to create the first VM, cannot proceed.
+		goto cleanup_kvm;
+	}
+
+	// Issue KVM_ALLOCATE_RMA on the first VM using the hardcoded ioctl number.
+	fd1 = ioctl(vmfd1, SYZ_KVM_ALLOCATE_RMA_89dbb28a, &rma_size1);
+	if (fd1 < 0) {
+		// The ioctl might not be supported or failed for another reason.
+		// The precondition for the MR (successful source call) is not met.
+		goto cleanup_vm1;
+	}
+
+	// --- Follow-up Input Execution ---
+	// Create the second VM.
+	vmfd2 = ioctl(kvmfd, KVM_CREATE_VM, 0);
+	if (vmfd2 == -1) {
+		// Failed to create the second VM, cannot proceed with the check.
+		goto cleanup_fd1;
+	}
+
+	// Issue KVM_ALLOCATE_RMA on the second VM.
+	fd2 = ioctl(vmfd2, SYZ_KVM_ALLOCATE_RMA_89dbb28a, &rma_size2);
+	if (fd2 < 0) {
+		// The second call failed. The MR condition "assuming both calls succeed" is not met.
+		goto cleanup_vm2;
+	}
+
+	// --- Output Relation Check ---
+	// Both calls succeeded. Now, verify the output relation.
+	if (rma_size1 != rma_size2 || fd1 == fd2) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	// --- Cleanup ---
+	// Close all acquired file descriptors.
+	close(fd2);
+cleanup_vm2:
+	close(vmfd2);
+cleanup_fd1:
+	close(fd1);
+cleanup_vm1:
+	close(vmfd1);
+cleanup_kvm:
+	close(kvmfd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_8acbfeee.h b/executor/MRs/syz_mr_8acbfeee.h
new file mode 100644
index 000000000..3f9f953fc
--- /dev/null
+++ b/executor/MRs/syz_mr_8acbfeee.h
@@ -0,0 +1,184 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/core_locking/Callbacks/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Robustness of rdma_destroy_id against multiple pending address resolution requests
+// - Source input: A sequence of `rdma_cm` API calls. First, an `rdma_cm_id` is created. Second, a single asynchronous `rdma_resolve_addr` operation is initiated on this ID. Third, `rdma_destroy_id` is called immediately on the same ID to cancel the pending operation and destroy the ID.
+// - Follow-up input: A sequence of `rdma_cm` API calls similar to the source input. First, an `rdma_cm_id` is created. Second, *two* asynchronous `rdma_resolve_addr` operations are initiated on this ID. Third, `rdma_destroy_id` is called immediately on the same ID.
+// - Input relation: The follow-up input is derived from the source input by adding one more `rdma_resolve_addr` operation before the final `rdma_destroy_id` call.
+// - Output relation: The system should exhibit safe and consistent behavior for both the source and follow-up inputs. Specifically, in both cases, the `rdma_destroy_id` call must successfully complete, all pending address resolution requests must be safely canceled, and all associated resources must be cleanly released without causing any kernel crashes, memory corruption, or use-after-free errors.
+
+#include <arpa/inet.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <netinet/in.h>
+#include <rdma/rdma_user_cm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Metamorphic Relation MR1: Robustness of rdma_destroy_id against multiple pending address resolution requests.
+ *
+ * This function implements the metamorphic relation by directly using the ioctl interface
+ * for the RDMA Connection Manager (/dev/infiniband/rdma_cm). This version is corrected
+ * to align with the specific UAPI definitions and C++ compilation rules of the syzkaller environment.
+ *
+ * @param a0 A volatile long used to derive a port number for the RDMA connection.
+ * @param a1 A volatile long, unused in this specific MR but included for API consistency.
+ * @return Returns 0. The function's primary purpose is to print a violation message to stderr
+ *         if the metamorphic relation is not satisfied.
+ */
+static long syz_mr_8acbfeee(volatile long a0, volatile long a1)
+{
+	int source_success = 0;
+	int followup_success = 0;
+
+	// --- Source Case: 1 rdma_resolve_addr + 1 rdma_destroy_id ---
+	{
+		// Declare all variables at the top of the block to avoid "goto crosses initialization".
+		int cm_fd = -1;
+		__u32 id = (__u32)-1;
+		int ret;
+		struct rdma_ucm_create_id create_cmd;
+		struct rdma_ucm_destroy_id destroy_cmd;
+		struct sockaddr_in sin;
+		// This struct ensures the correct memory layout for the ioctl, with address data
+		// immediately following the command struct. This works around compiler issues
+		// with flexible array members by not referring to the member by name.
+		struct {
+			struct rdma_ucm_resolve_addr cmd;
+			char addr_buf[sizeof(struct sockaddr_in)];
+		} resolve_buf;
+
+		cm_fd = open("/dev/infiniband/rdma_cm", O_RDWR);
+		if (cm_fd < 0) {
+			goto cleanup_source;
+		}
+
+		memset(&create_cmd, 0, sizeof(create_cmd));
+		create_cmd.ps = RDMA_PS_TCP;
+		ret = ioctl(cm_fd, RDMA_USER_CM_CMD_CREATE_ID, &create_cmd);
+		if (ret) {
+			goto cleanup_source;
+		}
+		id = create_cmd.uid;
+
+		memset(&sin, 0, sizeof(sin));
+		sin.sin_family = AF_INET;
+		sin.sin_port = htons((unsigned short)((a0 % 32768) + 32768));
+		if (inet_pton(AF_INET, "127.0.0.1", &sin.sin_addr) <= 0) {
+			goto cleanup_source;
+		}
+
+		memset(&resolve_buf, 0, sizeof(resolve_buf));
+		resolve_buf.cmd.id = id;
+		resolve_buf.cmd.timeout_ms = 1000;
+		resolve_buf.cmd.dst_size = sizeof(sin);
+		// Copy address data into the buffer part of our combined struct.
+		memcpy(resolve_buf.addr_buf, &sin, sizeof(sin));
+
+		// Pass a pointer to the start of the buffer. The kernel will read
+		// sizeof(cmd) + cmd.dst_size bytes, correctly consuming the address.
+		ret = ioctl(cm_fd, RDMA_USER_CM_CMD_RESOLVE_ADDR, &resolve_buf.cmd);
+		if (ret) {
+			goto cleanup_source;
+		}
+
+		destroy_cmd.id = id;
+		ret = ioctl(cm_fd, RDMA_USER_CM_CMD_DESTROY_ID, &destroy_cmd);
+		if (ret) {
+			id = (__u32)-1;
+			goto cleanup_source;
+		}
+		id = (__u32)-1;
+
+		source_success = 1;
+
+	cleanup_source:
+		if (id != (__u32)-1 && cm_fd >= 0) {
+			destroy_cmd.id = id;
+			ioctl(cm_fd, RDMA_USER_CM_CMD_DESTROY_ID, &destroy_cmd);
+		}
+		if (cm_fd >= 0) {
+			close(cm_fd);
+		}
+	}
+
+	// --- Follow-up Case: 2 rdma_resolve_addr attempts + 1 rdma_destroy_id ---
+	{
+		int cm_fd = -1;
+		__u32 id = (__u32)-1;
+		int ret;
+		struct rdma_ucm_create_id create_cmd;
+		struct rdma_ucm_destroy_id destroy_cmd;
+		struct sockaddr_in sin;
+		struct {
+			struct rdma_ucm_resolve_addr cmd;
+			char addr_buf[sizeof(struct sockaddr_in)];
+		} resolve_buf;
+
+		cm_fd = open("/dev/infiniband/rdma_cm", O_RDWR);
+		if (cm_fd < 0) {
+			goto cleanup_followup;
+		}
+
+		memset(&create_cmd, 0, sizeof(create_cmd));
+		create_cmd.ps = RDMA_PS_TCP;
+		ret = ioctl(cm_fd, RDMA_USER_CM_CMD_CREATE_ID, &create_cmd);
+		if (ret) {
+			goto cleanup_followup;
+		}
+		id = create_cmd.uid;
+
+		memset(&sin, 0, sizeof(sin));
+		sin.sin_family = AF_INET;
+		sin.sin_port = htons((unsigned short)((a0 % 32768) + 32768));
+		if (inet_pton(AF_INET, "127.0.0.1", &sin.sin_addr) <= 0) {
+			goto cleanup_followup;
+		}
+
+		memset(&resolve_buf, 0, sizeof(resolve_buf));
+		resolve_buf.cmd.id = id;
+		resolve_buf.cmd.timeout_ms = 1000;
+		resolve_buf.cmd.dst_size = sizeof(sin);
+		memcpy(resolve_buf.addr_buf, &sin, sizeof(sin));
+
+		ret = ioctl(cm_fd, RDMA_USER_CM_CMD_RESOLVE_ADDR, &resolve_buf.cmd);
+		if (ret) {
+			goto cleanup_followup;
+		}
+
+		ret = ioctl(cm_fd, RDMA_USER_CM_CMD_RESOLVE_ADDR, &resolve_buf.cmd);
+		if (!(ret == -1 && errno == EINPROGRESS)) {
+			goto cleanup_followup;
+		}
+
+		destroy_cmd.id = id;
+		ret = ioctl(cm_fd, RDMA_USER_CM_CMD_DESTROY_ID, &destroy_cmd);
+		if (ret) {
+			id = (__u32)-1;
+			goto cleanup_followup;
+		}
+		id = (__u32)-1;
+
+		followup_success = 1;
+
+	cleanup_followup:
+		if (id != (__u32)-1 && cm_fd >= 0) {
+			destroy_cmd.id = id;
+			ioctl(cm_fd, RDMA_USER_CM_CMD_DESTROY_ID, &destroy_cmd);
+		}
+		if (cm_fd >= 0) {
+			close(cm_fd);
+		}
+	}
+
+	// --- Output Relation Check ---
+	if (source_success && !followup_success) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_8bbf48b4.h b/executor/MRs/syz_mr_8bbf48b4.h
new file mode 100644
index 000000000..60c35331e
--- /dev/null
+++ b/executor/MRs/syz_mr_8bbf48b4.h
@@ -0,0 +1,84 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.79-KVM_CREATE_DEVICE/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Successful device creation implies the device type is supported.
+// - Source input: A call to the `KVM_CREATE_DEVICE` ioctl with a `struct kvm_create_device` where the `type` field is set to a specific device type and the `flags` field is set to 0.
+// - Follow-up input: A call to the `KVM_CREATE_DEVICE` ioctl with a `struct kvm_create_device` where the `type` field is the same as the source input, but the `flags` field is set to `KVM_CREATE_DEVICE_TEST`.
+// - Input relation: The follow-up input is obtained by setting the `KVM_CREATE_DEVICE_TEST` bit in the `flags` field of the source input's `struct kvm_create_device`, keeping the `type` field identical.
+// - Output relation: If the source input call succeeds (returns 0), then the follow-up input call must also succeed (return 0). This is because the `KVM_CREATE_DEVICE_TEST` flag is specified to only test for device type support, and a successful creation inherently proves that the device type is supported.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// MR1: Successful device creation implies the device type is supported.
+// Source input: A call to the KVM_CREATE_DEVICE ioctl with flags = 0.
+// Follow-up input: A call to the KVM_CREATE_DEVICE ioctl with flags = KVM_CREATE_DEVICE_TEST.
+// Input relation: The follow-up input has the KVM_CREATE_DEVICE_TEST flag set, while the type remains the same.
+// Output relation: If the source call succeeds (returns 0), the follow-up call must also succeed (return 0).
+static long syz_mr_8bbf48b4(volatile long a0)
+{
+	// The input parameter a0 represents the device type for KVM_CREATE_DEVICE.
+	uint32_t dev_type = (uint32_t)a0;
+
+	// 1. Perform preparation: Open KVM and create a VM.
+	int kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot open KVM, the test cannot be performed.
+		return -1;
+	}
+
+	int vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		// Cannot create VM, the test cannot be performed.
+		close(kvm_fd);
+		return -1;
+	}
+
+	long res_src = -1;
+	long res_flw = -1;
+	int dev_fd = -1;
+
+	// 2. Get the source output.
+	struct kvm_create_device src_dev = {0};
+	src_dev.type = dev_type;
+	src_dev.flags = 0; // Source input has flags set to 0.
+
+	res_src = ioctl(vm_fd, KVM_CREATE_DEVICE, &src_dev);
+	if (res_src == 0) {
+		// On success, the ioctl returns 0 and fills the fd field.
+		// We must save it for later cleanup.
+		dev_fd = src_dev.fd;
+	}
+
+	// 3. If the source call succeeded, perform the follow-up call.
+	if (res_src == 0) {
+		// The premise of the MR is met.
+		// Get the follow-up output.
+		struct kvm_create_device flw_dev = {0};
+		flw_dev.type = dev_type; // Same type as source.
+		flw_dev.flags = KVM_CREATE_DEVICE_TEST; // Set the TEST flag.
+
+		res_flw = ioctl(vm_fd, KVM_CREATE_DEVICE, &flw_dev);
+
+		// 4. Check the output relation.
+		// If source succeeded (res_src == 0) but follow-up failed (res_flw != 0),
+		// the metamorphic relation is violated.
+		if (res_flw != 0) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+	// 5. Perform cleanup.
+	if (dev_fd != -1) {
+		close(dev_fd);
+	}
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_8ce7d16e.h b/executor/MRs/syz_mr_8ce7d16e.h
new file mode 100644
index 000000000..e42b4f05f
--- /dev/null
+++ b/executor/MRs/syz_mr_8ce7d16e.h
@@ -0,0 +1,117 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/6.6-KVM_CAP_IRQ_MPIC/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Invariance of Failure for Out-of-Bounds MPIC CPU Number
+// - Source input: A `KVM_ENABLE_CAP` ioctl call on a valid vCPU file descriptor. The capability structure is configured with `cap.cap = KVM_CAP_IRQ_MPIC`, `cap.args[0]` set to a valid MPIC device file descriptor, and `cap.args[1]` set to an invalid MPIC CPU number `N` (e.g., a number greater than or equal to the maximum supported VCPUs, or a negative number).
+// - Follow-up input: An identical `KVM_ENABLE_CAP` ioctl call as the source input, but with `cap.args[1]` set to a different invalid MPIC CPU number `M` (e.g., `M = N + 1`).
+// - Input relation: The follow-up input is generated by replacing the out-of-bounds MPIC CPU number `N` in the source input with another out-of-bounds number `M`.
+// - Output relation: Both the source and follow-up ioctl calls should fail. Specifically, both calls should return -1 and set the `errno` to the same error code (e.g., `EINVAL`).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * MR1: Invariance of Failure for Out-of-Bounds MPIC CPU Number
+ *
+ * This metamorphic relation tests that the KVM_ENABLE_CAP ioctl with the
+ * KVM_CAP_IRQ_MPIC capability consistently fails with the same error code when
+ * provided with different out-of-bounds MPIC CPU numbers.
+ *
+ * - Source Input: An ioctl(vcpu_fd, KVM_ENABLE_CAP, &cap) call where
+ *   cap.cap is KVM_CAP_IRQ_MPIC and cap.args[1] is an invalid CPU number N
+ *   (e.g., a value >= the maximum supported VCPUs).
+ *
+ * - Follow-up Input: An identical ioctl call, but with cap.args[1] set to a
+ *   different invalid CPU number M (e.g., N + 1).
+ *
+ * - Output Relation: Both ioctl calls are expected to fail by returning -1,
+ *   and the value of errno should be identical for both calls.
+ */
+static long syz_mr_8ce7d16e(volatile long a0, volatile long a1)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long res1 = 0, res2 = 0;
+	int errno1 = 0, errno2 = 0;
+	int max_vcpus = 0;
+
+	// 1. Preparation: Set up the necessary KVM environment.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is not available, cannot perform the test.
+		return 0;
+	}
+
+	// Check if the KVM_CAP_IRQ_MPIC capability is supported. On success, this
+	// returns the maximum number of VCPUs supported by the in-kernel MPIC.
+	max_vcpus = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_IRQ_MPIC);
+	if (max_vcpus <= 0) {
+		// The capability is not supported, so we cannot perform the test.
+		close(kvm_fd);
+		return 0;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return 0;
+	}
+
+	// Create the in-kernel MPIC (Multi-Processor Interrupt Controller).
+	if (ioctl(vm_fd, KVM_CREATE_IRQCHIP, 0) == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// Create a vCPU to issue the ioctl on.
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// 2. Get the source and follow-up inputs and their corresponding outputs.
+	// Source input: Use an invalid CPU number N >= max_vcpus.
+	long source_cpu_num = max_vcpus;
+	struct kvm_enable_cap cap_source;
+	memset(&cap_source, 0, sizeof(cap_source));
+	cap_source.cap = KVM_CAP_IRQ_MPIC;
+	// Note: For PowerPC's in-kernel MPIC, args[0] must be 0.
+	cap_source.args[0] = 0;
+	cap_source.args[1] = source_cpu_num;
+
+	res1 = ioctl(vcpu_fd, KVM_ENABLE_CAP, &cap_source);
+	errno1 = errno;
+
+	// Follow-up input: Use another invalid CPU number M.
+	long followup_cpu_num = source_cpu_num + 1;
+	struct kvm_enable_cap cap_followup;
+	memset(&cap_followup, 0, sizeof(cap_followup));
+	cap_followup.cap = KVM_CAP_IRQ_MPIC;
+	cap_followup.args[0] = 0;
+	cap_followup.args[1] = followup_cpu_num;
+
+	res2 = ioctl(vcpu_fd, KVM_ENABLE_CAP, &cap_followup);
+	errno2 = errno;
+
+	// 3. Check if the output relation is satisfied.
+	// The relation requires that both calls fail (return -1) and set the same errno.
+	// If this condition is not met, the MR is violated.
+	if (!(res1 == -1 && res2 == -1 && errno1 == errno2)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	// 4. Perform cleanup by closing all opened file descriptors.
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_8dda4646.h b/executor/MRs/syz_mr_8dda4646.h
new file mode 100644
index 000000000..a94b0770e
--- /dev/null
+++ b/executor/MRs/syz_mr_8dda4646.h
@@ -0,0 +1,207 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/3.-Extensions/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Memory access to an address outside of any defined memory slot should cause a VM-exit.
+// - Source input: A KVM virtual machine state where the guest is about to execute an instruction to access a valid guest physical address (GPA). This state includes:
+//     1. A set of configured memory slots, including at least one slot `S` covering the GPA range `[G, G + L)`.
+//     2. The guest's instruction pointer is set to execute a memory access instruction (e.g., `MOV`) targeting a valid GPA `P`, where `G <= P < G + L`.
+// - Follow-up input: A KVM virtual machine state identical to the source input, except for the target address of the memory access instruction. This state includes:
+//     1. The exact same set of configured memory slots as the source input.
+//     2. The guest's instruction pointer is set to execute the same memory access instruction, but targeting an invalid GPA `P'` that does not fall within any configured memory slot (e.g., `P' >= G + L`).
+// - Input relation: The follow-up input is created by modifying the source input's guest state to change the target address of the next memory access instruction from a valid GPA (`P`) to an invalid one (`P'`). All other aspects of the VM, especially the memory slot configuration, remain unchanged.
+// - Output relation: Let `O_S` be the output after executing the source input via `KVM_RUN`, and `O_F` be the output for the follow-up input.
+//     1. The execution of the source input (`O_S`) should not result in a VM-exit caused by a memory fault. The guest instruction should complete successfully (the VM may exit for other reasons later, such as executing HLT).
+//     2. The execution of the follow-up input (`O_F`) must result in an immediate VM-exit with an exit reason indicating a memory mapping fault (e.g., `KVM_EXIT_MMIO` on x86 architectures, or an exit due to an EPT violation).
+//     3. Therefore, the exit reason of `O_S` (if any) must be different from the exit reason of `O_F`, which must specifically indicate a failure to resolve the guest physical address.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Define the guest memory layout.
+// We create one 4KB memory slot starting at GPA 0x1000.
+#define GUEST_MEM_SIZE_8dda4646 0x1000
+#define GUEST_PHYS_ADDR_8dda4646 0x1000
+#define GUEST_CODE_START_8dda4646 0x1000
+
+// The source case will access this valid address within the memory slot.
+#define VALID_TARGET_GPA_8dda4646 0x1500
+// The follow-up case will access this invalid address outside the memory slot.
+#define INVALID_TARGET_GPA_8dda4646 0x3000
+
+static long syz_mr_8dda4646(volatile long a0, volatile long a1)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	size_t mmap_size = 0;
+	struct kvm_run* run = NULL;
+	uint8_t* guest_mem = (uint8_t*)MAP_FAILED;
+	long source_exit_reason = -1;
+	long followup_exit_reason = -1;
+	long ret = 0;
+
+	// Declare all variables at the top to avoid "crosses initialization" errors with goto in C++.
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs;
+	struct kvm_regs initial_regs;
+	uint8_t* code_ptr = NULL;
+
+	// 1. Perform preparation: Set up KVM, VM, VCPU, and a memory region.
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd == -1) {
+		ret = -1;
+		goto cleanup_kvm;
+	}
+
+	guest_mem = (uint8_t*)mmap(NULL, GUEST_MEM_SIZE_8dda4646, PROT_READ | PROT_WRITE,
+				   MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		ret = -1;
+		goto cleanup_vm;
+	}
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = GUEST_PHYS_ADDR_8dda4646;
+	region.memory_size = GUEST_MEM_SIZE_8dda4646;
+	region.userspace_addr = (uint64_t)guest_mem;
+
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		ret = -1;
+		goto cleanup_mem;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)0);
+	if (vcpu_fd == -1) {
+		ret = -1;
+		goto cleanup_mem;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size < sizeof(struct kvm_run)) {
+		ret = -1;
+		goto cleanup_vcpu;
+	}
+
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED,
+				    vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL; // Ensure run is NULL for cleanup check
+		ret = -1;
+		goto cleanup_vcpu;
+	}
+
+	// Prepare a common initial state for the VCPU for both runs.
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+
+	// Set up a basic 32-bit protected mode with a flat memory model.
+	sregs.cr0 |= 1; // Enable protected mode (CR0_PE)
+	sregs.cs.base = 0;
+	sregs.cs.limit = 0xffffffff;
+	sregs.cs.g = 1;
+	sregs.cs.db = 1;
+	sregs.cs.s = 1;
+	sregs.cs.present = 1;
+	sregs.cs.type = 11; // Code: Execute/Read
+
+	sregs.ds.base = 0;
+	sregs.ds.limit = 0xffffffff;
+	sregs.ds.g = 1;
+	sregs.ds.db = 1;
+	sregs.ds.s = 1;
+	sregs.ds.present = 1;
+	sregs.ds.type = 3; // Data: Read/Write
+
+	sregs.es = sregs.ss = sregs.fs = sregs.gs = sregs.ds;
+
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+
+	memset(&initial_regs, 0, sizeof(initial_regs));
+	initial_regs.rip = GUEST_CODE_START_8dda4646;
+	initial_regs.rsp = GUEST_PHYS_ADDR_8dda4646 + GUEST_MEM_SIZE_8dda4646 - 16;
+	initial_regs.rflags = 2; // A mandatory bit must be set.
+
+	// 2. Get the source output.
+	// The source guest will execute: mov [VALID_TARGET_GPA], eax; hlt
+	code_ptr = guest_mem;
+	*code_ptr++ = 0xa3; // mov [imm32], eax
+	*(uint32_t*)code_ptr = (uint32_t)VALID_TARGET_GPA_8dda4646;
+	code_ptr += 4;
+	*code_ptr = 0xf4; // hlt
+
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &initial_regs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+
+	if (ioctl(vcpu_fd, KVM_RUN, 0) != -1) {
+		source_exit_reason = run->exit_reason;
+	}
+
+	// 3. Get the follow-up output.
+	// The follow-up guest will execute: mov [INVALID_TARGET_GPA], eax; hlt
+	code_ptr = guest_mem;
+	*code_ptr++ = 0xa3; // mov [imm32], eax
+	*(uint32_t*)code_ptr = (uint32_t)INVALID_TARGET_GPA_8dda4646;
+	code_ptr += 4;
+	*code_ptr = 0xf4; // hlt
+
+	// Reset VCPU to the same initial state as the source run.
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &initial_regs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+
+	if (ioctl(vcpu_fd, KVM_RUN, 0) != -1) {
+		followup_exit_reason = run->exit_reason;
+	}
+
+	// 4. Check if the output relation is violated.
+	// The source run should not exit due to a memory fault (KVM_EXIT_MMIO).
+	// The follow-up run MUST exit due to a memory fault (KVM_EXIT_MMIO).
+	if (!(source_exit_reason != KVM_EXIT_MMIO &&
+	      followup_exit_reason == KVM_EXIT_MMIO)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+// Cleanup resources in reverse order of allocation.
+cleanup_run:
+	if (run)
+		munmap(run, mmap_size);
+cleanup_vcpu:
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+cleanup_mem:
+	if (guest_mem != MAP_FAILED)
+		munmap(guest_mem, GUEST_MEM_SIZE_8dda4646);
+cleanup_vm:
+	if (vm_fd != -1)
+		close(vm_fd);
+cleanup_kvm:
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_9031a534.h b/executor/MRs/syz_mr_9031a534.h
new file mode 100644
index 000000000..ffc81dbc3
--- /dev/null
+++ b/executor/MRs/syz_mr_9031a534.h
@@ -0,0 +1,104 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/fuse/fuse/Definitions/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Filesystem operations fail after the filesystem daemon is terminated.
+// - Source input: A sequence of two operations: (1) Mount a FUSE filesystem, which is served by a specific filesystem daemon process. (2) Execute a valid filesystem command `C` (e.g., `ls <mountpoint>`, `cat <mountpoint>/file`) that is expected to succeed.
+// - Follow-up input: A sequence of three operations: (1) Mount the same FUSE filesystem, served by the same filesystem daemon process. (2) Terminate the filesystem daemon process. (3) Execute the same filesystem command `C`.
+// - Input relation: The follow-up input is derived from the source input by inserting an operation to terminate the filesystem daemon process between the mount operation and the filesystem command `C`.
+// - Output relation: The source output should indicate the successful execution of command `C`. In contrast, the follow-up output should indicate that the execution of command `C` failed, typically with an I/O error, because the connection to the daemon was broken.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <limits.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/mount.h>
+#include <sys/stat.h>
+#include <sys/wait.h>
+#include <unistd.h>
+
+/**
+ * @brief Implements a metamorphic relation that tests filesystem operation failure after a resource is removed.
+ *
+ * This version avoids external dependencies like FUSE by using a standard tmpfs mount.
+ *
+ * 1. Source Run: Mounts a tmpfs filesystem, creates a file, and successfully `stat`s it.
+ * 2. Follow-up Run: Mounts a tmpfs filesystem, creates a file, unmounts the filesystem,
+ *    and then attempts to `stat` the file, which should fail.
+ *
+ * This tests a principle similar to the original MR (operation fails after daemon termination)
+ * but is achievable with standard syscalls.
+ *
+ * @param a0 A volatile long parameter, unused in this MR but required by the function signature.
+ * @return 0 on completion.
+ */
+static long syz_mr_9031a534(volatile long a0)
+{
+	(void)a0;
+
+	// 1. Preparation: Create a temporary mount point directory.
+	char mnt_template[] = "/tmp/mr1_mnt_XXXXXX";
+	char* mnt_dir = mkdtemp(mnt_template);
+
+	if (!mnt_dir) {
+		perror("[SyzMeta] Preparation failed: mkdtemp");
+		return -1;
+	}
+
+	// Path to the test file within the mounted filesystem.
+	char test_file_path[PATH_MAX];
+	snprintf(test_file_path, sizeof(test_file_path), "%s/testfile", mnt_dir);
+
+	long source_ret = -1;
+	long followup_ret = -1;
+
+	// 2. Source execution: mount, create file, stat (should succeed)
+	if (mount("tmpfs", mnt_dir, "tmpfs", 0, NULL) == 0) {
+		int fd = creat(test_file_path, 0644);
+		if (fd != -1) {
+			close(fd);
+			struct stat st;
+			source_ret = stat(test_file_path, &st);
+		} else {
+			perror("[SyzMeta] Source run creat failed");
+		}
+		// Clean up the source run mount.
+		umount(mnt_dir);
+	} else {
+		perror("[SyzMeta] Source run mount failed");
+	}
+
+	// 3. Follow-up execution: mount, create file, unmount, stat (should fail)
+	if (mount("tmpfs", mnt_dir, "tmpfs", 0, NULL) == 0) {
+		int fd = creat(test_file_path, 0644);
+		if (fd != -1) {
+			close(fd);
+			// Key step of the MR: remove the resource (the filesystem).
+			if (umount(mnt_dir) == 0) {
+				struct stat st;
+				// This stat should fail because the mount point is now just an empty directory.
+				followup_ret = stat(test_file_path, &st);
+			} else {
+				perror("[SyzMeta] Follow-up run umount failed");
+			}
+		} else {
+			perror("[SyzMeta] Follow-up run creat failed");
+			umount(mnt_dir); // Attempt cleanup
+		}
+	} else {
+		perror("[SyzMeta] Follow-up run mount failed");
+	}
+
+	// 4. Check output relation
+	// Source command should succeed (return value 0).
+	// Follow-up command should fail (return value -1).
+	if (source_ret != 0 || followup_ret != -1) {
+		fprintf(stderr, "[SyzMeta]: MR is violated! (source_ret=%ld, followup_ret=%ld)\n", source_ret, followup_ret);
+	}
+
+	// 5. Cleanup
+	rmdir(mnt_dir);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_98ce7ce8.h b/executor/MRs/syz_mr_98ce7ce8.h
new file mode 100644
index 000000000..db5c28953
--- /dev/null
+++ b/executor/MRs/syz_mr_98ce7ce8.h
@@ -0,0 +1,99 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.5-KVM_GET_VCPU_MMAP_SIZE/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Invariance of KVM_GET_VCPU_MMAP_SIZE irrespective of creating new vCPUs
+// - Source input:
+//   1. Open `/dev/kvm` to obtain a system file descriptor `kvm_fd`.
+//   2. Call `ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE)` and record the returned size as `S1`.
+//
+// - Follow-up input:
+//   1. Using the same `kvm_fd`, create a VM via the `KVM_CREATE_VM` ioctl.
+//   2. Create one or more vCPUs for that VM via the `KVM_CREATE_VCPU` ioctl.
+//   3. Call `ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE)` again and record the returned size as `S2`.
+//
+// - Input relation: The follow-up input is derived from the source input by performing state-changing operations (creating a VM and vCPUs) using the same system file descriptor.
+//
+// - Output relation: The returned sizes should be identical, i.e., `S1` must be equal to `S2`.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * MR1: Invariance of KVM_GET_VCPU_MMAP_SIZE irrespective of creating new vCPUs.
+ * This function implements the metamorphic relation described.
+ * It checks if the size returned by KVM_GET_VCPU_MMAP_SIZE remains constant
+ * before and after creating a VM and a vCPU.
+ *
+ * @param a0 This parameter is unused but required to match the syz-meta framework's function signature.
+ * @return 0 on successful execution of the test, -1 on setup or syscall failure.
+ */
+static long syz_mr_98ce7ce8(volatile long a0)
+{
+	long kvm_fd = -1;
+	long vm_fd = -1;
+	long vcpu_fd = -1;
+	long s1 = -1;
+	long s2 = -1;
+
+	// 1. Perform preparation and get the source output.
+	// Open the KVM device.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// If /dev/kvm cannot be opened, we cannot proceed. This is a setup failure, not an MR violation.
+		return -1;
+	}
+
+	// Get the initial VCPU MMAP size.
+	s1 = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0);
+	if (s1 < 0) {
+		// If the ioctl fails, we cannot get the source output.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Derive the follow-up input by changing the system state.
+	// Create a new virtual machine.
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		// Failed to create a VM, cannot proceed.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Create a new vCPU for the VM. The vCPU ID is 0.
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		// Failed to create a vCPU, cannot proceed.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 3. Get the follow-up output.
+	// Get the VCPU MMAP size again after creating the VM and vCPU.
+	s2 = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0);
+	if (s2 < 0) {
+		// If the second ioctl fails, we cannot get the follow-up output.
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Cleanup all opened file descriptors before checking the relation.
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	// 4. Check if the outputs satisfy the output relation.
+	// The size should be invariant.
+	if (s1 != s2) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_99025952.h b/executor/MRs/syz_mr_99025952.h
new file mode 100644
index 000000000..a18584a21
--- /dev/null
+++ b/executor/MRs/syz_mr_99025952.h
@@ -0,0 +1,97 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/fuse/fuse/Filesystem-type/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The 'source' argument for a 'fuse' type mount is not interpreted by the kernel
+// - Source input: A `mount(2)` system call with the filesystem type set to `fuse`. The call has a specific string `S1` for the first argument (`source`), a valid mount point for the `target` argument, and a set of mount options.
+// - Follow-up input: A `mount(2)` system call with the same parameters as the source input, except the `source` argument is changed to a different string `S2`, where `S1` is not equal to `S2`.
+// - Input relation: The follow-up input is generated by replacing the `source` string `S1` in the source `mount` call with a different arbitrary string `S2`. All other parameters (target, filesystem type, mount flags, and data) remain identical.
+// - Output relation: The return code of the `mount` system call for the source input should be identical to the return code for the follow-up input. If the mount fails, the `errno` value should also be the same in both cases.
+
+#include <errno.h>
+#include <stdio.h>
+#include <string.h>
+#include <sys/mount.h>
+#include <sys/stat.h>
+#include <sys/types.h>
+#include <unistd.h>
+
+/**
+ * MR1: The 'source' argument for a 'fuse' type mount is not interpreted by the kernel.
+ * - Source input: A `mount(2)` system call with the filesystem type set to `fuse`.
+ *   The call has a specific string `S1` for the first argument (`source`), a valid
+ *   mount point for the `target` argument, and a set of mount options.
+ * - Follow-up input: A `mount(2)` system call with the same parameters as the source
+ *   input, except the `source` argument is changed to a different string `S2`,
+ *   where `S1` is not equal to `S2`.
+ * - Input relation: The follow-up input is generated by replacing the `source`
+ *   string `S1` in the source `mount` call with a different arbitrary string `S2`.
+ *   All other parameters (target, filesystem type, mount flags, and data) remain identical.
+ * - Output relation: The return code of the `mount` system call for the source input
+ *   should be identical to the return code for the follow-up input. If the mount fails,
+ *   the `errno` value should also be the same in both cases.
+ */
+static long syz_mr_99025952(volatile long source, volatile long target, volatile long mountflags, volatile long data)
+{
+	// Cast inputs to their proper types.
+	const char* p_source1 = (const char*)source;
+	const char* p_target = (const char*)target;
+	unsigned long p_mountflags = (unsigned long)mountflags;
+	const void* p_data = (const void*)data;
+
+	// Prepare the follow-up source string S2, ensuring it's different from S1.
+	const char* p_source2 = "syz_meta_follow_up_source";
+	if (p_source1 != NULL && strcmp(p_source1, p_source2) == 0) {
+		p_source2 = "syz_meta_another_different_source";
+	}
+
+	// Handle the case where the target path is NULL, which would crash mkdir.
+	// In this case, both mount calls should fail identically (e.g., with EFAULT).
+	if (p_target == NULL) {
+		long ret1 = mount(p_source1, p_target, "fuse", p_mountflags, p_data);
+		int errno1 = errno;
+		long ret2 = mount(p_source2, p_target, "fuse", p_mountflags, p_data);
+		int errno2 = errno;
+
+		if (ret1 != ret2 || (ret1 < 0 && errno1 != errno2)) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		}
+		return 0;
+	}
+
+	// 1. Preparation: Create the target directory.
+	int dir_created = 0;
+	if (mkdir(p_target, 0755) == 0) {
+		dir_created = 1;
+	} else if (errno != EEXIST) {
+		// If we can't create the mount point (e.g., permission denied),
+		// we can still proceed. Both mount calls should fail identically.
+	}
+
+	// 2. Get the source output
+	long ret1 = mount(p_source1, p_target, "fuse", p_mountflags, p_data);
+	int errno1 = errno;
+
+	// If the source mount succeeded, unmount it to reset the state for the follow-up call.
+	if (ret1 == 0) {
+		umount(p_target);
+	}
+
+	// 3. Get the follow-up output
+	long ret2 = mount(p_source2, p_target, "fuse", p_mountflags, p_data);
+	int errno2 = errno;
+
+	// 4. Cleanup
+	if (ret2 == 0) {
+		umount(p_target);
+	}
+	if (dir_created) {
+		rmdir(p_target);
+	}
+
+	// 5. Check the output relation
+	if (ret1 != ret2 || (ret1 < 0 && errno1 != errno2)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_9955c520.h b/executor/MRs/syz_mr_9955c520.h
new file mode 100644
index 000000000..c4037c93b
--- /dev/null
+++ b/executor/MRs/syz_mr_9955c520.h
@@ -0,0 +1,263 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_verbs/dev-files/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 9 iterations
+// MR1 (corrected): Character-device naming follows the “infiniband/%k” rule.
+// - Source input: Boot (or hot-plug) a single RDMA HCA that registers one userspace-verbs character device whose kernel name is “uverbs0”.
+// - Follow-up input: Boot (or hot-plug) a second identical HCA so that the kernel now exposes two userspace-verbs character devices whose kernel names are “uverbs0” and “uverbs1”.
+// - Input relation: The follow-up input is obtained by adding one more RDMA device, thereby causing the kernel to create an additional “uverbs*” device whose %k value is “uverbs1”.
+// - Output relation: After udev processing, the set of device nodes under /dev must satisfy:
+//     - Source output: /dev/infiniband/uverbs0 exists.
+//     - Follow-up output: both /dev/infiniband/uverbs0 and /dev/infiniband/uverbs1 exist, and no other new paths are created.
+//   In other words, for every kernel device “uverbsX”, udev must create the node /dev/infiniband/uverbsX, precisely reflecting the rule NAME="infiniband/%k".
+
+#include <dirent.h>
+#include <fcntl.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/stat.h>
+#include <unistd.h>
+
+#define STATE_FILE_9955c520 "/tmp/syz_mr1_state"
+#define IB_DEV_DIR_9955c520 "/dev/infiniband"
+#define UVERBS0_BASENAME_9955c520 "uverbs0"
+#define UVERBS1_BASENAME_9955c520 "uverbs1"
+#define MAX_FILES_9955c520 64
+#define MAX_FILENAME_LEN_9955c520 256
+
+// Helper to allocate a 2D char array on the heap.
+static char** allocate_2d_array_9955c520(int rows, int cols)
+{
+	char** arr = (char**)malloc(rows * sizeof(char*));
+	if (!arr) {
+		perror("[SyzMeta]: malloc for row pointers failed");
+		return NULL;
+	}
+	for (int i = 0; i < rows; i++) {
+		arr[i] = (char*)malloc(cols * sizeof(char));
+		if (!arr[i]) {
+			perror("[SyzMeta]: malloc for column failed");
+			for (int j = 0; j < i; j++) {
+				free(arr[j]);
+			}
+			free(arr);
+			return NULL;
+		}
+	}
+	return arr;
+}
+
+// Helper to free a 2D char array.
+static void free_2d_array_9955c520(char** arr, int rows)
+{
+	if (!arr)
+		return;
+	for (int i = 0; i < rows; i++) {
+		free(arr[i]);
+	}
+	free(arr);
+}
+
+// Helper to check if a path exists and is a character/block/regular device file.
+static int file_exists_9955c520(const char* path)
+{
+	struct stat st;
+	if (stat(path, &st) == 0) {
+		return S_ISCHR(st.st_mode) || S_ISBLK(st.st_mode) || S_ISREG(st.st_mode);
+	}
+	return 0;
+}
+
+// Helper to check if a path exists and is a directory.
+static int dir_exists_9955c520(const char* path)
+{
+	struct stat st;
+	if (stat(path, &st) == 0) {
+		return S_ISDIR(st.st_mode);
+	}
+	return 0;
+}
+
+// Helper to read directory entries into a dynamically allocated list.
+// Returns the number of entries read, or -1 on error.
+static int read_dir_entries_9955c520(const char* path, char** entries)
+{
+	DIR* d;
+	struct dirent* dir;
+	int count = 0;
+
+	d = opendir(path);
+	if (!d) {
+		perror("[SyzMeta]: opendir failed");
+		return -1;
+	}
+
+	while ((dir = readdir(d)) != NULL) {
+		if (strcmp(dir->d_name, ".") == 0 || strcmp(dir->d_name, "..") == 0) {
+			continue;
+		}
+		if (count >= MAX_FILES_9955c520) {
+			fprintf(stderr, "[SyzMeta]: Warning: Too many files in %s, truncating list.\n", path);
+			break;
+		}
+		snprintf(entries[count], MAX_FILENAME_LEN_9955c520, "%.*s", (int)(MAX_FILENAME_LEN_9955c520 - 1), dir->d_name);
+		count++;
+	}
+	closedir(d);
+	return count;
+}
+
+// Helper to compare two lists of strings (as sets).
+// Returns 1 if equal, 0 otherwise.
+static int compare_string_sets_9955c520(char** list1, int count1,
+					char** list2, int count2)
+{
+	if (count1 != count2) {
+		return 0;
+	}
+
+	int matched_in_list2[MAX_FILES_9955c520] = {0};
+
+	for (int i = 0; i < count1; i++) {
+		int found_match = 0;
+		for (int j = 0; j < count2; j++) {
+			if (!matched_in_list2[j] && strcmp(list1[i], list2[j]) == 0) {
+				matched_in_list2[j] = 1;
+				found_match = 1;
+				break;
+			}
+		}
+		if (!found_match) {
+			return 0;
+		}
+	}
+	return 1;
+}
+
+static long syz_mr_9955c520(volatile long a0, volatile long a1)
+{
+	(void)a0;
+	(void)a1;
+
+	if (access(STATE_FILE_9955c520, F_OK) != 0) {
+		// --- Source Run ---
+		if (!dir_exists_9955c520(IB_DEV_DIR_9955c520)) {
+			return -1;
+		}
+		char uverbs0_path[MAX_FILENAME_LEN_9955c520];
+		snprintf(uverbs0_path, sizeof(uverbs0_path), "%s/%s", IB_DEV_DIR_9955c520, UVERBS0_BASENAME_9955c520);
+		if (!file_exists_9955c520(uverbs0_path)) {
+			return -1;
+		}
+
+		char** source_files = allocate_2d_array_9955c520(MAX_FILES_9955c520, MAX_FILENAME_LEN_9955c520);
+		if (!source_files) {
+			return -1;
+		}
+
+		int source_count = read_dir_entries_9955c520(IB_DEV_DIR_9955c520, source_files);
+		if (source_count < 0) {
+			free_2d_array_9955c520(source_files, MAX_FILES_9955c520);
+			return -1;
+		}
+
+		FILE* fp = fopen(STATE_FILE_9955c520, "w");
+		if (!fp) {
+			perror("[SyzMeta]: fopen state file for writing");
+			free_2d_array_9955c520(source_files, MAX_FILES_9955c520);
+			return -1;
+		}
+
+		for (int i = 0; i < source_count; i++) {
+			fprintf(fp, "%s\n", source_files[i]);
+		}
+		fclose(fp);
+		free_2d_array_9955c520(source_files, MAX_FILES_9955c520);
+
+	} else {
+		// --- Follow-up Run ---
+		long ret = 0;
+		char** source_files = NULL;
+		char** followup_files = NULL;
+		char** followup_prime = NULL;
+		FILE* fp = NULL;
+		int source_count = 0;
+		int followup_count = 0;
+		int is_violated = 0;
+
+		char uverbs0_path[MAX_FILENAME_LEN_9955c520];
+		snprintf(uverbs0_path, sizeof(uverbs0_path), "%s/%s", IB_DEV_DIR_9955c520, UVERBS0_BASENAME_9955c520);
+		char uverbs1_path[MAX_FILENAME_LEN_9955c520];
+		snprintf(uverbs1_path, sizeof(uverbs1_path), "%s/%s", IB_DEV_DIR_9955c520, UVERBS1_BASENAME_9955c520);
+
+		if (!dir_exists_9955c520(IB_DEV_DIR_9955c520) || !file_exists_9955c520(uverbs0_path) || !file_exists_9955c520(uverbs1_path)) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+			goto cleanup;
+		}
+
+		source_files = allocate_2d_array_9955c520(MAX_FILES_9955c520, MAX_FILENAME_LEN_9955c520);
+		followup_files = allocate_2d_array_9955c520(MAX_FILES_9955c520, MAX_FILENAME_LEN_9955c520);
+		followup_prime = allocate_2d_array_9955c520(MAX_FILES_9955c520, MAX_FILENAME_LEN_9955c520);
+		if (!source_files || !followup_files || !followup_prime) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		fp = fopen(STATE_FILE_9955c520, "r");
+		if (!fp) {
+			perror("[SyzMeta]: fopen state file for reading");
+			ret = -1;
+			goto cleanup;
+		}
+
+		char line[MAX_FILENAME_LEN_9955c520 + 2];
+		while (source_count < MAX_FILES_9955c520 && fgets(line, sizeof(line), fp)) {
+			line[strcspn(line, "\n")] = 0;
+			if (strlen(line) > 0) {
+				snprintf(source_files[source_count], MAX_FILENAME_LEN_9955c520, "%.*s", (int)(MAX_FILENAME_LEN_9955c520 - 1), line);
+				source_count++;
+			}
+		}
+		fclose(fp);
+		fp = NULL;
+
+		followup_count = read_dir_entries_9955c520(IB_DEV_DIR_9955c520, followup_files);
+		if (followup_count < 0) {
+			ret = -1;
+			goto cleanup;
+		}
+
+		if (followup_count != source_count + 1) {
+			is_violated = 1;
+		} else {
+			int followup_prime_count = 0;
+			for (int i = 0; i < followup_count; i++) {
+				if (strcmp(followup_files[i], UVERBS1_BASENAME_9955c520) != 0) {
+					if (followup_prime_count < MAX_FILES_9955c520) {
+						snprintf(followup_prime[followup_prime_count], MAX_FILENAME_LEN_9955c520, "%.*s", (int)(MAX_FILENAME_LEN_9955c520 - 1), followup_files[i]);
+						followup_prime_count++;
+					}
+				}
+			}
+
+			if (!compare_string_sets_9955c520(source_files, source_count, followup_prime, followup_prime_count)) {
+				is_violated = 1;
+			}
+		}
+
+		if (is_violated) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		}
+
+	cleanup:
+		if (fp)
+			fclose(fp);
+		free_2d_array_9955c520(source_files, MAX_FILES_9955c520);
+		free_2d_array_9955c520(followup_files, MAX_FILES_9955c520);
+		free_2d_array_9955c520(followup_prime, MAX_FILES_9955c520);
+		remove(STATE_FILE_9955c520);
+		return ret;
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_9b9aa407.h b/executor/MRs/syz_mr_9b9aa407.h
new file mode 100644
index 000000000..3e1888180
--- /dev/null
+++ b/executor/MRs/syz_mr_9b9aa407.h
@@ -0,0 +1,234 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/6.7-KVM_CAP_IRQ_XICS/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The order of connecting different vCPUs to an XICS device is commutative.
+// - Source input: A KVM virtual machine with an associated XICS device and at least two vCPUs, let's call them `vcpu_A` and `vcpu_B`. The source input is a sequence of two operations:
+//     1. Connect `vcpu_A` to a specific XICS CPU number `i` using `KVM_CAP_IRQ_XICS`.
+//     2. Connect `vcpu_B` to a different XICS CPU number `j` using `KVM_CAP_IRQ_XICS`.
+// - Follow-up input: The same initial KVM virtual machine and XICS device. The follow-up input is a sequence of the same two operations but in a different order:
+//     1. Connect `vcpu_B` to the XICS CPU number `j`.
+//     2. Connect `vcpu_A` to the XICS CPU number `i`.
+// - Input relation: The follow-up input is a permutation of the sequence of connection operations performed in the source input.
+// - Output relation: The final state of the virtual machine after the source sequence of operations should be identical to the final state after the follow-up sequence. This includes the success of all individual connection operations and the subsequent behavior of the guest when interrupts are injected to either vCPU.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdbool.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Define KVM_DEV_XICS_GRP_CPU_NODES if not already defined by the headers
+#ifndef KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407
+#define KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407 1
+#endif
+
+static long syz_mr_9b9aa407(volatile long vcpu_id_A, volatile long vcpu_id_B)
+{
+	// The MR tests connecting two *different* vCPUs.
+	if (vcpu_id_A == vcpu_id_B) {
+		return 0;
+	}
+
+	// Define distinct XICS CPU numbers for the two vCPUs.
+	const int xics_cpu_i = 0;
+	const int xics_cpu_j = 1;
+
+	bool source_ok = false;
+	bool followup_ok = false;
+
+	// --- Source Execution: Connect A, then B ---
+	{
+		int kvm_fd = -1, vm_fd = -1, xics_fd = -1, vcpu_fd_A = -1, vcpu_fd_B = -1;
+		long ret;
+		// Declare all variables at the top of the block to avoid "crosses initialization" errors.
+		struct kvm_create_device dev;
+		struct kvm_device_attr attr_set_A, attr_set_B, attr_get_A, attr_get_B;
+		long res_set_A, res_set_B;
+		int read_vcpu_fd_A = -1, read_vcpu_fd_B = -1;
+
+		kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+		if (kvm_fd == -1)
+			goto cleanup_source;
+
+		ret = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_IRQ_XICS);
+		if (ret <= 0) // XICS not supported
+			goto cleanup_source;
+
+		vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+		if (vm_fd == -1)
+			goto cleanup_source;
+
+		dev = (struct kvm_create_device){
+		    .type = KVM_DEV_TYPE_XICS,
+		    .fd = 0,
+		    .flags = 0,
+		};
+		if (ioctl(vm_fd, KVM_CREATE_DEVICE, &dev) == -1)
+			goto cleanup_source;
+		xics_fd = dev.fd;
+
+		vcpu_fd_A = ioctl(vm_fd, KVM_CREATE_VCPU, vcpu_id_A);
+		if (vcpu_fd_A == -1)
+			goto cleanup_source;
+
+		vcpu_fd_B = ioctl(vm_fd, KVM_CREATE_VCPU, vcpu_id_B);
+		if (vcpu_fd_B == -1)
+			goto cleanup_source;
+
+		// 1. Connect vCPU A to XICS CPU i
+		attr_set_A = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_i,
+		    .addr = (uint64_t)&vcpu_fd_A,
+		};
+		res_set_A = ioctl(xics_fd, KVM_SET_DEVICE_ATTR, &attr_set_A);
+
+		// 2. Connect vCPU B to XICS CPU j
+		attr_set_B = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_j,
+		    .addr = (uint64_t)&vcpu_fd_B,
+		};
+		res_set_B = ioctl(xics_fd, KVM_SET_DEVICE_ATTR, &attr_set_B);
+
+		// Verify final state
+		attr_get_A = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_i,
+		    .addr = (uint64_t)&read_vcpu_fd_A,
+		};
+		ioctl(xics_fd, KVM_GET_DEVICE_ATTR, &attr_get_A);
+
+		attr_get_B = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_j,
+		    .addr = (uint64_t)&read_vcpu_fd_B,
+		};
+		ioctl(xics_fd, KVM_GET_DEVICE_ATTR, &attr_get_B);
+
+		if (res_set_A == 0 && res_set_B == 0 &&
+		    read_vcpu_fd_A == vcpu_fd_A && read_vcpu_fd_B == vcpu_fd_B) {
+			source_ok = true;
+		}
+
+	cleanup_source:
+		if (vcpu_fd_B != -1)
+			close(vcpu_fd_B);
+		if (vcpu_fd_A != -1)
+			close(vcpu_fd_A);
+		if (xics_fd != -1)
+			close(xics_fd);
+		if (vm_fd != -1)
+			close(vm_fd);
+		if (kvm_fd != -1)
+			close(kvm_fd);
+	}
+
+	// --- Follow-up Execution: Connect B, then A ---
+	{
+		int kvm_fd = -1, vm_fd = -1, xics_fd = -1, vcpu_fd_A = -1, vcpu_fd_B = -1;
+		long ret;
+		// Declare all variables at the top of the block.
+		struct kvm_create_device dev;
+		struct kvm_device_attr attr_set_A, attr_set_B, attr_get_A, attr_get_B;
+		long res_set_A, res_set_B;
+		int read_vcpu_fd_A = -1, read_vcpu_fd_B = -1;
+
+		kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+		if (kvm_fd == -1)
+			goto cleanup_followup;
+
+		ret = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_IRQ_XICS);
+		if (ret <= 0)
+			goto cleanup_followup;
+
+		vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+		if (vm_fd == -1)
+			goto cleanup_followup;
+
+		dev = (struct kvm_create_device){
+		    .type = KVM_DEV_TYPE_XICS,
+		    .fd = 0,
+		    .flags = 0,
+		};
+		if (ioctl(vm_fd, KVM_CREATE_DEVICE, &dev) == -1)
+			goto cleanup_followup;
+		xics_fd = dev.fd;
+
+		vcpu_fd_A = ioctl(vm_fd, KVM_CREATE_VCPU, vcpu_id_A);
+		if (vcpu_fd_A == -1)
+			goto cleanup_followup;
+
+		vcpu_fd_B = ioctl(vm_fd, KVM_CREATE_VCPU, vcpu_id_B);
+		if (vcpu_fd_B == -1)
+			goto cleanup_followup;
+
+		// 1. Connect vCPU B to XICS CPU j
+		attr_set_B = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_j,
+		    .addr = (uint64_t)&vcpu_fd_B,
+		};
+		res_set_B = ioctl(xics_fd, KVM_SET_DEVICE_ATTR, &attr_set_B);
+
+		// 2. Connect vCPU A to XICS CPU i
+		attr_set_A = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_i,
+		    .addr = (uint64_t)&vcpu_fd_A,
+		};
+		res_set_A = ioctl(xics_fd, KVM_SET_DEVICE_ATTR, &attr_set_A);
+
+		// Verify final state
+		attr_get_A = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_i,
+		    .addr = (uint64_t)&read_vcpu_fd_A,
+		};
+		ioctl(xics_fd, KVM_GET_DEVICE_ATTR, &attr_get_A);
+
+		attr_get_B = (struct kvm_device_attr){
+		    .flags = 0,
+		    .group = KVM_DEV_XICS_GRP_CPU_NODES_9b9aa407,
+		    .attr = (uint64_t)xics_cpu_j,
+		    .addr = (uint64_t)&read_vcpu_fd_B,
+		};
+		ioctl(xics_fd, KVM_GET_DEVICE_ATTR, &attr_get_B);
+
+		if (res_set_A == 0 && res_set_B == 0 &&
+		    read_vcpu_fd_A == vcpu_fd_A && read_vcpu_fd_B == vcpu_fd_B) {
+			followup_ok = true;
+		}
+
+	cleanup_followup:
+		if (vcpu_fd_B != -1)
+			close(vcpu_fd_B);
+		if (vcpu_fd_A != -1)
+			close(vcpu_fd_A);
+		if (xics_fd != -1)
+			close(xics_fd);
+		if (vm_fd != -1)
+			close(vm_fd);
+		if (kvm_fd != -1)
+			close(kvm_fd);
+	}
+
+	// --- Output Relation Check ---
+	// The outcome of both sequences should be the same. If one succeeded
+	// and the other failed, the commutative property is violated.
+	if (source_ok != followup_ok) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_9f95d9e3.h b/executor/MRs/syz_mr_9f95d9e3.h
new file mode 100644
index 000000000..f8a180f06
--- /dev/null
+++ b/executor/MRs/syz_mr_9f95d9e3.h
@@ -0,0 +1,190 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.128-KVM_XEN_VCPU_SET_ATTR/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Additive property of KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST
+// - Source input: A sequence of two `KVM_XEN_VCPU_SET_ATTR` ioctl calls executed on the same vCPU.
+//     1. The first call uses a `struct kvm_xen_vcpu_attr` with `type` set to `KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST`. Its `u.runstate` member contains a set of valid adjustment values, let's call it `A1`. The constraint `A1.state_entry_time = A1.time_running + A1.time_runnable + A1.time_blocked + A1.time_offline` must be satisfied.
+//     2. The second call also uses `type = KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST` with another set of valid adjustment values, `A2`, which also satisfies the time summation constraint.
+// - Follow-up input: A single `KVM_XEN_VCPU_SET_ATTR` ioctl call executed on an identical initial vCPU state.
+//     1. The call uses a `struct kvm_xen_vcpu_attr` with `type` set to `KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST`. Its `u.runstate` member, `A_sum`, is the component-wise sum of `A1` and `A2` (e.g., `A_sum.time_running = A1.time_running + A2.time_running`). The `state` field of `A_sum` is set to the value from `A2.state`, as it is the last state applied in the source sequence.
+// - Input relation: The follow-up input is a single adjustment operation whose parameters are the sum of the parameters of the two adjustment operations in the source input. This is based on the principle that two consecutive additions can be combined into a single addition.
+// - Output relation: The final runstate of the vCPU after the source sequence of two ioctl calls should be identical to the final runstate of the vCPU after the single follow-up ioctl call. Additionally, all ioctl calls in both sequences should return 0, indicating success.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * @brief Helper function to create a KVM VM and a vCPU with Xen capabilities enabled.
+ *
+ * This function encapsulates the boilerplate for setting up a KVM virtual machine
+ * and a virtual CPU, which is required for both the source and follow-up test cases.
+ * It ensures that Xen-specific ioctls can be used on the resulting vCPU.
+ *
+ * @param kvm_fd A file descriptor for the /dev/kvm device.
+ * @param vm_fd_out A pointer to an integer where the created VM file descriptor will be stored.
+ * @return The file descriptor for the created vCPU on success, or -1 on failure.
+ */
+static int create_xen_vcpu_9f95d9e3(int kvm_fd, int* vm_fd_out)
+{
+	int vm_fd, vcpu_fd;
+	struct kvm_xen_hvm_config cfg = {0};
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd < 0) {
+		return -1;
+	}
+
+	if (ioctl(vm_fd, KVM_XEN_HVM_CONFIG, &cfg) < 0) {
+		close(vm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)0);
+	if (vcpu_fd < 0) {
+		close(vm_fd);
+		return -1;
+	}
+
+	*vm_fd_out = vm_fd;
+	return vcpu_fd;
+}
+
+/**
+ * @brief Metamorphic relation implementation for the additive property of KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST.
+ *
+ * This function tests the property that two consecutive runstate adjustments on a vCPU
+ * are equivalent to a single adjustment that is the component-wise sum of the two.
+ * To fit within syzkaller's 9-argument limit, the two 32-bit 'state' parameters are packed into the first 64-bit argument 'a0'.
+ *
+ * @param a0 Packed states: lower 32 bits for the first adjustment (A1), upper 32 bits for the second (A2).
+ * @param a1-a4 Parameters for A1: time_running, time_runnable, time_blocked, time_offline.
+ * @param a5-a8 Parameters for A2: time_running, time_runnable, time_blocked, time_offline.
+ * @return 0 on completion.
+ */
+static long syz_mr_9f95d9e3(volatile long a0, volatile long a1, volatile long a2, volatile long a3, volatile long a4,
+			    volatile long a5, volatile long a6, volatile long a7, volatile long a8)
+{
+	int kvm_fd, vm_fd_src, vcpu_fd_src, vm_fd_fup, vcpu_fd_fup;
+	long ret_src1, ret_src2, ret_fup, ret_get_src, ret_get_fup;
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot perform the test if KVM is not available.
+		return -1;
+	}
+
+	// 1. Preparation: Create two identical vCPUs for source and follow-up tests.
+	vm_fd_src = -1;
+	vcpu_fd_src = create_xen_vcpu_9f95d9e3(kvm_fd, &vm_fd_src);
+	if (vcpu_fd_src < 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	vm_fd_fup = -1;
+	vcpu_fd_fup = create_xen_vcpu_9f95d9e3(kvm_fd, &vm_fd_fup);
+	if (vcpu_fd_fup < 0) {
+		close(vcpu_fd_src);
+		close(vm_fd_src);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Unpack the state values from the first argument.
+	uint32_t state1 = (uint32_t)a0;
+	uint32_t state2 = (uint32_t)(a0 >> 32);
+
+	// 2. Source Execution: Apply two consecutive adjustments.
+	struct kvm_xen_vcpu_attr attr_src1 = {
+	    .type = KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST,
+	};
+	attr_src1.u.runstate.state = state1;
+	attr_src1.u.runstate.time_running = (uint64_t)a1;
+	attr_src1.u.runstate.time_runnable = (uint64_t)a2;
+	attr_src1.u.runstate.time_blocked = (uint64_t)a3;
+	attr_src1.u.runstate.time_offline = (uint64_t)a4;
+	attr_src1.u.runstate.state_entry_time = attr_src1.u.runstate.time_running +
+						attr_src1.u.runstate.time_runnable +
+						attr_src1.u.runstate.time_blocked +
+						attr_src1.u.runstate.time_offline;
+	ret_src1 = ioctl(vcpu_fd_src, KVM_XEN_VCPU_SET_ATTR, &attr_src1);
+
+	struct kvm_xen_vcpu_attr attr_src2 = {
+	    .type = KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST,
+	};
+	attr_src2.u.runstate.state = state2;
+	attr_src2.u.runstate.time_running = (uint64_t)a5;
+	attr_src2.u.runstate.time_runnable = (uint64_t)a6;
+	attr_src2.u.runstate.time_blocked = (uint64_t)a7;
+	attr_src2.u.runstate.time_offline = (uint64_t)a8;
+	attr_src2.u.runstate.state_entry_time = attr_src2.u.runstate.time_running +
+						attr_src2.u.runstate.time_runnable +
+						attr_src2.u.runstate.time_blocked +
+						attr_src2.u.runstate.time_offline;
+	ret_src2 = ioctl(vcpu_fd_src, KVM_XEN_VCPU_SET_ATTR, &attr_src2);
+
+	// Get the source output (final runstate).
+	struct kvm_xen_vcpu_attr attr_out_src = {
+	    .type = KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_DATA,
+	};
+	ret_get_src = ioctl(vcpu_fd_src, KVM_XEN_VCPU_GET_ATTR, &attr_out_src);
+
+	// 3. Follow-up Execution: Apply a single combined adjustment.
+	struct kvm_xen_vcpu_attr attr_fup = {
+	    .type = KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_ADJUST,
+	};
+	// Per MR, state is from the second operation, times are summed.
+	attr_fup.u.runstate.state = state2;
+	attr_fup.u.runstate.time_running = (uint64_t)a1 + (uint64_t)a5;
+	attr_fup.u.runstate.time_runnable = (uint64_t)a2 + (uint64_t)a6;
+	attr_fup.u.runstate.time_blocked = (uint64_t)a3 + (uint64_t)a7;
+	attr_fup.u.runstate.time_offline = (uint64_t)a4 + (uint64_t)a8;
+	attr_fup.u.runstate.state_entry_time = attr_fup.u.runstate.time_running +
+					       attr_fup.u.runstate.time_runnable +
+					       attr_fup.u.runstate.time_blocked +
+					       attr_fup.u.runstate.time_offline;
+	ret_fup = ioctl(vcpu_fd_fup, KVM_XEN_VCPU_SET_ATTR, &attr_fup);
+
+	// Get the follow-up output (final runstate).
+	struct kvm_xen_vcpu_attr attr_out_fup = {
+	    .type = KVM_XEN_VCPU_ATTR_TYPE_RUNSTATE_DATA,
+	};
+	ret_get_fup = ioctl(vcpu_fd_fup, KVM_XEN_VCPU_GET_ATTR, &attr_out_fup);
+
+	// 4. Output Relation Check.
+	int violation = 0;
+	// Condition 1: All SET ioctl calls should return 0.
+	if (ret_src1 != 0 || ret_src2 != 0 || ret_fup != 0) {
+		violation = 1;
+	}
+
+	// Condition 2: Final runstates should be identical.
+	// This can only be checked if we successfully retrieved both states.
+	if (ret_get_src == 0 && ret_get_fup == 0) {
+		if (memcmp(&attr_out_src.u.runstate, &attr_out_fup.u.runstate, sizeof(attr_out_src.u.runstate)) != 0) {
+			violation = 1;
+		}
+	} else {
+		// Cannot verify the final state, but this is an issue with observation,
+		// not necessarily a violation of the MR's logic. We proceed without flagging.
+	}
+
+	if (violation) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	// 5. Cleanup.
+	close(vcpu_fd_fup);
+	close(vm_fd_fup);
+	close(vcpu_fd_src);
+	close(vm_fd_src);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_a1912f81.h b/executor/MRs/syz_mr_a1912f81.h
new file mode 100644
index 000000000..ece619e3d
--- /dev/null
+++ b/executor/MRs/syz_mr_a1912f81.h
@@ -0,0 +1,79 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.56-KVM_GET_TSC_KHZ/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The TSC frequency of a vCPU should be stable across consecutive queries.
+// - Source input: A KVM virtual machine with a created vCPU. The operation is to call the `KVM_GET_TSC_KHZ` ioctl on the vCPU's file descriptor.
+// - Follow-up input: The same KVM virtual machine and vCPU in the same state as the source input. The operation is to call the `KVM_GET_TSC_KHZ` ioctl again on the same vCPU's file descriptor.
+// - Input relation: The follow-up input is generated by re-executing the `KVM_GET_TSC_KHZ` ioctl immediately after the source execution, with no intervening operations that would alter the vCPU's underlying hardware configuration (e.g., VM migration, CPU model changes).
+// - Output relation: The return value from the source ioctl call must be identical to the return value from the follow-up ioctl call. This holds true for both successful returns (the TSC frequency in KHz) and error returns (e.g., -EIO).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * MR1: The TSC frequency of a vCPU should be stable across consecutive queries.
+ * - Source input: A KVM virtual machine with a created vCPU. The operation is to call the `KVM_GET_TSC_KHZ` ioctl on the vCPU's file descriptor.
+ * - Follow-up input: The same KVM virtual machine and vCPU in the same state as the source input. The operation is to call the `KVM_GET_TSC_KHZ` ioctl again on the same vCPU's file descriptor.
+ * - Input relation: The follow-up input is generated by re-executing the `KVM_GET_TSC_KHZ` ioctl immediately after the source execution, with no intervening operations that would alter the vCPU's underlying hardware configuration.
+ * - Output relation: The return value from the source ioctl call must be identical to the return value from the follow-up ioctl call.
+ *
+ * @param a0 This parameter is a placeholder to match the required function signature and is not used in this specific MR.
+ * @return 0 on successful execution of the test logic, -1 on setup failure.
+ */
+static long syz_mr_a1912f81(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long source_output, follow_up_output;
+
+	// 1. Perform preparation: Set up KVM, VM, and vCPU.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// KVM might not be available, cannot perform the test.
+		// This is a setup failure, not a violation.
+		return -1;
+	}
+
+	int ret = ioctl(kvm_fd, KVM_GET_API_VERSION, NULL);
+	if (ret != KVM_API_VERSION) {
+		// API version mismatch, cannot proceed.
+		close(kvm_fd);
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get the source output by calling KVM_GET_TSC_KHZ.
+	source_output = ioctl(vcpu_fd, KVM_GET_TSC_KHZ, NULL);
+
+	// 3. Get the follow-up output by calling KVM_GET_TSC_KHZ again immediately.
+	follow_up_output = ioctl(vcpu_fd, KVM_GET_TSC_KHZ, NULL);
+
+	// 4. Check the output relation.
+	// The return values (both success and error codes) should be identical.
+	if (source_output != follow_up_output) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	// 5. Cleanup: Close all file descriptors.
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_a421a672.h b/executor/MRs/syz_mr_a421a672.h
new file mode 100644
index 000000000..7b6a74166
--- /dev/null
+++ b/executor/MRs/syz_mr_a421a672.h
@@ -0,0 +1,118 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.98-KVM_CREATE_SPAPR_TCE_64/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: The ioctl must reject any non-zero value in the reserved `flags` field.
+// - Source input: A `struct kvm_create_spapr_tce_64` with the `flags` field set to 0. All other fields (`liobn`, `page_shift`, `offset`, `size`) are set with values that constitute an otherwise valid request.
+// - Follow-up input: The same `struct kvm_create_spapr_tce_64` as the source input, but with the `flags` field set to a non-zero value (e.g., 1).
+// - Input relation: The follow-up input is obtained by changing only the `flags` field of the source input from 0 to a non-zero value.
+// - Output relation: The ioctl call with the source input must succeed and return a valid file descriptor. The ioctl call with the follow-up input must fail with the error code `-EINVAL`.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <linux/types.h> // For __u32, __u64 types
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// The syzkaller build environment can be complex. Sometimes the ioctl macro
+// (KVM_CREATE_SPAPR_TCE_64) is defined by the build system or headers, but the
+// associated struct (kvm_create_spapr_tce_64) is not, leading to an
+// "incomplete type" error.
+// To fix this robustly, we define the struct unconditionally. This ensures
+// the compiler always knows its layout.
+// We then guard the ioctl macro definition with #ifndef to avoid a "redefined"
+// error in cases where the build environment does provide it.
+
+struct kvm_create_spapr_tce_64 {
+	__u64 liobn;
+	__u32 page_shift;
+	__u32 flags;
+	__u64 offset;
+	__u64 size;
+};
+
+#ifndef KVM_CREATE_SPAPR_TCE_64
+#define KVM_CREATE_SPAPR_TCE_64 _IOWR(0xAE, 0xb8, struct kvm_create_spapr_tce_64)
+#endif
+
+// MR: The ioctl KVM_CREATE_SPAPR_TCE_64 must reject any non-zero value in the reserved `flags` field.
+static long syz_mr_a421a672(volatile long liobn, volatile long page_shift, volatile long offset, volatile long size)
+{
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	long src_res = -1;
+	long follow_up_res = -1;
+	int src_errno, follow_up_errno;
+
+	// 1. Perform preparation.
+	// We need a KVM VM file descriptor to issue the ioctl.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot open KVM, so we cannot perform the test.
+		return -1;
+	}
+
+	// This ioctl is specific to the SPAPR machine type (PowerPC).
+	// Check if the host supports this capability before proceeding.
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_SPAPR_TCE_64) <= 0) {
+		close(kvm_fd);
+		// The capability is not supported, so the MR is not applicable.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Prepare and execute the source call.
+	struct kvm_create_spapr_tce_64 src_tce;
+	memset(&src_tce, 0, sizeof(src_tce));
+	src_tce.liobn = (__u64)liobn;
+	src_tce.page_shift = (__u32)page_shift;
+	src_tce.offset = (__u64)offset;
+	src_tce.size = (__u64)size;
+	src_tce.flags = 0; // Source input has flags = 0.
+
+	src_res = ioctl(vm_fd, KVM_CREATE_SPAPR_TCE_64, &src_tce);
+	src_errno = errno;
+
+	// The MR is only applicable if the source call with valid flags (0) succeeds.
+	// If it fails, the other parameters were likely invalid, so we cannot test the relation.
+	if (src_res < 0) {
+		goto cleanup;
+	}
+
+	// 3. Prepare and execute the follow-up call based on the input relation.
+	struct kvm_create_spapr_tce_64 follow_up_tce;
+	memcpy(&follow_up_tce, &src_tce, sizeof(follow_up_tce));
+	follow_up_tce.flags = 1; // Follow-up input has a non-zero flags value.
+
+	follow_up_res = ioctl(vm_fd, KVM_CREATE_SPAPR_TCE_64, &follow_up_tce);
+	follow_up_errno = errno;
+
+	// 4. Check if the outputs satisfy the output relation.
+	// Output relation: Source call succeeds (src_res >= 0), and follow-up call fails with -EINVAL.
+	if (follow_up_res != -1 || follow_up_errno != EINVAL) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 5. Perform cleanup.
+	if (src_res >= 0) {
+		// The source call returned a valid file descriptor that must be closed.
+		close(src_res);
+	}
+	if (vm_fd >= 0) {
+		close(vm_fd);
+	}
+	if (kvm_fd >= 0) {
+		close(kvm_fd);
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_a92dfc4f.h b/executor/MRs/syz_mr_a92dfc4f.h
new file mode 100644
index 000000000..40294fb1f
--- /dev/null
+++ b/executor/MRs/syz_mr_a92dfc4f.h
@@ -0,0 +1,94 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.30-KVM_SET_CLOCK/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The clock value obtained after setting it should be greater than or equal to the set value.
+// - Source input: A `KVM_SET_CLOCK` ioctl call on a VM with a `kvm_clock_data` structure whose `clock` field is set to a valid timestamp value `C`.
+// - Follow-up input: A `KVM_GET_CLOCK` ioctl call on the same VM.
+// - Input relation: The follow-up `KVM_GET_CLOCK` call is performed immediately after the source `KVM_SET_CLOCK` call successfully completes.
+// - Output relation: Let the return value of the source operation be `ret_S` and the return value of the follow-up operation be `ret_F`. Let the timestamp value obtained from the follow-up operation be `C'`. The following relations should hold:
+//   1. `ret_S` should be 0 (success).
+//   2. `ret_F` should be 0 (success).
+//   3. `C'` should be greater than or equal to `C` (`C' >= C`).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Implements the metamorphic relation:
+ * MR1: The clock value obtained after setting it should be greater than or equal to the set value.
+ *
+ * @param a0 Represents the timestamp value 'C' to be set for the KVM guest clock.
+ * @return 0 on completion.
+ */
+static long syz_mr_a92dfc4f(volatile long a0)
+{
+	// --- Preparation ---
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	long ret_S = -1;
+	long ret_F = -1;
+
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// If KVM is not available, we cannot test the relation.
+		return -1;
+	}
+
+	// Create a Virtual Machine (VM)
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		// If VM creation fails, we cannot test the relation.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// --- Source Operation ---
+	// Prepare the clock data structure for setting the clock
+	struct kvm_clock_data clock_data_set = {0};
+	clock_data_set.clock = a0; // 'C' from the source input
+
+	// Perform the KVM_SET_CLOCK ioctl call
+	ret_S = ioctl(vm_fd, KVM_SET_CLOCK, &clock_data_set);
+
+	// --- Follow-up Operation ---
+	// Prepare the clock data structure for getting the clock
+	struct kvm_clock_data clock_data_get = {0};
+
+	// The MR is only applicable if the source operation succeeds.
+	if (ret_S == 0) {
+		// Perform the KVM_GET_CLOCK ioctl call immediately after
+		ret_F = ioctl(vm_fd, KVM_GET_CLOCK, &clock_data_get);
+	}
+
+	// --- Check Output Relation ---
+	// 1. ret_S should be 0 (success).
+	if (ret_S != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	} else {
+		// 2. ret_F should be 0 (success).
+		if (ret_F != 0) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+		// 3. C' should be greater than or equal to C (C' >= C).
+		else if (clock_data_get.clock < clock_data_set.clock) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+	// --- Cleanup ---
+	if (vm_fd != -1) {
+		close(vm_fd);
+	}
+	if (kvm_fd != -1) {
+		close(kvm_fd);
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_b0377842.h b/executor/MRs/syz_mr_b0377842.h
new file mode 100644
index 000000000..12baf304e
--- /dev/null
+++ b/executor/MRs/syz_mr_b0377842.h
@@ -0,0 +1,121 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.109-KVM_PPC_GET_CPU_CHAR/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: The host CPU characteristics reported by KVM_PPC_GET_CPU_CHAR are independent of the specific VM instance.
+// - Source input: A KVM VM instance `VM_A`, on which the `KVM_PPC_GET_CPU_CHAR` ioctl is invoked.
+// - Follow-up input: A new KVM VM instance `VM_B`, created independently of `VM_A` on the same host system, on which the `KVM_PPC_GET_CPU_CHAR` ioctl is invoked.
+// - Input relation: The follow-up input is generated by creating a new, distinct VM instance on the same host and invoking the same ioctl. The state and configuration of the source VM (`VM_A`) do not influence the creation or state of the follow-up VM (`VM_B`).
+// - Output relation: The `struct kvm_ppc_cpu_char` returned from the source ioctl call on `VM_A` must be identical to the `struct kvm_ppc_cpu_char` returned from the follow-up ioctl call on `VM_B`. All corresponding fields (`character`, `behaviour`, `character_mask`, `behaviour_mask`) in the two output structures must be equal.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h> // For uint32_t
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// The KVM_PPC_GET_CPU_CHAR ioctl and its associated struct kvm_ppc_cpu_char
+// are specific to the PowerPC architecture. When compiling on a different
+// architecture (like x86_64), the linux/kvm.h header does not define this
+// structure, leading to an "incomplete type" compilation error.
+// To fix this, we provide the definition of the struct here. This ensures
+// the code compiles on non-PPC hosts while not conflicting with the header
+// definition when compiled on a PPC host (as syzkaller's build system
+// typically prevents redefinitions).
+struct kvm_ppc_cpu_char {
+	uint32_t character;
+	uint32_t behaviour;
+	uint32_t character_mask;
+	uint32_t behaviour_mask;
+	uint32_t flags;
+	uint32_t reserved[7];
+};
+
+// This function implements the metamorphic relation MR1.
+// MR1: The host CPU characteristics reported by KVM_PPC_GET_CPU_CHAR are independent of the specific VM instance.
+// The function will:
+// 1. Open the KVM device.
+// 2. Check if the required KVM capability for PowerPC CPU characteristics is present.
+// 3. Create a first KVM virtual machine (VM_A).
+// 4. Call the KVM_PPC_GET_CPU_CHAR ioctl on VM_A to get the source output.
+// 5. Create a second, independent KVM virtual machine (VM_B).
+// 6. Call the KVM_PPC_GET_CPU_CHAR ioctl on VM_B to get the follow-up output.
+// 7. Compare the structures returned from both calls.
+// 8. If they are not identical, it indicates a violation of the metamorphic relation, and a message is printed.
+// 9. Clean up all resources (close file descriptors).
+static long syz_mr_b0377842(volatile long a0)
+{
+	// The input parameter a0 is unused in this MR implementation,
+	// but is kept to conform to the required function signature.
+	(void)a0;
+
+	int kvm_fd = -1;
+	int vm_fd_A = -1;
+	int vm_fd_B = -1;
+	long ret = 0;
+
+	// 1. Preliminary preparation: Open KVM and check for capabilities.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is not available. The test is not applicable.
+		return 0;
+	}
+
+	// This MR is specific to the PowerPC architecture and requires the
+	// KVM_CAP_PPC_GET_CPU_CHAR capability.
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_PPC_GET_CPU_CHAR) <= 0) {
+		// The capability is not supported. The test is not applicable.
+		close(kvm_fd);
+		return 0;
+	}
+
+	// 2. Get the source output from the first VM instance (VM_A).
+	vm_fd_A = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd_A == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	struct kvm_ppc_cpu_char char_A;
+	// Zero-initialize the struct for safety, although the kernel should fill it completely.
+	memset(&char_A, 0, sizeof(char_A));
+	if (ioctl(vm_fd_A, KVM_PPC_GET_CPU_CHAR, &char_A) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output from a new VM instance (VM_B).
+	vm_fd_B = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd_B == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	struct kvm_ppc_cpu_char char_B;
+	memset(&char_B, 0, sizeof(char_B));
+	if (ioctl(vm_fd_B, KVM_PPC_GET_CPU_CHAR, &char_B) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 4. Check if the outputs satisfy the output relation.
+	// The output relation requires the entire kvm_ppc_cpu_char structure
+	// to be identical for both VM instances.
+	if (memcmp(&char_A, &char_B, sizeof(struct kvm_ppc_cpu_char)) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	// 5. Perform cleanup.
+	if (vm_fd_A != -1)
+		close(vm_fd_A);
+	if (vm_fd_B != -1)
+		close(vm_fd_B);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_b49fee5b.h b/executor/MRs/syz_mr_b49fee5b.h
new file mode 100644
index 000000000..f5e3d01cf
--- /dev/null
+++ b/executor/MRs/syz_mr_b49fee5b.h
@@ -0,0 +1,394 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/7.17-KVM_CAP_EXCEPTION_PAYLOAD/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Toggling KVM_CAP_EXCEPTION_PAYLOAD changes how L2 guest debug exception details are delivered to the L1 hypervisor.
+// - Source input: A sequence of operations to set up and run a nested guest (L2) that triggers a debug exception (#DB), with the `KVM_CAP_EXCEPTION_PAYLOAD` capability disabled. This includes:
+//   1. Creating a VM and VCPU with nested virtualization enabled.
+//   2. Ensuring `KVM_CAP_EXCEPTION_PAYLOAD` is disabled (or left as default).
+//   3. Running an L1 hypervisor that configures its L2 guest and intercepts #DB exceptions.
+//   4. Triggering a #DB exception within the L2 guest (e.g., via a hardware breakpoint).
+// - Follow-up input: The same sequence of operations as the source input, but with the `KVM_CAP_EXCEPTION_PAYLOAD` capability enabled for the VM.
+// - Input relation: The follow-up input is obtained by enabling the `KVM_CAP_EXCEPTION_PAYLOAD` capability on the VM used in the source input. All other setup and guest execution steps remain identical.
+// - Output relation: When the #DB exception in L2 causes an emulated VM-exit to L1:
+//   1. In the source output (capability disabled), the VCPU's `DR6` register, as observed by L1, will be directly modified by KVM to reflect the status of the debug event.
+//   2. In the follow-up output (capability enabled), the `DR6` register will remain unmodified from its pre-exception state. The debug event information will instead be delivered via the `exception_payload` field in the `kvm_vcpu_events` structure, with the `exception.has_payload` flag set.
+
+#include <asm/vmx.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdbool.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// VMCS Field Encodings from asm/vmx.h
+#define GUEST_ES_SELECTOR_b49fee5b 0x00000800
+#define GUEST_CS_SELECTOR_b49fee5b 0x00000802
+#define GUEST_SS_SELECTOR_b49fee5b 0x00000804
+#define GUEST_DS_SELECTOR_b49fee5b 0x00000806
+#define GUEST_FS_SELECTOR_b49fee5b 0x00000808
+#define GUEST_GS_SELECTOR_b49fee5b 0x0000080a
+#define GUEST_LDTR_SELECTOR_b49fee5b 0x0000080c
+#define GUEST_TR_SELECTOR_b49fee5b 0x0000080e
+#define HOST_ES_SELECTOR_b49fee5b 0x00000c00
+#define HOST_CS_SELECTOR_b49fee5b 0x00000c02
+#define HOST_SS_SELECTOR_b49fee5b 0x00000c04
+#define HOST_DS_SELECTOR_b49fee5b 0x00000c06
+#define HOST_FS_SELECTOR_b49fee5b 0x00000c08
+#define HOST_GS_SELECTOR_b49fee5b 0x00000c0a
+#define HOST_TR_SELECTOR_b49fee5b 0x00000c0c
+#define GUEST_ES_LIMIT_b49fee5b 0x00004800
+#define GUEST_CS_LIMIT_b49fee5b 0x00004802
+#define GUEST_SS_LIMIT_b49fee5b 0x00004804
+#define GUEST_DS_LIMIT_b49fee5b 0x00004806
+#define GUEST_FS_LIMIT_b49fee5b 0x00004808
+#define GUEST_GS_LIMIT_b49fee5b 0x0000480a
+#define GUEST_LDTR_LIMIT_b49fee5b 0x0000480c
+#define GUEST_TR_LIMIT_b49fee5b 0x0000480e
+#define GUEST_GDTR_LIMIT_b49fee5b 0x00004810
+#define GUEST_IDTR_LIMIT_b49fee5b 0x00004812
+#define GUEST_ES_AR_BYTES_b49fee5b 0x00004814
+#define GUEST_CS_AR_BYTES_b49fee5b 0x00004816
+#define GUEST_SS_AR_BYTES_b49fee5b 0x00004818
+#define GUEST_DS_AR_BYTES_b49fee5b 0x0000481a
+#define GUEST_FS_AR_BYTES_b49fee5b 0x0000481c
+#define GUEST_GS_AR_BYTES_b49fee5b 0x0000481e
+#define GUEST_LDTR_AR_BYTES_b49fee5b 0x00004820
+#define GUEST_TR_AR_BYTES_b49fee5b 0x00004822
+#define PIN_BASED_VM_EXEC_CONTROL_b49fee5b 0x00004000
+#define CPU_BASED_VM_EXEC_CONTROL_b49fee5b 0x00004002
+#define EXCEPTION_BITMAP_b49fee5b 0x00004004
+#define VM_EXIT_CONTROLS_b49fee5b 0x0000400c
+#define VM_ENTRY_CONTROLS_b49fee5b 0x00004012
+#define GUEST_ES_BASE_b49fee5b 0x00006800
+#define GUEST_CS_BASE_b49fee5b 0x00006802
+#define GUEST_SS_BASE_b49fee5b 0x00006804
+#define GUEST_DS_BASE_b49fee5b 0x00006806
+#define GUEST_FS_BASE_b49fee5b 0x00006808
+#define GUEST_GS_BASE_b49fee5b 0x0000680a
+#define GUEST_LDTR_BASE_b49fee5b 0x0000680c
+#define GUEST_TR_BASE_b49fee5b 0x0000680e
+#define GUEST_GDTR_BASE_b49fee5b 0x00006810
+#define GUEST_IDTR_BASE_b49fee5b 0x00006812
+#define GUEST_CR0_b49fee5b 0x00006800
+#define GUEST_CR3_b49fee5b 0x00006802
+#define GUEST_CR4_b49fee5b 0x00006804
+#define GUEST_DR0_b49fee5b 0x0000681a
+#define GUEST_DR6_b49fee5b 0x0000681a
+#define GUEST_DR7_b49fee5b 0x0000681c
+#define HOST_CR0_b49fee5b 0x00006c00
+#define HOST_CR3_b49fee5b 0x00006c02
+#define HOST_CR4_b49fee5b 0x00006c04
+#define HOST_RIP_b49fee5b 0x00006c16
+#define GUEST_RFLAGS_b49fee5b 0x00006820
+#define GUEST_RIP_b49fee5b 0x0000681e
+
+#define GUEST_MEM_SIZE_b49fee5b (16 * 4096)
+#define PAGE_SIZE_b49fee5b 4096
+
+// Guest memory layout
+#define L1_PML4_ADDR_b49fee5b 0x1000
+#define L1_PDP_ADDR_b49fee5b 0x2000
+#define L1_PD_ADDR_b49fee5b 0x3000
+#define L1_PT_ADDR_b49fee5b 0x4000
+#define L1_GDT_ADDR_b49fee5b 0x5000
+#define VMXON_ADDR_b49fee5b 0x6000
+#define VMCS_ADDR_b49fee5b 0x7000
+#define L1_CODE_ADDR_b49fee5b 0x8000
+#define L2_CODE_ADDR_b49fee5b 0x9000
+#define L1_STACK_ADDR_b49fee5b 0xa000
+#define L1_EXIT_HANDLER_ADDR_b49fee5b 0xb000
+
+#define L1_GDT_ENTRY_CS_b49fee5b 1
+#define L1_GDT_ENTRY_DS_b49fee5b 2
+
+#define INITIAL_DR6_b49fee5b 0xffff0ff0
+
+// L2 guest code: a single NOP which will trigger the breakpoint
+static const uint8_t l2_guest_code_b49fee5b[] = {0x90}; // nop
+
+// L1 hypervisor code (assembly)
+static const uint8_t l1_hypervisor_code_b49fee5b[] = {
+    // Enable VMX
+    0xf3, 0x0f, 0xa7, 0x3f, // vmxon [rdi]
+    // Clear and load VMCS
+    0x66, 0x0f, 0xc7, 0x36, // vmclear [rsi]
+    0x0f, 0xc7, 0x36, // vmptrld [rsi]
+
+#define VMWRITE_b49fee5b(field, value)                                                                                                    \
+	0x48, 0xc7, 0xc0, (field) & 0xff, ((field) >> 8) & 0xff, ((field) >> 16) & 0xff, ((field) >> 24) & 0xff, /* mov rax, field */     \
+	    0x48, 0xc7, 0xc2, (value) & 0xff, ((value) >> 8) & 0xff, ((value) >> 16) & 0xff, ((value) >> 24) & 0xff, /* mov rdx, value */ \
+	    0x0f, 0x79, 0xd0 /* vmwrite rdx, rax */
+
+    // Setup Host state for L1
+    VMWRITE_b49fee5b(HOST_CS_SELECTOR_b49fee5b, L1_GDT_ENTRY_CS_b49fee5b * 8),
+    VMWRITE_b49fee5b(HOST_DS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8),
+    VMWRITE_b49fee5b(HOST_ES_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8),
+    VMWRITE_b49fee5b(HOST_SS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8),
+    VMWRITE_b49fee5b(HOST_FS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8),
+    VMWRITE_b49fee5b(HOST_GS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8),
+    0x49, 0x89, 0xcb, // mov r11, r9
+    0x48, 0xc7, 0xc0, 0x16, 0x6c, 0x00, 0x00, // mov rax, HOST_RIP
+    0x4c, 0x89, 0xda, // mov rdx, r11
+    0x0f, 0x79, 0xd0, // vmwrite rdx, rax
+    VMWRITE_b49fee5b(HOST_CR0_b49fee5b, 0x60050033),
+    VMWRITE_b49fee5b(HOST_CR3_b49fee5b, L1_PML4_ADDR_b49fee5b),
+    VMWRITE_b49fee5b(HOST_CR4_b49fee5b, 0x2620),
+
+    // Setup Guest state for L2
+    VMWRITE_b49fee5b(GUEST_CS_SELECTOR_b49fee5b, L1_GDT_ENTRY_CS_b49fee5b * 8),
+    VMWRITE_b49fee5b(GUEST_CS_BASE_b49fee5b, 0),
+    VMWRITE_b49fee5b(GUEST_CS_LIMIT_b49fee5b, 0xffffffff),
+    VMWRITE_b49fee5b(GUEST_CS_AR_BYTES_b49fee5b, 0xa09b),
+    VMWRITE_b49fee5b(GUEST_DS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8),
+    VMWRITE_b49fee5b(GUEST_DS_BASE_b49fee5b, 0),
+    VMWRITE_b49fee5b(GUEST_DS_LIMIT_b49fee5b, 0xffffffff),
+    VMWRITE_b49fee5b(GUEST_DS_AR_BYTES_b49fee5b, 0xc093),
+    VMWRITE_b49fee5b(GUEST_ES_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8), VMWRITE_b49fee5b(GUEST_ES_BASE_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_ES_LIMIT_b49fee5b, 0xffffffff), VMWRITE_b49fee5b(GUEST_ES_AR_BYTES_b49fee5b, 0xc093),
+    VMWRITE_b49fee5b(GUEST_SS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8), VMWRITE_b49fee5b(GUEST_SS_BASE_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_SS_LIMIT_b49fee5b, 0xffffffff), VMWRITE_b49fee5b(GUEST_SS_AR_BYTES_b49fee5b, 0xc093),
+    VMWRITE_b49fee5b(GUEST_FS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8), VMWRITE_b49fee5b(GUEST_FS_BASE_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_FS_LIMIT_b49fee5b, 0xffffffff), VMWRITE_b49fee5b(GUEST_FS_AR_BYTES_b49fee5b, 0xc093),
+    VMWRITE_b49fee5b(GUEST_GS_SELECTOR_b49fee5b, L1_GDT_ENTRY_DS_b49fee5b * 8), VMWRITE_b49fee5b(GUEST_GS_BASE_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_GS_LIMIT_b49fee5b, 0xffffffff), VMWRITE_b49fee5b(GUEST_GS_AR_BYTES_b49fee5b, 0xc093),
+    VMWRITE_b49fee5b(GUEST_LDTR_SELECTOR_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_LDTR_BASE_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_LDTR_LIMIT_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_LDTR_AR_BYTES_b49fee5b, 0x10000),
+    VMWRITE_b49fee5b(GUEST_TR_SELECTOR_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_TR_BASE_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_TR_LIMIT_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_TR_AR_BYTES_b49fee5b, 0x8b),
+    VMWRITE_b49fee5b(GUEST_GDTR_BASE_b49fee5b, L1_GDT_ADDR_b49fee5b), VMWRITE_b49fee5b(GUEST_GDTR_LIMIT_b49fee5b, 3 * 8 - 1),
+    VMWRITE_b49fee5b(GUEST_IDTR_BASE_b49fee5b, 0), VMWRITE_b49fee5b(GUEST_IDTR_LIMIT_b49fee5b, 0xffff),
+
+    VMWRITE_b49fee5b(GUEST_CR0_b49fee5b, 0x80050031),
+    VMWRITE_b49fee5b(GUEST_CR3_b49fee5b, L1_PML4_ADDR_b49fee5b),
+    VMWRITE_b49fee5b(GUEST_CR4_b49fee5b, 0x2000),
+    VMWRITE_b49fee5b(GUEST_RFLAGS_b49fee5b, 0x2),
+    0x49, 0x89, 0xc3, // mov r11, r8
+    0x48, 0xc7, 0xc0, 0x1e, 0x68, 0x00, 0x00, // mov rax, GUEST_RIP
+    0x4c, 0x89, 0xda, // mov rdx, r11
+    0x0f, 0x79, 0xd0, // vmwrite rdx, rax
+
+    // Setup debug registers for #DB trap
+    0x48, 0xc7, 0xc0, 0x1a, 0x68, 0x00, 0x00, // mov rax, GUEST_DR0
+    0x4c, 0x89, 0xc2, // mov rdx, r8 (L2_CODE_ADDR)
+    0x0f, 0x79, 0xd0, // vmwrite rdx, rax
+    VMWRITE_b49fee5b(GUEST_DR7_b49fee5b, 0x401),
+    VMWRITE_b49fee5b(GUEST_DR6_b49fee5b, INITIAL_DR6_b49fee5b),
+    0x48, 0xc7, 0xc1, INITIAL_DR6_b49fee5b & 0xff, (INITIAL_DR6_b49fee5b >> 8) & 0xff, (INITIAL_DR6_b49fee5b >> 16) & 0xff, (INITIAL_DR6_b49fee5b >> 24) & 0xff, // mov rcx, INITIAL_DR6
+
+    // Setup VM-execution controls
+    VMWRITE_b49fee5b(CPU_BASED_VM_EXEC_CONTROL_b49fee5b, 0x80000000),
+    VMWRITE_b49fee5b(PIN_BASED_VM_EXEC_CONTROL_b49fee5b, 0),
+    VMWRITE_b49fee5b(VM_EXIT_CONTROLS_b49fee5b, 0x200000),
+    VMWRITE_b49fee5b(VM_ENTRY_CONTROLS_b49fee5b, 0),
+    VMWRITE_b49fee5b(EXCEPTION_BITMAP_b49fee5b, 1 << 1),
+
+    // Launch L2
+    0x0f, 0x01, 0xc2, // vmlaunch
+    0xeb, 0xfe, // jmp .
+
+    // L1 exit handler code
+    0x48, 0xc7, 0xc0, 0x1a, 0x68, 0x00, 0x00, // mov rax, GUEST_DR6
+    0x48, 0x31, 0xd2, // xor rdx, rdx
+    0x0f, 0x78, 0xd0, // vmread rdx, rax
+    0x48, 0x39, 0xca, // cmp rdx, rcx
+    0x74, 0x05, // je unchanged
+    0x48, 0xc7, 0xc0, 0x01, 0x00, 0x00, 0x00, // mov rax, 1 (DR6 changed)
+    0xeb, 0x03, // jmp report
+    // unchanged:
+    0x48, 0xc7, 0xc0, 0x02, 0x00, 0x00, 0x00, // mov rax, 2 (DR6 unchanged)
+    // report:
+    0x0f, 0x01, 0xc1, // vmcall
+    0xf4, // hlt
+};
+
+static void setup_pml4_b49fee5b(uint8_t* mem)
+{
+	uint64_t* pml4 = (uint64_t*)(mem + L1_PML4_ADDR_b49fee5b);
+	uint64_t* pdp = (uint64_t*)(mem + L1_PDP_ADDR_b49fee5b);
+	uint64_t* pd = (uint64_t*)(mem + L1_PD_ADDR_b49fee5b);
+	pml4[0] = L1_PDP_ADDR_b49fee5b | 0x3;
+	pdp[0] = L1_PD_ADDR_b49fee5b | 0x3;
+	pd[0] = L1_PT_ADDR_b49fee5b | 0x3;
+	for (int i = 0; i < 512; ++i) {
+		uint64_t* pt = (uint64_t*)(mem + L1_PT_ADDR_b49fee5b);
+		pt[i] = (i * PAGE_SIZE_b49fee5b) | 0x3;
+	}
+}
+
+static void setup_gdt_b49fee5b(uint8_t* mem)
+{
+	uint64_t* gdt = (uint64_t*)(mem + L1_GDT_ADDR_b49fee5b);
+	gdt[0] = 0;
+	gdt[L1_GDT_ENTRY_CS_b49fee5b] = 0x00209b0000000000;
+	gdt[L1_GDT_ENTRY_DS_b49fee5b] = 0x00c0930000000000;
+}
+
+struct run_result_b49fee5b {
+	long rax;
+	bool has_payload;
+};
+
+static int run_test_case_b49fee5b(int vcpu_fd, int vm_fd, bool enable_payload)
+{
+	struct kvm_regs regs;
+	struct kvm_sregs sregs;
+	struct kvm_run* run;
+
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) < 0)
+		return -1;
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs) < 0)
+		return -1;
+
+	sregs.cr0 = 0x60050033;
+	sregs.cr3 = L1_PML4_ADDR_b49fee5b;
+	sregs.cr4 = 0x2620;
+	sregs.efer = 0xd01;
+	sregs.cs.selector = L1_GDT_ENTRY_CS_b49fee5b * 8;
+	sregs.cs.base = 0;
+	sregs.cs.limit = 0xffffffff;
+	sregs.cs.g = 1;
+	sregs.cs.l = 1;
+	sregs.ds.selector = L1_GDT_ENTRY_DS_b49fee5b * 8;
+	sregs.ds.base = 0;
+	sregs.ds.limit = 0xffffffff;
+	sregs.ds.g = 1;
+	sregs.es = sregs.fs = sregs.gs = sregs.ss = sregs.ds;
+	sregs.gdt.base = L1_GDT_ADDR_b49fee5b;
+	sregs.gdt.limit = 3 * 8 - 1;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) < 0)
+		return -1;
+
+	regs.rip = L1_CODE_ADDR_b49fee5b;
+	regs.rsp = L1_STACK_ADDR_b49fee5b + PAGE_SIZE_b49fee5b - 8;
+	regs.rflags = 2;
+	regs.rdi = VMXON_ADDR_b49fee5b;
+	regs.rsi = VMCS_ADDR_b49fee5b;
+	regs.r8 = L2_CODE_ADDR_b49fee5b;
+	regs.r9 = L1_EXIT_HANDLER_ADDR_b49fee5b;
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) < 0)
+		return -1;
+
+	if (enable_payload) {
+		struct kvm_enable_cap cap;
+		memset(&cap, 0, sizeof(cap));
+		cap.cap = KVM_CAP_EXCEPTION_PAYLOAD;
+		if (ioctl(vm_fd, KVM_ENABLE_CAP, &cap) < 0)
+			return -1;
+	}
+
+	run = (struct kvm_run*)mmap(NULL, 0x1000, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED)
+		return -1;
+
+	int ret = ioctl(vcpu_fd, KVM_RUN, NULL);
+	munmap(run, 0x1000);
+	return ret;
+}
+
+static long syz_mr_b49fee5b(volatile long a0, volatile long a1)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	uint8_t* mem = NULL;
+	struct run_result_b49fee5b source_res, follow_up_res;
+	uint32_t vmcs_rev_id;
+	bool source_ok, followup_ok;
+	struct {
+		struct kvm_msrs header;
+		struct kvm_msr_entry entry;
+	} msrs_to_get;
+	struct kvm_userspace_memory_region region;
+
+	memset(&source_res, 0, sizeof(source_res));
+	memset(&follow_up_res, 0, sizeof(follow_up_res));
+
+	kvm_fd = open("/dev/kvm", O_RDWR);
+	if (kvm_fd < 0)
+		return -1;
+
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_NESTED_STATE) <= 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	mem = (uint8_t*)mmap(NULL, GUEST_MEM_SIZE_b49fee5b, PROT_READ | PROT_WRITE, MAP_SHARED | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		mem = NULL;
+		goto cleanup;
+	}
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = GUEST_MEM_SIZE_b49fee5b;
+	region.userspace_addr = (uint64_t)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0)
+		goto cleanup;
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		vcpu_fd = -1;
+		goto cleanup;
+	}
+
+	msrs_to_get.header.nmsrs = 1;
+	msrs_to_get.entry.index = MSR_IA32_VMX_BASIC;
+	if (ioctl(vcpu_fd, KVM_GET_MSRS, &msrs_to_get) < 0)
+		goto cleanup_vcpu;
+
+	vmcs_rev_id = (uint32_t)msrs_to_get.entry.data;
+	*(uint32_t*)(mem + VMXON_ADDR_b49fee5b) = vmcs_rev_id;
+	*(uint32_t*)(mem + VMCS_ADDR_b49fee5b) = vmcs_rev_id;
+
+	setup_pml4_b49fee5b(mem);
+	setup_gdt_b49fee5b(mem);
+	memcpy(mem + L1_CODE_ADDR_b49fee5b, l1_hypervisor_code_b49fee5b, sizeof(l1_hypervisor_code_b49fee5b));
+	memcpy(mem + L1_EXIT_HANDLER_ADDR_b49fee5b, l1_hypervisor_code_b49fee5b + 246, 38);
+	memcpy(mem + L2_CODE_ADDR_b49fee5b, l2_guest_code_b49fee5b, sizeof(l2_guest_code_b49fee5b));
+
+	// --- Source Run ---
+	if (run_test_case_b49fee5b(vcpu_fd, vm_fd, false) == 0) {
+		struct kvm_regs regs;
+		if (ioctl(vcpu_fd, KVM_GET_REGS, &regs) == 0) {
+			source_res.rax = regs.rax;
+		}
+	}
+
+	// --- Follow-up Run ---
+	if (run_test_case_b49fee5b(vcpu_fd, vm_fd, true) == 0) {
+		struct kvm_regs regs;
+		struct kvm_vcpu_events events;
+		if (ioctl(vcpu_fd, KVM_GET_REGS, &regs) == 0) {
+			follow_up_res.rax = regs.rax;
+		}
+		if (ioctl(vcpu_fd, KVM_GET_VCPU_EVENTS, &events) == 0) {
+			follow_up_res.has_payload = (events.flags & KVM_VCPUEVENT_VALID_PAYLOAD);
+		}
+	}
+
+	source_ok = (source_res.rax == 1);
+	followup_ok = (follow_up_res.rax == 2 && follow_up_res.has_payload);
+
+	if (!source_ok || !followup_ok) {
+		fprintf(stderr, "[SyzMeta]: MR is violated! "
+				"Source(rax:%ld) Follow-up(rax:%ld, payload:%d)\n",
+			source_res.rax, follow_up_res.rax, follow_up_res.has_payload);
+	}
+
+cleanup_vcpu:
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+cleanup:
+	if (mem)
+		munmap(mem, GUEST_MEM_SIZE_b49fee5b);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_b4dad044.h b/executor/MRs/syz_mr_b4dad044.h
new file mode 100644
index 000000000..08152b3ac
--- /dev/null
+++ b/executor/MRs/syz_mr_b4dad044.h
@@ -0,0 +1,210 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_mad/Sending-MADs/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 6 iterations
+// MR-payload-independence: The data payload of a Management Datagram (MAD) does not determine the success or failure of an independent write transaction.
+// - Source sequence:
+//     1. `fd₁ = open("/dev/infiniband/rdma_umad", O_RDWR)`
+//     2. `ioctl(fd₁, UMAD_REGISTER_AGENT, …)` to get `agent₁`.
+//     3. Build `buf₁` containing a MAD with a header (using `agent₁`, a fixed destination LID, etc.) and a data payload `P_src` of length `L`.
+//     4. Perform `write(fd₁, buf₁, sizeof(hdr)+L)`, resulting in `ret₁` and `err₁`.
+//     5. `close(fd₁)`.
+// - Follow-up sequence:
+//     6. `fd₂ = open("/dev/infiniband/rdma_umad", O_RDWR)`
+//     7. `ioctl(fd₂, UMAD_REGISTER_AGENT, …)` to get `agent₂`.
+//     8. Build `buf₂` identical to `buf₁` except that its `hdr.id` is `agent₂` and its data payload is `P_flw` (where `|P_flw}| = L` and `P_flw ≠ P_src`).
+//     9. Perform `write(fd₂, buf₂, sizeof(hdr)+L)`, resulting in `ret₂` and `err₂`.
+//     10. `close(fd₂)`.
+// - Input relation: The follow-up sequence uses a buffer (`buf₂`) that is identical to the source buffer (`buf₁`) in all header fields except for the session-specific `agent_id`, and has a different data payload of the same length.
+// - Output relation:
+//     1. The outcomes of the two `write()` calls must be in the same class: one succeeds if and only if the other succeeds. (Success is defined as returning the number of bytes written; failure is defined as returning a negative value).
+//     2. If both writes succeed, any MAD later received at the destination that matches the non-ID header fields of `buf₁` must carry the payload `P_src`, and any that matches `buf₂` must carry `P_flw`.
+
+#include <endian.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// --- Start of definitions from infiniband headers ---
+// These definitions are copied here to remove the dependency on libibumad-dev
+// and make the code self-contained for the syzkaller executor.
+// The conflicting typedefs for __u8, __u16 etc. have been removed as they are
+// provided by the syzkaller build environment.
+
+// From <infiniband/verbs.h>
+#define IB_MGMT_CLASS_SUBN_LID_ROUTED_b4dad044 0x03
+
+// From <infiniband/mad.h>
+#define UMAD_ATTR_NODE_DESC_b4dad044 0x0010
+
+// From <infiniband/umad.h>
+#define UMAD_METHOD_GET_b4dad044 0x01
+
+struct umad_reg_b4dad044 {
+	__u32 id;
+	__u32 flags;
+	__u64 oui;
+	__u8 mgmt_class;
+	__u8 mgmt_class_version;
+	__u8 reserved[6];
+};
+
+struct umad_hdr_b4dad044 {
+	__u8 base_version;
+	__u8 mgmt_class;
+	__u8 class_version;
+	__u8 method;
+	__be16 status;
+	__be16 class_specific;
+	__be64 tid;
+	__be16 attr_id;
+	__be16 resv;
+	__be32 attr_mod;
+	__u32 id;
+	__u32 qkey;
+	__u32 qpn;
+	__be16 lid;
+	__u8 sl;
+	__u8 path_bits;
+	__u8 grh_present;
+	__u8 gid_index;
+	__u8 hop_limit;
+	__u8 traffic_class;
+	__u8 gid[16];
+	__u8 reserved[4];
+	__be16 pkey_index;
+	__be16 rlid;
+};
+
+#define UMAD_REGISTER_AGENT_b4dad044 _IOWR('u', 1, struct umad_reg_b4dad044)
+
+// --- End of definitions from infiniband headers ---
+
+#define PAYLOAD_LEN_b4dad044 64
+#define BUF_SIZE_b4dad044 (sizeof(struct umad_hdr_b4dad044) + PAYLOAD_LEN_b4dad044)
+#define DEV_PATH_b4dad044 "/dev/infiniband/rdma_umad"
+
+static long syz_mr_b4dad044(volatile long a0, volatile long a1)
+{
+	// Declare all variables at the top of the function to avoid "crosses initialization" errors with goto.
+	int fd1 = -1, fd2 = -1;
+	long ret1 = -1, ret2 = -1;
+	char buf1[BUF_SIZE_b4dad044];
+	char buf2[BUF_SIZE_b4dad044];
+	struct umad_hdr_b4dad044* hdr1 = NULL;
+	struct umad_hdr_b4dad044* hdr2 = NULL;
+	struct umad_reg_b4dad044 reg;
+	long agent1_id = -1;
+	long agent2_id = -1;
+	int success1 = 0;
+	int success2 = 0;
+
+	// Input relation: payloads P_src and P_flw must be different.
+	// We use a0 and a1 to generate payloads. If they are the same, we cannot test.
+	if (a0 == a1) {
+		return 0;
+	}
+
+	// Preliminary check: device existence. Silently skip if not present.
+	if (access(DEV_PATH_b4dad044, F_OK) != 0) {
+		return 0;
+	}
+
+	// --- Source Sequence ---
+
+	// 1. fd₁ = open("/dev/infiniband/rdma_umad", O_RDWR)
+	fd1 = open(DEV_PATH_b4dad044, O_RDWR);
+	if (fd1 < 0) {
+		goto cleanup;
+	}
+
+	// 2. ioctl(fd₁, UMAD_REGISTER_AGENT, …) to get agent₁
+	memset(&reg, 0, sizeof(reg));
+	reg.mgmt_class = IB_MGMT_CLASS_SUBN_LID_ROUTED_b4dad044;
+	reg.mgmt_class_version = 1;
+	if (ioctl(fd1, UMAD_REGISTER_AGENT_b4dad044, &reg) < 0) {
+		goto cleanup;
+	}
+	agent1_id = reg.id;
+
+	// 3. Build buf₁ with payload P_src
+	hdr1 = (struct umad_hdr_b4dad044*)buf1;
+	memset(buf1, 0, BUF_SIZE_b4dad044);
+	hdr1->id = agent1_id;
+	hdr1->method = UMAD_METHOD_GET_b4dad044;
+	hdr1->attr_id = htobe16(UMAD_ATTR_NODE_DESC_b4dad044);
+	hdr1->mgmt_class = IB_MGMT_CLASS_SUBN_LID_ROUTED_b4dad044;
+	hdr1->base_version = 1;
+	hdr1->class_version = 1;
+	hdr1->tid = htobe64(0xdeadbeefcafebabe);
+	hdr1->rlid = htobe16(1); // Destination LID 1 (e.g., Subnet Manager)
+	memset(buf1 + sizeof(struct umad_hdr_b4dad044), (int)a0, PAYLOAD_LEN_b4dad044);
+
+	// 4. write(fd₁, buf₁, ...)
+	ret1 = write(fd1, buf1, BUF_SIZE_b4dad044);
+
+	// 5. close(fd₁)
+	close(fd1);
+	fd1 = -1;
+
+	// --- Follow-up Sequence ---
+
+	// 6. fd₂ = open("/dev/infiniband/rdma_umad", O_RDWR)
+	fd2 = open(DEV_PATH_b4dad044, O_RDWR);
+	if (fd2 < 0) {
+		goto cleanup;
+	}
+
+	// 7. ioctl(fd₂, UMAD_REGISTER_AGENT, …) to get agent₂
+	memset(&reg, 0, sizeof(reg));
+	reg.mgmt_class = IB_MGMT_CLASS_SUBN_LID_ROUTED_b4dad044;
+	reg.mgmt_class_version = 1;
+	if (ioctl(fd2, UMAD_REGISTER_AGENT_b4dad044, &reg) < 0) {
+		goto cleanup;
+	}
+	agent2_id = reg.id;
+
+	// 8. Build buf₂ with payload P_flw
+	memcpy(buf2, buf1, BUF_SIZE_b4dad044);
+	hdr2 = (struct umad_hdr_b4dad044*)buf2;
+	hdr2->id = agent2_id; // Update agent_id
+	memset(buf2 + sizeof(struct umad_hdr_b4dad044), (int)a1, PAYLOAD_LEN_b4dad044);
+
+	// 9. write(fd₂, buf₂, ...)
+	ret2 = write(fd2, buf2, BUF_SIZE_b4dad044);
+
+	// 10. close(fd₂)
+	close(fd2);
+	fd2 = -1;
+
+	// --- Output Relation Check ---
+
+	// 1. The outcomes of the two write() calls must be in the same class.
+	success1 = (ret1 >= 0);
+	success2 = (ret2 >= 0);
+
+	if (success1 != success2) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 2. Payload integrity check at the receiver.
+	// This part of the MR is not implemented as it requires a separate listening
+	// entity on the destination, which is beyond the scope of a single,
+	// self-contained test function. The primary testable hypothesis here is that
+	// the local `write` syscall's success or failure is independent of the
+	// data payload, which is covered by the check above.
+
+cleanup:
+	if (fd1 != -1) {
+		close(fd1);
+	}
+	if (fd2 != -1) {
+		close(fd2);
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_b65b85a4.h b/executor/MRs/syz_mr_b65b85a4.h
new file mode 100644
index 000000000..a828e4508
--- /dev/null
+++ b/executor/MRs/syz_mr_b65b85a4.h
@@ -0,0 +1,167 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.62-KVM_CREATE_SPAPR_TCE/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 8 iterations
+// MR1: The observable size of the created TCE table equals the exact number of 64-bit entries implied by window_size, and scales linearly.
+// - Source input: A call to the `KVM_CREATE_SPAPR_TCE` ioctl with parameters `liobn = L1` and `window_size = S`, where `S` is a positive multiple of 4096.
+// - Follow-up input: A call to the `KVM_CREATE_SPAPR_TCE` ioctl with parameters `liobn = L2` and `window_size = k * S`, where `L2` is a different logical IO bus number from `L1`, and `k` is a positive integer.
+// - Input relation: The follow-up input uses a `window_size` that is an integer multiple (`k`) of the source input's `window_size`, and a different `liobn` to create a distinct table.
+// - Output relation: Both ioctl calls should succeed and return valid file descriptors, `fd_src` and `fd_follow`. Let `size_src` and `size_follow` be the sizes reported by `fstat()` on `fd_src` and `fd_follow` respectively. The following relations must hold true:
+//     1. `size_src == (S / 4096) * 8`
+//     2. `size_follow == (k * S / 4096) * 8`
+//     3. `size_follow == k * size_src
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h> // For KVM ioctls and structs
+#include <linux/types.h> // For __u32, __u64
+#include <stdio.h>
+#include <stdlib.h>
+#include <sys/ioctl.h>
+#include <sys/stat.h>
+#include <unistd.h>
+
+// This metamorphic relation implementation is specific to the KVM on the
+// PowerPC architecture, as it uses the KVM_CREATE_SPAPR_TCE ioctl.
+
+// Manually define the PowerPC-specific KVM structure to allow compilation
+// on other architectures (like the x86_64 syzkaller build environment).
+// This definition must match the one in the kernel's arch/powerpc/include/uapi/asm/kvm.h
+struct kvm_create_spapr_tce {
+	__u32 liobn;
+	__u64 window_size;
+};
+
+/**
+ * syz_mr - Implements MR1 for KVM_CREATE_SPAPR_TCE.
+ *
+ * @param liobn1        The 'liobn' for the source ioctl call (L1).
+ * @param window_size_s The 'window_size' for the source ioctl call (S).
+ * @param liobn2        The 'liobn' for the follow-up ioctl call (L2), must be different from liobn1.
+ * @param k             The multiplier for the follow-up 'window_size'.
+ *
+ * This function tests that the size of a created TCE table scales linearly
+ * with the window_size parameter. It creates two TCE tables with window sizes
+ * S and k*S, and verifies that their file sizes are as expected and that the
+ * follow-up size is k times the source size.
+ */
+static long syz_mr_b65b85a4(volatile long liobn1, volatile long window_size_s, volatile long liobn2, volatile long k)
+{
+	// Move all variable declarations to the top of the function to avoid
+	// "crosses initialization" errors with the goto statement.
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	long fd_src = -1;
+	long fd_follow = -1;
+	struct kvm_create_spapr_tce tce_src;
+	struct kvm_create_spapr_tce tce_follow;
+	struct stat stat_src, stat_follow;
+	off_t size_src = 0;
+	off_t size_follow = 0;
+	long long expected_size_src = 0;
+	long long expected_size_follow = 0;
+	int violation = 0;
+
+	// 1. Perform preparation and input validation.
+
+	// The MR is defined for a positive S that is a multiple of 4096.
+	if (window_size_s <= 0 || (window_size_s % 4096) != 0) {
+		return -1;
+	}
+	// The MR is defined for a positive integer k.
+	if (k <= 0) {
+		return -1;
+	}
+	// The MR requires two distinct tables, so liobn must be different.
+	if (liobn1 == liobn2) {
+		return -1;
+	}
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot proceed if KVM is not available.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd == -1) {
+		// If VM creation fails, we cannot test the MR.
+		goto cleanup;
+	}
+
+	// 2. Get the source and follow-up outputs.
+
+	// Source ioctl call
+	tce_src.liobn = (__u32)liobn1;
+	tce_src.window_size = (__u64)window_size_s;
+
+	fd_src = ioctl(vm_fd, KVM_CREATE_SPAPR_TCE, &tce_src);
+	if (fd_src < 0) {
+		// Per the MR, the call should succeed. If it fails, we cannot test the relation.
+		goto cleanup;
+	}
+
+	// Follow-up ioctl call
+	tce_follow.liobn = (__u32)liobn2;
+	// Use long long for multiplication to avoid overflow before assignment.
+	tce_follow.window_size = (__u64)((long long)k * window_size_s);
+
+	fd_follow = ioctl(vm_fd, KVM_CREATE_SPAPR_TCE, &tce_follow);
+	if (fd_follow < 0) {
+		// Per the MR, the call should succeed. If it fails, we cannot test the relation.
+		goto cleanup;
+	}
+
+	// Get file sizes for the created TCE tables.
+	if (fstat(fd_src, &stat_src) == -1) {
+		goto cleanup;
+	}
+	size_src = stat_src.st_size;
+
+	if (fstat(fd_follow, &stat_follow) == -1) {
+		goto cleanup;
+	}
+	size_follow = stat_follow.st_size;
+
+	// 3. Check if the outputs satisfy the output relation.
+
+	// Each entry in the TCE table is 8 bytes (64-bit).
+	// The number of entries is window_size / page_size (assumed 4096).
+	expected_size_src = ((long long)window_size_s / 4096) * 8;
+	expected_size_follow = (((long long)k * window_size_s) / 4096) * 8;
+
+	// Relation 1: size_src == (S / 4096) * 8
+	if (size_src != expected_size_src) {
+		violation = 1;
+	}
+
+	// Relation 2: size_follow == (k * S / 4096) * 8
+	if (size_follow != expected_size_follow) {
+		violation = 1;
+	}
+
+	// Relation 3: size_follow == k * size_src
+	if (size_follow != (long long)k * size_src) {
+		violation = 1;
+	}
+
+	if (violation) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 4. Perform cleanup.
+	if (fd_src != -1) {
+		close(fd_src);
+	}
+	if (fd_follow != -1) {
+		close(fd_follow);
+	}
+	if (vm_fd != -1) {
+		close(vm_fd);
+	}
+	if (kvm_fd != -1) {
+		close(kvm_fd);
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_b704cea7.h b/executor/MRs/syz_mr_b704cea7.h
new file mode 100644
index 000000000..813d5e954
--- /dev/null
+++ b/executor/MRs/syz_mr_b704cea7.h
@@ -0,0 +1,136 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_mad/dev-files/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Consistency between /dev nodes and sysfs attributes for umad/issm devices
+// - Source input: Choose an integer `N` such that the character devices `/dev/infiniband/umadN` and `/dev/infiniband/issmN` exist. The input action is to read the contents of the files `/sys/class/infiniband_mad/umadN/ibdev` and `/sys/class/infiniband_mad/umadN/port`.
+// - Follow-up input: For the same integer `N`, the input action is to read the contents of the files `/sys/class/infiniband_mad/issmN/ibdev` and `/sys/class/infiniband_mad/issmN/port`.
+// - Input relation: The follow-up input is constructed by using the same device index `N` as the source input but querying the sysfs attributes for the `issm` device instead of the `umad` device.
+// - Output relation: The content read from `/sys/class/infiniband_mad/umadN/ibdev` must be identical to the content read from `/sys/class/infiniband_mad/issmN/ibdev`. Similarly, the content read from `/sys/class/infiniband_mad/umadN/port` must be identical to the content read from `/sys/class/infiniband_mad/issmN/port`.
+
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/stat.h>
+#include <unistd.h>
+
+#define MAX_PATH_LEN_b704cea7 256
+#define MAX_CONTENT_LEN_b704cea7 256
+
+/**
+ * @brief Reads the entire content of a file into a dynamically allocated buffer.
+ *
+ * @param path The path to the file to be read.
+ * @return A pointer to a newly allocated string containing the file content,
+ *         or NULL if an error occurs (e.g., file not found, memory allocation failed).
+ *         The caller is responsible for freeing the returned buffer.
+ */
+static char* read_file_content_b704cea7(const char* path)
+{
+	FILE* fp = fopen(path, "r");
+	if (!fp) {
+		return NULL;
+	}
+
+	char* buffer = (char*)malloc(MAX_CONTENT_LEN_b704cea7);
+	if (!buffer) {
+		fclose(fp);
+		return NULL;
+	}
+
+	// Read one line from the sysfs file.
+	if (fgets(buffer, MAX_CONTENT_LEN_b704cea7, fp) == NULL) {
+		free(buffer);
+		fclose(fp);
+		return NULL;
+	}
+
+	fclose(fp);
+
+	// Remove trailing newline character, if any, for consistent comparison.
+	size_t len = strlen(buffer);
+	if (len > 0 && buffer[len - 1] == '\n') {
+		buffer[len - 1] = '\0';
+	}
+
+	return buffer;
+}
+
+/**
+ * @brief Implements the metamorphic relation MR1.
+ *
+ * This MR checks for consistency between the sysfs attributes of corresponding
+ * umad and issm devices in the InfiniBand subsystem. It verifies that for a
+ * given device index N, the 'ibdev' and 'port' attributes are identical for
+ * both /dev/infiniband/umadN and /dev/infiniband/issmN.
+ *
+ * @param N The device index to check. This corresponds to the 'N' in the device paths.
+ * @return 0 on completion. The metamorphic relation violation is reported to stderr.
+ */
+static long syz_mr_b704cea7(volatile long N)
+{
+	// 1. Perform preparation and precondition checks.
+	// The MR is only applicable if both /dev/infiniband/umadN and
+	// /dev/infiniband/issmN character devices exist.
+	char umad_dev_path[MAX_PATH_LEN_b704cea7];
+	char issm_dev_path[MAX_PATH_LEN_b704cea7];
+
+	snprintf(umad_dev_path, sizeof(umad_dev_path), "/dev/infiniband/umad%ld", N);
+	snprintf(issm_dev_path, sizeof(issm_dev_path), "/dev/infiniband/issm%ld", N);
+
+	if (access(umad_dev_path, F_OK) != 0 || access(issm_dev_path, F_OK) != 0) {
+		// Precondition not met, the MR is not applicable for this input N.
+		return 0;
+	}
+
+	// 2. Construct paths for source and follow-up inputs based on the input relation.
+	char umad_ibdev_path[MAX_PATH_LEN_b704cea7];
+	char umad_port_path[MAX_PATH_LEN_b704cea7];
+	char issm_ibdev_path[MAX_PATH_LEN_b704cea7];
+	char issm_port_path[MAX_PATH_LEN_b704cea7];
+
+	snprintf(umad_ibdev_path, sizeof(umad_ibdev_path), "/sys/class/infiniband_mad/umad%ld/ibdev", N);
+	snprintf(umad_port_path, sizeof(umad_port_path), "/sys/class/infiniband_mad/umad%ld/port", N);
+	snprintf(issm_ibdev_path, sizeof(issm_ibdev_path), "/sys/class/infiniband_mad/issm%ld/ibdev", N);
+	snprintf(issm_port_path, sizeof(issm_port_path), "/sys/class/infiniband_mad/issm%ld/port", N);
+
+	// 3. Get the corresponding source and follow-up outputs.
+	char* umad_ibdev_content = read_file_content_b704cea7(umad_ibdev_path);
+	char* umad_port_content = read_file_content_b704cea7(umad_port_path);
+	char* issm_ibdev_content = read_file_content_b704cea7(issm_ibdev_path);
+	char* issm_port_content = read_file_content_b704cea7(issm_port_path);
+
+	// If any of the sysfs files could not be read, the MR is not applicable.
+	if (!umad_ibdev_content || !umad_port_content || !issm_ibdev_content || !issm_port_content) {
+		// Cleanup any memory that was successfully allocated before returning.
+		free(umad_ibdev_content);
+		free(umad_port_content);
+		free(issm_ibdev_content);
+		free(issm_port_content);
+		return 0;
+	}
+
+	// 4. Check if the outputs satisfy the output relation.
+	// This block replaces the placeholder 'if (output_relation_violated)'.
+	int violation = 0;
+	if (strcmp(umad_ibdev_content, issm_ibdev_content) != 0) {
+		violation = 1;
+	}
+	if (strcmp(umad_port_content, issm_port_content) != 0) {
+		violation = 1;
+	}
+
+	if (violation) {
+		// Note the semicolon at the end of the line, which fixes the compilation error.
+		// Also, a newline character `\n` is added for better formatting.
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Perform cleanup.
+	free(umad_ibdev_content);
+	free(umad_port_content);
+	free(issm_ibdev_content);
+	free(issm_port_content);
+
+	// A return statement is required for non-void functions. This fixes the error.
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_bd3e1cc5.h b/executor/MRs/syz_mr_bd3e1cc5.h
new file mode 100644
index 000000000..fe062ab64
--- /dev/null
+++ b/executor/MRs/syz_mr_bd3e1cc5.h
@@ -0,0 +1,192 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/ipoib/Partitions-and-P_Keys/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Creation and subsequent deletion of an IPoIB child interface should be reversible.
+// - Source input: An operation to create a new IPoIB child interface for a given parent interface (e.g., `ib0`) using a valid and currently unused P_Key (e.g., `0x8001`). This operation transforms the system from an initial state `S_initial` (where the set of network interfaces is `N_initial`) to a new state `S_source`.
+// - Follow-up input: An operation to delete the exact IPoIB child interface that was created by the source input, using the same parent interface and P_Key. This operation transforms the system from state `S_source` to a final state `S_follow-up`.
+// - Input relation: The follow-up input is the inverse operation of the source input, targeting the specific child interface created by the source input.
+// - Output relation: The source operation should succeed, resulting in a new child interface being added. The follow-up operation should also succeed. The set of network interfaces present in the final state `S_follow-up` must be identical to the set of network interfaces that were present in the initial state `S_initial`.
+
+#include <dirent.h>
+#include <errno.h>
+#include <fcntl.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/stat.h>
+#include <unistd.h>
+
+// Helper function to free the memory allocated for the interface list.
+static void free_interfaces_bd3e1cc5(char** interfaces, int count)
+{
+	if (interfaces) {
+		for (int i = 0; i < count; i++) {
+			free(interfaces[i]);
+		}
+		free(interfaces);
+	}
+}
+
+// Helper function to retrieve the current list of network interfaces from /sys/class/net.
+static int get_net_interfaces_bd3e1cc5(char*** interfaces_ptr, int* count_ptr)
+{
+	*interfaces_ptr = NULL;
+	*count_ptr = 0;
+	const char* path = "/sys/class/net";
+	DIR* dir = opendir(path);
+	if (!dir) {
+		// Cannot open /sys/class/net, cannot proceed.
+		return -1;
+	}
+
+	struct dirent* entry;
+	while ((entry = readdir(dir)) != NULL) {
+		if (strcmp(entry->d_name, ".") == 0 || strcmp(entry->d_name, "..") == 0) {
+			continue;
+		}
+
+		char** new_interfaces = (char**)realloc(*interfaces_ptr, (*count_ptr + 1) * sizeof(char*));
+		if (!new_interfaces) {
+			free_interfaces_bd3e1cc5(*interfaces_ptr, *count_ptr);
+			closedir(dir);
+			return -1;
+		}
+		*interfaces_ptr = new_interfaces;
+
+		(*interfaces_ptr)[*count_ptr] = strdup(entry->d_name);
+		if (!(*interfaces_ptr)[*count_ptr]) {
+			free_interfaces_bd3e1cc5(*interfaces_ptr, *count_ptr);
+			closedir(dir);
+			return -1;
+		}
+		(*count_ptr)++;
+	}
+	closedir(dir);
+	return 0;
+}
+
+// Comparison function for qsort to sort interface names alphabetically.
+static int compare_strings_bd3e1cc5(const void* a, const void* b)
+{
+	return strcmp(*(const char**)a, *(const char**)b);
+}
+
+// Writes a string to a specified sysfs path. Returns bytes written or -1 on error.
+static ssize_t write_to_sysfs_bd3e1cc5(const char* path, const char* value)
+{
+	int fd = open(path, O_WRONLY);
+	if (fd == -1) {
+		return -1;
+	}
+	ssize_t written = write(fd, value, strlen(value));
+	close(fd);
+	return written;
+}
+
+/**
+ * syz_mr - Implements the metamorphic relation for IPoIB child interface creation and deletion.
+ * @a0: A pointer to a string representing the parent InfiniBand interface name (e.g., "ib0").
+ * @a1: A value to be used for generating the P_Key for the child interface.
+ *
+ * This function tests the reversibility of creating and then deleting an IPoIB child interface.
+ * It performs the following steps:
+ * 1. Records the initial set of network interfaces.
+ * 2. Creates a new IPoIB child interface using the provided parent and P_Key.
+ * 3. Deletes the same child interface.
+ * 4. Records the final set of network interfaces.
+ * 5. Compares the initial and final sets of interfaces. They must be identical.
+ * If the deletion fails or the sets are not identical, it reports a violation.
+ */
+static long syz_mr_bd3e1cc5(volatile long a0, volatile long a1)
+{
+	const char* parent_iface = (const char*)a0;
+	long p_key_base = (long)a1;
+	char** initial_interfaces = NULL;
+	int initial_count = 0;
+	char** final_interfaces = NULL;
+	int final_count = 0;
+	int violation = 0;
+
+	// 1. Perform preparation
+	if (parent_iface == NULL || strnlen(parent_iface, 32) >= 32) {
+		return -1; // Invalid parent interface name input
+	}
+
+	// Sanitize P_Key to a valid format (16-bit with the high bit set).
+	// 0xFFFF is the broadcast key and is invalid for child creation.
+	long p_key = 0x8000 | (p_key_base & 0x7FFE);
+	if (p_key == 0)
+		p_key = 0x8001; // Avoid pkey 0
+
+	char pkey_str[20];
+	snprintf(pkey_str, sizeof(pkey_str), "%#lx", p_key);
+
+	char path_buf[256];
+	snprintf(path_buf, sizeof(path_buf), "/sys/class/net/%s/create_child", parent_iface);
+	if (access(path_buf, W_OK) != 0) {
+		// Parent interface does not support IPoIB child creation. MR is not applicable.
+		return -1;
+	}
+
+	// Attempt to delete the child interface first to ensure a clean state.
+	snprintf(path_buf, sizeof(path_buf), "/sys/class/net/%s/delete_child", parent_iface);
+	write_to_sysfs_bd3e1cc5(path_buf, pkey_str);
+	usleep(100000); // Wait for potential deletion to complete.
+
+	// Get the initial set of network interfaces.
+	if (get_net_interfaces_bd3e1cc5(&initial_interfaces, &initial_count) != 0) {
+		return -1; // Failed to get initial state.
+	}
+
+	// 2. Get the follow-up inputs and execute source/follow-up calls
+	// Source operation: Create the child interface.
+	snprintf(path_buf, sizeof(path_buf), "/sys/class/net/%s/create_child", parent_iface);
+	if (write_to_sysfs_bd3e1cc5(path_buf, pkey_str) <= 0) {
+		// Source operation failed, cannot test the relation.
+		free_interfaces_bd3e1cc5(initial_interfaces, initial_count);
+		return -1;
+	}
+	usleep(100000); // Wait for creation to complete.
+
+	// Follow-up operation: Delete the child interface.
+	snprintf(path_buf, sizeof(path_buf), "/sys/class/net/%s/delete_child", parent_iface);
+	if (write_to_sysfs_bd3e1cc5(path_buf, pkey_str) <= 0) {
+		// Follow-up operation failed, which is a violation of the relation.
+		violation = 1;
+	}
+	usleep(100000); // Wait for deletion to complete.
+
+	// 3. Get the corresponding source output and follow-up output
+	// Get the final set of network interfaces.
+	if (get_net_interfaces_bd3e1cc5(&final_interfaces, &final_count) != 0) {
+		free_interfaces_bd3e1cc5(initial_interfaces, initial_count);
+		return -1; // Failed to get final state.
+	}
+
+	// 4. If outputs do not satisfy the output relation, a prompt message is printed
+	// Compare the initial and final sets of interfaces.
+	if (!violation) {
+		if (initial_count != final_count) {
+			violation = 1;
+		} else {
+			qsort(initial_interfaces, initial_count, sizeof(char*), compare_strings_bd3e1cc5);
+			qsort(final_interfaces, final_count, sizeof(char*), compare_strings_bd3e1cc5);
+			for (int i = 0; i < initial_count; i++) {
+				if (strcmp(initial_interfaces[i], final_interfaces[i]) != 0) {
+					violation = 1;
+					break;
+				}
+			}
+		}
+	}
+
+	if (violation) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// Cleanup allocated memory.
+	free_interfaces_bd3e1cc5(initial_interfaces, initial_count);
+	free_interfaces_bd3e1cc5(final_interfaces, final_count);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_c11de9b2.h b/executor/MRs/syz_mr_c11de9b2.h
new file mode 100644
index 000000000..2b127bad4
--- /dev/null
+++ b/executor/MRs/syz_mr_c11de9b2.h
@@ -0,0 +1,257 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.21-KVM_SET_SIGNAL_MASK/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Unblocking a signal using `KVM_SET_SIGNAL_MASK` should cause a subsequent `KVM_RUN` to be interrupted by that signal.
+// - Source input: A sequence of operations on a vCPU, under the precondition that the executing thread's original signal mask does not block a specific signal `S` (e.g., `SIGUSR1`), where `S` is not `SIGKILL` or `SIGSTOP`. The operations are:
+//   1. A call to the `KVM_SET_SIGNAL_MASK` ioctl with a signal mask `mask_S` that **blocks** the signal `S`.
+//   2. A call to the `KVM_RUN` ioctl.
+//   3. While `KVM_RUN` is executing, the signal `S` is sent to the thread.
+// - Follow-up input: A sequence of operations on the same vCPU in the same state, under the same precondition. The operations are:
+//   1. A call to the `KVM_SET_SIGNAL_MASK` ioctl with a signal mask `mask_F` that **does not block** the signal `S`.
+//   2. A call to the `KVM_RUN` ioctl.
+//   3. While `KVM_RUN` is executing, the signal `S` is sent to the thread.
+// - Input relation: The follow-up input is derived from the source input by changing the signal mask provided to `KVM_SET_SIGNAL_MASK`. The new mask, `mask_F`, is identical to `mask_S` except that the signal `S` is unblocked. All other aspects, including the VM/vCPU state and the signal being sent, remain the same.
+// - Output relation: Let `ret_S` be the return value of `KVM_RUN` for the source input and `ret_F` be the return value for the follow-up input. The `KVM_RUN` call for the follow-up input should be interrupted by the signal `S`, resulting in `ret_F` being `-1` and `errno` being `EINTR`. In contrast, the `KVM_RUN` call for the source input should not be interrupted by `S`, and thus its return value `ret_S` should not be `-1` with an `errno` of `EINTR` as a result of that signal.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <pthread.h>
+#include <signal.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <sys/wait.h>
+#include <unistd.h>
+
+#define GUEST_MEM_SIZE_c11de9b2 0x1000
+#define TEST_SIGNAL_c11de9b2 SIGUSR1
+
+// Dummy signal handler to prevent process termination.
+static void dummy_handler_c11de9b2(int sig)
+{
+}
+
+// Helper function to set up a basic KVM VM and vCPU.
+// Returns 0 on success, -1 on failure.
+static int setup_kvm_c11de9b2(int* kvm_fd_ptr, int* vm_fd_ptr, int* vcpu_fd_ptr, void** guest_mem_ptr)
+{
+	int kvm_fd, vm_fd, vcpu_fd;
+	void* guest_mem;
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	guest_mem = mmap(NULL, GUEST_MEM_SIZE_c11de9b2, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Place a 'hlt' instruction at the beginning of guest memory.
+	*(unsigned char*)guest_mem = 0xf4;
+
+	struct kvm_userspace_memory_region region = {
+	    .slot = 0,
+	    .guest_phys_addr = 0,
+	    .memory_size = GUEST_MEM_SIZE_c11de9b2,
+	    .userspace_addr = (uint64_t)guest_mem,
+	};
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		munmap(guest_mem, GUEST_MEM_SIZE_c11de9b2);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		munmap(guest_mem, GUEST_MEM_SIZE_c11de9b2);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Declare register structs before any potential gotos that cross them.
+	struct kvm_sregs sregs;
+	struct kvm_regs regs;
+
+	// Set up special registers (sregs) for real mode.
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		goto fail;
+	}
+	sregs.cs.base = 0;
+	sregs.cs.selector = 0;
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		goto fail;
+	}
+
+	// Set up general purpose registers (regs).
+	regs.rip = 0; // Start execution at address 0.
+	regs.rflags = 0x2; // Minimal required flags.
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		goto fail;
+	}
+
+	*kvm_fd_ptr = kvm_fd;
+	*vm_fd_ptr = vm_fd;
+	*vcpu_fd_ptr = vcpu_fd;
+	*guest_mem_ptr = guest_mem;
+
+	return 0;
+
+fail:
+	close(vcpu_fd);
+	munmap(guest_mem, GUEST_MEM_SIZE_c11de9b2);
+	close(vm_fd);
+	close(kvm_fd);
+	return -1;
+}
+
+// Helper function to clean up KVM resources.
+static void cleanup_kvm_c11de9b2(int kvm_fd, int vm_fd, int vcpu_fd, void* guest_mem)
+{
+	if (guest_mem != MAP_FAILED)
+		munmap(guest_mem, GUEST_MEM_SIZE_c11de9b2);
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+}
+
+static long syz_mr_c11de9b2(volatile long a0)
+{
+	(void)a0; // The input parameter is not used in this MR.
+	long ret_S = 0, ret_F = 0;
+	int errno_S = 0, errno_F = 0;
+
+	// 1. Perform preparation.
+	// Set up a dummy signal handler for TEST_SIGNAL to override the default action.
+	struct sigaction sa_new;
+	struct sigaction sa_old;
+	memset(&sa_new, 0, sizeof(sa_new));
+	sa_new.sa_handler = dummy_handler_c11de9b2;
+
+	if (sigaction(TEST_SIGNAL_c11de9b2, &sa_new, &sa_old) == -1) {
+		return -1; // Cannot set up handler.
+	}
+
+	// Ensure TEST_SIGNAL is not blocked for the current thread, saving the old mask.
+	sigset_t procmask_new, procmask_old;
+	sigprocmask(SIG_BLOCK, NULL, &procmask_new);
+	sigdelset(&procmask_new, TEST_SIGNAL_c11de9b2);
+	if (sigprocmask(SIG_SETMASK, &procmask_new, &procmask_old) != 0) {
+		sigaction(TEST_SIGNAL_c11de9b2, &sa_old, NULL); // Restore handler.
+		return -1; // Cannot change thread mask.
+	}
+
+	// 2. Get the source output.
+	{
+		int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+		void* guest_mem = MAP_FAILED;
+		if (setup_kvm_c11de9b2(&kvm_fd, &vm_fd, &vcpu_fd, &guest_mem) != 0) {
+			goto cleanup;
+		}
+
+		// Create a signal mask that BLOCKS the test signal.
+		struct {
+			struct kvm_signal_mask h;
+			sigset_t s;
+		} mask_s_buf;
+		mask_s_buf.h.len = sizeof(sigset_t);
+		sigemptyset(&mask_s_buf.s);
+		sigaddset(&mask_s_buf.s, TEST_SIGNAL_c11de9b2);
+
+		if (ioctl(vcpu_fd, KVM_SET_SIGNAL_MASK, &mask_s_buf) == -1) {
+			cleanup_kvm_c11de9b2(kvm_fd, vm_fd, vcpu_fd, guest_mem);
+			goto cleanup;
+		}
+
+		pid_t pid = fork();
+		if (pid == -1) {
+			cleanup_kvm_c11de9b2(kvm_fd, vm_fd, vcpu_fd, guest_mem);
+			goto cleanup;
+		}
+
+		if (pid == 0) { // Child process
+			usleep(20000); // Wait for parent to enter KVM_RUN.
+			kill(getppid(), TEST_SIGNAL_c11de9b2);
+			exit(0);
+		} else { // Parent process
+			ret_S = ioctl(vcpu_fd, KVM_RUN, 0);
+			errno_S = errno;
+			wait(NULL); // Reap the child.
+		}
+		cleanup_kvm_c11de9b2(kvm_fd, vm_fd, vcpu_fd, guest_mem);
+	}
+
+	// 3. Get the follow-up output.
+	{
+		int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+		void* guest_mem = MAP_FAILED;
+		if (setup_kvm_c11de9b2(&kvm_fd, &vm_fd, &vcpu_fd, &guest_mem) != 0) {
+			goto cleanup;
+		}
+
+		// Create a signal mask that DOES NOT block the test signal.
+		struct {
+			struct kvm_signal_mask h;
+			sigset_t s;
+		} mask_f_buf;
+		mask_f_buf.h.len = sizeof(sigset_t);
+		sigemptyset(&mask_f_buf.s);
+
+		if (ioctl(vcpu_fd, KVM_SET_SIGNAL_MASK, &mask_f_buf) == -1) {
+			cleanup_kvm_c11de9b2(kvm_fd, vm_fd, vcpu_fd, guest_mem);
+			goto cleanup;
+		}
+
+		pid_t pid = fork();
+		if (pid == -1) {
+			cleanup_kvm_c11de9b2(kvm_fd, vm_fd, vcpu_fd, guest_mem);
+			goto cleanup;
+		}
+
+		if (pid == 0) { // Child process
+			usleep(20000); // Wait for parent to enter KVM_RUN.
+			kill(getppid(), TEST_SIGNAL_c11de9b2);
+			exit(0);
+		} else { // Parent process
+			ret_F = ioctl(vcpu_fd, KVM_RUN, 0);
+			errno_F = errno;
+			wait(NULL); // Reap the child.
+		}
+		cleanup_kvm_c11de9b2(kvm_fd, vm_fd, vcpu_fd, guest_mem);
+	}
+
+	// 4. Check the output relation.
+	// The relation is violated if:
+	// - The follow-up run was NOT interrupted (it should have been).
+	// OR
+	// - The source run WAS interrupted by the signal (it should not have been).
+	if ((ret_F != -1 || errno_F != EINTR) || (ret_S == -1 && errno_S == EINTR)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 5. Perform cleanup.
+	// Restore the original thread signal mask.
+	sigprocmask(SIG_SETMASK, &procmask_old, NULL);
+	// Restore the original signal handler.
+	sigaction(TEST_SIGNAL_c11de9b2, &sa_old, NULL);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_c20a878e.h b/executor/MRs/syz_mr_c20a878e.h
new file mode 100644
index 000000000..e475c760c
--- /dev/null
+++ b/executor/MRs/syz_mr_c20a878e.h
@@ -0,0 +1,200 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/ppp/ppp_generic/PPP-Generic-Driver-and-Channel-Interface/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Graceful handling of empty packets
+// - Source input: A request to transmit a valid, non-empty data packet `P_valid` over a PPP interface. The packet has a length greater than zero.
+// - Follow-up input: A request to transmit an empty (zero-length) data packet `P_empty` over the same PPP interface.
+// - Input relation: The follow-up input is created by replacing the data payload of the source input with an empty payload.
+// - Output relation: The transmission of the source input `P_valid` should succeed, which can be verified by observing an increment in the transmission statistics for the interface. The processing of the follow-up input `P_empty` should be handled gracefully by the PPP driver without causing any system crash, kernel panic, or memory corruption. The driver should reject or drop the empty packet, which can be verified by observing that transmission error counters may increment while success counters do not, and the system remains stable.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/if_ppp.h>
+#include <net/if.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/socket.h>
+#include <sys/un.h>
+#include <unistd.h>
+
+// Structure to hold network interface statistics we are interested in.
+struct net_stats_c20a878e {
+	unsigned long long tx_packets;
+};
+
+// Helper function to parse /proc/net/dev and retrieve statistics for a given interface.
+static int get_net_stats_c20a878e(const char* ifname, struct net_stats_c20a878e* stats)
+{
+	FILE* fp = fopen("/proc/net/dev", "r");
+	if (!fp) {
+		return -1;
+	}
+
+	char line[256];
+	// Skip the two header lines
+	if (!fgets(line, sizeof(line), fp) || !fgets(line, sizeof(line), fp)) {
+		fclose(fp);
+		return -1;
+	}
+
+	while (fgets(line, sizeof(line), fp)) {
+		char current_ifname[IFNAMSIZ];
+		unsigned long long dummy;
+		unsigned long long tx_pkts;
+
+		// Format:
+		// face |bytes    packets errs drop fifo frame compressed multicast|bytes    packets errs drop fifo colls carrier compressed
+		int items = sscanf(line, " %[^:]:%llu %llu %llu %llu %llu %llu %llu %llu %llu %llu",
+				   current_ifname, &dummy, &dummy, &dummy, &dummy, &dummy, &dummy, &dummy, &dummy,
+				   &dummy, &tx_pkts);
+
+		if (items >= 11 && strcmp(current_ifname, ifname) == 0) {
+			stats->tx_packets = tx_pkts;
+			fclose(fp);
+			return 0; // Found
+		}
+	}
+	fclose(fp);
+	return -1; // Not found
+}
+
+static long syz_mr_c20a878e(volatile long a0)
+{
+	// 1. Preparation: Create and configure a PPP interface
+	int ppp_fd = open("/dev/ppp", O_RDWR);
+	if (ppp_fd < 0) {
+		// Cannot run test if /dev/ppp is not available or no permissions
+		return -1;
+	}
+
+	int unit = -1;
+	if (ioctl(ppp_fd, PPPIOCNEWUNIT, &unit) < 0) {
+		// Failed to create a new PPP unit
+		close(ppp_fd);
+		return -1;
+	}
+
+	char ifname[IFNAMSIZ];
+	snprintf(ifname, sizeof(ifname), "ppp%d", unit);
+
+	// Create a dummy socket to act as the channel for the PPP unit
+	int chan_fd = socket(AF_UNIX, SOCK_DGRAM, 0);
+	if (chan_fd < 0) {
+		close(ppp_fd);
+		return -1;
+	}
+
+	// Attach the channel to the PPP unit. Writes to ppp_fd will now go through this channel.
+	if (ioctl(ppp_fd, PPPIOCATTCHAN, &chan_fd) < 0) {
+		close(chan_fd);
+		close(ppp_fd);
+		return -1;
+	}
+
+	// Bring the interface up. This is crucial for packet transmission.
+	int ctl_sock = socket(AF_INET, SOCK_DGRAM, 0);
+	if (ctl_sock < 0) {
+		close(chan_fd);
+		close(ppp_fd);
+		return -1;
+	}
+
+	struct ifreq ifr;
+	memset(&ifr, 0, sizeof(ifr));
+	// Use snprintf for safe string copy to avoid truncation warnings.
+	snprintf(ifr.ifr_name, IFNAMSIZ, "%s", ifname);
+
+	// Get current flags
+	if (ioctl(ctl_sock, SIOCGIFFLAGS, &ifr) == 0) {
+		// Set the IFF_UP flag to enable the interface
+		ifr.ifr_flags |= IFF_UP;
+		if (ioctl(ctl_sock, SIOCSIFFLAGS, &ifr) < 0) {
+			// If we can't bring the interface up, the test cannot proceed
+			close(ctl_sock);
+			close(chan_fd);
+			close(ppp_fd);
+			return -1;
+		}
+	} else {
+		// Failed to get interface flags
+		close(ctl_sock);
+		close(chan_fd);
+		close(ppp_fd);
+		return -1;
+	}
+	close(ctl_sock);
+
+	// Give the interface a moment to settle and appear in /proc/net/dev
+	usleep(50000);
+
+	// 2. Get initial transmission statistics
+	struct net_stats_c20a878e stats_before, stats_after_valid, stats_after_empty;
+	if (get_net_stats_c20a878e(ifname, &stats_before) != 0) {
+		close(chan_fd);
+		close(ppp_fd);
+		return -1;
+	}
+
+	// 3. Source input: send a valid, non-empty packet
+	// A minimal PPP frame includes Address (0xff), Control (0x03), and Protocol (e.g., 0x0021 for IP).
+	long payload_size = a0 % 512 + 1; // Ensure payload size is > 0
+	long frame_size = payload_size + 4;
+	unsigned char p_valid[frame_size];
+	p_valid[0] = 0xff; // Address
+	p_valid[1] = 0x03; // Control
+	p_valid[2] = 0x00; // Protocol (High byte - IP)
+	p_valid[3] = 0x21; // Protocol (Low byte - IP)
+	memset(&p_valid[4], 'A', payload_size);
+
+	if (write(ppp_fd, p_valid, frame_size) != frame_size) {
+		// If the valid packet write fails, we can't test the relation.
+		close(chan_fd);
+		close(ppp_fd);
+		return -1;
+	}
+
+	usleep(50000); // Allow time for statistics to update
+
+	// Get statistics after sending the valid packet
+	if (get_net_stats_c20a878e(ifname, &stats_after_valid) != 0) {
+		close(chan_fd);
+		close(ppp_fd);
+		return -1;
+	}
+
+	// 4. Follow-up input: attempt to send an empty (zero-length) packet
+	// Use the return value in a condition to silence the compiler warning.
+	// We don't need to handle the result, just acknowledge it.
+	if (write(ppp_fd, NULL, 0) < 0) {
+		// Do nothing.
+	}
+
+	usleep(50000); // Allow time for statistics to update
+
+	// Get final statistics after sending the empty packet
+	if (get_net_stats_c20a878e(ifname, &stats_after_empty) != 0) {
+		close(chan_fd);
+		close(ppp_fd);
+		return -1;
+	}
+
+	// 5. Output relation check
+	// Source check: The transmission of the valid packet should succeed, incrementing tx_packets.
+	int source_ok = (stats_after_valid.tx_packets > stats_before.tx_packets);
+
+	if (source_ok) {
+		// Follow-up check: The empty packet should be dropped/rejected.
+		// This means the success counter (tx_packets) should NOT increment further.
+		if (stats_after_empty.tx_packets > stats_after_valid.tx_packets) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+	// 6. Cleanup
+	close(chan_fd);
+	close(ppp_fd); // Closing this fd should destroy the ppp interface
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_c3320296.h b/executor/MRs/syz_mr_c3320296.h
new file mode 100644
index 000000000..7b9abf4d1
--- /dev/null
+++ b/executor/MRs/syz_mr_c3320296.h
@@ -0,0 +1,220 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.27-KVM_CAP_X86_MSR_FILTER/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: MSR access is rejected when the filter denies it.
+// - Source input: A KVM virtual machine (VM #1) is created and configured with an MSR filter rule that explicitly ALLOWS access to a specific MSR, `MSR_X`. A guest is then launched within this VM that executes an instruction (e.g., `RDMSR` or `WRMSR`) to access `MSR_X`.
+// - Follow-up input: A second KVM virtual machine (VM #2) is created with a configuration identical to VM #1 in every respect, except its MSR filter is configured with a rule that explicitly DENIES access to the same `MSR_X`. The same guest code is launched to access `MSR_X`.
+// - Input relation: The follow-up input is derived from the source input by changing the action of the MSR filter rule for `MSR_X` from `ALLOW` to `DENY`. All other aspects of the VM and guest configuration remain identical.
+// - Output relation: In the source output, the guest's access to `MSR_X` should proceed according to KVM's normal handling (e.g., pass-through to hardware, emulation, or exit to userspace), and no fault should be injected into the guest due to the filter. In the follow-up output, the guest's access to `MSR_X` must be rejected by KVM, resulting in a general protection fault with an error code of 0 (`#GP(0)`) being injected into the guest.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// Define MSR_IA32_TSC locally to remove dependency on <asm/msr-index.h>
+#define MSR_IA32_TSC_c3320296 0x00000010
+
+// Define control register bits locally to remove header dependencies
+#define X86_CR0_PE_c3320296 0x00000001
+#define X86_CR0_PG_c3320296 0x80000000
+#define X86_CR0_WP_c3320296 0x00010000
+#define X86_CR4_PAE_c3320296 0x00000020
+
+// Guest memory size (16MB to be safe with page tables and stack)
+#define GUEST_MEM_SIZE_c3320296 (16 * 1024 * 1024)
+// Guest code will be placed at 1MB
+#define CODE_START_c3320296 0x100000
+
+// Page table addresses in guest physical memory
+#define PML4_ADDR_c3320296 0x10000
+#define PDPT_ADDR_c3320296 0x11000
+#define PD_ADDR_c3320296 0x12000
+
+// 64-bit page table entry flags
+#define PTE_PRESENT_c3320296 0x01
+#define PTE_RW_c3320296 0x02
+#define PTE_HUGE_c3320296 0x80
+
+static long syz_mr_c3320296(volatile long a0, volatile long a1)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	uint8_t* mem = NULL;
+	struct kvm_run* run = NULL;
+	int mmap_size;
+	long ret = 0;
+	uint32_t msr_index = (uint32_t)a0;
+	long source_exit_reason = 0;
+	long followup_exit_reason = 0;
+
+	// Move all declarations to the top to avoid "crosses initialization" errors.
+	struct kvm_userspace_memory_region region;
+	uint64_t *pml4, *pdpt, *pd;
+	struct kvm_sregs sregs;
+	struct kvm_segment seg;
+	struct kvm_regs regs;
+	struct kvm_msr_filter filter;
+	unsigned char code[] = {0x0f, 0x32, 0xf4}; // rdmsr; hlt
+
+	if (msr_index == 0) {
+		msr_index = MSR_IA32_TSC_c3320296;
+	}
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		return -1;
+	}
+
+	if (ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_X86_MSR_FILTER) <= 0) {
+		close(kvm_fd);
+		return 0;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		ret = -1;
+		goto cleanup_kvm;
+	}
+
+	mem = (uint8_t*)mmap(NULL, GUEST_MEM_SIZE_c3320296, PROT_READ | PROT_WRITE,
+			     MAP_PRIVATE | MAP_ANONYMOUS | MAP_NORESERVE, -1, 0);
+	if (mem == MAP_FAILED) {
+		ret = -1;
+		goto cleanup_vm;
+	}
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = GUEST_MEM_SIZE_c3320296;
+	region.userspace_addr = (uint64_t)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		ret = -1;
+		goto cleanup_mem;
+	}
+
+	memcpy(mem + CODE_START_c3320296, code, sizeof(code));
+
+	pml4 = (uint64_t*)(mem + PML4_ADDR_c3320296);
+	pdpt = (uint64_t*)(mem + PDPT_ADDR_c3320296);
+	pd = (uint64_t*)(mem + PD_ADDR_c3320296);
+
+	pml4[0] = PDPT_ADDR_c3320296 | PTE_PRESENT_c3320296 | PTE_RW_c3320296;
+	pdpt[0] = PD_ADDR_c3320296 | PTE_PRESENT_c3320296 | PTE_RW_c3320296;
+	pd[0] = 0 | PTE_PRESENT_c3320296 | PTE_RW_c3320296 | PTE_HUGE_c3320296;
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		ret = -1;
+		goto cleanup_mem;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size < 0 || (size_t)mmap_size < sizeof(struct kvm_run)) {
+		ret = -1;
+		goto cleanup_vcpu;
+	}
+
+	run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		run = NULL;
+		ret = -1;
+		goto cleanup_vcpu;
+	}
+
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+
+	sregs.cr3 = PML4_ADDR_c3320296;
+	sregs.cr4 |= X86_CR4_PAE_c3320296;
+	sregs.cr0 = X86_CR0_PE_c3320296 | X86_CR0_PG_c3320296 | X86_CR0_WP_c3320296;
+	sregs.efer |= EFER_LME | EFER_LMA;
+
+	memset(&seg, 0, sizeof(seg));
+	seg.base = 0;
+	seg.limit = 0xffffffff;
+	seg.selector = 1 << 3;
+	seg.type = 11;
+	seg.present = 1;
+	seg.s = 1;
+	seg.l = 1;
+	seg.g = 1;
+	sregs.cs = seg;
+
+	seg.selector = 2 << 3;
+	seg.type = 3;
+	sregs.ds = sregs.es = sregs.fs = sregs.gs = sregs.ss = seg;
+
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = CODE_START_c3320296;
+	regs.rcx = msr_index;
+	regs.rflags = 2;
+	regs.rsp = GUEST_MEM_SIZE_c3320296 - 0x1000;
+
+	// --- Source Input Run (ALLOW) ---
+	memset(&filter, 0, sizeof(filter));
+	filter.flags = KVM_MSR_FILTER_DEFAULT_DENY;
+	filter.ranges[0].base = msr_index;
+	filter.ranges[0].nmsrs = 1;
+	filter.ranges[0].flags = KVM_MSR_FILTER_READ;
+
+	if (ioctl(vcpu_fd, KVM_X86_SET_MSR_FILTER, &filter) < 0) {
+		goto cleanup_run;
+	}
+
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1 || ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+	if (ioctl(vcpu_fd, KVM_RUN, NULL) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+	source_exit_reason = run->exit_reason;
+
+	// --- Follow-up Input Run (DENY) ---
+	filter.ranges[0].flags = 0;
+	if (ioctl(vcpu_fd, KVM_X86_SET_MSR_FILTER, &filter) < 0) {
+		goto cleanup_run;
+	}
+
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1 || ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+	if (ioctl(vcpu_fd, KVM_RUN, NULL) == -1) {
+		ret = -1;
+		goto cleanup_run;
+	}
+	followup_exit_reason = run->exit_reason;
+
+	if (source_exit_reason != KVM_EXIT_HLT || followup_exit_reason != KVM_EXIT_SHUTDOWN) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup_run:
+	if (run)
+		munmap(run, mmap_size);
+cleanup_vcpu:
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+cleanup_mem:
+	if (mem)
+		munmap(mem, GUEST_MEM_SIZE_c3320296);
+cleanup_vm:
+	if (vm_fd != -1)
+		close(vm_fd);
+cleanup_kvm:
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_c67a179b.h b/executor/MRs/syz_mr_c67a179b.h
new file mode 100644
index 000000000..69f276211
--- /dev/null
+++ b/executor/MRs/syz_mr_c67a179b.h
@@ -0,0 +1,336 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.34-KVM_SET_DEBUGREGS/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Consistency of DR6 state after guest modification across different VM-Exit reasons
+// - Source input: A sequence of operations on a vCPU:
+//   1. Use the `KVM_SET_DEBUGREGS` ioctl to configure the vCPU's debug registers such that DR interception is disabled (e.g., by setting DR7) and DR6 is set to an initial known value, `V_initial`.
+//   2. Run the vCPU with guest code that modifies the hardware DR6 register to a new value, `V_guest`, and then immediately performs a controlled VM-Exit (e.g., by executing a `VMCALL` instruction).
+//   3. After the `KVM_RUN` ioctl returns, the corresponding source output is obtained by calling the `KVM_GET_DEBUGREGS` ioctl.
+//
+// - Follow-up input: A sequence of operations on the same vCPU, reset to the same initial state:
+//   1. Use the `KVM_SET_DEBUGREGS` ioctl to configure the debug registers identically to the source input (DR interception disabled, DR6 set to `V_initial`).
+//   2. Run the vCPU with guest code that modifies the hardware DR6 register to the same new value, `V_guest`, but instead of exiting immediately, it enters a state (e.g., a tight loop) that causes an asynchronous VM-Exit, such as one triggered by a preemption timer.
+//   3. After the `KVM_RUN` ioctl returns due to the asynchronous exit, the corresponding follow-up output is obtained by calling the `KVM_GET_DEBUGREGS` ioctl.
+//
+// - Input relation: The follow-up input is derived from the source input by changing the reason for the VM-Exit that occurs after the guest modifies DR6. The source input uses a controlled, instruction-based exit, while the follow-up input uses an asynchronous, external event-based exit. The initial state and the guest's modification to DR6 are identical in both cases.
+//
+// - Output relation: On any VM-Exit, KVM is expected to save the current hardware debug register state into its internal vCPU structures. Therefore, the `struct kvm_debugregs` retrieved by `KVM_GET_DEBUGREGS` in the source and follow-up outputs should be identical. Specifically, the `dr6` field in both outputs must reflect the new value `V_guest` set by the guest.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <pthread.h>
+#include <signal.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <time.h>
+#include <unistd.h>
+
+#define GUEST_MEM_SIZE_c67a179b 0x10000 // 64KB is enough
+#define GUEST_CODE_GPA_c67a179b 0x1000
+#define GUEST_STACK_GPA_c67a179b (GUEST_MEM_SIZE_c67a179b - 0x10)
+
+// Helper struct for the signal-sending thread
+struct thread_data_c67a179b {
+	pthread_t main_tid;
+};
+
+// Empty signal handler to catch the signal and interrupt KVM_RUN
+static void sig_handler_c67a179b(int signo)
+{
+	// This handler allows the signal to interrupt the KVM_RUN ioctl
+}
+
+// Thread function that waits a bit and then sends a signal to the main thread
+static void* send_signal_thread_c67a179b(void* arg)
+{
+	struct thread_data_c67a179b* data = (struct thread_data_c67a179b*)arg;
+	// Wait for a short period to allow the guest to execute its instructions
+	struct timespec ts = {.tv_sec = 0, .tv_nsec = 20 * 1000 * 1000}; // 20ms
+	nanosleep(&ts, NULL);
+	pthread_kill(data->main_tid, SIGALRM);
+	return NULL;
+}
+
+// Helper function to set up a basic 64-bit KVM environment (VM, vCPU, memory)
+static int setup_vcpu_c67a179b(int kvm_fd, int* vm_fd_ptr, int* vcpu_fd_ptr,
+			       void** guest_mem_ptr, struct kvm_run** run_ptr)
+{
+	int vm_fd, vcpu_fd;
+	void* guest_mem;
+	struct kvm_run* run;
+	int run_size;
+	struct kvm_sregs sregs;
+	uint64_t pml4_addr, pdpt_addr, pd_addr, gdt_addr;
+	uint64_t *pml4, *pdpt, *pd, *gdt;
+	struct kvm_userspace_memory_region region;
+	struct kvm_segment seg;
+	struct kvm_regs regs;
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		perror("KVM_CREATE_VM");
+		return -1;
+	}
+
+	guest_mem = mmap(NULL, GUEST_MEM_SIZE_c67a179b, PROT_READ | PROT_WRITE,
+			 MAP_SHARED | MAP_ANONYMOUS, -1, 0);
+	if (guest_mem == MAP_FAILED) {
+		perror("mmap guest_mem");
+		close(vm_fd);
+		return -1;
+	}
+
+	region = (struct kvm_userspace_memory_region){
+	    .slot = 0,
+	    .guest_phys_addr = 0,
+	    .memory_size = GUEST_MEM_SIZE_c67a179b,
+	    .userspace_addr = (uint64_t)guest_mem,
+	};
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) == -1) {
+		perror("KVM_SET_USER_MEMORY_REGION");
+		munmap(guest_mem, GUEST_MEM_SIZE_c67a179b);
+		close(vm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		perror("KVM_CREATE_VCPU");
+		munmap(guest_mem, GUEST_MEM_SIZE_c67a179b);
+		close(vm_fd);
+		return -1;
+	}
+
+	run_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0);
+	if (run_size < 0) {
+		perror("KVM_GET_VCPU_MMAP_SIZE");
+		close(vcpu_fd);
+		munmap(guest_mem, GUEST_MEM_SIZE_c67a179b);
+		close(vm_fd);
+		return -1;
+	}
+	run = (struct kvm_run*)mmap(NULL, run_size, PROT_READ | PROT_WRITE,
+				    MAP_SHARED, vcpu_fd, 0);
+	if (run == MAP_FAILED) {
+		perror("mmap kvm_run");
+		close(vcpu_fd);
+		munmap(guest_mem, GUEST_MEM_SIZE_c67a179b);
+		close(vm_fd);
+		return -1;
+	}
+
+	// Setup SREGS for 64-bit long mode
+	if (ioctl(vcpu_fd, KVM_GET_SREGS, &sregs) == -1) {
+		perror("KVM_GET_SREGS");
+		goto fail;
+	}
+
+	// Setup minimal page tables for identity mapping
+	pml4_addr = 0x2000;
+	pdpt_addr = 0x3000;
+	pd_addr = 0x4000;
+	pml4 = (uint64_t*)((char*)guest_mem + pml4_addr);
+	pdpt = (uint64_t*)((char*)guest_mem + pdpt_addr);
+	pd = (uint64_t*)((char*)guest_mem + pd_addr);
+	pml4[0] = pdpt_addr | 0x3; // Present, R/W
+	pdpt[0] = pd_addr | 0x3; // Present, R/W
+	pd[0] = 0x0 | 0x83; // Present, R/W, PS (2MB page)
+
+	sregs.cr3 = pml4_addr;
+	sregs.cr4 = CR4_PAE;
+	sregs.cr0 = CR0_PE | CR0_PG;
+	sregs.efer = EFER_LME | EFER_LMA;
+
+	// Setup GDT
+	gdt_addr = 0x5000;
+	gdt = (uint64_t*)((char*)guest_mem + gdt_addr);
+	gdt[0] = 0; // Null descriptor
+	gdt[1] = 0x0020980000000000ULL; // 64-bit code segment
+	gdt[2] = 0x00c0920000000000ULL; // Data segment
+
+	sregs.gdt.base = gdt_addr;
+	sregs.gdt.limit = 3 * 8 - 1;
+
+	seg = (struct kvm_segment){.base = 0, .limit = 0xffffffff, .selector = 1 << 3, .type = 10, .present = 1, .dpl = 0, .db = 0, .s = 1, .l = 1, .g = 1};
+	sregs.cs = seg;
+	seg.selector = 2 << 3;
+	seg.type = 2; // Read/Write data
+	seg.l = 0;
+	seg.db = 1;
+	sregs.ds = sregs.es = sregs.fs = sregs.gs = sregs.ss = seg;
+
+	if (ioctl(vcpu_fd, KVM_SET_SREGS, &sregs) == -1) {
+		perror("KVM_SET_SREGS");
+		goto fail;
+	}
+
+	// Setup REGS
+	regs = (struct kvm_regs){
+	    .rsp = GUEST_STACK_GPA_c67a179b,
+	    .rip = GUEST_CODE_GPA_c67a179b,
+	    .rflags = 2,
+	};
+	if (ioctl(vcpu_fd, KVM_SET_REGS, &regs) == -1) {
+		perror("KVM_SET_REGS");
+		goto fail;
+	}
+
+	*vm_fd_ptr = vm_fd;
+	*vcpu_fd_ptr = vcpu_fd;
+	*guest_mem_ptr = guest_mem;
+	*run_ptr = run;
+	return 0;
+
+fail:
+	munmap(run, ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0));
+	close(vcpu_fd);
+	munmap(guest_mem, GUEST_MEM_SIZE_c67a179b);
+	close(vm_fd);
+	return -1;
+}
+
+// Helper function to clean up KVM resources
+static void cleanup_vcpu_c67a179b(int vm_fd, int vcpu_fd, void* guest_mem,
+				  struct kvm_run* run, int kvm_fd)
+{
+	int run_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, 0);
+	if (run_size > 0)
+		munmap(run, run_size);
+	close(vcpu_fd);
+	munmap(guest_mem, GUEST_MEM_SIZE_c67a179b);
+	close(vm_fd);
+}
+
+static long syz_mr_c67a179b(volatile long a0, volatile long a1)
+{
+	uint64_t V_initial = (uint64_t)a0;
+	uint64_t V_guest = (uint64_t)a1;
+
+	// Use fixed known-good values if inputs from fuzzer are not suitable
+	if (V_initial == 0)
+		V_initial = 0xffff0ff0;
+	if (V_guest == 0)
+		V_guest = 0xdeadbeefcafebabe;
+
+	int kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		perror("open /dev/kvm");
+		return -1;
+	}
+
+	// --- Source Run: Controlled VM-Exit (VMCALL) ---
+	int vm_fd_src = -1, vcpu_fd_src = -1;
+	void* guest_mem_src = NULL;
+	struct kvm_run* run_src = NULL;
+	struct kvm_debugregs dregs_out_src = {0};
+
+	if (setup_vcpu_c67a179b(kvm_fd, &vm_fd_src, &vcpu_fd_src, &guest_mem_src, &run_src) != 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 1. SET_DEBUGREGS with initial DR6 value and disabled DR interception
+	struct kvm_debugregs dregs_in = {
+	    .dr6 = V_initial,
+	    .dr7 = 0x400, // Disable DR interception
+	};
+	if (ioctl(vcpu_fd_src, KVM_SET_DEBUGREGS, &dregs_in) == -1) {
+		perror("KVM_SET_DEBUGREGS (source)");
+		cleanup_vcpu_c67a179b(vm_fd_src, vcpu_fd_src, guest_mem_src, run_src, kvm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Run guest code that modifies DR6 and performs VMCALL
+	unsigned char code_src[] = {
+	    0x48, 0xb8, 0, 0, 0, 0, 0, 0, 0, 0, // mov rax, V_guest
+	    0x0f, 0x23, 0xf0, // mov dr6, rax
+	    0x0f, 0x01, 0xc1, // vmcall
+	};
+	memcpy(&code_src[2], &V_guest, sizeof(V_guest));
+	memcpy((char*)guest_mem_src + GUEST_CODE_GPA_c67a179b, code_src, sizeof(code_src));
+
+	if (ioctl(vcpu_fd_src, KVM_RUN, 0) == -1) {
+		perror("KVM_RUN (source)");
+	}
+
+	// 3. GET_DEBUGREGS to get the source output
+	if (ioctl(vcpu_fd_src, KVM_GET_DEBUGREGS, &dregs_out_src) == -1) {
+		perror("KVM_GET_DEBUGREGS (source)");
+	}
+
+	cleanup_vcpu_c67a179b(vm_fd_src, vcpu_fd_src, guest_mem_src, run_src, kvm_fd);
+
+	// --- Follow-up Run: Asynchronous VM-Exit (Signal) ---
+	int vm_fd_fup = -1, vcpu_fd_fup = -1;
+	void* guest_mem_fup = NULL;
+	struct kvm_run* run_fup = NULL;
+	struct kvm_debugregs dregs_out_fup = {0};
+
+	if (setup_vcpu_c67a179b(kvm_fd, &vm_fd_fup, &vcpu_fd_fup, &guest_mem_fup, &run_fup) != 0) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 1. SET_DEBUGREGS with the same initial state
+	if (ioctl(vcpu_fd_fup, KVM_SET_DEBUGREGS, &dregs_in) == -1) {
+		perror("KVM_SET_DEBUGREGS (follow-up)");
+		cleanup_vcpu_c67a179b(vm_fd_fup, vcpu_fd_fup, guest_mem_fup, run_fup, kvm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Run guest code that modifies DR6 and waits for an interrupt
+	unsigned char code_fup[] = {
+	    0x48, 0xb8, 0, 0, 0, 0, 0, 0, 0, 0, // mov rax, V_guest
+	    0x0f, 0x23, 0xf0, // mov dr6, rax
+	    0xf4, // hlt
+	};
+	memcpy(&code_fup[2], &V_guest, sizeof(V_guest));
+	memcpy((char*)guest_mem_fup + GUEST_CODE_GPA_c67a179b, code_fup, sizeof(code_fup));
+
+	// Setup signal handler and thread to cause an async exit
+	struct sigaction sa, old_sa;
+	memset(&sa, 0, sizeof(sa));
+	sa.sa_handler = sig_handler_c67a179b;
+	sigaction(SIGALRM, &sa, &old_sa);
+
+	pthread_t tid;
+	struct thread_data_c67a179b data = {.main_tid = pthread_self()};
+	if (pthread_create(&tid, NULL, send_signal_thread_c67a179b, &data) != 0) {
+		perror("pthread_create");
+		sigaction(SIGALRM, &old_sa, NULL);
+		cleanup_vcpu_c67a179b(vm_fd_fup, vcpu_fd_fup, guest_mem_fup, run_fup, kvm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	if (ioctl(vcpu_fd_fup, KVM_RUN, 0) == -1 && errno != EINTR) {
+		perror("KVM_RUN (follow-up)");
+	}
+
+	pthread_join(tid, NULL);
+	sigaction(SIGALRM, &old_sa, NULL); // Restore old signal handler
+
+	// 3. GET_DEBUGREGS to get the follow-up output
+	if (ioctl(vcpu_fd_fup, KVM_GET_DEBUGREGS, &dregs_out_fup) == -1) {
+		perror("KVM_GET_DEBUGREGS (follow-up)");
+	}
+
+	cleanup_vcpu_c67a179b(vm_fd_fup, vcpu_fd_fup, guest_mem_fup, run_fup, kvm_fd);
+	close(kvm_fd);
+
+	// --- Output Relation Check ---
+	// The dr6 value should be updated to V_guest in both cases and they should be identical.
+	if (dregs_out_src.dr6 != V_guest || dregs_out_fup.dr6 != V_guest ||
+	    dregs_out_src.dr6 != dregs_out_fup.dr6) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_c7d8e332.h b/executor/MRs/syz_mr_c7d8e332.h
new file mode 100644
index 000000000..c2ac3b520
--- /dev/null
+++ b/executor/MRs/syz_mr_c7d8e332.h
@@ -0,0 +1,221 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/core_locking/Hot-plug/impl/mr.h
+// Code generated by gemini-2.5-pro after 4 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Graceful Termination of In-Flight Operations on Device Removal
+// - Source input: A sequence of operations that successfully initiates and completes an asynchronous task on a registered RDMA device. For example, creating an `rdma_cm_id` and successfully resolving an address using `rdma_resolve_addr()`.
+// - Follow-up input: The same sequence of operations to initiate the asynchronous task, but followed immediately by an operation that simulates a surprise hot-unplug of the RDMA device (e.g., by calling `ib_unregister_device()`) before the asynchronous task completes.
+// - Input relation: The follow-up input is generated by injecting a device removal event into the source input's sequence, specifically between the initiation and the expected completion of the asynchronous task.
+// - Output relation: The source output should be the successful completion of the task (e.g., receiving an `RDMA_CM_EVENT_ADDR_RESOLVED` event). The follow-up output must not be a system crash, hang, or memory corruption (like a use-after-free). Instead, the in-flight operation should be gracefully terminated, and a corresponding error event (e.g., `RDMA_CM_EVENT_DEVICE_REMOVAL` or `RDMA_CM_EVENT_ADDR_ERROR`) should be reported. All resources associated with the terminated task must be correctly cleaned up.
+
+#include <arpa/inet.h>
+#include <dlfcn.h>
+#include <errno.h>
+#include <net/if.h>
+#include <netdb.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/wait.h>
+#include <unistd.h>
+
+// Manually define necessary structures and enums from <rdma/rdma_cma.h>
+// to remove the compile-time dependency on the header file.
+struct rdma_event_channel_c7d8e332;
+struct rdma_cm_id_c7d8e332;
+
+enum rdma_cm_event_type {
+	RDMA_CM_EVENT_ADDR_RESOLVED = 0,
+	RDMA_CM_EVENT_ADDR_ERROR = 1,
+	RDMA_CM_EVENT_DEVICE_REMOVAL = 11,
+};
+
+struct rdma_cm_event_c7d8e332 {
+	enum rdma_cm_event_type event;
+	int status;
+	// The rest of the struct is not needed for this MR.
+};
+
+enum rdma_port_space {
+	RDMA_PS_TCP_XXX = 0x0106,
+};
+
+// Define function pointer types for cleaner casting.
+typedef struct rdma_event_channel_c7d8e332* (*rdma_create_event_channel_t)(void);
+typedef int (*rdma_create_id_t)(struct rdma_event_channel_c7d8e332* channel, struct rdma_cm_id_c7d8e332** id,
+				void* context, enum rdma_port_space ps);
+typedef int (*rdma_resolve_addr_t)(struct rdma_cm_id_c7d8e332* id, struct sockaddr* src_addr,
+				   struct sockaddr* dst_addr, int timeout_ms);
+typedef int (*rdma_get_cm_event_t)(struct rdma_event_channel_c7d8e332* channel,
+				   struct rdma_cm_event_c7d8e332** event);
+typedef int (*rdma_ack_cm_event_t)(struct rdma_cm_event_c7d8e332* event);
+typedef int (*rdma_destroy_id_t)(struct rdma_cm_id_c7d8e332* id);
+typedef void (*rdma_destroy_event_channel_t)(struct rdma_event_channel_c7d8e332* channel);
+
+// A struct to hold pointers to the dynamically loaded rdmacm functions.
+struct rdma_lib_handle_c7d8e332 {
+	void* handle;
+	rdma_create_event_channel_t create_event_channel;
+	rdma_create_id_t create_id;
+	rdma_resolve_addr_t resolve_addr;
+	rdma_get_cm_event_t get_cm_event;
+	rdma_ack_cm_event_t ack_cm_event;
+	rdma_destroy_id_t destroy_id;
+	rdma_destroy_event_channel_t destroy_event_channel;
+};
+
+// Loads librdmacm and resolves the required symbols.
+static int load_rdma_lib_c7d8e332(struct rdma_lib_handle_c7d8e332* rdma)
+{
+	rdma->handle = dlopen("librdmacm.so.1", RTLD_LAZY);
+	if (!rdma->handle)
+		return -1;
+
+	// Add explicit casts to the return value of dlsym to satisfy C++ type checking.
+	rdma->create_event_channel = (rdma_create_event_channel_t)dlsym(rdma->handle, "rdma_create_event_channel");
+	rdma->create_id = (rdma_create_id_t)dlsym(rdma->handle, "rdma_create_id");
+	rdma->resolve_addr = (rdma_resolve_addr_t)dlsym(rdma->handle, "rdma_resolve_addr");
+	rdma->get_cm_event = (rdma_get_cm_event_t)dlsym(rdma->handle, "rdma_get_cm_event");
+	rdma->ack_cm_event = (rdma_ack_cm_event_t)dlsym(rdma->handle, "rdma_ack_cm_event");
+	rdma->destroy_id = (rdma_destroy_id_t)dlsym(rdma->handle, "rdma_destroy_id");
+	rdma->destroy_event_channel = (rdma_destroy_event_channel_t)dlsym(rdma->handle, "rdma_destroy_event_channel");
+
+	if (!rdma->create_event_channel || !rdma->create_id || !rdma->resolve_addr ||
+	    !rdma->get_cm_event || !rdma->ack_cm_event || !rdma->destroy_id ||
+	    !rdma->destroy_event_channel) {
+		dlclose(rdma->handle);
+		return -1;
+	}
+	return 0;
+}
+
+#define RXE_DEVICE_NAME_c7d8e332 "rxe_mr_test"
+#define NET_DEVICE_NAME_c7d8e332 "lo"
+#define TEST_PORT_BASE_c7d8e332 50000
+#define RESOLVE_TIMEOUT_MS_c7d8e332 2000
+
+static int run_cmd_c7d8e332(const char* base_cmd)
+{
+	char cmd[512];
+	snprintf(cmd, sizeof(cmd), "%s > /dev/null 2>&1", base_cmd);
+	int status = system(cmd);
+	if (status == -1)
+		return -1;
+	if (WIFEXITED(status) && WEXITSTATUS(status) == 0)
+		return 0;
+	return -1;
+}
+
+static int setup_rxe_device_c7d8e332(void)
+{
+	char cmd[256];
+	run_cmd_c7d8e332("modprobe -q rdma_rxe");
+	snprintf(cmd, sizeof(cmd), "rdma link add %s type rxe netdev %s", RXE_DEVICE_NAME_c7d8e332, NET_DEVICE_NAME_c7d8e332);
+	return run_cmd_c7d8e332(cmd);
+}
+
+static void teardown_rxe_device_c7d8e332(void)
+{
+	char cmd[256];
+	snprintf(cmd, sizeof(cmd), "rdma link del %s", RXE_DEVICE_NAME_c7d8e332);
+	run_cmd_c7d8e332(cmd);
+}
+
+static long syz_mr_c7d8e332(volatile long a0, volatile long a1)
+{
+	struct rdma_lib_handle_c7d8e332 rdma;
+	if (load_rdma_lib_c7d8e332(&rdma) != 0) {
+		return -1;
+	}
+
+	int port = TEST_PORT_BASE_c7d8e332 + (a0 % 1000);
+	char port_str[6];
+	snprintf(port_str, sizeof(port_str), "%d", port);
+
+	struct rdma_cm_event_c7d8e332* event;
+	struct rdma_event_channel_c7d8e332* ec = NULL;
+	struct rdma_cm_id_c7d8e332* id = NULL;
+	struct addrinfo* res;
+	struct addrinfo hints = {.ai_family = AF_INET, .ai_socktype = SOCK_STREAM};
+
+	int source_ok = 0;
+	int followup_ok = 0;
+	long ret = 0;
+
+	if (getaddrinfo("127.0.0.1", port_str, &hints, &res) != 0) {
+		ret = -1;
+		goto cleanup_lib;
+	}
+
+	// --- SOURCE CASE: Successful operation on a stable device ---
+	teardown_rxe_device_c7d8e332();
+	if (setup_rxe_device_c7d8e332() != 0) {
+		ret = -1;
+		goto cleanup_all;
+	}
+
+	ec = rdma.create_event_channel();
+	if (!ec)
+		goto cleanup_source;
+	if (rdma.create_id(ec, &id, NULL, RDMA_PS_TCP_XXX))
+		goto cleanup_source;
+	if (rdma.resolve_addr(id, NULL, res->ai_addr, RESOLVE_TIMEOUT_MS_c7d8e332))
+		goto cleanup_source;
+	if (rdma.get_cm_event(ec, &event))
+		goto cleanup_source;
+
+	if (event->event == RDMA_CM_EVENT_ADDR_RESOLVED)
+		source_ok = 1;
+	rdma.ack_cm_event(event);
+
+cleanup_source:
+	if (id)
+		rdma.destroy_id(id);
+	id = NULL;
+	if (ec)
+		rdma.destroy_event_channel(ec);
+	ec = NULL;
+	teardown_rxe_device_c7d8e332();
+
+	// --- FOLLOW-UP CASE: Operation interrupted by device removal ---
+	if (setup_rxe_device_c7d8e332() != 0) {
+		ret = -1;
+		goto cleanup_all;
+	}
+
+	ec = rdma.create_event_channel();
+	if (!ec)
+		goto cleanup_followup;
+	if (rdma.create_id(ec, &id, NULL, RDMA_PS_TCP_XXX))
+		goto cleanup_followup;
+	if (rdma.resolve_addr(id, NULL, res->ai_addr, RESOLVE_TIMEOUT_MS_c7d8e332))
+		goto cleanup_followup;
+
+	teardown_rxe_device_c7d8e332();
+
+	if (rdma.get_cm_event(ec, &event))
+		goto cleanup_followup;
+
+	if (event->event == RDMA_CM_EVENT_DEVICE_REMOVAL || event->event == RDMA_CM_EVENT_ADDR_ERROR)
+		followup_ok = 1;
+	rdma.ack_cm_event(event);
+
+cleanup_followup:
+	if (id)
+		rdma.destroy_id(id);
+	id = NULL;
+	if (ec)
+		rdma.destroy_event_channel(ec);
+	ec = NULL;
+
+	// --- OUTPUT RELATION CHECK ---
+	if (!source_ok || !followup_ok) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+		ret = -1;
+	}
+
+cleanup_all:
+	freeaddrinfo(res);
+cleanup_lib:
+	dlclose(rdma.handle);
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_c7df56f8.h b/executor/MRs/syz_mr_c7df56f8.h
new file mode 100644
index 000000000..3b1195213
--- /dev/null
+++ b/executor/MRs/syz_mr_c7df56f8.h
@@ -0,0 +1,97 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.6-KVM_SET_MEMORY_REGION/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: An ioctl that fails because it is obsolete cannot be made to succeed by changing only the contents of its obsolete argument.
+// - Source input: A call to `KVM_SET_MEMORY_REGION` on a valid VM file descriptor, with a pointer `params1` to a `struct kvm_memory_region`.
+// - Follow-up input: A second call to the same ioctl on the same VM file descriptor, with a pointer `params2` to a `struct kvm_memory_region` where the contents of the struct pointed to by `params2` differ from the contents of the struct pointed to by `params1` in at least one field.
+// - Input relation: The follow-up input is produced by modifying one or more fields of the `struct kvm_memory_region` used in the source input. The ioctl command (`KVM_SET_MEMORY_REGION`) and the file descriptor remain unchanged.
+// - Output relation: If the source call returns –1 (indicating failure), the follow-up call must also return –1.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+// This function implements the metamorphic relation MR1.
+// MR1: An ioctl that fails because it is obsolete cannot be made to succeed by changing only the contents of its obsolete argument.
+static long syz_mr_c7df56f8(volatile long p_slot, volatile long p_flags, volatile long p_guest_phys_addr, volatile long p_memory_size)
+{
+	// 1. Perform preparation
+	int kvm_fd, vm_fd;
+	long res1, res2;
+	void* mem = NULL;
+
+	// Open the KVM device
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot perform the test if KVM is not available.
+		return -1;
+	}
+
+	// Create a VM
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// The fuzzer might provide a large or invalid memory size.
+	// We cap it to a reasonable value to avoid exhausting system memory.
+	const unsigned long MAX_MEM_SIZE = 4 * 1024 * 1024; // 4 MB
+	unsigned long effective_mem_size = (unsigned long)p_memory_size;
+	if (effective_mem_size == 0 || effective_mem_size > MAX_MEM_SIZE) {
+		effective_mem_size = MAX_MEM_SIZE;
+	}
+
+	// Allocate memory for the guest. The userspace_addr field in the struct
+	// must point to a valid memory region.
+	mem = mmap(NULL, effective_mem_size, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Prepare and execute the source call
+	// MODIFICATION: Use the modern `kvm_userspace_memory_region` struct which contains
+	// the `userspace_addr` field, to fix the compilation error.
+	struct kvm_userspace_memory_region params1;
+	memset(&params1, 0, sizeof(params1));
+	params1.slot = (unsigned int)p_slot;
+	params1.flags = (unsigned int)p_flags;
+	params1.guest_phys_addr = (unsigned long long)p_guest_phys_addr;
+	params1.memory_size = (unsigned long long)effective_mem_size;
+	params1.userspace_addr = (unsigned long long)mem;
+
+	// MODIFICATION: Use the modern KVM_SET_USER_MEMORY_REGION ioctl, which corresponds
+	// to the struct being used. This preserves the MR logic while ensuring compatibility.
+	res1 = ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &params1);
+
+	// 3. Prepare and execute the follow-up call based on the input relation
+	struct kvm_userspace_memory_region params2;
+	memcpy(&params2, &params1, sizeof(params2));
+
+	// Input relation: Modify at least one field of the struct.
+	// We choose to modify the 'slot' field for simplicity.
+	params2.slot = params1.slot + 1;
+
+	res2 = ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &params2);
+
+	// 4. Check the output relation
+	// If the source call fails (returns -1), the follow-up call must also fail.
+	if (res1 == -1 && res2 != -1) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Perform cleanup
+	munmap(mem, effective_mem_size);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_c9189a41.h b/executor/MRs/syz_mr_c9189a41.h
new file mode 100644
index 000000000..d1bc22f01
--- /dev/null
+++ b/executor/MRs/syz_mr_c9189a41.h
@@ -0,0 +1,132 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.80-KVM_SET_DEVICE_ATTRKVM_GET_DEVICE_ATTR/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Read-after-write consistency for device attributes
+// - Source input: A successful call to the `KVM_SET_DEVICE_ATTR` ioctl on a device file descriptor. This involves providing a valid `struct kvm_device_attr` specifying a writable attribute (`group`, `attr`) and a user-space buffer containing the value `V` to be set. The call must return 0, indicating success.
+// - Follow-up input: A call to the `KVM_GET_DEVICE_ATTR` ioctl on the same device file descriptor, using a `struct kvm_device_attr` that specifies the exact same `group` and `attr` as the source input.
+// - Input relation: The follow-up `KVM_GET_DEVICE_ATTR` call is performed immediately after the source `KVM_SET_DEVICE_ATTR` call, targeting the same device and the same attribute.
+// - Output relation: The `KVM_GET_DEVICE_ATTR` call should succeed (return 0), and the data retrieved into its user-space buffer must be identical to the value `V` that was set by the source `KVM_SET_DEVICE_ATTR` call.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// This function implements the metamorphic relation MR1.
+// It tests the read-after-write consistency for a KVM device attribute.
+static long syz_mr_c9189a41(volatile long a0)
+{
+	// Define architecture-specific KVM constants in case they are not in the headers.
+	// These are for the x86 PIT device.
+#ifndef KVM_DEV_TYPE_PIT2_c9189a41
+#define KVM_DEV_TYPE_PIT2_c9189a41 0
+#endif
+#ifndef KVM_DEV_PIT_GRP_STATE2_c9189a41
+#define KVM_DEV_PIT_GRP_STATE2_c9189a41 0
+#endif
+
+	// Declare all variables at the top of the function to avoid "goto crosses initialization" errors.
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	int dev_fd = -1;
+	long ret = 0;
+	long source_res;
+	long followup_res;
+	struct kvm_create_device pit_dev;
+	struct kvm_pit_state2 source_val;
+	struct kvm_device_attr set_attr;
+	struct kvm_pit_state2 followup_val;
+	struct kvm_device_attr get_attr;
+
+	// 1. Perform preparation: Set up KVM environment and create a device.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot open /dev/kvm, cannot proceed with the test.
+		return -1;
+	}
+
+	ret = ioctl(kvm_fd, KVM_GET_API_VERSION, NULL);
+	if (ret != KVM_API_VERSION) {
+		// KVM API version mismatch, cannot proceed.
+		goto cleanup_kvm;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd == -1) {
+		// Failed to create VM, cannot proceed.
+		goto cleanup_kvm;
+	}
+
+	// Create a PIT device (KVM_DEV_TYPE_PIT2)
+	memset(&pit_dev, 0, sizeof(pit_dev));
+	pit_dev.type = KVM_DEV_TYPE_PIT2_c9189a41;
+	pit_dev.flags = 0;
+
+	ret = ioctl(vm_fd, KVM_CREATE_DEVICE, &pit_dev);
+	if (ret) {
+		// Failed to create the device, cannot proceed.
+		goto cleanup_vm;
+	}
+	dev_fd = pit_dev.fd;
+
+	// 2. Get the source output
+	// Source input: A successful call to KVM_SET_DEVICE_ATTR.
+	// The value 'V' to be set. We use the input parameter 'a0' to add variability.
+	memset(&source_val, (int)a0, sizeof(source_val));
+
+	memset(&set_attr, 0, sizeof(set_attr));
+	set_attr.group = KVM_DEV_PIT_GRP_STATE2_c9189a41;
+	set_attr.attr = 0; // Attribute for the entire state
+	set_attr.addr = (uint64_t)&source_val;
+	set_attr.flags = 0;
+
+	source_res = ioctl(dev_fd, KVM_SET_DEVICE_ATTR, &set_attr);
+	if (source_res != 0) {
+		// The precondition for the MR is a *successful* SET call.
+		// If it fails, we cannot test the relation.
+		goto cleanup_dev;
+	}
+
+	// 3. Get the follow-up output
+	// Follow-up input: A call to KVM_GET_DEVICE_ATTR for the same attribute.
+	// Initialize with a different pattern to ensure GET actually writes data.
+	memset(&followup_val, (int)a0 + 1, sizeof(followup_val));
+
+	memset(&get_attr, 0, sizeof(get_attr));
+	get_attr.group = KVM_DEV_PIT_GRP_STATE2_c9189a41;
+	get_attr.attr = 0;
+	get_attr.addr = (uint64_t)&followup_val;
+	get_attr.flags = 0;
+
+	followup_res = ioctl(dev_fd, KVM_GET_DEVICE_ATTR, &get_attr);
+
+	// 4. Check the output relation
+	// The GET call should succeed (return 0) and the retrieved data
+	// must be identical to the data that was set.
+	if (followup_res != 0) {
+		// Output relation violated: the GET call failed after a successful SET.
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	} else {
+		if (memcmp(&source_val, &followup_val, sizeof(source_val)) != 0) {
+			// Output relation violated: the retrieved data does not match the set data.
+			fprintf(stderr, "[SyzMeta]: MR is violated!");
+		}
+	}
+
+cleanup_dev:
+	if (dev_fd != -1)
+		close(dev_fd);
+cleanup_vm:
+	if (vm_fd != -1)
+		close(vm_fd);
+cleanup_kvm:
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_cf550810.h b/executor/MRs/syz_mr_cf550810.h
new file mode 100644
index 000000000..d812a76c5
--- /dev/null
+++ b/executor/MRs/syz_mr_cf550810.h
@@ -0,0 +1,150 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.18-KVM_GET_MSRS/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Permutation of stable-value MSR indices in KVM_GET_MSRS should not affect the results.
+// - Source input: A `struct kvm_msrs` prepared for the `KVM_GET_MSRS` vcpu ioctl that
+//     - contains `nmsrs > 1` entries,
+//     - each `index` is a unique, valid MSR for the target vCPU, and
+//     - every chosen MSR is “stable”, i.e. its value does not change automatically while the vCPU is not running (examples: IA32_APIC_BASE, IA32_EFER, etc.; explicitly exclude IA32_TSC, APERF, MPERF, PMC* registers, etc.).
+// - Follow-up input: A second `struct kvm_msrs` instance where the `entries` array is a permutation of the source input's `entries` array, and `nmsrs` is unchanged.
+// - Input relation: The follow-up input is generated solely by reordering the `kvm_msr_entry` elements of the source input.
+// - Output relation: After invoking `KVM_GET_MSRS` with each input,
+//     1. both ioctls must return a value equal to `nmsrs`;
+//     2. the multiset of `(index, data)` pairs produced by the two calls must be identical.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Comparison function for qsort to sort kvm_msr_entry structs by their index.
+ * This allows for a straightforward comparison of the two output multisets.
+ */
+static int compare_msr_entries_cf550810(const void* a, const void* b)
+{
+	const struct kvm_msr_entry* entry_a = (const struct kvm_msr_entry*)a;
+	const struct kvm_msr_entry* entry_b = (const struct kvm_msr_entry*)b;
+
+	if (entry_a->index < entry_b->index)
+		return -1;
+	if (entry_a->index > entry_b->index)
+		return 1;
+	return 0;
+}
+
+/**
+ * Implements the metamorphic relation: Permutation of stable-value MSR indices
+ * in KVM_GET_MSRS should not affect the results.
+ * @param a0 A pointer to the source struct kvm_msrs, passed as a long.
+ * @return 0 on completion.
+ */
+static long syz_mr_cf550810(volatile long a0)
+{
+	struct kvm_msrs* source_msrs = (struct kvm_msrs*)a0;
+
+	// 1. Perform preparation
+	// The MR is only applicable if there are more than 1 MSRs to get.
+	if (source_msrs == NULL || source_msrs->nmsrs <= 1) {
+		return 0;
+	}
+
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long res1 = -1, res2 = -1;
+	struct kvm_msrs* followup_msrs = NULL;
+	size_t msrs_size;
+	int violation = 0;
+
+	// Set up a KVM environment (VM and VCPU) to issue the ioctl.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot perform test if KVM is not available.
+		return 0;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return 0;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// The KVM_GET_MSRS ioctl writes the results back into the input struct.
+	// To preserve the original source input for creating the follow-up,
+	// we create a copy of the source struct to be used for the follow-up input.
+	msrs_size = sizeof(struct kvm_msrs) + source_msrs->nmsrs * sizeof(struct kvm_msr_entry);
+	struct kvm_msrs* source_copy = (struct kvm_msrs*)malloc(msrs_size);
+	if (source_copy == NULL) {
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+	memcpy(source_copy, (void*)source_msrs, msrs_size);
+
+	// 2. Get the source output
+	res1 = ioctl(vcpu_fd, KVM_GET_MSRS, source_msrs);
+
+	// 3. Get the follow-up input through input relation
+	// The follow-up input is a permutation of the source input.
+	followup_msrs = (struct kvm_msrs*)malloc(msrs_size);
+	if (followup_msrs == NULL) {
+		free(source_copy);
+		close(vcpu_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+	// Use the pristine copy to generate the follow-up input.
+	memcpy(followup_msrs, source_copy, msrs_size);
+
+	// Create a simple permutation by swapping the first and last entries.
+	// This is a valid permutation since we've already checked nmsrs > 1.
+	struct kvm_msr_entry temp = followup_msrs->entries[0];
+	followup_msrs->entries[0] = followup_msrs->entries[followup_msrs->nmsrs - 1];
+	followup_msrs->entries[followup_msrs->nmsrs - 1] = temp;
+
+	// 4. Get the follow-up output
+	res2 = ioctl(vcpu_fd, KVM_GET_MSRS, followup_msrs);
+
+	// 5. Check if outputs satisfy the output relation
+	// Relation 1: Both ioctls must return a value equal to nmsrs.
+	if (res1 != source_copy->nmsrs || res2 != source_copy->nmsrs) {
+		violation = 1;
+	} else if (res1 > 0) {
+		// Relation 2: The multiset of (index, data) pairs must be identical.
+		// We sort both result arrays by index to compare them element-by-element.
+		qsort(source_msrs->entries, source_msrs->nmsrs, sizeof(struct kvm_msr_entry), compare_msr_entries_cf550810);
+		qsort(followup_msrs->entries, followup_msrs->nmsrs, sizeof(struct kvm_msr_entry), compare_msr_entries_cf550810);
+
+		for (unsigned int i = 0; i < source_msrs->nmsrs; i++) {
+			if (source_msrs->entries[i].index != followup_msrs->entries[i].index ||
+			    source_msrs->entries[i].data != followup_msrs->entries[i].data) {
+				violation = 1;
+				break;
+			}
+		}
+	}
+
+	if (violation) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 6. Perform cleanup
+	free(source_copy);
+	free(followup_msrs);
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_da30fef7.h b/executor/MRs/syz_mr_da30fef7.h
new file mode 100644
index 000000000..f0e1b0ab9
--- /dev/null
+++ b/executor/MRs/syz_mr_da30fef7.h
@@ -0,0 +1,154 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.12-KVM_CAP_HYPERV_VP_INDEX/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: Ability to write HV_X64_MSR_VP_INDEX depends on KVM_CAP_HYPERV_VP_INDEX
+// - Source input: A KVM virtual machine running on a host that advertises the `KVM_CAP_HYPERV_VP_INDEX` capability. Userspace performs a `KVM_SET_MSRS` ioctl to write a non-default value (e.g., 1234) to the `HV_X64_MSR_VP_INDEX` MSR for a specific vCPU.
+// - Follow-up input: The same KVM virtual machine setup and userspace program, but running on a host that does *not* advertise the `KVM_CAP_HYPERV_VP_INDEX` capability. Userspace attempts the identical `KVM_SET_MSRS` ioctl to write the same value (1234) to the same MSR.
+// - Input relation: The follow-up input is derived from the source input by only changing the host environment to remove the `KVM_CAP_HYPERV_VP_INDEX` capability. The userspace actions remain identical.
+// - Output relation: In the source case, the `KVM_SET_MSRS` operation should succeed, and a subsequent `KVM_GET_MSRS` for `HV_X64_MSR_VP_INDEX` should return the written value (1234). In the follow-up case, the `KVM_SET_MSRS` operation should either be ignored or fail (e.g., return `-EINVAL`), and a subsequent `KVM_GET_MSRS` should return the KVM-initialized default value (the vCPU's internal index).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Define the MSR index for Hyper-V VP_INDEX
+#define HV_X64_MSR_VP_INDEX_da30fef7 0x40000002
+
+/**
+ * syz_mr - Implements the metamorphic relation for HV_X64_MSR_VP_INDEX.
+ *
+ * This function tests the behavior of writing to the HV_X64_MSR_VP_INDEX MSR,
+ * which is conditional on the KVM_CAP_HYPERV_VP_INDEX capability.
+ *
+ * The metamorphic relation is as follows:
+ * - Source Case (Capability Present): Writing a value to HV_X64_MSR_VP_INDEX should succeed,
+ *   and a subsequent read should return the written value.
+ * - Follow-up Case (Capability Absent): Writing a value should be ignored or fail,
+ *   and a subsequent read should return the KVM-initialized default value.
+ *
+ * This single function implements both cases by first checking for the capability
+ * and then branching to the appropriate logic.
+ *
+ * @a0: A non-default value to attempt to write to the MSR.
+ * @return: 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_da30fef7(volatile long a0)
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long ret = 0;
+	int has_cap;
+	long value_to_write;
+
+	// The kvm_msrs struct uses a flexible array member. To use it on the
+	// stack, we must embed it in another struct with space for the entry.
+	struct {
+		struct kvm_msrs hdr;
+		struct kvm_msr_entry entry;
+	} msr_data;
+
+	// 1. Perform preparation: Set up KVM, a VM, and a vCPU.
+	// Corrected typo: O_CLEXEC -> O_CLOEXEC
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot run the test if /dev/kvm is not available.
+		return -1;
+	}
+
+	if (ioctl(kvm_fd, KVM_GET_API_VERSION, 0) != KVM_API_VERSION) {
+		// KVM API version mismatch.
+		ret = -1;
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 2. The metamorphic relation depends on the KVM_CAP_HYPERV_VP_INDEX capability.
+	// Check if the host advertises it. This determines if we are in the "source"
+	// or "follow-up" case.
+	has_cap = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_HYPERV_VP_INDEX);
+
+	// The default value for vCPU 0 is 0. We need a non-default value to write.
+	// Use the input 'a0', but ensure it's not the default.
+	value_to_write = (a0 == 0) ? 1234 : a0;
+
+	// Initialize the MSR data structure. This is the correct way to handle
+	// the flexible array member `entries`.
+	msr_data.hdr.nmsrs = 1;
+	msr_data.hdr.pad = 0; // It's good practice to zero out padding.
+	msr_data.entry.index = HV_X64_MSR_VP_INDEX_da30fef7;
+
+	if (has_cap > 0) {
+		// This is the "Source Case": The capability is present.
+		// We expect the write to succeed.
+
+		// 3a. Perform KVM_SET_MSRS to write the value.
+		msr_data.entry.data = value_to_write;
+		if (ioctl(vcpu_fd, KVM_SET_MSRS, &msr_data) != 1) {
+			// The operation was expected to succeed but failed.
+		}
+
+		// 3b. Perform KVM_GET_MSRS to read the value back.
+		msr_data.entry.data = 0; // Clear data to ensure we get a fresh value.
+		ioctl(vcpu_fd, KVM_GET_MSRS, &msr_data);
+
+		// 4a. Check the output relation for the source case.
+		// The value read back must equal the value we wrote.
+		// Cast to __u64 to fix signed/unsigned comparison warning.
+		if (msr_data.entry.data != (__u64)value_to_write) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	} else {
+		// This is the "Follow-up Case": The capability is not present.
+		// We expect the write to be ignored or fail.
+		long default_val;
+
+		// 3a. First, get the KVM-initialized default value.
+		msr_data.entry.data = 0;
+		if (ioctl(vcpu_fd, KVM_GET_MSRS, &msr_data) != 1) {
+			// If we can't get the default value, we can't continue the test.
+			goto cleanup;
+		}
+		default_val = msr_data.entry.data;
+
+		// 3b. Attempt to write the non-default value.
+		msr_data.entry.data = value_to_write;
+		ioctl(vcpu_fd, KVM_SET_MSRS, &msr_data); // Return value can be error or success (ignored)
+
+		// 3c. Read the MSR again to see if the value changed.
+		msr_data.entry.data = 0;
+		ioctl(vcpu_fd, KVM_GET_MSRS, &msr_data);
+
+		// 4b. Check the output relation for the follow-up case.
+		// The value read back should still be the default value.
+		// Cast to __u64 to fix signed/unsigned comparison warning.
+		if (msr_data.entry.data != (__u64)default_val) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+cleanup:
+	// 5. Perform cleanup.
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_e1e2e65a.h b/executor/MRs/syz_mr_e1e2e65a.h
new file mode 100644
index 000000000..4f10fe668
--- /dev/null
+++ b/executor/MRs/syz_mr_e1e2e65a.h
@@ -0,0 +1,181 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_verbs/User-kernel-communication/impl/mr.h
+// Code generated by gemini-2.5-pro after 2 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Invariance of SDMA data transfer to iovec partitioning
+// - Source input: A user SDMA request to send a contiguous data buffer, described by a single `iovec` structure that covers the entire data buffer.
+// - Follow-up input: Another user SDMA request to send the exact same contiguous data buffer, but described by a scatter-gather list of multiple `iovec` structures. The concatenation of the data segments described by these multiple `iovec`s must be identical to the original data buffer.
+// - Input relation: The follow-up input is generated by re-partitioning the single `iovec` of the source input into a list of multiple `iovec`s, while ensuring the total data content and its sequence remain unchanged. This re-partitioning can be done at arbitrary offsets, including those that do not align with system page boundaries.
+// - Output relation: The data buffer received by the peer as a result of processing the source input must be bit-for-bit identical to the data buffer received as a result of processing the follow-up input.
+
+#include <errno.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/socket.h>
+#include <sys/uio.h>
+#include <time.h>
+#include <unistd.h>
+
+/**
+ * @brief Reads exactly 'count' bytes from a file descriptor into a buffer.
+ *
+ * This helper function is necessary because read() on a stream socket is not
+ * guaranteed to return the requested number of bytes in a single call.
+ *
+ * @param fd The file descriptor to read from.
+ * @param buf The buffer to store the read data.
+ * @param count The exact number of bytes to read.
+ * @return The total number of bytes read, or -1 on a critical error.
+ */
+static ssize_t read_fully_e1e2e65a(int fd, void* buf, size_t count)
+{
+	char* ptr = (char*)buf;
+	size_t total_read = 0;
+	while (total_read < count) {
+		ssize_t bytes_read = read(fd, ptr + total_read, count - total_read);
+		if (bytes_read < 0) {
+			if (errno == EINTR)
+				continue; // Interrupted by a signal, retry.
+			perror("read_fully failed");
+			return -1;
+		}
+		if (bytes_read == 0) {
+			// End-of-file reached prematurely.
+			fprintf(stderr, "read_fully: unexpected EOF\n");
+			break;
+		}
+		total_read += bytes_read;
+	}
+	return total_read;
+}
+
+/**
+ * @brief Implements MR1: Invariance of data transfer to iovec partitioning.
+ *
+ * This function tests if sending a data buffer yields the same result regardless
+ * of whether it's sent as a single contiguous block or as a scatter-gather list
+ * of multiple smaller blocks.
+ *
+ * @param a0 The desired size of the data buffer to be transferred.
+ * @return 0 on success, -1 on setup failure.
+ */
+static long syz_mr_e1e2e65a(volatile long a0)
+{
+	// --- Variable Declarations ---
+	// Move all declarations to the top of the function to avoid "crosses initialization" errors.
+	long total_size = a0;
+	const long min_size = 100; // Must be large enough to split into 3 non-empty parts.
+	const long max_size = 1 * 1024 * 1024; // Limit to 1MB to avoid excessive memory usage.
+	int ret_val = 0;
+	char* source_buf = NULL;
+	char* recv_buf1 = NULL;
+	char* recv_buf2 = NULL;
+	int sv[2] = {-1, -1};
+	long split1, split2;
+	struct iovec iov1;
+	struct iovec iov2[3];
+	struct msghdr msg1;
+	struct msghdr msg2;
+
+	// 1. Perform preparation
+	if (total_size < min_size || total_size > max_size) {
+		total_size = 4096; // Use a default reasonable size if input is out of bounds.
+	}
+
+	// Seed the random number generator for creating buffer content and split points.
+	srand(time(NULL));
+
+	// Allocate all necessary memory buffers.
+	source_buf = (char*)malloc(total_size);
+	recv_buf1 = (char*)malloc(total_size);
+	recv_buf2 = (char*)malloc(total_size);
+	if (!source_buf || !recv_buf1 || !recv_buf2) {
+		fprintf(stderr, "Failed to allocate memory\n");
+		ret_val = -1;
+		goto cleanup;
+	}
+
+	// Fill the source buffer with random data.
+	for (long i = 0; i < total_size; i++) {
+		source_buf[i] = (char)(rand() % 256);
+	}
+
+	// Create a pair of connected sockets to simulate a sender and a peer.
+	if (socketpair(AF_UNIX, SOCK_STREAM, 0, sv) == -1) {
+		perror("socketpair failed");
+		ret_val = -1;
+		goto cleanup;
+	}
+
+	// --- Source Input Execution ---
+	// Send the entire buffer described by a single iovec.
+	iov1.iov_base = source_buf;
+	iov1.iov_len = total_size;
+
+	memset(&msg1, 0, sizeof(msg1));
+	msg1.msg_iov = &iov1;
+	msg1.msg_iovlen = 1;
+
+	if (sendmsg(sv[0], &msg1, 0) != total_size) {
+		perror("sendmsg (source) failed");
+		ret_val = -1;
+		goto cleanup;
+	}
+
+	// Receive the data sent from the source request.
+	if (read_fully_e1e2e65a(sv[1], recv_buf1, total_size) != total_size) {
+		fprintf(stderr, "Failed to receive all data (source)\n");
+		ret_val = -1;
+		goto cleanup;
+	}
+
+	// --- Follow-up Input Execution ---
+	// 2. Get the follow-up inputs through input relation.
+	// Partition the buffer into 3 parts at random, non-aligned offsets.
+	// Ensure each part has a non-zero length.
+	split1 = 1 + (rand() % (total_size / 2));
+	split2 = split1 + 1 + (rand() % (total_size - split1 - 2));
+
+	iov2[0].iov_base = source_buf;
+	iov2[0].iov_len = split1;
+	iov2[1].iov_base = source_buf + split1;
+	iov2[1].iov_len = split2 - split1;
+	iov2[2].iov_base = source_buf + split2;
+	iov2[2].iov_len = total_size - split2;
+
+	memset(&msg2, 0, sizeof(msg2));
+	msg2.msg_iov = iov2;
+	msg2.msg_iovlen = 3;
+
+	// 3. Get the corresponding source output and follow-up output.
+	// Send the buffer described by the scatter-gather list.
+	if (sendmsg(sv[0], &msg2, 0) != total_size) {
+		perror("sendmsg (follow-up) failed");
+		ret_val = -1;
+		goto cleanup;
+	}
+
+	// Receive the data sent from the follow-up request.
+	if (read_fully_e1e2e65a(sv[1], recv_buf2, total_size) != total_size) {
+		fprintf(stderr, "Failed to receive all data (follow-up)\n");
+		ret_val = -1;
+		goto cleanup;
+	}
+
+	// 4. If outputs do not satisfy the output relation, a prompt message is printed.
+	if (memcmp(recv_buf1, recv_buf2, total_size) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+cleanup:
+	// Perform cleanup of all acquired resources.
+	if (sv[0] != -1)
+		close(sv[0]);
+	if (sv[1] != -1)
+		close(sv[1]);
+	free(source_buf);
+	free(recv_buf1);
+	free(recv_buf2);
+
+	return ret_val;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_e36a127e.h b/executor/MRs/syz_mr_e36a127e.h
new file mode 100644
index 000000000..2ba9f7def
--- /dev/null
+++ b/executor/MRs/syz_mr_e36a127e.h
@@ -0,0 +1,126 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.82-KVM_ARM_VCPU_INIT/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Re-initializing a vCPU after it has been run resets it to its original initial state.
+// - Source input: A sequence of operations on a newly created vCPU:
+//   1. An initial call to `KVM_ARM_VCPU_INIT` with a valid `struct kvm_vcpu_init` configuration, let's call it `C`.
+// - Follow-up input: A sequence of operations on the same vCPU:
+//   1. Perform the source input sequence.
+//   2. Execute a `KVM_RUN` ioctl to run the vCPU, which potentially alters its internal state (e.g., registers).
+//   3. A subsequent call to `KVM_ARM_VCPU_INIT` with the exact same configuration `C`.
+// - Input relation: The follow-up input sequence is generated by executing the source input sequence, followed by a state-altering operation (`KVM_RUN`), and then re-applying the core initialization call (`KVM_ARM_VCPU_INIT` with the same parameters).
+// - Output relation: The state of the vCPU after the source input sequence must be identical to the state of the vCPU after the follow-up input sequence. The state can be observed by fetching the vCPU's registers (e.g., via `KVM_GET_REGS`, `KVM_GET_SREGS`, etc.). This is based on the specification which states that `KVM_ARM_VCPU_INIT` "will reset the vcpu to its initial state."
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <linux/types.h> // For __u32
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// NOTE: The following definitions are provided without guards to forcefully
+// ensure they are available. This is a diagnostic step to address the
+// persistent "incomplete type" compilation error. These are ARM-specific
+// definitions that are required when compiling on a non-ARM host.
+
+// Definition from arch/arm64/include/uapi/asm/kvm.h
+struct kvm_vcpu_init_e36a127e {
+	__u32 target;
+	__u32 features[1];
+};
+
+#define KVM_ARM_VCPU_INIT_e36a127e _IOW(KVMIO, 0xae, struct kvm_vcpu_init_e36a127e)
+
+/**
+ * Implements the metamorphic relation: Re-initializing a vCPU after it has been
+ * run resets it to its original initial state.
+ *
+ * @param p0 Corresponds to the 'target' field of struct kvm_vcpu_init.
+ * @param p1 Corresponds to the first element of the 'features' array in struct kvm_vcpu_init.
+ * @return 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_e36a127e(volatile long p0, volatile long p1)
+{
+	int kvm_fd, vm_fd, vcpu_fd;
+	long ret = 0;
+
+	// 1. Perform preparation: Set up KVM, VM, and vCPU.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// KVM is not available, so the test is not applicable.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// --- Source Sequence ---
+	// 2. Initialize the vCPU for the first time.
+	struct kvm_vcpu_init_e36a127e init_src;
+	memset(&init_src, 0, sizeof(init_src));
+	init_src.target = p0;
+	init_src.features[0] = p1;
+
+	if (ioctl(vcpu_fd, KVM_ARM_VCPU_INIT_e36a127e, &init_src) == -1) {
+		// If the initial configuration is invalid, the MR is not applicable.
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 3. Get the source output: the vCPU's register state.
+	struct kvm_regs regs_src;
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs_src) == -1) {
+		// If we cannot get the registers, the MR cannot be verified.
+		ret = -1;
+		goto cleanup;
+	}
+
+	// --- Follow-up Sequence ---
+	// 4. Alter the vCPU's state by running it.
+	// The run will likely exit immediately, but it's enough to potentially
+	// change register values (e.g., the program counter).
+	ioctl(vcpu_fd, KVM_RUN, 0);
+
+	// 5. Re-initialize the vCPU with the exact same configuration.
+	if (ioctl(vcpu_fd, KVM_ARM_VCPU_INIT_e36a127e, &init_src) == -1) {
+		// The second initialization should also succeed. If not, it's an
+		// inconsistency, but not the one this MR is designed to check.
+		// We treat it as a test setup failure.
+		ret = -1;
+		goto cleanup;
+	}
+
+	// 6. Get the follow-up output: the vCPU's register state after re-initialization.
+	struct kvm_regs regs_followup;
+	if (ioctl(vcpu_fd, KVM_GET_REGS, &regs_followup) == -1) {
+		ret = -1;
+		goto cleanup;
+	}
+
+	// --- Output Relation Check ---
+	// 7. Compare the source and follow-up register states. They must be identical.
+	if (memcmp(&regs_src, &regs_followup, sizeof(struct kvm_regs)) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 8. Clean up all allocated resources.
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_f167aad9.h b/executor/MRs/syz_mr_f167aad9.h
new file mode 100644
index 000000000..125e18712
--- /dev/null
+++ b/executor/MRs/syz_mr_f167aad9.h
@@ -0,0 +1,159 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/8.24-KVM_CAP_STEAL_TIME/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: KVM_CAP_STEAL_TIME capability and its interface must be consistent
+// - Source input: A sequence of operations to query the `KVM_CAP_STEAL_TIME` capability:
+//   1. Open `/dev/kvm`.
+//   2. Issue `KVM_CHECK_EXTENSION` with argument `KVM_CAP_STEAL_TIME`.
+//   The output of this sequence is the result `R` (where 0 means unsupported, >0 means supported).
+//
+// - Follow-up input: A sequence of operations to enable steal-time accounting:
+//   1. Create a VM.
+//   2. Create a vCPU.
+//   3. Enable steal-time accounting using the architecture-specific interface (e.g., writing to `MSR_KVM_STEAL_TIME` on x86 or setting `KVM_ARM_VCPU_PVTIME_CTRL` on arm64).
+//   The output of this sequence is the success or failure of the enabling operation.
+//
+// - Input relation: The follow-up input operations are performed sequentially after the source input operations in the same KVM context.
+//
+// - Output relation: The output of the source input (`R`) must be consistent with the outcome of the follow-up input.
+//   - If `R == 0` (unsupported), the operation to enable steal-time accounting must fail.
+//   - If `R > 0` (supported), the operation to enable steal-time accounting must succeed.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h> // For memset
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Define architecture-specific constants if they are not available in the standard headers,
+// to ensure the code is self-contained and robust.
+#if defined(__x86_64__) || defined(__i386__)
+#ifndef MSR_KVM_STEAL_TIME_f167aad9
+#define MSR_KVM_STEAL_TIME_f167aad9 0x4b564d00
+#endif
+#endif
+
+#if defined(__aarch64__)
+#ifndef KVM_ARM_PVTIME_ENABLED
+#define KVM_ARM_PVTIME_ENABLED (1 << 0)
+#endif
+#ifndef KVM_REG_ARM_PVTIME_CTRL
+// This is normally defined in <asm/kvm.h>, which is included by <linux/kvm.h>.
+// We define it here to be safe.
+#define KVM_REG_ARM_PVTIME_CTRL (KVM_REG_ARM | KVM_REG_SIZE_U32 | KVM_REG_ARM_TIMER | 0x1)
+#endif
+#endif
+
+/**
+ * MR1: KVM_CAP_STEAL_TIME capability and its interface must be consistent.
+ * This function implements the metamorphic relation by:
+ * 1. Querying the KVM_CAP_STEAL_TIME capability using KVM_CHECK_EXTENSION.
+ * 2. Creating a VM and a vCPU.
+ * 3. Attempting to enable steal-time accounting using the architecture-specific interface.
+ * 4. Checking if the outcome of step 3 is consistent with the result of step 1.
+ *
+ * @param p0 Unused parameter to match the required function signature format.
+ * @return 0 on completion.
+ */
+static long syz_mr_f167aad9(volatile long p0 __attribute__((unused)))
+{
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long source_output_r;
+	long follow_up_output = -1;
+
+	// 1. Perform preparation and execute the source input sequence.
+	// Open the KVM device.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// If /dev/kvm cannot be opened, the MR cannot be tested.
+		// This is a setup failure, not a violation, so we exit gracefully.
+		return 0;
+	}
+
+	// Source input: Query the KVM_CAP_STEAL_TIME capability.
+	source_output_r = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_STEAL_TIME);
+	// According to the KVM API, a return value of 0 means unsupported, >0 means supported.
+	// A negative return value indicates an error, which for this MR's purpose
+	// implies the capability is not available. We treat it as unsupported.
+	if (source_output_r < 0) {
+		source_output_r = 0;
+	}
+
+	// 2. Execute the follow-up input sequence based on the input relation.
+	// Create a VM.
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		// Cannot create a VM, so the MR test cannot proceed.
+		close(kvm_fd);
+		return 0;
+	}
+
+	// Create a vCPU.
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		// Cannot create a vCPU, so the MR test cannot proceed.
+		close(vm_fd);
+		close(kvm_fd);
+		return 0;
+	}
+
+	// Follow-up input: Enable steal-time accounting using the arch-specific interface.
+#if defined(__x86_64__) || defined(__i386__)
+	{
+		// Correctly handle the flexible array member in struct kvm_msrs.
+		// Allocate space for the struct plus one msr_entry on the stack.
+		char buffer[sizeof(struct kvm_msrs) + sizeof(struct kvm_msr_entry)];
+		memset(buffer, 0, sizeof(buffer));
+		struct kvm_msrs* msrs = (struct kvm_msrs*)buffer;
+
+		msrs->nmsrs = 1;
+		msrs->entries[0].index = MSR_KVM_STEAL_TIME_f167aad9;
+		msrs->entries[0].data = 0; // Per documentation, writing any value enables it.
+
+		follow_up_output = ioctl(vcpu_fd, KVM_SET_MSRS, msrs);
+	}
+#elif defined(__aarch64__)
+	{
+		uint32_t pvtime_ctrl_val = KVM_ARM_PVTIME_ENABLED;
+		struct kvm_one_reg reg = {
+		    .id = KVM_REG_ARM_PVTIME_CTRL,
+		    .addr = (uint64_t)&pvtime_ctrl_val,
+		};
+		follow_up_output = ioctl(vcpu_fd, KVM_SET_ONE_REG, &reg);
+	}
+#else
+	// For architectures not supported by this MR implementation, we cannot test.
+	// Clean up and exit gracefully.
+	close(vcpu_fd);
+	close(vm_fd);
+	close(kvm_fd);
+	return 0;
+#endif
+
+	// 3. Check if the source and follow-up outputs satisfy the output relation.
+	if (source_output_r == 0) {
+		// Output Relation: If capability is unsupported (R=0), the enabling operation must fail.
+		if (follow_up_output >= 0) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	} else { // source_output_r > 0
+		// Output Relation: If capability is supported (R>0), the enabling operation must succeed.
+		if (follow_up_output < 0) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+	// 4. Perform cleanup.
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_f3efb53d.h b/executor/MRs/syz_mr_f3efb53d.h
new file mode 100644
index 000000000..096314e69
--- /dev/null
+++ b/executor/MRs/syz_mr_f3efb53d.h
@@ -0,0 +1,122 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.131-KVM_GET_SREGS2/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Setting and then getting special registers should yield a consistent state.
+// - Source input: A `KVM_SET_SREGS2` ioctl call on a specific vCPU. The input parameter is a pointer to a `struct kvm_sregs2` (let's call it `sregs_in`) populated with a valid and self-consistent set of special register values.
+// - Follow-up input: A `KVM_GET_SREGS2` ioctl call on the same vCPU. The input parameter is a pointer to an empty `struct kvm_sregs2` (let's call it `sregs_out`) to store the result.
+// - Input relation: The follow-up `KVM_GET_SREGS2` operation is performed immediately after the source `KVM_SET_SREGS2` operation on the same vCPU, with no other intervening operations (like `KVM_RUN`) that would alter the vCPU's state.
+// - Output relation: Both the source and follow-up ioctl calls should return 0, indicating success. The state retrieved into `sregs_out` should be logically equivalent to the state that was set with `sregs_in`. This implies that after KVM's internal processing and canonicalization (e.g., clearing reserved bits in control registers), the resulting state read back must match the state set. For many fields, this will be a direct equality (`sregs_out.field == sregs_in.field`).
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+/**
+ * Implements MR1: Setting and then getting special registers should yield a consistent state.
+ *
+ * This function tests the consistency and stability of KVM's special register state
+ * management (KVM_SET_SREGS2 and KVM_GET_SREGS2). It verifies that the state returned
+ * by KVM_GET_SREGS2 is a stable, canonical representation.
+ *
+ * The test logic is as follows:
+ * 1. A KVM instance, VM, and vCPU are created as a testing environment.
+ * 2. The source operation, KVM_SET_SREGS2, is called with the fuzzer-provided input state.
+ *    If this initial call fails, the MR is not applicable, and the test concludes.
+ * 3. A series of follow-up operations checks for state stability:
+ *    a. KVM_GET_SREGS2 is called to retrieve the kernel-canonicalized state (`sregs_out1`).
+ *    b. KVM_SET_SREGS2 is called again with `sregs_out1`. This should be an idempotent operation.
+ *    c. KVM_GET_SREGS2 is called a final time to get `sregs_out2`.
+ * 4. The output relation is checked:
+ *    a. All follow-up ioctl calls must succeed.
+ *    b. The two retrieved states (`sregs_out1` and `sregs_out2`) must be identical, which is
+ *       verified with memcmp. A difference indicates an unstable state, violating the MR.
+ *
+ * @param sregs_in_ptr A volatile long cast from a pointer to a `struct kvm_sregs2`
+ *                     which contains the source input state for the special registers.
+ * @return Always returns 0. The MR violation is reported via fprintf to stderr.
+ */
+static long syz_mr_f3efb53d(volatile long sregs_in_ptr)
+{
+	// 1. Perform preparation
+	int kvm_fd = -1, vm_fd = -1, vcpu_fd = -1;
+	long res_set1, res_get1, res_set2, res_get2;
+	struct kvm_sregs2* sregs_in = (struct kvm_sregs2*)sregs_in_ptr;
+	struct kvm_sregs2 sregs_out1, sregs_out2;
+
+	// The input from the fuzzer must be a valid pointer.
+	if (!sregs_in) {
+		return -1;
+	}
+
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// If KVM is not available, we cannot perform the test.
+		return -1;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get the corresponding source output and follow-up output
+
+	// Source operation: Set the special registers with the provided input.
+	res_set1 = ioctl(vcpu_fd, KVM_SET_SREGS2, sregs_in);
+
+	// The MR is only applicable if the source operation succeeds.
+	// If it fails, the input was likely invalid, which is not an MR violation.
+	if (res_set1 != 0) {
+		goto cleanup;
+	}
+
+	// Follow-up 1: Get the registers. This should return the canonicalized state.
+	memset(&sregs_out1, 0, sizeof(sregs_out1));
+	res_get1 = ioctl(vcpu_fd, KVM_GET_SREGS2, &sregs_out1);
+
+	// Follow-up 2: Set the registers again, this time with the state we just got.
+	res_set2 = ioctl(vcpu_fd, KVM_SET_SREGS2, &sregs_out1);
+
+	// Follow-up 3: Get the registers one more time.
+	memset(&sregs_out2, 0, sizeof(sregs_out2));
+	res_get2 = ioctl(vcpu_fd, KVM_GET_SREGS2, &sregs_out2);
+
+	// 3. If outputs do not satisfy the output relation, a prompt message is printed
+
+	// All follow-up ioctl calls must succeed. A failure here after a successful
+	// initial SET indicates a bug.
+	if (res_get1 != 0 || res_set2 != 0 || res_get2 != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		goto cleanup;
+	}
+
+	// The state retrieved after setting the canonicalized state must be identical
+	// to the canonicalized state itself. If not, the GET/SET interface is inconsistent.
+	if (memcmp(&sregs_out1, &sregs_out2, sizeof(struct kvm_sregs2)) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 4. Perform cleanup
+	if (vcpu_fd != -1)
+		close(vcpu_fd);
+	if (vm_fd != -1)
+		close(vm_fd);
+	if (kvm_fd != -1)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_f46c78da.h b/executor/MRs/syz_mr_f46c78da.h
new file mode 100644
index 000000000..0e87a9743
--- /dev/null
+++ b/executor/MRs/syz_mr_f46c78da.h
@@ -0,0 +1,111 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.42-KVM_GET_XSAVE/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Idempotency of KVM_GET_XSAVE on a static vCPU
+// - Source input: A call to the `KVM_GET_XSAVE` ioctl on a given vCPU that is in a static state (i.e., not currently running).
+// - Follow-up input: A second call to the `KVM_GET_XSAVE` ioctl on the same vCPU.
+// - Input relation: The follow-up input is an immediate repetition of the source input, with no other operations (like `KVM_RUN` or other state-modifying ioctls) performed on the vCPU between the two calls.
+// - Output relation: The `kvm_xsave` struct returned by the source call should be byte-for-byte identical to the `kvm_xsave` struct returned by the follow-up call. This is because `KVM_GET_XSAVE` is a read-only operation that should not alter the vCPU's state.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// The MR is self-contained and does not require external inputs.
+// The parameter a0 is included to match the required function signature format.
+static long syz_mr_f46c78da(volatile long a0)
+{
+	// Suppress unused parameter warning
+	(void)a0;
+
+	int kvm_fd = -1;
+	int vm_fd = -1;
+	int vcpu_fd = -1;
+	long ret = 0;
+	// The kvm_xsave struct is dynamically sized. We allocate a large enough buffer.
+	// 4096 bytes is generally sufficient for modern CPUs.
+	const size_t xsave_size = 4096;
+	struct kvm_xsave* xsave1 = NULL;
+	struct kvm_xsave* xsave2 = NULL;
+
+	// 1. Perform preparation: Set up a KVM VM and a vCPU.
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		// Cannot open KVM, cannot perform the test.
+		return -1;
+	}
+
+	ret = ioctl(kvm_fd, KVM_GET_API_VERSION, NULL);
+	if (ret != KVM_API_VERSION) {
+		// KVM API version mismatch.
+		goto cleanup;
+	}
+
+	// Check if the KVM_CAP_XSAVE extension is available.
+	ret = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_XSAVE);
+	if (!ret) {
+		// KVM_GET_XSAVE is not supported, cannot perform the test.
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd < 0) {
+		goto cleanup;
+	}
+
+	vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu_fd < 0) {
+		goto cleanup;
+	}
+
+	// Allocate memory for the two kvm_xsave structs.
+	xsave1 = (struct kvm_xsave*)malloc(xsave_size);
+	xsave2 = (struct kvm_xsave*)malloc(xsave_size);
+	if (!xsave1 || !xsave2) {
+		goto cleanup;
+	}
+	memset(xsave1, 0, xsave_size);
+	memset(xsave2, 0, xsave_size);
+
+	// 2. Get the source output.
+	// This is the first call to KVM_GET_XSAVE on the static vCPU.
+	ret = ioctl(vcpu_fd, KVM_GET_XSAVE, xsave1);
+	if (ret < 0) {
+		// The ioctl failed, cannot continue the test.
+		goto cleanup;
+	}
+
+	// 3. Get the follow-up output.
+	// This is the second, immediate call to KVM_GET_XSAVE on the same vCPU.
+	// No other operations are performed in between.
+	ret = ioctl(vcpu_fd, KVM_GET_XSAVE, xsave2);
+	if (ret < 0) {
+		// The ioctl failed, cannot continue the test.
+		goto cleanup;
+	}
+
+	// 4. Check if the outputs satisfy the output relation.
+	// The two kvm_xsave structs should be byte-for-byte identical.
+	if (memcmp(xsave1, xsave2, xsave_size) != 0) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+cleanup:
+	// 5. Perform cleanup.
+	if (xsave1)
+		free(xsave1);
+	if (xsave2)
+		free(xsave2);
+	if (vcpu_fd >= 0)
+		close(vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_f4bced4c.h b/executor/MRs/syz_mr_f4bced4c.h
new file mode 100644
index 000000000..da9fa62e4
--- /dev/null
+++ b/executor/MRs/syz_mr_f4bced4c.h
@@ -0,0 +1,88 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.127-KVM_XEN_HVM_GET_ATTR/impl/mr.h
+// Code generated by gemini-2.5-pro after 6 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Setting a Xen HVM attribute and then getting it should return the same value.
+// - Source input: A call to the `KVM_XEN_HVM_SET_ATTR` ioctl. The parameter is a `struct kvm_xen_hvm_attr` with a specific `type` and a `value` to be set. Let's denote the value as `V_set`.
+// - Follow-up input: A call to the `KVM_XEN_HVM_GET_ATTR` ioctl. The parameter is a `struct kvm_xen_hvm_attr` with the same `type` as the source input.
+// - Input relation: The follow-up input uses the same attribute `type` as the source input but is used in a `GET` operation instead of a `SET` operation. This transformation is performed immediately after the source operation on the same VM.
+// - Output relation: The source `KVM_XEN_HVM_SET_ATTR` call should return 0 (success). The follow-up `KVM_XEN_HVM_GET_ATTR` call should also return 0 (success), and the value returned in its `kvm_xen_hvm_attr` structure should be equal to `V_set`.
+
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+// Define KVM_XEN_HVM_ENABLE if it's not available in the build environment's headers.
+#ifndef KVM_XEN_HVM_ENABLE_f4bced4c
+#define KVM_XEN_HVM_ENABLE_f4bced4c _IOW(KVMIO, 0x7a, __u32)
+#endif
+
+/**
+ * Implements the metamorphic relation: Setting a Xen HVM attribute and then getting it should return the same value.
+ * @param a0 Corresponds to the 'type' field of struct kvm_xen_hvm_attr.
+ * @param a1 Corresponds to the value of the attribute to be set.
+ * @return 0 on successful execution of the test, -1 on setup failure.
+ */
+static long syz_mr_f4bced4c(volatile long a0, volatile long a1)
+{
+	// 1. Perform preparation.
+	int kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// Cannot proceed if KVM is not available.
+		return -1;
+	}
+
+	int vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Enable Xen HVM capability. This is required for the ATTR ioctls.
+	uint32_t xen_hvm_flags = 0;
+	if (ioctl(vm_fd, KVM_XEN_HVM_ENABLE_f4bced4c, &xen_hvm_flags) != 0) {
+		// If enabling fails, we cannot test the MR.
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Prepare the arguments for the source and follow-up calls.
+	struct kvm_xen_hvm_attr attr_set;
+	memset(&attr_set, 0, sizeof(attr_set));
+	attr_set.type = (uint32_t)a0;
+
+	struct kvm_xen_hvm_attr attr_get;
+	memset(&attr_get, 0, sizeof(attr_get));
+	attr_get.type = (uint32_t)a0;
+
+	// FIX: Due to contradictory compiler errors about the struct member name,
+	// we access the 'value' field directly via its known memory offset.
+	// The union containing the value starts after 'type' (4 bytes) and 'pad' (12 bytes).
+	// Offset = 16 bytes.
+	uint64_t* value_ptr_set = (uint64_t*)((char*)&attr_set + sizeof(uint32_t) * 4);
+	*value_ptr_set = (uint64_t)a1;
+
+	// 3. Get the corresponding source output and follow-up output.
+	long ret_set = ioctl(vm_fd, KVM_XEN_HVM_SET_ATTR, &attr_set);
+	long ret_get = ioctl(vm_fd, KVM_XEN_HVM_GET_ATTR, &attr_get);
+
+	// 4. If outputs do not satisfy the output relation, a prompt message is printed.
+	// Read the returned value from the same fixed offset.
+	uint64_t* value_ptr_get = (uint64_t*)((char*)&attr_get + sizeof(uint32_t) * 4);
+	uint64_t read_value = *value_ptr_get;
+
+	if (ret_set != 0 || ret_get != 0 || read_value != (uint64_t)a1) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!");
+	}
+
+	// 5. Perform cleanup.
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_f70978b0.h b/executor/MRs/syz_mr_f70978b0.h
new file mode 100644
index 000000000..35421fb8e
--- /dev/null
+++ b/executor/MRs/syz_mr_f70978b0.h
@@ -0,0 +1,108 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.105-KVM_X86_SETUP_MCE/impl/mr.h
+// Code generated by gemini-2.5-pro after 3 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 2 iterations
+// MR1: A valid MCE bank count is accepted, whereas an invalid bank count is rejected.
+// - Source input: Create a new vCPU (vcpu₁). Issue the `KVM_X86_SETUP_MCE` ioctl with a parameter `mcg_cap` whose bank-count field (bits 7:0) is a valid value `N` (where `N` is less than or equal to the maximum number of banks reported by `KVM_CAP_MCE`). All other capability bits are set to supported values.
+// - Follow-up input: Create another new vCPU (vcpu₂) in the same VM. Issue the `KVM_X86_SETUP_MCE` ioctl with a parameter `mcg_cap'`, which is obtained from `mcg_cap` by changing only the bank-count field to an invalid value `N'` (where `N'` is greater than the maximum supported number of banks). All other bits remain unchanged.
+// - Input relation: The follow-up input `mcg_cap'` is derived from the source input `mcg_cap` by modifying the bank-count field to a value that exceeds the host's maximum supported limit, while keeping all other fields identical.
+// - Output relation: The `ioctl` call on vcpu₁ must succeed and return `0`. The `ioctl` call on vcpu₂ must fail and return `-EINVAL`, because the requested number of banks is invalid.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h> // For uint64_t
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <unistd.h>
+
+static long syz_mr_f70978b0(volatile long a0)
+{
+	// 1. Perform preparation: Set up KVM environment
+	int kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd == -1) {
+		// If KVM is not available, we cannot perform the test.
+		return -1;
+	}
+
+	int api_version = ioctl(kvm_fd, KVM_GET_API_VERSION, 0);
+	if (api_version != KVM_API_VERSION) {
+		// KVM API version mismatch, cannot proceed.
+		close(kvm_fd);
+		return -1;
+	}
+
+	// Check for MCE capability and get the maximum number of supported banks.
+	long max_banks = ioctl(kvm_fd, KVM_CHECK_EXTENSION, KVM_CAP_MCE);
+	if (max_banks <= 0) {
+		// MCE capability is not supported or no banks are available.
+		// The metamorphic relation cannot be tested.
+		close(kvm_fd);
+		return -1;
+	}
+
+	int vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, 0);
+	if (vm_fd == -1) {
+		close(kvm_fd);
+		return -1;
+	}
+
+	// 2. Get source and follow-up inputs and execute them
+	// Source input execution
+	int vcpu1_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 0);
+	if (vcpu1_fd == -1) {
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// The source input `a0` is a bank count. We need to ensure it's a valid one.
+	// A valid bank count `N` must be `0 < N <= max_banks`.
+	// We clamp the input `a0` to this range to ensure we have a valid source input.
+	long valid_n = a0;
+	if (valid_n <= 0 || valid_n > max_banks) {
+		valid_n = 1; // Default to a known valid value if input is out of range.
+	}
+
+	// The parameter for KVM_X86_SETUP_MCE is a __u64 value for mcg_cap.
+	uint64_t mcg_cap_s = 0;
+	// Set the bank-count field (bits 7:0). Other capability bits are 0.
+	mcg_cap_s = valid_n;
+
+	long res1 = ioctl(vcpu1_fd, KVM_X86_SETUP_MCE, &mcg_cap_s);
+
+	// Follow-up input execution
+	int vcpu2_fd = ioctl(vm_fd, KVM_CREATE_VCPU, 1);
+	if (vcpu2_fd == -1) {
+		close(vcpu1_fd);
+		close(vm_fd);
+		close(kvm_fd);
+		return -1;
+	}
+
+	// The follow-up input uses an invalid bank count `N' > max_banks`.
+	long invalid_n_prime = max_banks + 1;
+
+	uint64_t mcg_cap_f = 0;
+	// Keep other capability bits the same as the source input (all 0s).
+	mcg_cap_f = invalid_n_prime;
+
+	long res2 = ioctl(vcpu2_fd, KVM_X86_SETUP_MCE, &mcg_cap_f);
+	int errno2 = errno; // Save errno immediately after the call.
+
+	// 3. Check the output relation
+	// The source call should succeed (return 0).
+	// The follow-up call should fail with -EINVAL.
+	if (res1 != 0 || !(res2 == -1 && errno2 == EINVAL)) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 4. Perform cleanup
+	close(vcpu2_fd);
+	close(vcpu1_fd);
+	close(vm_fd);
+	close(kvm_fd);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_f85ca149.h b/executor/MRs/syz_mr_f85ca149.h
new file mode 100644
index 000000000..1f143074b
--- /dev/null
+++ b/executor/MRs/syz_mr_f85ca149.h
@@ -0,0 +1,150 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/rdma_cm/user_verbs/Memory-pinning/impl/mr.h
+// Code generated by gemini-2.5-pro after 1 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: Duplicate registration of a memory region results in a cumulative increase in the pinned memory counter.
+// - Source input: A request from a user process to register a memory region `R` of size `S`. The process's `pinned_vm` count before this operation is `P_initial`.
+// - Follow-up input: A second, subsequent request from the same user process to register the exact same memory region `R`.
+// - Input relation: The follow-up input is a re-execution of the source input's registration request for the identical memory region, without deregistering it first.
+// - Output relation: Let `P_source` be the `pinned_vm` count after the source input is processed, and `P_followup` be the count after the follow-up input is processed. Based on the specification that "Pages that are pinned multiple times are counted each time they are pinned", the increase in the counter for both operations should be the same. Therefore, the expected relation is `(P_source - P_initial) == (P_followup - P_source)`.
+
+#include <errno.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+/**
+ * @brief Reads the VmPinned value from /proc/self/status.
+ *
+ * This helper function opens the status file for the current process,
+ * searches for the "VmPinned:" line, and parses the value.
+ *
+ * @return The value of VmPinned in kB on success, or -1 on failure.
+ */
+static long get_vm_pinned_f85ca149()
+{
+	FILE* f = fopen("/proc/self/status", "r");
+	if (!f) {
+		perror("fopen /proc/self/status");
+		return -1;
+	}
+
+	char* line = NULL;
+	size_t len = 0;
+	long pinned_kb = -1;
+
+	while (getline(&line, &len, f) != -1) {
+		if (strncmp(line, "VmPinned:", 9) == 0) {
+			// Skip "VmPinned:" and parse the long value
+			if (sscanf(line + 9, "%ld", &pinned_kb) != 1) {
+				pinned_kb = -1; // Parse error
+			}
+			break;
+		}
+	}
+
+	free(line);
+	fclose(f);
+	return pinned_kb;
+}
+
+/**
+ * @brief Implements MR1: Duplicate registration of a memory region results in a
+ * cumulative increase in the pinned memory counter.
+ *
+ * This function tests the behavior of memory pinning when a region is pinned
+ * multiple times. It allocates a memory region, pins it, records the change
+ * in the process's `VmPinned` counter, pins the same region again, and
+ * checks if the counter increases by the same amount. The `mlock` system call
+ * is used to perform the pinning.
+ *
+ * @param a0 The size of the memory region to be registered (pinned).
+ * @return 0 on successful execution of the test, -1 on setup or syscall failure.
+ */
+static long syz_mr_f85ca149(volatile long a0)
+{
+	long size = a0;
+
+	// 1. Perform preparation
+	if (size <= 0) {
+		// Invalid size for memory allocation.
+		return -1;
+	}
+
+	long page_size = sysconf(_SC_PAGESIZE);
+	if (page_size == -1) {
+		perror("sysconf(_SC_PAGESIZE)");
+		return -1;
+	}
+	// Align the size to the next multiple of the page size for mlock.
+	size = (size + page_size - 1) & ~(page_size - 1);
+
+	// Allocate a memory region R.
+	void* addr = mmap(NULL, size, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (addr == MAP_FAILED) {
+		perror("mmap");
+		return -1;
+	}
+
+	// Get the initial pinned memory count (P_initial).
+	long p_initial = get_vm_pinned_f85ca149();
+	if (p_initial == -1) {
+		fprintf(stderr, "Failed to read initial VmPinned count\n");
+		munmap(addr, size);
+		return -1;
+	}
+
+	// 2. Get the source output
+	// Execute the source input: register (mlock) the memory region R.
+	if (mlock(addr, size) != 0) {
+		perror("mlock (source)");
+		munmap(addr, size);
+		return -1;
+	}
+
+	// Get the pinned memory count after the source operation (P_source).
+	long p_source = get_vm_pinned_f85ca149();
+	if (p_source == -1) {
+		fprintf(stderr, "Failed to read VmPinned count after source mlock\n");
+		munlock(addr, size);
+		munmap(addr, size);
+		return -1;
+	}
+
+	// 3. Get the follow-up output
+	// Execute the follow-up input: register (mlock) the same memory region R again.
+	if (mlock(addr, size) != 0) {
+		// If the second mlock fails, we cannot verify the relation.
+		// This could happen due to resource limits (RLIMIT_MEMLOCK).
+		// We treat this as a test setup failure rather than a violation.
+		perror("mlock (follow-up)");
+		munlock(addr, size);
+		munmap(addr, size);
+		return -1;
+	}
+
+	// Get the pinned memory count after the follow-up operation (P_followup).
+	long p_followup = get_vm_pinned_f85ca149();
+	if (p_followup == -1) {
+		fprintf(stderr, "Failed to read VmPinned count after follow-up mlock\n");
+		munlock(addr, size);
+		munmap(addr, size);
+		return -1;
+	}
+
+	// 4. Check the output relation
+	long increase_source = p_source - p_initial;
+	long increase_followup = p_followup - p_source;
+
+	if (increase_source != increase_followup) {
+		fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+	}
+
+	// 5. Perform cleanup
+	// A single munlock is sufficient to unlock the entire region.
+	munlock(addr, size);
+	munmap(addr, size);
+
+	return 0;
+}
\ No newline at end of file
diff --git a/executor/MRs/syz_mr_fa2fc31c.h b/executor/MRs/syz_mr_fa2fc31c.h
new file mode 100644
index 000000000..5463c8087
--- /dev/null
+++ b/executor/MRs/syz_mr_fa2fc31c.h
@@ -0,0 +1,203 @@
+// Code from /vol/SyzMeta/workdir/MRs/android/common-android13-5.15/kvm/api/4.38-KVM_GET_MP_STATE/impl/mr.h
+// Code generated by gemini-2.5-pro after 5 iterations
+// MR identified by gemini-2.5-pro and calibrated by o3 after 1 iterations
+// MR1: State transition of an Application Processor vCPU from UNINITIALIZED to INIT_RECEIVED upon receiving an INIT signal.
+// - Source input: A KVM virtual machine on an x86 host with an in-kernel IRQ chip created (`KVM_CREATE_IRQCHIP`). The VM has at least two vCPUs, with one being an Application Processor (AP), for example, vCPU1. The source operation is to call the `KVM_GET_MP_STATE` ioctl on this AP vCPU.
+// - Follow-up input: The same virtual machine setup as the source input. An INIT Inter-Processor Interrupt (IPI) is sent to the AP vCPU. This is typically achieved by having another vCPU (e.g., the Bootstrap Processor, vCPU0) execute code that writes to its local APIC's Interrupt Command Register (ICR) to trigger the INIT IPI. After the IPI is sent, the `KVM_GET_MP_STATE` ioctl is called again on the same AP vCPU.
+// - Input relation: The follow-up input is obtained by performing an action that sends an INIT signal to the target AP vCPU, which was previously in an uninitialized state.
+// - Output relation: Let the output of the source `KVM_GET_MP_STATE` call be `O_source` and the output of the follow-up call be `O_follow-up`. If the initial state `O_source.mp_state` is `KVM_MP_STATE_UNINITIALIZED`, then the subsequent state `O_follow-up.mp_state` must be `KVM_MP_STATE_INIT_RECEIVED`.
+
+#include <errno.h>
+#include <fcntl.h>
+#include <linux/kvm.h>
+#include <stdint.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <string.h>
+#include <sys/ioctl.h>
+#include <sys/mman.h>
+#include <unistd.h>
+
+#define GUEST_MEM_SIZE_fa2fc31c 0x100000
+#define GUEST_CODE_ADDR_fa2fc31c 0x1000
+#define BSP_VCPU_ID_fa2fc31c 0
+
+static long syz_mr_fa2fc31c(volatile long ap_vcpu_id)
+{
+	// Define the constant template at the top to prevent "crosses initialization" errors.
+	static const uint8_t code_template[] = {
+	    0xb8, 0x00, 0x00, 0x00, 0x00, // mov eax, (apic_id << 24)
+	    0xa3, 0x10, 0x03, 0xe0, 0xfe, // mov [0xfee00310], eax
+	    0xb8, 0x00, 0x45, 0x00, 0x00, // mov eax, 0x4500 (INIT IPI)
+	    0xa3, 0x00, 0x03, 0xe0, 0xfe, // mov [0xfee00300], eax
+	    0xf4 // hlt
+	};
+
+	long ret = 0;
+	int kvm_fd = -1, vm_fd = -1, bsp_vcpu_fd = -1, ap_vcpu_fd = -1;
+	int mmap_size = 0;
+	struct kvm_run* bsp_run = NULL;
+	uint8_t* mem = NULL;
+
+	struct kvm_userspace_memory_region region;
+	struct kvm_sregs sregs;
+	struct kvm_regs regs;
+	struct kvm_mp_state source_state, followup_state;
+	uint32_t apic_id_dest;
+	uint8_t code[sizeof(code_template)];
+
+	if (ap_vcpu_id == BSP_VCPU_ID_fa2fc31c) {
+		return -1;
+	}
+
+	// Corrected typo: O_CLOCEXEC -> O_CLOEXEC
+	kvm_fd = open("/dev/kvm", O_RDWR | O_CLOEXEC);
+	if (kvm_fd < 0) {
+		perror("open /dev/kvm");
+		return -1;
+	}
+
+	int version = ioctl(kvm_fd, KVM_GET_API_VERSION, NULL);
+	if (version != 12) {
+		fprintf(stderr, "KVM API version is not 12\n");
+		ret = -1;
+		goto cleanup;
+	}
+
+	vm_fd = ioctl(kvm_fd, KVM_CREATE_VM, (unsigned long)0);
+	if (vm_fd < 0) {
+		perror("KVM_CREATE_VM");
+		ret = -1;
+		goto cleanup;
+	}
+
+	if (ioctl(vm_fd, KVM_CREATE_IRQCHIP, 0) < 0) {
+		perror("KVM_CREATE_IRQCHIP");
+		ret = -1;
+		goto cleanup;
+	}
+
+	mem = (uint8_t*)mmap(NULL, GUEST_MEM_SIZE_fa2fc31c, PROT_READ | PROT_WRITE, MAP_PRIVATE | MAP_ANONYMOUS, -1, 0);
+	if (mem == MAP_FAILED) {
+		perror("mmap guest memory");
+		mem = NULL;
+		ret = -1;
+		goto cleanup;
+	}
+
+	memset(&region, 0, sizeof(region));
+	region.slot = 0;
+	region.guest_phys_addr = 0;
+	region.memory_size = GUEST_MEM_SIZE_fa2fc31c;
+	region.userspace_addr = (uint64_t)mem;
+	if (ioctl(vm_fd, KVM_SET_USER_MEMORY_REGION, &region) < 0) {
+		perror("KVM_SET_USER_MEMORY_REGION");
+		ret = -1;
+		goto cleanup;
+	}
+
+	bsp_vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)BSP_VCPU_ID_fa2fc31c);
+	if (bsp_vcpu_fd < 0) {
+		perror("KVM_CREATE_VCPU for BSP");
+		ret = -1;
+		goto cleanup;
+	}
+
+	ap_vcpu_fd = ioctl(vm_fd, KVM_CREATE_VCPU, (unsigned long)ap_vcpu_id);
+	if (ap_vcpu_fd < 0) {
+		perror("KVM_CREATE_VCPU for AP");
+		ret = -1;
+		goto cleanup;
+	}
+
+	mmap_size = ioctl(kvm_fd, KVM_GET_VCPU_MMAP_SIZE, NULL);
+	if (mmap_size < 0) {
+		perror("KVM_GET_VCPU_MMAP_SIZE");
+		ret = -1;
+		goto cleanup;
+	}
+	bsp_run = (struct kvm_run*)mmap(NULL, mmap_size, PROT_READ | PROT_WRITE, MAP_SHARED, bsp_vcpu_fd, 0);
+	if (bsp_run == MAP_FAILED) {
+		perror("mmap kvm_run for BSP");
+		bsp_run = NULL;
+		ret = -1;
+		goto cleanup;
+	}
+
+	apic_id_dest = (uint32_t)ap_vcpu_id;
+	memcpy(code, code_template, sizeof(code_template));
+	*(uint32_t*)&code[1] = apic_id_dest << 24;
+	memcpy(mem + GUEST_CODE_ADDR_fa2fc31c, code, sizeof(code));
+
+	if (ioctl(bsp_vcpu_fd, KVM_GET_SREGS, &sregs) < 0) {
+		perror("KVM_GET_SREGS");
+		ret = -1;
+		goto cleanup;
+	}
+	sregs.cr0 |= 1;
+	sregs.cs.base = 0;
+	sregs.cs.limit = 0xffffffff;
+	sregs.cs.g = 1;
+	sregs.cs.db = 1;
+	sregs.cs.present = 1;
+	sregs.cs.s = 1;
+	sregs.cs.type = 11;
+	sregs.ds = sregs.es = sregs.fs = sregs.gs = sregs.ss = sregs.cs;
+	sregs.ss.type = 3;
+	if (ioctl(bsp_vcpu_fd, KVM_SET_SREGS, &sregs) < 0) {
+		perror("KVM_SET_SREGS");
+		ret = -1;
+		goto cleanup;
+	}
+
+	memset(&regs, 0, sizeof(regs));
+	regs.rip = GUEST_CODE_ADDR_fa2fc31c;
+	regs.rflags = 2;
+	regs.rsp = GUEST_MEM_SIZE_fa2fc31c - 0x100;
+	if (ioctl(bsp_vcpu_fd, KVM_SET_REGS, &regs) < 0) {
+		perror("KVM_SET_REGS");
+		ret = -1;
+		goto cleanup;
+	}
+
+	// --- Metamorphic Relation Logic ---
+	if (ioctl(ap_vcpu_fd, KVM_GET_MP_STATE, &source_state) < 0) {
+		perror("KVM_GET_MP_STATE (source)");
+		ret = -1;
+		goto cleanup;
+	}
+
+	if (source_state.mp_state == KVM_MP_STATE_UNINITIALIZED) {
+		if (ioctl(bsp_vcpu_fd, KVM_RUN, NULL) < 0) {
+			perror("KVM_RUN");
+			ret = -1;
+			goto cleanup;
+		}
+
+		if (ioctl(ap_vcpu_fd, KVM_GET_MP_STATE, &followup_state) < 0) {
+			perror("KVM_GET_MP_STATE (follow-up)");
+			ret = -1;
+			goto cleanup;
+		}
+
+		if (followup_state.mp_state != KVM_MP_STATE_INIT_RECEIVED) {
+			fprintf(stderr, "[SyzMeta]: MR is violated!\n");
+		}
+	}
+
+cleanup:
+	if (bsp_run)
+		munmap(bsp_run, mmap_size);
+	if (mem)
+		munmap(mem, GUEST_MEM_SIZE_fa2fc31c);
+	if (ap_vcpu_fd >= 0)
+		close(ap_vcpu_fd);
+	if (bsp_vcpu_fd >= 0)
+		close(bsp_vcpu_fd);
+	if (vm_fd >= 0)
+		close(vm_fd);
+	if (kvm_fd >= 0)
+		close(kvm_fd);
+
+	return ret;
+}
\ No newline at end of file
diff --git a/executor/common_kvm_ppc64.h b/executor/common_kvm_ppc64.h
index 82d781772..c154d7092 100644
--- a/executor/common_kvm_ppc64.h
+++ b/executor/common_kvm_ppc64.h
@@ -62,7 +62,7 @@
 #define PATB_HR (1UL << 63)
 #define PRTB_MASK 0x0ffffffffffff000UL
 
-#define ALIGNUP(p, q) ((void*)(((unsigned long)(p) + (q)-1) & ~((q)-1)))
+#define ALIGNUP(p, q) ((void*)(((unsigned long)(p) + (q) - 1) & ~((q) - 1)))
 #define MAX(a, b) (((a) > (b)) ? (a) : (b))
 
 #ifndef KVM_REG_PPC_DEC_EXPIRY
@@ -387,8 +387,8 @@ static volatile long syz_kvm_setup_cpu(volatile long a0, volatile long a1, volat
 	if (kvmppc_set_one_reg(cpufd, KVM_REG_PPC_PID, &pid))
 		return -1;
 
-		// Hypercalls need to be enable so we enable them all here to
-		// allow fuzzing
+	// Hypercalls need to be enable so we enable them all here to
+	// allow fuzzing
 #define MAX_HCALL 0x450
 	for (unsigned hcall = 4; hcall < MAX_HCALL; hcall += 4)
 		kvm_vm_enable_cap(vmfd, KVM_CAP_PPC_ENABLE_HCALL, hcall, 1);
diff --git a/executor/common_linux.h b/executor/common_linux.h
index 562fe703b..f930dce27 100644
--- a/executor/common_linux.h
+++ b/executor/common_linux.h
@@ -874,18 +874,15 @@ static void initialize_devlink_pci(void)
 #if SYZ_EXECUTOR || SYZ_WIFI || __NR_syz_80211_inject_frame || __NR_syz_80211_join_ibss
 
 #define WIFI_INITIAL_DEVICE_COUNT 2
-#define WIFI_MAC_BASE                              \
-	{                                          \
-		0x08, 0x02, 0x11, 0x00, 0x00, 0x00 \
-	}
-#define WIFI_IBSS_BSSID                            \
-	{                                          \
-		0x50, 0x50, 0x50, 0x50, 0x50, 0x50 \
-	}
-#define WIFI_IBSS_SSID                             \
-	{                                          \
-		0x10, 0x10, 0x10, 0x10, 0x10, 0x10 \
-	}
+#define WIFI_MAC_BASE \
+	{             \
+	    0x08, 0x02, 0x11, 0x00, 0x00, 0x00}
+#define WIFI_IBSS_BSSID \
+	{               \
+	    0x50, 0x50, 0x50, 0x50, 0x50, 0x50}
+#define WIFI_IBSS_SSID \
+	{              \
+	    0x10, 0x10, 0x10, 0x10, 0x10, 0x10}
 #define WIFI_DEFAULT_FREQUENCY 2412
 #define WIFI_DEFAULT_SIGNAL 0
 #define WIFI_DEFAULT_RX_RATE 1
@@ -1506,26 +1503,26 @@ static void initialize_netdevices(void)
 		const char* type;
 		const char* dev;
 	} devtypes[] = {
-		// Note: ip6erspan device can't be added if ip6gretap exists in the same namespace.
-		{"ip6gretap", "ip6gretap0"},
-		{"bridge", "bridge0"},
-		{"vcan", "vcan0"},
-		{"bond", "bond0"},
-		{"team", "team0"},
-		{"dummy", "dummy0"},
+	    // Note: ip6erspan device can't be added if ip6gretap exists in the same namespace.
+	    {"ip6gretap", "ip6gretap0"},
+	    {"bridge", "bridge0"},
+	    {"vcan", "vcan0"},
+	    {"bond", "bond0"},
+	    {"team", "team0"},
+	    {"dummy", "dummy0"},
 #if SYZ_EXECUTOR || SYZ_NIC_VF
-		{"nicvf", "nicvf0"},
-#endif
-		{"nlmon", "nlmon0"},
-		{"caif", "caif0"},
-		{"batadv", "batadv0"},
-		// Note: this adds vxcan0/vxcan1 pair, similar to veth (creating vxcan0 would fail).
-		{"vxcan", "vxcan1"},
-		// This adds connected veth0 and veth1 devices.
-		{"veth", 0},
-		{"wireguard", "wg0"},
-		{"wireguard", "wg1"},
-		{"wireguard", "wg2"},
+	    {"nicvf", "nicvf0"},
+#endif
+	    {"nlmon", "nlmon0"},
+	    {"caif", "caif0"},
+	    {"batadv", "batadv0"},
+	    // Note: this adds vxcan0/vxcan1 pair, similar to veth (creating vxcan0 would fail).
+	    {"vxcan", "vxcan1"},
+	    // This adds connected veth0 and veth1 devices.
+	    {"veth", 0},
+	    {"wireguard", "wg0"},
+	    {"wireguard", "wg1"},
+	    {"wireguard", "wg2"},
 	};
 	const char* devmasters[] = {"bridge", "bond", "team", "batadv"};
 	// If you extend this array, also update netdev_addr_id in vnet.txt
@@ -1535,67 +1532,67 @@ static void initialize_netdevices(void)
 		int macsize;
 		bool noipv6;
 	} devices[] = {
-		{"lo", ETH_ALEN},
-		{"sit0", 0},
-		{"bridge0", ETH_ALEN},
-		{"vcan0", 0, true},
-		{"tunl0", 0},
-		{"gre0", 0},
-		{"gretap0", ETH_ALEN},
-		{"ip_vti0", 0},
-		{"ip6_vti0", 0},
-		{"ip6tnl0", 0},
-		{"ip6gre0", 0},
-		{"ip6gretap0", ETH_ALEN},
-		{"erspan0", ETH_ALEN},
-		{"bond0", ETH_ALEN},
-		{"veth0", ETH_ALEN},
-		{"veth1", ETH_ALEN},
-		{"team0", ETH_ALEN},
-		{"veth0_to_bridge", ETH_ALEN},
-		{"veth1_to_bridge", ETH_ALEN},
-		{"veth0_to_bond", ETH_ALEN},
-		{"veth1_to_bond", ETH_ALEN},
-		{"veth0_to_team", ETH_ALEN},
-		{"veth1_to_team", ETH_ALEN},
-		{"veth0_to_hsr", ETH_ALEN},
-		{"veth1_to_hsr", ETH_ALEN},
-		{"hsr0", 0},
-		{"dummy0", ETH_ALEN},
+	    {"lo", ETH_ALEN},
+	    {"sit0", 0},
+	    {"bridge0", ETH_ALEN},
+	    {"vcan0", 0, true},
+	    {"tunl0", 0},
+	    {"gre0", 0},
+	    {"gretap0", ETH_ALEN},
+	    {"ip_vti0", 0},
+	    {"ip6_vti0", 0},
+	    {"ip6tnl0", 0},
+	    {"ip6gre0", 0},
+	    {"ip6gretap0", ETH_ALEN},
+	    {"erspan0", ETH_ALEN},
+	    {"bond0", ETH_ALEN},
+	    {"veth0", ETH_ALEN},
+	    {"veth1", ETH_ALEN},
+	    {"team0", ETH_ALEN},
+	    {"veth0_to_bridge", ETH_ALEN},
+	    {"veth1_to_bridge", ETH_ALEN},
+	    {"veth0_to_bond", ETH_ALEN},
+	    {"veth1_to_bond", ETH_ALEN},
+	    {"veth0_to_team", ETH_ALEN},
+	    {"veth1_to_team", ETH_ALEN},
+	    {"veth0_to_hsr", ETH_ALEN},
+	    {"veth1_to_hsr", ETH_ALEN},
+	    {"hsr0", 0},
+	    {"dummy0", ETH_ALEN},
 #if SYZ_EXECUTOR || SYZ_NIC_VF
-		{"nicvf0", 0, true},
-#endif
-		{"nlmon0", 0},
-		{"vxcan0", 0, true},
-		{"vxcan1", 0, true},
-		{"caif0", ETH_ALEN}, // TODO: up'ing caif fails with ENODEV
-		{"batadv0", ETH_ALEN},
-		{netdevsim, ETH_ALEN},
-		{"xfrm0", ETH_ALEN},
-		{"veth0_virt_wifi", ETH_ALEN},
-		{"veth1_virt_wifi", ETH_ALEN},
-		{"virt_wifi0", ETH_ALEN},
-		{"veth0_vlan", ETH_ALEN},
-		{"veth1_vlan", ETH_ALEN},
-		{"vlan0", ETH_ALEN},
-		{"vlan1", ETH_ALEN},
-		{"macvlan0", ETH_ALEN},
-		{"macvlan1", ETH_ALEN},
-		{"ipvlan0", ETH_ALEN},
-		{"ipvlan1", ETH_ALEN},
-		{"veth0_macvtap", ETH_ALEN},
-		{"veth1_macvtap", ETH_ALEN},
-		{"macvtap0", ETH_ALEN},
-		{"macsec0", ETH_ALEN},
-		{"veth0_to_batadv", ETH_ALEN},
-		{"veth1_to_batadv", ETH_ALEN},
-		{"batadv_slave_0", ETH_ALEN},
-		{"batadv_slave_1", ETH_ALEN},
-		{"geneve0", ETH_ALEN},
-		{"geneve1", ETH_ALEN},
-		{"wg0", 0},
-		{"wg1", 0},
-		{"wg2", 0},
+	    {"nicvf0", 0, true},
+#endif
+	    {"nlmon0", 0},
+	    {"vxcan0", 0, true},
+	    {"vxcan1", 0, true},
+	    {"caif0", ETH_ALEN}, // TODO: up'ing caif fails with ENODEV
+	    {"batadv0", ETH_ALEN},
+	    {netdevsim, ETH_ALEN},
+	    {"xfrm0", ETH_ALEN},
+	    {"veth0_virt_wifi", ETH_ALEN},
+	    {"veth1_virt_wifi", ETH_ALEN},
+	    {"virt_wifi0", ETH_ALEN},
+	    {"veth0_vlan", ETH_ALEN},
+	    {"veth1_vlan", ETH_ALEN},
+	    {"vlan0", ETH_ALEN},
+	    {"vlan1", ETH_ALEN},
+	    {"macvlan0", ETH_ALEN},
+	    {"macvlan1", ETH_ALEN},
+	    {"ipvlan0", ETH_ALEN},
+	    {"ipvlan1", ETH_ALEN},
+	    {"veth0_macvtap", ETH_ALEN},
+	    {"veth1_macvtap", ETH_ALEN},
+	    {"macvtap0", ETH_ALEN},
+	    {"macsec0", ETH_ALEN},
+	    {"veth0_to_batadv", ETH_ALEN},
+	    {"veth1_to_batadv", ETH_ALEN},
+	    {"batadv_slave_0", ETH_ALEN},
+	    {"batadv_slave_1", ETH_ALEN},
+	    {"geneve0", ETH_ALEN},
+	    {"geneve1", ETH_ALEN},
+	    {"wg0", 0},
+	    {"wg1", 0},
+	    {"wg2", 0},
 	};
 	int sock = socket(AF_NETLINK, SOCK_RAW, NETLINK_ROUTE);
 	if (sock == -1)
@@ -2100,7 +2097,7 @@ struct btf_header {
 };
 
 #define BTF_INFO_KIND(info) (((info) >> 24) & 0x0f)
-#define BTF_INFO_VLEN(info) ((info)&0xffff)
+#define BTF_INFO_VLEN(info) ((info) & 0xffff)
 
 #define BTF_KIND_INT 1
 #define BTF_KIND_ARRAY 3
@@ -5114,41 +5111,41 @@ static void setup_sysctl()
 		const char* data;
 	} files[] = {
 #if GOARCH_amd64 || GOARCH_386
-		// nmi_check_duration() prints "INFO: NMI handler took too long" on slow debug kernels.
-		// It happens a lot in qemu, and the messages are frequently corrupted
-		// (intermixed with other kernel output as they are printed from NMI)
-		// and are not matched against the suppression in pkg/report.
-		// This write prevents these messages from being printed.
-		{"/sys/kernel/debug/x86/nmi_longest_ns", "10000000000"},
-#endif
-		{"/proc/sys/kernel/hung_task_check_interval_secs", "20"},
-		// bpf_jit_kallsyms and disabling bpf_jit_harden are required
-		// for unwinding through bpf functions.
-		{"/proc/sys/net/core/bpf_jit_kallsyms", "1"},
-		{"/proc/sys/net/core/bpf_jit_harden", "0"},
-		// This is to provide more useful info in crash reports.
-		{"/proc/sys/kernel/kptr_restrict", "0"},
-		{"/proc/sys/kernel/softlockup_all_cpu_backtrace", "1"},
-		// This is to restrict effects of recursive exponential mounts, for details see
-		// "mnt: Add a per mount namespace limit on the number of mounts" commit.
-		{"/proc/sys/fs/mount-max", "100"},
-		// Dumping all tasks to console can take too long.
-		{"/proc/sys/vm/oom_dump_tasks", "0"},
-		// Executor hits lots of SIGSEGVs, no point in logging them.
-		{"/proc/sys/debug/exception-trace", "0"},
-		{"/proc/sys/kernel/printk", "7 4 1 3"},
-		// Faster gc (1 second) is intended to make tests more repeatable.
-		{"/proc/sys/kernel/keys/gc_delay", "1"},
-		// We always want to prefer killing the allocating test process rather than somebody else
-		// (sshd or another random test process).
-		{"/proc/sys/vm/oom_kill_allocating_task", "1"},
-		// This blocks some of the ways the fuzzer can trigger a reboot.
-		// ctrl-alt-del=0 tells kernel to signal cad_pid instead of rebooting.
-		// We set cad_pid to a transient process pid ctrl-alt-del a no-op.
-		// Note: we need to write a live process pid.
-		// For context see: https://groups.google.com/g/syzkaller-bugs/c/WqOY4TiRnFg/m/6P9u8lWZAQAJ
-		{"/proc/sys/kernel/ctrl-alt-del", "0"},
-		{"/proc/sys/kernel/cad_pid", tmppid},
+	    // nmi_check_duration() prints "INFO: NMI handler took too long" on slow debug kernels.
+	    // It happens a lot in qemu, and the messages are frequently corrupted
+	    // (intermixed with other kernel output as they are printed from NMI)
+	    // and are not matched against the suppression in pkg/report.
+	    // This write prevents these messages from being printed.
+	    {"/sys/kernel/debug/x86/nmi_longest_ns", "10000000000"},
+#endif
+	    {"/proc/sys/kernel/hung_task_check_interval_secs", "20"},
+	    // bpf_jit_kallsyms and disabling bpf_jit_harden are required
+	    // for unwinding through bpf functions.
+	    {"/proc/sys/net/core/bpf_jit_kallsyms", "1"},
+	    {"/proc/sys/net/core/bpf_jit_harden", "0"},
+	    // This is to provide more useful info in crash reports.
+	    {"/proc/sys/kernel/kptr_restrict", "0"},
+	    {"/proc/sys/kernel/softlockup_all_cpu_backtrace", "1"},
+	    // This is to restrict effects of recursive exponential mounts, for details see
+	    // "mnt: Add a per mount namespace limit on the number of mounts" commit.
+	    {"/proc/sys/fs/mount-max", "100"},
+	    // Dumping all tasks to console can take too long.
+	    {"/proc/sys/vm/oom_dump_tasks", "0"},
+	    // Executor hits lots of SIGSEGVs, no point in logging them.
+	    {"/proc/sys/debug/exception-trace", "0"},
+	    {"/proc/sys/kernel/printk", "7 4 1 3"},
+	    // Faster gc (1 second) is intended to make tests more repeatable.
+	    {"/proc/sys/kernel/keys/gc_delay", "1"},
+	    // We always want to prefer killing the allocating test process rather than somebody else
+	    // (sshd or another random test process).
+	    {"/proc/sys/vm/oom_kill_allocating_task", "1"},
+	    // This blocks some of the ways the fuzzer can trigger a reboot.
+	    // ctrl-alt-del=0 tells kernel to signal cad_pid instead of rebooting.
+	    // We set cad_pid to a transient process pid ctrl-alt-del a no-op.
+	    // Note: we need to write a live process pid.
+	    // For context see: https://groups.google.com/g/syzkaller-bugs/c/WqOY4TiRnFg/m/6P9u8lWZAQAJ
+	    {"/proc/sys/kernel/ctrl-alt-del", "0"},
+	    {"/proc/sys/kernel/cad_pid", tmppid},
 
 	};
 	for (size_t i = 0; i < sizeof(files) / sizeof(files[0]); i++) {
@@ -5833,3 +5830,433 @@ static long syz_pidfd_open(volatile long pid, volatile long flags)
 }
 
 #endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_3d58e22a
+#include "MRs/syz_mr_3d58e22a.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_0ce41170
+#include "MRs/syz_mr_0ce41170.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_1d1fd0d1
+#include "MRs/syz_mr_1d1fd0d1.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_9031a534
+#include "MRs/syz_mr_9031a534.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_1f3a13c8
+#include "MRs/syz_mr_1f3a13c8.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_3f12b8e9
+#include "MRs/syz_mr_3f12b8e9.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_99025952
+#include "MRs/syz_mr_99025952.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_77cb284e
+#include "MRs/syz_mr_77cb284e.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_8ce7d16e
+#include "MRs/syz_mr_8ce7d16e.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_c3320296
+#include "MRs/syz_mr_c3320296.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_825250bb
+#include "MRs/syz_mr_825250bb.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_14c38fa6
+#include "MRs/syz_mr_14c38fa6.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_7206cd80
+#include "MRs/syz_mr_7206cd80.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_69aac2da
+#include "MRs/syz_mr_69aac2da.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_3c76f4a3
+#include "MRs/syz_mr_3c76f4a3.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_b65b85a4
+#include "MRs/syz_mr_b65b85a4.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_75e3d2b4
+#include "MRs/syz_mr_75e3d2b4.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_580407a7
+#include "MRs/syz_mr_580407a7.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_a1912f81
+#include "MRs/syz_mr_a1912f81.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_9f95d9e3
+#include "MRs/syz_mr_9f95d9e3.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_561d57bb
+#include "MRs/syz_mr_561d57bb.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_e36a127e
+#include "MRs/syz_mr_e36a127e.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_c67a179b
+#include "MRs/syz_mr_c67a179b.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_28861e6f
+#include "MRs/syz_mr_28861e6f.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_f4bced4c
+#include "MRs/syz_mr_f4bced4c.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_8dda4646
+#include "MRs/syz_mr_8dda4646.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_1825cfdc
+#include "MRs/syz_mr_1825cfdc.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_b49fee5b
+#include "MRs/syz_mr_b49fee5b.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_c9189a41
+#include "MRs/syz_mr_c9189a41.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_9b9aa407
+#include "MRs/syz_mr_9b9aa407.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_110f3bc5
+#include "MRs/syz_mr_110f3bc5.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_f70978b0
+#include "MRs/syz_mr_f70978b0.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_7736935a
+#include "MRs/syz_mr_7736935a.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_76aa1e6b
+#include "MRs/syz_mr_76aa1e6b.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_8766e19b
+#include "MRs/syz_mr_8766e19b.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_6a51ab49
+#include "MRs/syz_mr_6a51ab49.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_5bc1836b
+#include "MRs/syz_mr_5bc1836b.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_3baa0980
+#include "MRs/syz_mr_3baa0980.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_da30fef7
+#include "MRs/syz_mr_da30fef7.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_6d17dd5e
+#include "MRs/syz_mr_6d17dd5e.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_203da363
+#include "MRs/syz_mr_203da363.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_89dbb28a
+#include "MRs/syz_mr_89dbb28a.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_45f170fe
+#include "MRs/syz_mr_45f170fe.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_647388cd
+#include "MRs/syz_mr_647388cd.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_351170b6
+#include "MRs/syz_mr_351170b6.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_c7df56f8
+#include "MRs/syz_mr_c7df56f8.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_2a839201
+#include "MRs/syz_mr_2a839201.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_3f3322b8
+#include "MRs/syz_mr_3f3322b8.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_4d896b11
+#include "MRs/syz_mr_4d896b11.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_43e7360b
+#include "MRs/syz_mr_43e7360b.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_a421a672
+#include "MRs/syz_mr_a421a672.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_63e076f2
+#include "MRs/syz_mr_63e076f2.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_65f5ce7f
+#include "MRs/syz_mr_65f5ce7f.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_f167aad9
+#include "MRs/syz_mr_f167aad9.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_f46c78da
+#include "MRs/syz_mr_f46c78da.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_c11de9b2
+#include "MRs/syz_mr_c11de9b2.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_353a3b59
+#include "MRs/syz_mr_353a3b59.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_811d6c8a
+#include "MRs/syz_mr_811d6c8a.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_b0377842
+#include "MRs/syz_mr_b0377842.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_6b728ac9
+#include "MRs/syz_mr_6b728ac9.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_f3efb53d
+#include "MRs/syz_mr_f3efb53d.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_a92dfc4f
+#include "MRs/syz_mr_a92dfc4f.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_fa2fc31c
+#include "MRs/syz_mr_fa2fc31c.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_8bbf48b4
+#include "MRs/syz_mr_8bbf48b4.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_7d90e6d8
+#include "MRs/syz_mr_7d90e6d8.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_98ce7ce8
+#include "MRs/syz_mr_98ce7ce8.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_62089dad
+#include "MRs/syz_mr_62089dad.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_6473f381
+#include "MRs/syz_mr_6473f381.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_cf550810
+#include "MRs/syz_mr_cf550810.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_c20a878e
+#include "MRs/syz_mr_c20a878e.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_38fcb3bb
+#include "MRs/syz_mr_38fcb3bb.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_71c06928
+#include "MRs/syz_mr_71c06928.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_70e04a46
+#include "MRs/syz_mr_70e04a46.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_9955c520
+#include "MRs/syz_mr_9955c520.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_e1e2e65a
+#include "MRs/syz_mr_e1e2e65a.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_f85ca149
+#include "MRs/syz_mr_f85ca149.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_c7d8e332
+#include "MRs/syz_mr_c7d8e332.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_8acbfeee
+#include "MRs/syz_mr_8acbfeee.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_70b74216
+#include "MRs/syz_mr_70b74216.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_2281e517
+#include "MRs/syz_mr_2281e517.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_13cd9ab1
+#include "MRs/syz_mr_13cd9ab1.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_b704cea7
+#include "MRs/syz_mr_b704cea7.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_57204f74
+#include "MRs/syz_mr_57204f74.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_b4dad044
+#include "MRs/syz_mr_b4dad044.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_23de5f08
+#include "MRs/syz_mr_23de5f08.h"
+
+#endif
+
+#if SYZ_EXECUTOR || __NR_syz_mr_bd3e1cc5
+#include "MRs/syz_mr_bd3e1cc5.h"
+
+#endif
diff --git a/executor/embed.go b/executor/embed.go
index 9a8a83141..0a30b0bf0 100644
--- a/executor/embed.go
+++ b/executor/embed.go
@@ -13,7 +13,7 @@ import (
 	"regexp"
 )
 
-//go:embed common*.h kvm*.h android/*.h
+//go:embed common*.h kvm*.h android/*.h MRs/*.h
 var src embed.FS
 
 // CommonHeader contains all executor common headers used by pkg/csource to generate C reproducers.
@@ -24,13 +24,13 @@ var CommonHeader = func() []byte {
 		panic(err)
 	}
 	headers := make(map[string]bool)
-	for _, glob := range []string{"*.h", "android/*.h"} {
+	for _, glob := range []string{"*.h", "android/*.h", "MRs/*.h"} {
 		files, err := fs.Glob(src, glob)
 		if err != nil {
 			panic(err)
 		}
 		for _, file := range files {
-			if file == "common.h" || file == "common_ext_example.h" {
+			if file == "common.h" || file == "common_ext_example.h" || file == "MRs/empty.h" {
 				continue
 			}
 			headers[file] = true
@@ -42,13 +42,21 @@ var CommonHeader = func() []byte {
 	for {
 		relacedSomething := false
 		for file := range headers {
-			replace := []byte("#include \"" + path.Base(file) + "\"")
-			if !bytes.Contains(data, replace) {
-				replace = []byte("#include \"android/" + path.Base(file) + "\"")
-				if !bytes.Contains(data, replace) {
-					continue
+			replaceCandidates := [][]byte{
+				[]byte("#include \"" + path.Base(file) + "\""),
+				[]byte("#include \"android/" + path.Base(file) + "\""),
+				[]byte("#include \"MRs/" + path.Base(file) + "\""),
+			}
+			var replace []byte
+			for _, candidate := range replaceCandidates {
+				if bytes.Contains(data, candidate) {
+					replace = bytes.Clone(candidate)
+					break
 				}
 			}
+			if replace == nil {
+				continue
+			}
 			contents, err := src.ReadFile(file)
 			if err != nil {
 				panic(err)
diff --git a/executor/executor_darwin.h b/executor/executor_darwin.h
index 76b939fcf..bb7956c20 100644
--- a/executor/executor_darwin.h
+++ b/executor/executor_darwin.h
@@ -118,6 +118,6 @@ static void cover_collect(cover_t* cov)
 {
 	struct ksancov_trace* trace = (struct ksancov_trace*)cov->data;
 	cov->size = ksancov_trace_head(trace);
-	cov->data_offset = ((int64_t) & (trace->pcs)) - ((int64_t)(cov->data));
+	cov->data_offset = ((int64_t)&(trace->pcs)) - ((int64_t)(cov->data));
 	cov->pc_offset = trace->offset;
 }
diff --git a/pkg/rpcserver/rpcserver.go b/pkg/rpcserver/rpcserver.go
index 69fd9e179..d49c17538 100644
--- a/pkg/rpcserver/rpcserver.go
+++ b/pkg/rpcserver/rpcserver.go
@@ -9,6 +9,7 @@ import (
 	"errors"
 	"fmt"
 	"net/url"
+	"path/filepath"
 	"slices"
 	"sort"
 	"strings"
@@ -34,10 +35,11 @@ type Config struct {
 	vminfo.Config
 	Stats
 
-	VMArch string
-	VMType string
-	RPC    string
-	VMLess bool
+	VMArch   string
+	VMType   string
+	RPC      string
+	MrvioDir string
+	VMLess   bool
 	// Hash adjacent PCs to form fuzzing feedback signal (otherwise just use coverage PCs as signal).
 	UseCoverEdges bool
 	// Filter signal/comparisons against target kernel text/data ranges.
@@ -157,10 +159,11 @@ func New(cfg *mgrconfig.Config, mgr Manager, stats Stats, debug bool) (Server, e
 			Sandbox:    sandbox,
 			SandboxArg: cfg.SandboxArg,
 		},
-		Stats:  stats,
-		VMArch: cfg.TargetVMArch,
-		RPC:    cfg.RPC,
-		VMLess: cfg.VMLess,
+		Stats:    stats,
+		VMArch:   cfg.TargetVMArch,
+		RPC:      cfg.RPC,
+		MrvioDir: filepath.Join(cfg.Workdir, "mrvio"),
+		VMLess:   cfg.VMLess,
 		// gVisor coverage is not a trace, so producing edges won't work.
 		UseCoverEdges: cfg.Experimental.CoverEdges && cfg.Type != targets.GVisor,
 		// gVisor/Starnix are not Linux, so filtering against Linux ranges won't work.
@@ -449,6 +452,7 @@ func (serv *server) CreateInstance(id int, injectExec chan<- bool, updInfo dispa
 		debug:         serv.cfg.Debug,
 		debugTimeouts: serv.cfg.DebugTimeouts,
 		sysTarget:     serv.sysTarget,
+		mrvioDir:      serv.cfg.MrvioDir,
 		injectExec:    injectExec,
 		infoc:         make(chan chan []byte),
 		requests:      make(map[int64]*queue.Request),
diff --git a/pkg/rpcserver/runner.go b/pkg/rpcserver/runner.go
index 29e79bad5..bd4e4da08 100644
--- a/pkg/rpcserver/runner.go
+++ b/pkg/rpcserver/runner.go
@@ -8,13 +8,16 @@ import (
 	"errors"
 	"fmt"
 	"os"
+	"path/filepath"
 	"slices"
+	"strings"
 	"sync"
 	"time"
 
 	"github.com/google/syzkaller/pkg/cover"
 	"github.com/google/syzkaller/pkg/flatrpc"
 	"github.com/google/syzkaller/pkg/fuzzer/queue"
+	"github.com/google/syzkaller/pkg/hash"
 	"github.com/google/syzkaller/pkg/log"
 	"github.com/google/syzkaller/pkg/osutil"
 	"github.com/google/syzkaller/pkg/report"
@@ -22,6 +25,7 @@ import (
 	"github.com/google/syzkaller/prog"
 	"github.com/google/syzkaller/sys/targets"
 	"github.com/google/syzkaller/vm/dispatcher"
+	"github.com/google/uuid"
 )
 
 type Runner struct {
@@ -46,6 +50,7 @@ type Runner struct {
 	lastExec      *LastExecuting
 	updInfo       dispatcher.UpdateInfo
 	resultCh      chan error
+	mrvioDir      string
 
 	// The mutex protects all the fields below.
 	mu          sync.Mutex
@@ -275,6 +280,8 @@ func (runner *Runner) sendRequest(req *queue.Request) error {
 		panic(err)
 	}
 	runner.nextRequestID++
+	// Return the output so that we can capture the metamorphic relation violation
+	req.ReturnOutput = true
 	id := runner.nextRequestID
 	var flags flatrpc.RequestFlag
 	if req.ReturnOutput {
@@ -414,6 +421,25 @@ func (runner *Runner) handleExecResult(msg *flatrpc.ExecResult) error {
 			runner.convertCallInfo(msg.Info.Extra)
 		}
 	}
+	// Check if MR is violated, and if so, save the program to workdir/mrvio.
+	if len(msg.Output) > 0 && strings.Contains(string(msg.Output), "[SyzMeta]: MR is violated!") {
+		// Let's classify MR-violated test inputs.
+		// Extract MR-related pseudo-syscall sequence in the req and construct mrSeq.
+		mrSeq := ""
+		for i := range len(req.Prog.Calls) {
+			callname := req.Prog.CallName(i)
+			if strings.HasPrefix(callname, "syz_mr_") {
+				mrSeq += callname
+			}
+		}
+		sig := hash.Hash([]byte(mrSeq))
+		// Using hash value of mrSeq for classification
+		dir := filepath.Join(runner.mrvioDir, sig.String())
+		osutil.MkdirAll(dir)
+		id := uuid.New().String()[:8]
+		fn := filepath.Join(dir, id)
+		osutil.WriteFile(fn, req.Prog.Serialize())
+	}
 	status := queue.Success
 	var resErr error
 	if msg.Error != "" {
diff --git a/pkg/vminfo/linux_syscalls.go b/pkg/vminfo/linux_syscalls.go
index 95e3e54eb..202536f1d 100644
--- a/pkg/vminfo/linux_syscalls.go
+++ b/pkg/vminfo/linux_syscalls.go
@@ -103,6 +103,92 @@ var linuxSyscallChecks = map[string]func(*checkContext, *prog.Syscall) string{
 	"syz_socket_connect_nvme_tcp": linuxSyzSocketConnectNvmeTCPSupported,
 	"syz_pidfd_open":              alwaysSupported,
 	"syz_create_resource":         alwaysSupported,
+	"syz_mr_bd3e1cc5":             alwaysSupported,
+	"syz_mr_23de5f08":             alwaysSupported,
+	"syz_mr_b4dad044":             alwaysSupported,
+	"syz_mr_57204f74":             alwaysSupported,
+	"syz_mr_b704cea7":             alwaysSupported,
+	"syz_mr_13cd9ab1":             alwaysSupported,
+	"syz_mr_2281e517":             alwaysSupported,
+	"syz_mr_70b74216":             alwaysSupported,
+	"syz_mr_8acbfeee":             alwaysSupported,
+	"syz_mr_c7d8e332":             alwaysSupported,
+	"syz_mr_f85ca149":             alwaysSupported,
+	"syz_mr_e1e2e65a":             alwaysSupported,
+	"syz_mr_9955c520":             alwaysSupported,
+	"syz_mr_70e04a46":             alwaysSupported,
+	"syz_mr_71c06928":             alwaysSupported,
+	"syz_mr_38fcb3bb":             alwaysSupported,
+	"syz_mr_c20a878e":             alwaysSupported,
+	"syz_mr_cf550810":             alwaysSupported,
+	"syz_mr_6473f381":             alwaysSupported,
+	"syz_mr_62089dad":             alwaysSupported,
+	"syz_mr_98ce7ce8":             alwaysSupported,
+	"syz_mr_7d90e6d8":             alwaysSupported,
+	"syz_mr_8bbf48b4":             alwaysSupported,
+	"syz_mr_fa2fc31c":             alwaysSupported,
+	"syz_mr_a92dfc4f":             alwaysSupported,
+	"syz_mr_f3efb53d":             alwaysSupported,
+	"syz_mr_6b728ac9":             alwaysSupported,
+	"syz_mr_b0377842":             alwaysSupported,
+	"syz_mr_811d6c8a":             alwaysSupported,
+	"syz_mr_353a3b59":             alwaysSupported,
+	"syz_mr_c11de9b2":             alwaysSupported,
+	"syz_mr_f46c78da":             alwaysSupported,
+	"syz_mr_f167aad9":             alwaysSupported,
+	"syz_mr_65f5ce7f":             alwaysSupported,
+	"syz_mr_63e076f2":             alwaysSupported,
+	"syz_mr_a421a672":             alwaysSupported,
+	"syz_mr_43e7360b":             alwaysSupported,
+	"syz_mr_4d896b11":             alwaysSupported,
+	"syz_mr_3f3322b8":             alwaysSupported,
+	"syz_mr_2a839201":             alwaysSupported,
+	"syz_mr_c7df56f8":             alwaysSupported,
+	"syz_mr_351170b6":             alwaysSupported,
+	"syz_mr_647388cd":             alwaysSupported,
+	"syz_mr_45f170fe":             alwaysSupported,
+	"syz_mr_89dbb28a":             alwaysSupported,
+	"syz_mr_203da363":             alwaysSupported,
+	"syz_mr_6d17dd5e":             alwaysSupported,
+	"syz_mr_da30fef7":             alwaysSupported,
+	"syz_mr_3baa0980":             alwaysSupported,
+	"syz_mr_5bc1836b":             alwaysSupported,
+	"syz_mr_6a51ab49":             alwaysSupported,
+	"syz_mr_8766e19b":             alwaysSupported,
+	"syz_mr_76aa1e6b":             alwaysSupported,
+	"syz_mr_7736935a":             alwaysSupported,
+	"syz_mr_f70978b0":             alwaysSupported,
+	"syz_mr_110f3bc5":             alwaysSupported,
+	"syz_mr_9b9aa407":             alwaysSupported,
+	"syz_mr_c9189a41":             alwaysSupported,
+	"syz_mr_b49fee5b":             alwaysSupported,
+	"syz_mr_1825cfdc":             alwaysSupported,
+	"syz_mr_8dda4646":             alwaysSupported,
+	"syz_mr_f4bced4c":             alwaysSupported,
+	"syz_mr_28861e6f":             alwaysSupported,
+	"syz_mr_c67a179b":             alwaysSupported,
+	"syz_mr_e36a127e":             alwaysSupported,
+	"syz_mr_561d57bb":             alwaysSupported,
+	"syz_mr_9f95d9e3":             alwaysSupported,
+	"syz_mr_a1912f81":             alwaysSupported,
+	"syz_mr_580407a7":             alwaysSupported,
+	"syz_mr_75e3d2b4":             alwaysSupported,
+	"syz_mr_b65b85a4":             alwaysSupported,
+	"syz_mr_3c76f4a3":             alwaysSupported,
+	"syz_mr_69aac2da":             alwaysSupported,
+	"syz_mr_7206cd80":             alwaysSupported,
+	"syz_mr_14c38fa6":             alwaysSupported,
+	"syz_mr_825250bb":             alwaysSupported,
+	"syz_mr_c3320296":             alwaysSupported,
+	"syz_mr_8ce7d16e":             alwaysSupported,
+	"syz_mr_77cb284e":             alwaysSupported,
+	"syz_mr_99025952":             alwaysSupported,
+	"syz_mr_3f12b8e9":             alwaysSupported,
+	"syz_mr_1f3a13c8":             alwaysSupported,
+	"syz_mr_9031a534":             alwaysSupported,
+	"syz_mr_1d1fd0d1":             alwaysSupported,
+	"syz_mr_0ce41170":             alwaysSupported,
+	"syz_mr_3d58e22a":             alwaysSupported,
 }
 
 func linuxSyzOpenDevSupported(ctx *checkContext, call *prog.Syscall) string {
diff --git a/sys/linux/metamorphic.txt b/sys/linux/metamorphic.txt
new file mode 100644
index 000000000..971907164
--- /dev/null
+++ b/sys/linux/metamorphic.txt
@@ -0,0 +1,258 @@
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_3d58e22a(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_0ce41170(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_1d1fd0d1(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_9031a534(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_1f3a13c8()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_3f12b8e9(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 3 iterations
+syz_mr_99025952(source ptr[in, string], target ptr[in, filename], mountflags flags[mount_flags], data ptr[in, string])
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_77cb284e(arg0 intptr, arg1 intptr, arg2 intptr, arg3 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_8ce7d16e(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_c3320296(a0 intptr, a1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_825250bb(guest_phys_addr intptr, memory_size intptr, target_phys_addr intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_14c38fa6()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_7206cd80(bitmap intptr, slot intptr, first_gfn intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_69aac2da(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_3c76f4a3(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_b65b85a4(liobn1 intptr, window_size_s intptr, liobn2 intptr, k intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_75e3d2b4(V_initial intptr, V_guest intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_580407a7(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_a1912f81(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_9f95d9e3(a0 intptr, a1 intptr, a2 intptr, a3 intptr, a4 intptr, a5 intptr, a6 intptr, a7 intptr, a8 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_561d57bb()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_e36a127e(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_c67a179b(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_28861e6f(conn_id intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_f4bced4c(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_8dda4646(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_1825cfdc()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_b49fee5b()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_c9189a41(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_9b9aa407(vcpu_id_A intptr, vcpu_id_B intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 6 iterations
+syz_mr_110f3bc5(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_f70978b0(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_7736935a(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_76aa1e6b(id_A intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_8766e19b(gfn intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 2 iterations
+syz_mr_6a51ab49(addr vma, len intptr, flags intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_5bc1836b(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_3baa0980()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_da30fef7(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_6d17dd5e(p_id intptr, p_addr intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_203da363(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_89dbb28a(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_45f170fe(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_647388cd(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_351170b6(fc1_in intptr, sel1_1_in intptr, sel2_1_in intptr, addr1_in intptr, ar1_in intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 4 iterations
+syz_mr_c7df56f8(slot intptr, flags intptr, guest_phys_addr intptr, memory_size intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_2a839201(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_3f3322b8()
+
+# Syzlang description generated by gemini-2.5-pro after 8 iterations
+syz_mr_4d896b11(nevents len[events_arr], flags intptr, fixed_counter_bitmap intptr, action intptr, events_arr ptr[in, array[int64]])
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_43e7360b()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_a421a672(liobn intptr, page_shift intptr, offset intptr, size intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_63e076f2(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_65f5ce7f(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_f167aad9(p0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_f46c78da()
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_c11de9b2(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_353a3b59(a0 intptr, a1 intptr, a2 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_811d6c8a(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_b0377842(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 5 iterations
+syz_mr_6b728ac9(arg0 intptr[0:1])
+
+# Syzlang description generated by gemini-2.5-pro after 2 iterations
+syz_mr_f3efb53d(sregs_in_ptr ptr[in, buffer[in]])
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_a92dfc4f(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_fa2fc31c(ap_vcpu_id intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_8bbf48b4(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_7d90e6d8(arg0 vma)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_98ce7ce8(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_62089dad(vcpu_id intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_6473f381(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 4 iterations
+syz_mr_cf550810(arg0 buffer[inout])
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_c20a878e(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_38fcb3bb(p1_size_v intptr, p2_size_v intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_71c06928(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 3 iterations
+syz_mr_70e04a46(arg0 intptr[1:1500])
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_9955c520(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_e1e2e65a(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_f85ca149(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_c7d8e332(a0 intptr, a1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_8acbfeee(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_70b74216(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 2 iterations
+syz_mr_2281e517(a0 intptr[0:2047])
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_13cd9ab1(arg0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_b704cea7(N intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_57204f74(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_b4dad044(arg0 intptr, arg1 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 1 iterations
+syz_mr_23de5f08(a0 intptr)
+
+# Syzlang description generated by gemini-2.5-pro after 3 iterations
+syz_mr_bd3e1cc5(parent_iface ptr[in, string], p_key_base intptr)
diff --git a/tools/kcovfuzzer/kcovfuzzer.c b/tools/kcovfuzzer/kcovfuzzer.c
index 7b749b364..ded9dccc3 100644
--- a/tools/kcovfuzzer/kcovfuzzer.c
+++ b/tools/kcovfuzzer/kcovfuzzer.c
@@ -56,7 +56,7 @@ void bpf(const char* data, long size)
 	attr.prog_type = BPF_PROG_TYPE_SOCKET_FILTER;
 	attr.insns = (uint64_t)data;
 	attr.insn_cnt = size / 8;
-	attr.license = (uint64_t) "GPL";
+	attr.license = (uint64_t)"GPL";
 
 	cover_start();
 	int pfd = syscall(SYS_bpf, BPF_PROG_LOAD, &attr, sizeof(attr));
diff --git a/tools/syz-declextract/syz-declextract.cpp b/tools/syz-declextract/syz-declextract.cpp
index 8ab77f0d3..02d48774e 100644
--- a/tools/syz-declextract/syz-declextract.cpp
+++ b/tools/syz-declextract/syz-declextract.cpp
@@ -395,7 +395,7 @@ private:
   }
 
 public:
-  RecordExtractor(const SourceManager *const SM) : SM(SM){};
+  RecordExtractor(const SourceManager *const SM) : SM(SM) {};
   std::string getFieldType(const QualType &fieldType, ASTContext *context, const std::string &fieldName,
                            const std::string &parent = "", bool isSyscallParam = false,
                            const std::string &fieldTypeName = "") {
diff --git a/vm/adb/adb.go b/vm/adb/adb.go
index 67336f008..60a47f0fa 100644
--- a/vm/adb/adb.go
+++ b/vm/adb/adb.go
@@ -73,6 +73,7 @@ var (
 	androidSerial = "^[0-9A-Za-z]+$"
 	ipAddress     = `^(?:localhost|(?:[0-9]{1,3}\.){3}[0-9]{1,3})\:(?:[0-9]{1,5})$` // cuttlefish or remote_device_proxy
 	emulatorID    = `^emulator\-\d+$`
+	containerName = `^common-android[0-9a-z-_:]+$` // Container name for launching cvd used in our experiment
 )
 
 func loadDevice(data []byte) (*Device, error) {
@@ -105,7 +106,8 @@ func ctor(env *vmimpl.Env) (vmimpl.Pool, error) {
 		return nil, fmt.Errorf("no adb devices specified")
 	}
 	// Device should be either regular serial number, a valid Cuttlefish ID, or an Android Emulator ID.
-	devRe := regexp.MustCompile(fmt.Sprintf("%s|%s|%s", androidSerial, ipAddress, emulatorID))
+	// In our experiment, the device id may also consist of the container name + port, so we relax the check condition.
+	devRe := regexp.MustCompile(fmt.Sprintf("%s|%s|%s|%s", androidSerial, ipAddress, emulatorID, containerName))
 	for _, dev := range cfg.Devices {
 		device, err := loadDevice(dev)
 		if err != nil {
